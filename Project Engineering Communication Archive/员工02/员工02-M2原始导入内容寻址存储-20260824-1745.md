# 员工02｜M2 原始导入内容寻址存储

时间：2026-08-24 17:45（Asia/Shanghai）  
状态：已完成第一刀，继续推进

## 本次完成

- 已阅读最新 PEC 控制文件及 M2 总派工，正式接单 `DATA-M2-001～010`、`IMP-M2-004～008`。
- 新增跨平台原始导入存储适配器，支持 Markdown、JSONL、TXT。
- 原文按 SHA-256 内容寻址并不可变保存，落盘路径不采用用户文件名，阻断 `../` 等路径穿越输入。
- 写入采用同目录临时文件、`sync_all`、原子重命名；失败时清理临时文件，不把半成品视为已提交导入。
- 相同字节跨文件名、跨扩展名复用同一 `storage_ref`，显式返回 `duplicate=true`，不覆盖原件。
- 增加空文件、未知格式、超限文件、路径穿越、重复导入测试；默认单文件上限暂设 128 MiB，后续由产品约束统一配置。

## 验证

- 新增 4 项定向单元测试首次执行：`4 passed, 0 failed`。
- `cargo check --offline`：通过。
- `cargo clippy --offline --lib -- -D warnings`：通过。
- 全量 `cargo test --offline` 当前被并行契约改动阻塞：`ModelRunRequest` 新增 `effective_run_profile` 后，5 处既有测试构造器尚未补字段；本次导入存储代码无相关编译错误。

## 需要协作

- 员工01：请完成 `effective_run_profile` 契约落地后同步修复 5 处既有 `ModelRunRequest` 测试构造器，并发布契约冻结 PEC；员工02随后重跑全量 Rust 门禁。
- 员工03/04：当前无需接入；等导入事务接口与 ParseReport 冻结后再联调 UI 和降级报告。

## 下一步

- 在员工01冻结 M2 实体与 FocusFrame 字段前，继续实现不冲突的数据底座：导入事务表、唯一指纹约束、失败/重启恢复状态与迁移测试。
- 不改 M1 Provider、取消、恢复和真实 API 行为。
