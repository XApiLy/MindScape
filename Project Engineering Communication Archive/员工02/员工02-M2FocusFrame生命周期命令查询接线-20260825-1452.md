# 员工02｜M2 FocusFrame 生命周期命令查询接线

时间：2026-08-25 14:52（Asia/Shanghai）  
状态：生命周期数据闭环完成，等待前端真实消费

## 已阅读与落实

- 已阅读最新 PEC 控制文件、员工01《M2 FocusFrame 查询投影契约》、员工03知识上下文占位 UI、员工04运行档案联调、员工05真实 DeepSeek 验收，以及 M2 数据/FocusFrame 契约。
- 员工01已冻结 `FocusFrameQueryProjection`；命令层现在直接返回该 DTO，FocusedContext 尚未编译时保持 `null`，不伪造无知识结果。
- 共享工作树中同事已先行完成 KernelService 生命周期命令骨架；本次完成冲突清理并以现有统一输入/输出边界为准，未保留重复命令定义。

## 本次完成

- `create_focus_frame`：创建 Active 生命周期记录，revision 从 1 开始。
- `close_focus_frame` / `reopen_focus_frame`：通过统一领域状态机执行，携带 `expectedRevision` 做乐观并发保护。
- `get_focus_frame_query`：从 SQLite 恢复生命周期状态并返回 `FocusFrameQueryProjection`，FocusedContext 可选为空。
- Tauri invoke handler 已注册以上命令；错误沿安全 `CommandError` 返回。
- 清理并行接线产生的重复 service/command/handler 定义，恢复单一命令入口。

## 验证

- `cargo check --offline`：通过。
- `cargo test --offline`：97 passed, 0 failed。
- 生命周期 SQLite 重启恢复、stale revision、重复转换和查询契约测试均通过。

## 契约、安全、恢复与接口影响

- 生命周期状态权威来自 SQLite，不读取 Canvas/React 状态推断。
- 查询返回不含 raw 正文、API Key 或完整 reasoning；FocusedContext 为空是合法“尚未编译”状态。
- 未改变 M1 运行、取消、Provider、凭据和导入事务语义。

## 需要协作与下一步（最多两个 WIP）

- **员工03/04**：现在可以按 `get_focus_frame_query` 消费结构化生命周期投影；请保持 `focusedContext: null` 与加载/错误状态区分。
- **员工01**：请复核命令输入使用 `FocusFrameLifecycleCommandInput`、返回使用 `FocusFrameQueryProjection` 与契约文档一致。
- WIP1：与员工03/04完成真实 Tauri 查询 IPC 接入和前端状态回归。
- WIP2：继续补 FocusedContextSnapshot 持久化/查询，避免在命令层伪造上下文内容。

