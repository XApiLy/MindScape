# 员工02｜M2 导入 Bundle 命令 IPC 接线

时间：2026-08-25 11:55（Asia/Shanghai）  
状态：已完成第五刀，raw 文件命令仍待下一刀

## 已阅读与落实

- 已阅读最新 PEC 控制文件、员工01《M2 FocusFrame 生命周期状态机》、员工03《M2 知识上下文占位 UI 与 IPC 阻塞》、员工04《M2 运行档案错误提示与跨团队联调》、员工05《M2 DeepSeek 流式事件夹具回归》，以及 M2 派单、M2 契约和 UI 占位交接规则。
- 员工01仍只冻结 FocusFrame 领域状态，未提供持久化查询；本次不提前接入 FocusFrame。
- 员工04明确前端只消费结构化导入 DTO；本次新增 Tauri 命令只调用 KernelService，不让前端直接读 SQLite/raw storage。

## 本次完成

- 新增 `persist_import_bundle` Tauri IPC 命令，输入为 `ImportSource`、`ImportRevision`、`ImportedMessage[]`、`ParseReport`。
- 在 `KernelService` 增加对应服务入口，继续复用员工01的 `validate_import_bundle` 和 SQLite 父先子事务落库。
- 将命令注册到 Tauri invoke handler；错误统一沿现有安全 `CommandError` 映射，不回显原文、Key 或底层 SQL。
- 保持 raw 内容寻址存储与结构化 Bundle 分离；本刀尚未伪造“文件已导入”，也未启用 Intake 预览。

## 验证

- `cargo check --offline`：通过。
- `cargo test --offline`：88 passed, 0 failed。
- `cargo fmt`：通过。

## 契约、安全、恢复与跨团队接口

- IPC 消费冻结 Import 契约，不新增前端领域副本；非法 bundle 在事务前被拒绝。
- 命令层不接触凭据、不调用 Provider、不执行导入文本；raw 文件仍需下一刀完成原子保存和失败清理。
- 当前命令可持久化已准备好的 Bundle，但尚不能独立完成“文件选择→解析→raw 保存→Bundle 提交”的完整导入闭环。

## 需要协作与下一步（最多两个 WIP）

- **员工04**：请评审命令参数/返回错误是否满足 Intake 结构化消费；当前命令不改变 UI 占位状态。
- **员工01**：请确认命令层继续以 `validate_import_bundle` 为唯一预检入口；无需新增字段。
- WIP1：接入 raw 文件原子保存与格式识别，形成通用 Markdown/JSONL/TXT 导入命令。
- WIP2：补充 ParseReport/source 查询 IPC 和导入失败后的 raw orphan 清理/重试测试。

