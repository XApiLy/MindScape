# 员工02｜真实 Chat 运行协调与启动恢复

> 提交时间：2026-08-18 15:31（UTC+8）  
> 覆盖任务：CORE-014～015、DATA-014～015  
> 状态：后端最小切片完成，等待员工01/04/05评审联调  
> 当前分支：`docs/REL-002-pec-retention`（Draft PR 尚未创建）

## 相比上一份的变化

- 从“ModelRun与事件可落库”推进到“后端统一创建运行、事务应用终态、启动恢复和查询投影”。
- 前端不再需要自行构造完整ModelRunRequest；新增单一`start_model_run`边界供员工04切换。
- 启动残留pending/streaming运行会确定性转为failed，保留已落库部分回答。

## 已阅读与落实

- 已阅读V1范围、2026-08-17产品共识、总路线、M1派单、内核总览、前五人责任书、RC1、安全威胁模型、Git与PEC制度、本地数据/凭据边界及员工01/03/04/05最新PEC。
- 落实影响：当前WIP仅限CORE-014～015与DATA-014～015；未推进导入、多Provider、Harness、Skill/MCP或视觉工作。
- 根据员工01冻结候选，不新增`interrupted`平行枚举；进程中断采用`failed + application_interrupted`安全错误表达，等待员工01确认。

## 本次完成与证据

- 新增`StartModelRunInput`与`start_model_run`：后端校验输入、创建pending Node、冻结ContextSnapshot并创建ModelRun。
- 幂等键可复用已有运行，避免重试静默复制节点。
- 每个运行事件先落库，再投递Channel；Channel关闭会触发取消但不阻止后续cancelled终态持久化。
- completed/cancelled/failed事件与Node状态、Provider/模型、部分assistant内容在同一SQLite事务提交。
- Provider在未产出failed事件时返回错误，协调器补写统一failed终态。
- 应用启动扫描残留运行及孤立pending节点；重复扫描无二次副作用。
- 新增`list_model_runs`恢复查询投影，并同步TypeScript镜像与客户端入口。

## 契约、数据和安全影响

- SQLite仍为schema v4，无新增迁移；强化了现有ModelRun与Node的事务一致性。
- 新增的运行查询投影和统一启动输入为加法契约，需员工01确认后冻结。
- Provider网络逻辑仍留在Provider层；数据层不读取凭据，ModelRunRequest不携带明文Key。
- 结构化命令错误和操作系统凭据边界未放宽。

## 验证结果

- `cargo test`：23项全部通过。
- `cargo fmt --all -- --check`：通过。
- `cargo clippy --all-targets --all-features -- -D warnings`：通过。
- `pnpm build`：通过。
- 覆盖连续/重复/跳号事件、completed原子终态、流式中断、部分内容保留和重复恢复。

## 风险与阻塞

- 当前工作树位于文档治理分支且含管理层/员工01未提交变更，尚不具备独立Draft PR条件。
- `.git`为只读，`git mv`无法创建`index.lock`；已执行同路径文件移动，后续暂存时需确认Git识别为rename。
- 员工04尚未切换`start_model_run`，当前旧三段式调用仍存在；在切换前不能宣称纵向闭环完成。
- `failed + application_interrupted`等价语义等待员工01冻结确认。

## 下一步 WIP（最多两项）

1. 与员工01评审统一启动输入、运行投影和中断语义，按Frozen V1结论收敛。
2. 与员工04/05联调统一命令、真实取消及Provider错误终态，补事件通道关闭集成测试。

## 需要同事配合

- **员工01**：评审新增加法契约及中断等价语义，确认可进入Frozen V1。
- **员工04**：移除前端`append_turn → run_model → complete_turn`编排，切换`start_model_run`和`list_model_runs`。
- **员工05**：真实Provider继续只输出统一事件；共同验证网络错误、取消和异常结束均形成唯一终态。
- **员工03**：后续从运行查询投影恢复节点终态；本轮未修改其视口接口。
