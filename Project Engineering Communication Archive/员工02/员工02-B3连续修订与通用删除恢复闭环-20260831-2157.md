# 员工02｜B3 连续修订与通用删除恢复闭环

> 提交时间：2026-08-31 21:57（UTC+8）  
> 覆盖阻断：`B3-REV-03`、`B3-REV-04`  
> 状态：**员工02已关闭 B3 两项实现阻断，等待员工01最终复评；M2 仍为 No-Go**  
> 当前基线：`a11061ecdfb4`（共享 dirty 集成工作树，约 231 项变更）

## 已阅读与落实

- 已重新阅读控制 PEC 顶部“2026-08-30：M2 Go 阻断收敛”“员工直接执行表”“2026-08-31 现场状态复审”、Review Lab 规则，以及员工01《B2 评审通过与 B3 恢复阻断》。本轮只修复员工01明确列出的 `B3-REV-03/04`，不启动 B6、不重复汇报 B2，也不生成个人 Acceptance Build。
- 已复核员工03正式删除调用链、员工05 B4/B5 验收边界、员工04/06当前报告；正式产品路径仍是 `kernelClient.deleteKnowledgeEntity → Tauri delete_knowledge_entity → KernelService::delete_knowledge_entity_and_vault`，因此恢复修复直接进入应用服务和启动流程，没有另造测试专用入口。
- 已复核 M2 派单、Markdown Vault/本地实体架构、数据布局、威胁模型与契约基线。具体落实为：SQLite 继续是恢复提交事实，Vault 只保存可读投影和受控 journal；所有 journal 相对路径与备份文件名继续经过既有安全校验，未扩大任意文件访问面。
- Rust/Tauri 实现继续遵守显式 `Result`、生产路径无 `unwrap/expect`、共享恢复锁和启动前收敛原则。当前未发现需要改变领域对象或 typed IPC 的冲突；唯一待确认事项是员工01对新恢复身份与通用删除事务边界的最终签字。

## 本轮关闭的 B 编号

### B3-REV-03：连续 DiscussionLog revision 不再被旧 journal 回滚

- `recover_discussion_transactions` 现在按日志 ID 建立 SQLite 最新 revision 映射；在单调 revision 契约下，`journal.revision <= latest_revision` 即视为已经提交。
- 可复现序列“revision 1 SQLite 已提交但索引失败遗留 journal → revision 2 成功 → 重启”现在只清理旧 journal，不恢复 revision 1 的 pre-image。
- 重启后 revision 2 的日志正文、Evidence 来源页、讨论索引和 SQLite revision 均保持一致，直接关闭员工01报告中的连续修订数据倒退问题。

### B3-REV-04：正式通用删除进入统一原子恢复边界

- 新增 `.entity-delete-transactions` journal，对目标实体页、所有关系邻居实体页、邻居 Evidence 来源页和全局实体索引保存字节级 pre-image；journal 与备份落盘并同步后才开始改 Vault。
- 正式删除现在持有共享 `vault_projection` 锁，先按删除后的关系集合移除目标并重投影存活邻居，再提交 SQLite 删除；Vault 任一步失败会先恢复全部文件，SQLite 不发生变化；SQLite 返回失败或冲突也会恢复 Vault。
- SQLite 已提交但进程在 journal 清理前退出时，启动恢复以“目标实体在 SQLite 已不存在”为提交事实：保持目标缺失，使用当前 SQLite 实体/关系重投影存活邻居，重建全局实体索引并清理 journal。
- 启动顺序已接入 `recover_knowledge_entity_delete_vault`；SQLite 增加只读 `list_all_knowledge_relations` 供恢复收敛。普通实体 Markdown 投影也进入同一锁，避免与删除并发写同一组文件。
- B5 脱敏审计新增 `pendingEntityDeleteTransactions=0`，因此唯一 clean Release 可直接检查是否遗留通用删除 journal。

## 跨模块证据

1. **连续修订恢复**：`discussion_log_old_committed_journal_keeps_a_newer_revision_on_restart` 覆盖 revision 1 遗留 journal、revision 2 成功与进程重启，断言最新日志、来源页、索引、SQLite revision 均为 revision 2，journal 被清理。
2. **正式应用删除链**：`deleting_knowledge_reprojects_vault_and_cleans_sqlite_indexes_after_restart` 通过 `delete_knowledge_entity_and_vault` 执行正式动作，断言 relation/FTS/vector 派生行清零、目标 Markdown 消失、存活邻居无悬空 Wikilink、Evidence 来源与实体索引正确，并在新服务实例恢复后再次核对。
3. **写入失败补偿**：`knowledge_delete_vault_failure_keeps_sqlite_and_files_unchanged` 在目标删除和邻居重投影之后注入实体索引写失败，断言 SQLite 实体/关系/检索事实未变，目标页、邻居页和索引逐字节恢复，journal 为空。
4. **提交后崩溃恢复**：`committed_knowledge_delete_journal_converges_from_sqlite_on_restart` 模拟 Vault 与 SQLite 均已提交、尚未来得及清 journal 的退出窗口；重启保持目标缺失、修复邻居和索引并清理 journal。
5. **适配器双终态**：`knowledge_delete_journal_restores_present_entity_and_keeps_committed_absence` 同时证明 SQLite 仍有目标时恢复 pre-image、SQLite 已无目标时保持删除终态，两条路径均幂等收敛。
6. **全量门禁**：`cargo test --all-targets --all-features --locked --quiet` 为 library `205 passed / 0 failed / 1 ignored`、B5 audit example `2 passed / 0 failed`；`cargo clippy --all-targets --all-features --locked -- -D warnings`、`cargo fmt --all -- --check`、`git diff --check`、秘密扫描均通过。

## 交付位置

- `desktop/src-tauri/src/adapters/markdown_vault.rs`：连续 revision 判定、通用删除 journal、回滚/提交/启动恢复及适配器测试。
- `desktop/src-tauri/src/application/mod.rs`：正式删除事务编排、共享投影锁、启动恢复入口及跨模块测试。
- `desktop/src-tauri/src/adapters/sqlite.rs`：启动恢复所需的全量关系只读查询。
- `desktop/src-tauri/src/lib.rs`：通用删除 journal 启动收敛接线。
- `desktop/src-tauri/examples/b5_focus_promotion_audit.rs` 与 `docs/engineering/evidence/employee02/m2-b2-persistence-audit/README.md`：通用删除 pending journal 脱敏门禁。

## 契约、数据、安全与恢复影响

- 不改变 KnowledgeEntity、DiscussionLog、EvidenceRef 或前端 typed IPC；通用删除的外部命令签名与返回值保持不变。
- 本地数据布局新增受控内部目录 `.entity-delete-transactions/`；内容仅为删除事务 manifest 和受控 Vault 文件 pre-image，不包含 API Key、Provider 响应或额外业务日志。
- 恢复身份继续以 SQLite 权威事实为准：DiscussionLog 使用同 ID 单调 revision，知识删除使用目标实体是否仍存在。恢复会重建可读投影和全局索引，不反向篡改 SQLite 领域事实。
- 本轮没有可见 UI 变化，因此按 Review Lab 规则不发布无意义 preview；没有生成 Build ID、EXE 或 SHA-256，唯一 Acceptance Release 仍由员工05在 B1 clean 冻结后发布。

## 剩余阻断与协作

1. **B3 只剩员工01复评**：请员工01复跑上述五个直接回归并复核 `journal.revision <= latest_revision`、Vault-first 通用删除、SQLite 权威启动收敛和共享锁边界；若无新增确定性问题，可正式关闭 B3。
2. **B5 仍未关闭**：请员工05把连续 revision 遗留 journal、通用删除写失败和“SQLite 已提交但 journal 未清”加入同一真实样本矩阵，并在唯一 clean Release 核对 `pendingEntityDeleteTransactions=0`、无悬空 Wikilink及 relation/FTS/vector 清理。
3. **B1 仍未关闭**：共享树仍 dirty，本报告不构成 clean candidate；B2 已由员工01关闭，B4 已关闭，均不重复计功。
4. **B6 未开始**：继续不抢占 B1/B3复评/B5；是否进入 M2 由后续控制 PEC 或创始人范围裁决决定。

## 下一步 WIP（最多两个）

1. 配合员工01完成 B3 最终事务/恢复复评，只修复复评发现的确定性问题。
2. 配合员工05把本轮恢复场景接入 B5 同一真实样本与唯一 clean Release 审计，不发布员工02个人构建。
