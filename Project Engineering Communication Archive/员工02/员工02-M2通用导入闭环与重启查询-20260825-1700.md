# 员工02｜M2 通用导入闭环与重启查询

时间：2026-08-25 17:00（Asia/Shanghai）  
状态：后端闭环完成，进入前端真实验收联调

## 已阅读与落实

- 已阅读最新 PEC 控制文件、员工01 FocusFrame 会话列表、员工03画布查询接入、员工04导入/FocusFrame 客户端、员工05索引安全边界，以及 M2 派单、Import/恢复/安全任务。
- 员工01确认通用 JSONL 的 content/text 与未知对象保真策略可接受；本轮继续保持角色/时间未恢复时不伪造，并由 ParseReport 表达。
- 员工04已提供 `importGenericFile` typed client；本轮补齐重启查询，使 UI 不依赖导入瞬间的内存结果。

## 本次完整交付

- 通用 Markdown/TXT/JSONL：raw 内容寻址原子保存、UTF-8/格式/大小校验、确定性解析、Bundle 预检和 SQLite 事务提交。
- 失败处理：解析或事务失败清理本次新建 raw；重复内容不误删既有 raw；损坏 JSONL 不产生半个 Bundle。
- 新增 `ImportBundleQueryProjection` Rust/TypeScript 契约：source、最新 revision、全部 ImportedMessage、ParseReport。
- 新增 `list_import_sources(conversationId)` 与 `get_import_bundle(sourceId)` Tauri 查询命令及 typed client。
- 重启后可按会话发现导入来源，并恢复最新 revision、消息、warnings/errors 和字段恢复状态。
- 启动恢复会清理 `.tmp` 中断写入，仅删除明确临时文件，不碰已提交内容和无关文件。

## 验证

- Rust：103 passed / 0 failed；Clippy `-D warnings`、fmt 通过。
- 前端：TypeScript、Chat 38/38、Canvas 20/20、生产构建通过（1831 modules）。
- SQLite 查询回归验证 source 列表与完整 Bundle round-trip。
- 启动恢复回归验证只清理中断临时文件。
- Secret scan：1404 个文本文件通过。

## 契约、安全与恢复影响

- 原文只存 app_data/imports 内容寻址目录；SQLite 保存不可变 ref 和结构化投影。
- 前端只经 Tauri typed client 访问，不直接读文件或数据库。
- 导入文本始终是不可信数据，不执行历史指令、不自动调用模型、不产生分析费用。
- 当前剩余恢复缺口：进程在 raw rename 后、SQLite commit 前被强杀时会留下内容寻址孤儿；其内容安全但需要后续引用扫描回收。

## 下一阶段与协作

- **员工04**：可开始接 Intake 真实导入、报告预览、重复/错误/恢复状态；使用 `listImportSources/getImportBundle` 做重启恢复。
- **员工03**：可消费 ImportSource/ParseReport 查询投影展示来源详情，不读取 raw storage。
- **员工01**：继续评审 ParseReport 的角色/时间恢复字段，不改变 generic 第一版保真语义。
- 员工02下一完整单元：真实样本矩阵、强杀孤儿引用扫描，以及知识实体/证据与 FocusedContextSnapshot 持久化。

