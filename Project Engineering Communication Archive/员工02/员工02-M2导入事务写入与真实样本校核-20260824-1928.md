# 员工02｜M2 导入事务写入与真实样本校核

时间：2026-08-24 19:28（Asia/Shanghai）  
状态：已完成第三刀，等待 IPC 接线

## 已阅读与落实

- 已阅读最新 PEC 控制文件、员工01《M2 知识上下文编译与运行档案评审》、员工03《M2 契约到画布投影适配》、员工04《M2 运行档案真实 UI 接线》、员工05《M2 运行档案真实发送接线》，以及 M2 派单、M2 契约、本地数据布局、导入/RAG 与 Markdown Vault 方案。
- 员工01已将知识候选与运行档案接线评审为可选加法；本次仍只消费已冻结 Import 契约，不把知识候选或 UI 状态反写进 SQLite。
- 按创始人授权检查 `E:\AI-Data` 的真实样本候选；选取一份本地 Markdown 测试清单做格式/敏感字段校核，未复制到仓库、未写入 PEC、未发现 API Key/Authorization/password/secret 命中。缓存、审计日志和凭据目录未作为导入样本。

## 本次完成

- 新增 `SqliteStore::persist_import_bundle`，以一个 SQLite 事务写入完整 `ImportSource`、`ImportRevision`、批量 `ImportedMessage` 和 `ParseReport`。
- 事务入口校验 source/revision/report 引用一致性及每条 ImportedMessage revision 一致性；任一失败整体回滚，避免半个导入进入图谱。
- 原始字节继续由内容寻址存储负责；数据库只保存 immutable `storage_ref`、hash 与结构化解析结果。
- M2 v6 迁移、内容寻址存储和全部旧运行链路保持兼容。

## 验证

- `cargo test --offline`：76 passed, 0 failed。
- `cargo clippy --all-targets --all-features --offline -- -D warnings`：通过。
- `cargo check --offline`：通过。
- `node scripts/check-secrets.mjs`：1310 个文本文件扫描通过。
- 真实样本仅做只读格式/敏感字段校核，没有上传、调用 Provider 或写入产品数据目录。

## 契约、数据、安全、恢复影响

- 事务写入与员工01冻结的契约字段一一对应，不改变 M1 schema 语义；导入失败可安全重试。
- Foreign key 与唯一 hash 约束继续阻止悬挂 revision、孤立消息和同源重复。
- 不记录原文到日志/PEC，不读取或保存密钥，不触发模型分析；`ImportAnalysisPolicy::Disabled` 保持有效。

## 需要协作与下一步（最多两个 WIP）

- **员工01**：请评审 `persist_import_bundle` 的事务边界与 v6 表字段；如无异议，确认可进入命令层。
- **员工04**：请提供 Intake 需要的导入命令输入/输出边界；前端继续只消费结构化结果。
- WIP1：接入通用导入 Tauri 命令：先原子保存 raw，再事务写入 bundle，错误时返回安全 ParseReport。
- WIP2：补充失败回滚、重复 hash、进程中断重启恢复与 source locator 查询测试。

