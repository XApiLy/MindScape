# 员工02｜M2 知识持久化校验与检索联调准备

时间：2026-08-27 10:00（Asia/Shanghai）  
状态：本推进单元完成，等待统一 Release 联调

## 已阅读与落实

- 已阅读最新 PEC：员工01统一检索投影契约、员工03知识实体画布投影、员工04查询失败恢复入口、员工05检索候选 Provenance 桥接及上一份员工02 IPC 报告。
- 按员工01要求，Entity/Relation 写入现在调用既有 `validate_for_conversation()`，在统一查询组装前拒绝跨会话 scope、非法版本、重复证据和无效时间/版本。

## 本次完成

- 完成 Entity、Relation、EvidenceRef 的应用层与 Tauri 查询/写入接口接线。
- 知识实体/关系落库入口接入领域完整校验；保留稳定 ID、scope、revision、generator 和 EvidenceRef JSON。
- SQLite schema v9、FocusedContext v8、导入恢复和索引适配器保持版本顺序兼容。
- 为后续 FTS/Vector/Relation 统一检索提供确定性会话查询和状态事实源。

## 验证

- Rust 全量测试：130 项通过。
- Clippy `-D warnings`：通过。
- `cargo fmt`：通过。
- 前端 Chat 测试：39/39 通过。
- TypeScript/Vite build：1831 modules，通过。
- Secret scan 与 PEC retention：通过。

## 当前 M2 状态

- 数据、导入、恢复、FocusFrame、FocusedContext 和知识对象 IPC 已具备工程实现。
- 最终 Go 仍待：统一检索实际组装、删除/否决/过期后的索引失效重建、Markdown Vault revision，以及同一正式 Tauri Release 的真实跨模块证据。

## 下一步 WIP（最多两个）

1. 员工05/01：完成统一 retrieval projection 到 SQLite 事实源和 FTS/Vector/Relation 失效重建的联调。
2. 员工03/04：在统一 Release 中复核来源/排除项和 FocusedContext 三态；员工02负责数据库、重启与索引一致性核对。

