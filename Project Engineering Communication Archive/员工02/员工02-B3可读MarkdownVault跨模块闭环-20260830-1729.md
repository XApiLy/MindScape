# 员工02｜B3 可读 Markdown Vault 跨模块闭环

时间：2026-08-30 17:29（Asia/Shanghai）  
覆盖任务：`B3` / DATA-M2 Markdown Vault、修订、EvidenceRef、Wikilink、讨论日志与恢复  
状态：**B3 员工02实现阻断已关闭；待员工01领域/事务评审及 B5 同一 clean Release 人工复验，M2 仍为 No-Go**

## 已阅读与落实

- 已重新阅读控制 PEC 顶部“2026-08-30：M2 Go 阻断收敛”和“员工直接执行表”，并逐份读取员工01、03、04、05、06当前 PEC；本轮只关闭 B3，不重复汇报已通过的原文导入或候选查询局部切片，也不发布 dirty Release。
- 已复核《M2 本地知识与聚焦连续性 Alpha 派单》《Markdown 知识库、Obsidian 互操作与分支记忆隔离》《M2 契约基线》；据此把“可读 Markdown”落实为实体、来源、会话/项目讨论日志和索引的真实文件，而不是 frontmatter/H1 空壳。
- Rust 实现继续使用显式 `KernelResult`、SQLite transaction、稳定修订和恢复测试；Tauri command 只穿过 `KernelService`，前端不直接接触数据库或 Vault 路径。
- 未发现与员工01领域门禁冲突；需要员工01评审的新边界是 DiscussionLog v1、SQLite v14 迁移和 SQLite/Vault 补偿回滚策略。

## 本次关闭的阻断

### B3：员工02实现阻断已关闭

- 新增版本化 `DiscussionLog` / `DiscussionLogProjection`，同时支持 workspace 下的 conversation scope 和 project scope，强制 EvidenceRef、稳定 ID、单调 revision 与安全 Vault 相对路径。
- SQLite schema 升至 v14，持久化讨论日志完整投影和修订历史，提供会话/项目/全局最新日志查询；迁移、重启和旧版本回滚测试保持通过。
- Markdown Vault 现在实际写入：
  - `entities/<entity-id>.md`：完整状态、作用域、别名、关系 Wikilink、EvidenceRef Wikilink；
  - `sources/<evidence-id>.md`：目标 URI、hash、excerpt 与可追溯 frontmatter；
  - `logs/discussions/<log-id>.md`：可编辑正文、会话/项目作用域、关联实体及来源 Wikilink；
  - `indexes/entities.md`、`indexes/discussions.md`：数据库权威状态生成的可读索引。
- 外部讨论日志编辑经受控 marker、稳定 ID、1 MiB 限制和内容 hash 校验回流为下一修订；旧 revision 在写文件前拒绝，不覆盖当前 Markdown。
- 文件替换使用 `.next/.previous`、flush/sync 和启动恢复；SQLite 落库失败时恢复上一日志修订或删除首次写入，避免 Vault 孤儿文件。启动时同时恢复中断替换，并按 SQLite 权威实体集合清理已删除实体文件。
- 否决实体会同步重投影 Markdown，并从 FTS/Vector 检索中失效；删除通过统一数据+Vault 服务入口删除实体、派生索引、实体文件并重建全局实体索引。
- 已增加 typed Tauri commands 与 TypeScript client：讨论日志投影、单条查询、会话列表、项目列表和外部编辑导入。

## 跨模块证据

- `application::tests::readable_vault_round_trips_evidence_discussion_edits_rejection_and_restart` 串联真实 SQLite 文件、EvidenceRef、Confirmed Entity、实体/来源 Markdown、会话/项目讨论日志、外部编辑回流、旧修订冲突、外键落库失败补偿、Rejected 检索失效、进程重开恢复和最终删除；不是单适配器 mock。
- `adapters::sqlite::tests::markdown_revision_conflict_rolls_back_entity_and_derived_indexes` 已按员工01请求补成直接断言：冲突后 Entity revision/name、FTS 命中、Vector snapshot、MarkdownProjection 历史均未改变，并在关闭/重开 SQLite 后再次核对。
- Vault 适配器测试直接读取生成文件，核对实体关系 Wikilink、EvidenceRef source page、discussion managed boundary 及 `.previous` 中断恢复。
- Rust 全量 `175/175`；Clippy `--all-targets --all-features --locked -D warnings`、fmt 通过。
- TypeScript/Vite production build 通过（2096 modules）；Chat `57/57`、Canvas `34/34`、Canvas 性能 `2/2`。

## 契约、数据、安全与恢复变化

- 新契约：`mindscape.discussion-log.v1`、`mindscape.discussion-log-projection.v1`；新 SQLite schema version `14`。员工01需评审 scope、revision 和补偿事务边界；员工03/04如消费日志只使用 typed IPC，不读取磁盘。
- 所有 Vault ID 先做路径穿越校验；外部编辑有大小、frontmatter ID、H1 和 managed boundary 校验；来源页不包含 API Key、Authorization 或 Provider 原始响应。
- SQLite 与文件系统无法组成单一物理事务，因此采用 revision preflight、进程内投影锁、原子替换、失败补偿和启动收敛。断电发生在文件系统与 SQLite 提交之间仍需 B5 在真实程序上执行故障注入复验，不能把自动化测试写成 clean Release 验收。

## 仍剩阻断 / 需要配合

1. **B3 评审与 B5 现场证据**：员工01评审 DiscussionLog/事务边界；员工05在唯一 clean Release 的同一真实样本中，用 Obsidian 或普通编辑器复验会话/项目日志、来源链接、外部编辑、冲突、否决、删除、断电/重启与索引一致性。本轮没有 Build ID、程序路径或 SHA-256，不声明 M2 Go。
2. **B1/B2/B4/B5**：B1 仍由员工01/05冻结 clean commit 和唯一 Release；B2 仍由员工01/03/04完成正式确认/否决/回流动作；B4 的真实语义 Embedding 质量、模型标识和故障回退仍由员工05关闭，员工02的持久化/重建/失效接口可参与联调；B5 尚未形成同一 clean 样本矩阵。
3. **B6 未开始**：按执行顺序不抢占 B1～B5；只有员工01确认 B3 评审通过且 WIP 腾出后，员工02才进入受控用户字体目录、校验、复制/删除/恢复和 typed IPC。若影响 Go 时间，请创始人决定移至 M3。

## 下一步 WIP（最多两个）

1. 配合员工01完成 B3 领域/事务评审，并修正评审发现的阻断问题。
2. 向员工05的 B5 同一 clean Release 矩阵提供 Vault 路径、外部编辑、故障恢复和重启核验步骤；不自行发布第二个候选。
