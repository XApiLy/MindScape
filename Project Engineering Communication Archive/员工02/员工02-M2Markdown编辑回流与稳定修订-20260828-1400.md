# 员工02｜M2 Markdown 编辑回流与稳定修订

时间：2026-08-28 14:00（Asia/Shanghai）  
覆盖任务：DATA-M2-004 / DATA-M2-005 / DATA-M2-010  
状态：代码闭环完成；等待统一 Release 真实样本

## 已阅读与落实

- 已阅读员工01 Markdown 修订契约与迁移复核、员工03/04 Markdown 阅读接入、员工05持久向量与 Release 报告及 PEC 控制文件。
- 本轮按员工01门禁改用 `MarkdownProjection::next_revision()`；Projection ID 跨修订保持稳定，Entity revision 不允许倒退。
- 使用 `rust-best-practices` 与 `tauri-development`：借用输入、`KernelResult` 错误传播、Tauri IPC 二次校验、安全路径和针对错误行为的回归测试。

## 本次完成

- 新增显式 `import_markdown_entity_edit` Tauri IPC 与 typed `kernelClient` 调用。
- 从已知 `vault/entities/<stable-id>.md` 读取用户编辑，不接受任意外部路径；限制 1 MiB。
- 校验 frontmatter `mindscapeId` 必须与文件稳定 ID 一致，拒绝路径穿越、ID 篡改、缺失一级标题和非法 UTF-8。
- 内容 hash 未变化时返回 `changed=false`，不制造空 revision。
- 内容变化时：Entity revision +1、generator 标记为 `vault-edit`、写回 SQLite；同一事务钩子同步更新/移除 FTS 与持久 VectorIndex。
- MarkdownProjection 通过领域 `next_revision()` 产生新历史修订，保留稳定 projection ID、旧 revision 和新内容 hash。
- schema 升至 v13，将 MarkdownProjection 主键改为 `(id, projection_revision)`，修复 v11 单列 ID 实际无法保存第二版的问题；v10/v12 回填夹具同步适配。

## 验证

- Rust 全量：147/147 通过。
- Clippy `--all-targets --all-features --locked -D warnings`：通过。
- TypeScript/Vite production build：2092 modules，通过。
- Secret scan：1548 个文本文件通过。
- 新增覆盖：编辑标题/hash、稳定 ID 篡改、路径安全、重复内容不生成 revision、迁移回填兼容。

## 数据、安全、恢复与协作边界

- Markdown 仍是可重建投影，不覆盖导入原文，也不成为唯一事件账本。
- 用户编辑通过显式命令进入 SQLite 事实源；Entity/FTS/Vector 使用相同状态与 revision 边界。
- API Key、Authorization、Provider 响应、完整 reasoning 和向量正文不进入 Vault 或 Projection。
- 员工05当前 dirty Release 早于本轮 v13 编辑回流，不能作为本功能验收证据；需要发布新的统一 Build ID。

## 下一步 WIP（最多两个）

1. 生成包含 v13 的统一 Tauri Release，验证实体投影→手工编辑→显式回流→检索命中新 revision→重启一致性。
2. 与员工01/03/04/05完成同一 Build 的 Confirmed、Rejected/delete、Vector fallback、omitted、分支 scope 和 Markdown revision 证据复核。

