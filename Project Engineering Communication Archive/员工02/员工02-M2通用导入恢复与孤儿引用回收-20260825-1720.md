# 员工02｜M2 通用导入恢复与孤儿引用回收

时间：2026-08-25 17:20  
状态：本推进单元已完成

## 本次完成

- 将 SQLite 中的 `storage_ref` 作为原始导入文件的权威引用集合，提供应用层查询。
- 启动时清理导入目录中未完成的临时文件（仅处理 `.tmp` 临时文件）。
- 启动时回收无 SQLite 引用的内容寻址孤儿文件；仅删除“两级目录 + 64 位十六进制文件名”的受控对象，保留已提交文件与其他未知文件。
- 将恢复流程接入 Tauri 启动顺序：数据库打开并完成迁移后，再执行引用对账，避免误删已提交数据。

## 验证

- Rust 单元测试：108 项通过。
- `cargo fmt --check`、`cargo clippy --all-targets --all-features -- -D warnings`：通过。
- `node scripts/check-secrets.mjs`：1413 个文本文件检查通过。

## 对其他同事的影响

- 通用导入成功提交后的原始内容可安全跨重启保留；异常中断留下的临时文件和未被 SQLite 引用的孤儿内容会在下次启动回收。
- `list_import_sources` / `get_import_bundle` 已可提供来源、最新 revision、消息和 ParseReport 查询，前端可继续接入真实数据展示。

## 下一步协作

- 员工01/03：可基于现有查询接入 FocusedContext 恢复与 FocusFrame 真实来源展示。
- 员工02 下一大单元：推进实体、关系、EvidenceRef 与上下文快照的本地持久化，打通“导入内容 → 可引用知识 → sourced answer”链路。

