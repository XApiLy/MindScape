# 员工02｜M2 导入预检与父子事务落库

时间：2026-08-25 00:12（Asia/Shanghai）  
状态：已完成第四刀，等待通用导入命令接线

## 已阅读与落实

- 已阅读最新 PEC 控制文件、员工01《M2 导入 Bundle 语义校验与上下文边界》、员工03《M2 知识上下文画布投影》、员工04《M2 占位 UI 交接标记》、员工05《M2 推理终态与用量回归矩阵》，以及 M2 派单、M2 契约、本地数据布局、RAG/Vault 方案。
- 员工01已提供唯一领域预检 `validate_import_bundle`；本次在 SQLite 事务入口消费该规则，不复制校验逻辑。
- 员工01明确 batch 可子先父后；本次通过事务内父先子拓扑排序满足 SQLite 即时外键约束。

## 本次完成

- `persist_import_bundle` 现在先执行统一 bundle 预检，再开始数据库事务。
- 支持输入顺序任意的 ImportedMessage，落库时自动父先子；校验仍拒绝缺父、自引用、环路、重复 ID/locator 和计数不一致。
- 新增重复内容 hash 回归：第二个 bundle 整体失败，第一份 source/revision/message 数据保持不变，不产生半条记录。
- 新增父子顺序落库回归：子消息先出现在输入数组时仍可成功提交。

## 验证

- `cargo test --offline`：84 passed, 0 failed。
- `cargo clippy --all-targets --all-features --offline -- -D warnings`：通过。
- `cargo fmt`：通过。

## 契约、安全与恢复影响

- 领域校验成为导入命令与 SQLite 共同入口，避免命令层/存储层语义漂移。
- 事务前预检阻止非法 bundle；事务内外键和唯一 hash 约束阻止悬挂、重复和半提交。
- 未读取或写入真实会话正文；`E:\AI-Data` 样本只读扫描，未复制、未上传、未调用 Provider。

## 需要协作与下一步（最多两个 WIP）

- **员工01**：请复核父先子排序与 `validate_import_bundle` 的消费位置；当前未改变契约字段。
- **员工04**：请确认通用导入 Tauri 命令的安全错误/ParseReport DTO；命令层接通后即可开启 Intake 真实预览。
- WIP1：实现通用导入命令的 raw 原子保存 + bundle 事务提交 + 失败清理/可重试返回。
- WIP2：补充 ParseReport/source 查询 IPC，供 Intake 和 Canvas 只读消费。

