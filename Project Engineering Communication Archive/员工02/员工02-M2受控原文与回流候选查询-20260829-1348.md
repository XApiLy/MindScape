# 员工02｜M2 受控原文与回流候选查询

时间：2026-08-29 13:48（Asia/Shanghai）  
覆盖任务：`IMP-M2-004～008` / `DATA-M2-001～010` / `ARC-M2-002` 联调  
状态：员工03、04提出的两项 typed IPC 数据阻塞已解除；等待前端消费和统一 Release 纵向验收

## 已阅读与落实

- 已读取 PEC 控制文件及员工01～06当前报告，重点复核员工03《M2 阻塞与跨员工协作清单》和员工04《M2 导入 Markdown 双预览与复制分层》。
- 员工04要求原文字节只能通过受控 typed IPC 读取；实现因此只接受 SQLite 已登记的 `sourceId`，不接受任意路径，也不由前端拼接 `storageRef`。
- 员工03要求回流候选由内核提供；实现直接从已持久化的 FocusFrame 恢复 `FocusPromotionCandidateSet`，前端无需从 `promoteRefs` 自行合成。
- 按 `rust-best-practices` 与 `tauri-development` 保持领域校验、应用查询、存储适配器和 Tauri IPC 分层；本轮不改变知识确认状态机或自动写回主线。

## 相比上一份 PEC 的变化

- 上一份已关闭 Markdown Entity/FTS/Vector/Projection 原子事务并发布 dirty 预验收程序；本轮继续收口两条跨员工读取阻塞，没有重发 Release。
- 新增 `get_raw_import_content`：先由 SQLite `ImportSource` 解析可信 `storageRef/contentHash`，再读取内容寻址原文；返回内容、完整字节数、哈希和截断状态。
- 新增 `get_focus_promotion_candidates`：按稳定 FocusFrame ID 查询，主线或未声明候选返回空；可携带 `expectedMemoryVersion`，版本过期返回完整性冲突。

## 本次完成

- `ImportStorage::read_verified_text` 严格校验 `<hash前两位>/<完整SHA-256>` 路径形状、存储尺寸上限、完整内容哈希和 UTF-8；预览上限 1 MiB，并只在字符边界截断。
- 原文读取接口不返回路径，不访问未登记文件；篡改、越界引用、非 UTF-8、超限和 I/O 失败均走结构化命令错误。
- Rust、TypeScript 和 `kernelClient` 已形成 `RawImportContentProjection` typed 链路。
- `FocusPromotionCandidateSet` 已接入应用查询、Tauri command、TypeScript 类型和客户端；候选从 SQLite 中已持久化的 FocusFrame 派生，关闭重启后保持相同 source frame、conversation、branch kind、memory version 和 refs。
- 新增原文 UTF-8 截断、路径越界、内容篡改、读取超限测试，以及回流候选重启恢复、主线/空候选、过期 memory version 测试。

## 验证证据

- Rust 全量：166/166 通过。
- Clippy：`--all-targets --all-features -- -D warnings` 通过；`cargo fmt --all -- --check` 通过。
- TypeScript/Vite production build：2094 modules，通过。
- Chat：50/50；Canvas：34/34；Canvas 性能：2/2。
- 本轮未生成新 Tauri Release；此前 Build 早于本轮接口，只能继续作为旧切片预验收，不能证明本轮 UI 或 M2 Go。

## 契约、数据、安全与恢复边界

- 不新增 SQLite schema：原文的可信定位来自既有 ImportSource；回流集合来自既有 FocusFrame JSON，均可在关闭重启后恢复。
- 原文接口只读且有界，不返回本地路径，不执行 Markdown/HTML，不泄露 Key、Authorization、Provider 响应、reasoning 或向量正文。
- 回流候选仅表示“等待用户确认”；主线和空声明诚实返回 `null`，过期 memory version 不静默降级，任何确认、否决或知识修订仍必须经过内核状态机。
- 共享 source tree 仍为 dirty；本轮测试通过不等于统一 Release 或 M2 Go。

## 需要配合

- 员工04：消费 `getRawImportContent(sourceId)` 完成受控源文件原样预览；根据 `truncated/byteLength` 明示 1 MiB 预览边界，不尝试读取任意路径。
- 员工03/04：消费 `getFocusPromotionCandidates(focusFrameId, expectedMemoryVersion)`，将 `null` 作为主线/无候选空态，将完整性冲突作为过期态，只展示“待确认”。
- 员工01：复核回流候选查询的主线/空集合/过期版本语义及上一轮 Markdown 原子事务关闭证据。

## 下一步 WIP（最多两个）

1. 配合员工03/04完成上述两个 typed IPC 的 UI loading/empty/error/restart 联调，并补同一真实样本证据。
2. 准备统一候选中的 Vault 编辑、候选回流、删除/否决、索引降级重建与关闭重启数据样本；代码冻结后再由批准脚本发布唯一 Release。
