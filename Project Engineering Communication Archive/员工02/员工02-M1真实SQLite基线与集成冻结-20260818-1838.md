# 员工02｜M1真实SQLite基线与集成冻结

> 提交时间：2026-08-18 18:38（UTC+8）  
> 覆盖任务：DATA-014/015、M1恢复与数据一致性收口  
> 状态：真实completed与用量落库基线通过；取消、强退、重启和视口仍为No-Go

## 相比上一份的变化

- 从自动测试转入真实Windows数据验收，以只读方式检查首次DeepSeek运行产生的正式Tauri数据库。
- 获得两次真实`completed`运行的脱敏落库基线：无永久pending、重复幂等键、重复序列、孤儿运行或外键违规。
- 确认两个真实运行各有`usage_updated`事件，两个completed终态均携带非空用量；界面与重启投影尚未验收。
- 按集成收口要求补齐`target-fixed/`和本地DeepSeek调试EXE禁提交规则，保留本地产物供窗口验收。

## 已阅读与落实

- 已完整阅读`docs/engineering/m1-pec-review-and-closeout-20260818.md`、PEC控制文件及员工01～06最新快照；执行重点由新增功能改为真实恢复和数据库一致性验收。
- 已补读本地数据布局与安全凭据边界：数据库只读采集不读取消息正文、请求JSON、事件正文、Key或本机用户名路径。
- 员工01已签署幂等比较与单进程准备锁；员工03已正式消费schema v5并完成13项画布测试，旧“等待接口”阻塞已解除。
- Review Lab不是领域事实源；真实SQLite证据独立保存，后续涉及强退/原生恢复的可视证据才使用`full`。

## 本次真实验收用例与证据

- 用例：首次真实DeepSeek completed运行的SQLite前置基线。
- 证据：`docs/engineering/evidence/employee02/m1-recovery-20260818/01-live-sqlite-baseline.md`。
- 数据库：schema v5，`integrity_check=ok`，外键违规0；1会话、2节点、2快照、2运行、24运行事件。
- 状态：ModelRun与Node均为`completed: 2`；pending/streaming候选0；重复幂等键0；重复序列0；孤儿运行0。
- 事件：`started: 2`、`text_delta: 18`、`usage_updated: 2`、`completed: 2`；终态用量对象存在且至少一个字段非空。
- 视口：真实表记录为0，因此真实双会话视口恢复未通过。

## 契约、数据、安全与恢复影响

- 未修改Frozen V1、SQLite schema v5、Provider或产品运行代码；只增加仓库忽略规则和脱敏验收证据。
- 数据库未复制进仓库；证据只含聚合计数、完整性结果、文件大小和校验哈希。
- 更正一次采集判断：初查按camelCase事件名统计导致误判无用量；按实际snake_case判别器复核后确认两条`usage_updated`，更正过程已明确记录。
- 本次尚未执行强退，未改变用户真实运行或凭据状态。

## 验证结果

- 真实SQLite只读检查：完整性、外键、终态唯一性、幂等、序列与孤儿检查通过。
- 用量事件与completed终态用量落库：通过；UI和重启投影：未验收。
- `git check-ignore`确认`desktop/MindScape-DeepSeek-Fixed.exe`与`desktop/src-tauri/target-fixed/`均被忽略且未跟踪。
- `.gitignore`的`git diff --check`通过；PEC七文件留存检查在报告替换后执行。

## 风险与剩余No-Go

- 真实取消、终态后无delta、部分内容保留仍未执行。
- 流式中强退、启动扫描、`application_interrupted`和历史无损恢复仍未执行，需要创始人在Windows窗口配合强退动作。
- 真实视口表当前为0；员工03需完成会话A/B移动、切换、关闭与重启。
- 当前仍位于混合共享分支，跨员工修改尚无可回滚集成提交；除`.gitignore`治理外未继续叠加产品代码。

## 下一步 WIP（最多两项）

1. 与员工04/05和创始人执行真实长回答停止，采集取消前后SQLite差异并签署唯一cancelled/部分内容/无后续delta。
2. 执行流式中强退和重启，采集启动扫描、application_interrupted、无永久pending及历史节点不变证据。

## 需要同事配合

- **员工01**：将本脱敏基线纳入统一M1验收表，并主持集成冻结/恢复PR，避免把共享树归于单一员工。
- **创始人、员工04/05**：使用真实Windows窗口执行长回答停止及流式中强退；Key仅留在系统凭据入口。
- **员工03**：完成真实双会话视口操作后通知员工02复查`canvas_viewports`唯一最终值。
- **员工06**：强退/恢复窗口证据可用后创建`full`视觉引用，但不得替代SQLite门禁。
