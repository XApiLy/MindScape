# 员工02｜M2 知识查询 IPC 与验收接线

时间：2026-08-26 09:30（Asia/Shanghai）  
状态：本推进单元完成，进入真实 Release 联调

## 已阅读与落实

- 已阅读最新员工01/03/04/05 PEC 及 M2 验收门禁。
- 按员工05索引边界，IPC 只传递结构化领域对象，不在前端或存储层重算检索排序。

## 本次完成

- 新增 Entity、Relation、EvidenceRef 的应用层 upsert/query 接口。
- 新增对应 Tauri commands：`upsert_knowledge_entity`、`list_knowledge_entities`、`upsert_knowledge_relation`、`list_knowledge_relations`、`upsert_evidence_ref`。
- `kernelClient` 新增同名 typed IPC 客户端，前端可消费真实 SQLite 数据。
- 保持会话作用域、稳定 ID、版本化 JSON 和确定性排序；空作用域/空 ID 在数据层拒绝。

## 验证

- Rust Clippy：通过。
- Chat 测试：39/39 通过。
- TypeScript/Vite build：1831 modules，通过。

## 当前 M2 验收状态

- 代码侧：导入、恢复、FocusFrame、FocusedContext、知识对象表和统一查询 IPC 已具备。
- 真实验收侧：仍需在同一个正式 Tauri Release 完成导入→实体/证据→检索→删除/否决失效→关闭重启查询，并保存 Build ID、SHA-256 和证据。

## 下一步 WIP（最多两个）

1. 员工05：接入 FTS/VectorIndex/Relation 查询与失效重建，返回统一 evidence/scope DTO。
2. 员工01/03/04：使用同一 Release 复核 FocusedContext 三态、来源展示和分支隔离；员工02负责 SQLite 对账与重启证据。

