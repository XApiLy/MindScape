# 员工02｜M1模型运行幂等并发收口

> 提交时间：2026-08-18 17:18（UTC+8）  
> 覆盖任务：CORE-014、DATA-014/015  
> 状态：幂等载荷与同进程并发准备不变量完成；等待真实取消/恢复矩阵

## 相比上一份的变化

- 同一幂等键只允许重放完全相同的运行输入；会话、父节点、分支、标题、提示词、Provider、Model、能力或预算任一变化都会安全拒绝。
- 模型运行的“幂等预查—Node创建—Run创建”进入所有`KernelService`克隆共享的准备锁，两个并发启动不再产生孤儿节点。
- 增加顺序异载荷和双线程同载荷测试，分别证明拒绝无副作用、并发只产生一个Node/Run并返回相同稳定ID。

## 已阅读与落实

- 开工前已阅读PEC控制文件及员工01～06全部当前报告，重点落实员工01对幂等键载荷一致性的下一步要求。
- 员工05已取得真实DeepSeek连接、首次生成和completed终态证据，因此本轮不重复Provider工作，转而收紧真实请求可能触发的数据一致性边界。
- 员工03的视口接入仍待推进，但正式schema v5与命令已交付；本轮不跨所有权直接修改画布UI。
- 未触碰导入、多Provider、Harness、Skill/MCP或整体视觉重构，继续服从M1 Real Chat Alpha唯一WIP。

## 本次完成与证据

- `desktop/src-tauri/src/application/mod.rs`：新增共享运行准备锁和`validate_idempotent_replay`，完整比较执行输入且不暴露原请求正文。
- 新增`idempotency_key_rejects_a_different_request_without_creating_an_orphan_node`：相同请求复用稳定ID，异载荷拒绝后仍仅一个节点和运行。
- 新增`concurrent_idempotent_preparation_creates_exactly_one_node_and_run`：Barrier同步双线程启动，验证相同Node ID、Run ID及数据库唯一事实。

## 契约、数据和安全影响

- 未修改Frozen V1字段、事件、终态或SQLite schema v5；这是既有`idempotencyKey`语义的确定性收紧。
- 拒绝错误只说明幂等键已用于不同请求，不回显旧/新提示词、上下文或敏感Provider数据。
- 准备锁只覆盖本地快速编译和事务写入，不覆盖网络流式阶段，不阻塞已启动运行的并发执行与取消。

## 验证结果

- `cargo test --offline --no-fail-fast`：44/44通过。
- `cargo clippy --offline --all-targets --all-features -- -D warnings`：通过。
- `pnpm test:chat`：14/14通过；`pnpm test:canvas`：9/9通过。
- `pnpm build`：通过，生产构建1823个模块。
- 修改文件已执行Rustfmt；PEC留存与`git diff --check`在报告替换后执行。

## 风险与阻塞

- 当前锁保证单个桌面进程内的并发幂等；未来若引入多进程写库，应下沉为SQLite单事务准备，但不属于当前M1桌面进程模型。
- 真实DeepSeek停止、超时、异常断流、用量和应用重启恢复仍未完成，M1 Alpha尚不可放行。
- `git mv`已按制度尝试，但当前PEC源文件尚未被Git跟踪，故使用已校验目标路径的安全移动归档；提交时需确认rename/add状态。

## 下一步 WIP（最多两项）

1. 配合员工03完成schema v5视口的多会话隔离、节流保存与重启恢复验收。
2. 配合员工04/05执行真实DeepSeek取消、部分内容、用量落库和异常退出恢复矩阵。

## 需要同事配合

- **员工01**：请复核幂等载荷比较范围与单进程准备锁符合Frozen V1语义。
- **员工03**：请立即消费已交付的`saveCanvasViewport/getCanvasViewport`完成CAN-014。
- **员工04/05**：长回答取消联调时请复用同一幂等键执行重放/异载荷边界验证，并检查无重复节点。
- **员工06**：取消、失败和恢复视觉规格不得把重复请求呈现为新的领域运行。
