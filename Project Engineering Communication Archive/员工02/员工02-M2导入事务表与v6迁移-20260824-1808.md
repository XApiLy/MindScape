# 员工02｜M2 导入事务表与 v6 迁移

时间：2026-08-24 18:08（Asia/Shanghai）  
状态：已完成第二刀，等待导入命令联调

## 已阅读与落实

- 已阅读当前 PEC 控制文件、员工01 M2 契约与 FocusFrame 首切片、员工03来源投影、员工04 Intake、员工05 Provider 能力目录，以及 M2 派单、本地数据布局、M2 本地实体/RAG与 Markdown Vault 方案。
- 员工01 已冻结 `ImportSource`、`ImportRevision`、`ImportedMessage`、`ParseReport` 权威字段；本次按契约落 SQLite v6 表，不反向改变领域语义。
- 员工04明确前端不直接读文件/SQLite；因此本次只提供持久化底座，后续通过结构化 IPC 暴露结果。

## 本次完成

- `SCHEMA_VERSION` 从 5 加法升级到 6，保留 v1～v5 迁移不变。
- 新增 `import_sources`：来源、会话归属、内容指纹、不可变 `storage_ref`，`content_hash UNIQUE` 保证重复导入幂等基础。
- 新增 `import_revisions`：适配器与版本、解析状态、来源外键；历史解析不覆盖。
- 新增 `imported_messages`：角色、ContentBlock JSON、发生时间、稳定 source locator、父消息和平台扩展 JSON。
- 新增 `parse_reports`：计数、字段恢复、warnings/errors JSON，与 revision 一对一并受外键约束。
- 所有新表在独立事务中迁移，SQLite 外键删除链保持来源删除时清理其 revision/message/report。

## 验证

- `cargo fmt --manifest-path desktop/src-tauri/Cargo.toml`：通过。
- `cargo test --manifest-path desktop/src-tauri/Cargo.toml --offline`：61 passed, 0 failed。
- 上一刀内容寻址测试仍全部通过；M1 Provider、取消、恢复测试未退化。

## 契约、安全与恢复影响

- 这是 SQLite v6 纯加法迁移，未修改 M1 表字段或 Provider/凭据边界。
- 原文正文仍只存内容寻址目录；数据库仅存 hash/ref 与结构化解析结果，不写 API Key、完整推理或秘密。
- `content_hash` 唯一约束与 revision 历史保留支持重复检测、失败重试和进程重启恢复；真正的状态写入事务命令尚未接入。

## 需要协作与下一步（最多两个 WIP）

- **员工01**：请评审 v6 表字段与 `m2-contract-baseline.md` 是否一一对应；如契约字段无变更，我将继续接入事务写入服务。
- **员工04**：待导入命令完成后消费结构化 `ParseReport`，不直接访问文件或 SQLite。
- WIP1：实现 `ImportSource`/`ImportRevision`/`ParseReport` 的事务写入 API，覆盖失败回滚与重启后可重试。
- WIP2：实现 `ImportedMessage` 批量写入与 source locator 唯一性/重复导入回归测试。

