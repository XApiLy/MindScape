# 员工02｜B2/B3 事务边界评审阻断关闭

> 提交时间：2026-08-31 17:28（UTC+8）  
> 覆盖任务：B2/B3-REV-01、B3-REV-02、B5 脱敏持久化审计交接  
> 状态：**员工02已关闭两项最终评审实现阻断，等待员工01复评；B1/B5仍未关闭，M2仍为 No-Go**  
> 基线：共享 dirty 集成工作树

## 相比上一份的变化

- 响应员工01 16:54 PEC，不再把既有 B2 局部测试通过写成最终关闭；本轮直接修复 Delete 后存活实体 Vault 页悬空 Wikilink，以及 DiscussionLog 来源页/索引不在同一恢复边界的问题。
- 新增可在 B5 唯一 clean Release 使用的只读脱敏审计程序与操作说明；审计程序现会检测旧 schema、四动作缺失、实体/Evidence/FTS/Vector/Relation/Vault 不一致、Delete 悬空链接及两类未清理 journal。
- 本轮无可见 UI 变化，因此不发布 Review Lab preview；也未生成个人 Acceptance Build、Build ID、EXE 或 SHA-256。

## 已阅读与落实

- 重新阅读控制 PEC 顶部“2026-08-30：M2 Go 阻断收敛”“员工直接执行表”、17:28 现场复审和员工01《B2/B3 最终集成评审阻断》。实现范围严格限制为 `B2/B3-REV-01`、`B3-REV-02` 与 B5 数据核对，不启动 B6。
- 按 Rust 最佳实践继续使用显式 `Result`、不可变 commit identity 和无生产 `unwrap/expect` 的恢复路径；按 Tauri 分层约束让启动阶段在状态注入前完成 journal 恢复与 SQLite 权威索引收敛。
- 接受员工01指出的“现有全绿断言范围不足”：新增测试直接比较字节级 pre-image、SQLite relation、存活 Markdown 内容、journal 目录和重启索引，不再只判断文件存在。

## 本轮关闭的 B 编号

### B2/B3-REV-01：Delete 邻接实体悬空 Wikilink 阻断已关闭

- Delete 首次执行会从删除前关系集中找出全部存活邻居，把邻居实体页、相关 Evidence 来源页、被删实体页和全局实体索引一起纳入同一个 focus decision journal。
- Vault 使用删除后的 relation 集合重投影邻居，再由 SQLite 原子事务删除 relation、实体、FTS/Vector；SQLite 冲突时所有纳入 journal 的文件按字节恢复。
- 已提交 Delete 的同 ID 重放会依据 SQLite 当前实体/关系重新投影该 conversation 的存活实体，能修复旧版本遗留的悬空 managed Wikilink；重启后重复执行仍保持一致。
- B5 审计新增 `deletedCandidateVaultLinks`，Delete 只有在 relation 行为零、存活实体页不再含目标 managed link 时才通过。

### B3-REV-02：DiscussionLog EvidenceRef/索引恢复阻断已关闭

- 新增独立 `.discussion-transactions` revision journal；commit identity 为 `discussionLogId + revision`，覆盖日志文件、所有 EvidenceRef 来源页和 `indexes/discussions.md` 的 pre-image。
- SQLite 外键/修订写入失败时，新日志和新来源页被删除，复用来源页与索引按原始字节恢复，不再以重新渲染旧领域对象代替文件补偿。
- SQLite 已提交但索引写入失败或进程中断时保留 journal；启动阶段读取 SQLite 最新 DiscussionLog revisions，重建讨论索引后才清理已提交 journal。未提交 journal 则反向恢复全部文件。
- 启动恢复即使没有遗留 journal，也会按 SQLite 当前 projections 重建讨论索引，确保 SQLite 是结构化权威、Markdown 是可恢复投影。

### B5：员工02脱敏审计交接子项已关闭（B5整体未关闭）

- 新增 `desktop/src-tauri/examples/b5_focus_promotion_audit.rs`：SQLite 以 read-only/query-only 打开，输出仅含计数、状态和 ID 的 SHA-256 前 12 位，不输出正文、完整 ID、EvidenceRef ID、Key 或真实路径。
- 新增 `docs/engineering/evidence/employee02/m2-b2-persistence-audit/README.md`：明确四动作、Reopen、进程重启、故障注入、退出码与 `violations=[]` 通过条件；真实用户数据只允许只读审计，破坏性故障只在隔离副本/自动化夹具执行。
- 对本机现有 app-data 只读试跑得到 schema v14、无 v16 decision table/四动作样本；工具正确输出结构化 violations 并以非零退出。该结果只证明旧库阻断识别有效，不作为 B5 证据，也未触发迁移或写入。

## 跨模块证据

1. `focus_promotion_delete_reprojects_surviving_relation_neighbors_on_replay_and_restart`：正式 application service → SQLite relation 删除 → Vault 被删文件/存活邻居/Evidence → 同 ID 重放 → KernelService 重启，直接断言存活页不含被删 ID。
2. `focus_delete_journal_restores_neighbors_and_committed_recovery_removes_stale_links`：Vault 删除计划覆盖邻居和来源页；模拟 SQLite 冲突后四类文件字节恢复，模拟已提交重启后被删文件、索引与存活链接保持终态。
3. `discussion_log_sqlite_failure_restores_new_and_reused_evidence_pages`：正式 application service 写 Vault 后触发 SQLite 外键失败；新来源无孤儿、复用来源字节不变、日志不存在、索引字节不变、journal 清空。
4. `discussion_log_committed_index_failure_converges_on_restart`：注入索引写失败，证明 SQLite revision 已提交且 journal 保留；重启服务后按 SQLite 重建索引并清理 journal，第二次恢复幂等为零。
5. `discussion_journal_restores_uncommitted_files_and_finishes_committed_index`：同一 revision journal 分别模拟提交前和提交后进程中断；前者恢复日志/来源/索引 pre-image，后者保留新日志/来源并生成已提交 revision 索引。
6. B5 审计跨 SQLite integrity/FK/decision/request/projection/entity/Evidence/FTS/Vector/Relation 与 Vault entity/index/journal，并对 Delete 扫描存活实体 managed Wikilink。

## 验证结果

- Rust 全量 `cargo test --all-targets --all-features --locked`：library `202 passed / 0 failed / 1 ignored`；example `2 passed / 0 failed`。ignored 仍为既有约 330 MB 固定生产语义模型下载质量用例。
- 新增评审阻断定向测试：5 项均通过；既有 focus promotion 定向回归同步通过。
- `cargo clippy --all-targets --all-features --locked -- -D warnings`、Rust fmt、`git diff --check`：通过。
- Secret scan：`1642` 个文本文件通过；PEC retention 在新报告替换后单独执行。
- 未修改 React/视觉行为，因此不重复运行或发布 UI preview；正式 B5 仍必须使用唯一 clean Tauri Release。

## 契约、数据、安全与恢复影响

- SQLite schema 仍为 v16，无新增领域字段；变化集中在 SQLite/Vault 跨介质投影的 journal 覆盖范围和启动收敛顺序。
- Discussion journal manifest 只保存稳定日志 ID、revision、受控相对路径与本地备份文件名；恢复路径只允许 `entities/`、`sources/`、`indexes/`、`logs/discussions/`，备份名只接受数字加 `.bak`。
- journal 位于 Vault 内部受控目录；正常提交/补偿后清理，崩溃后由启动流程按 SQLite commit identity 决定保留或回滚。
- B5 审计只读；本轮没有迁移或复制用户真实 app-data，没有读取/输出正文、Key 或完整稳定 ID。

## 剩余阻断

1. **B2/B3最终编号仍待员工01复评签字**：员工02已关闭 `REV-01/REV-02` 实现阻断，但不代替员工01最终事务边界评审。
2. **B1未关闭**：共享工作树仍 dirty，员工02不发布个人 Release；员工01需冻结 clean 集成范围。
3. **B5未关闭**：本机现有 app-data 仅为旧 schema v14，不是本次验收库。员工05仍需在唯一 clean Release 中执行同一真实样本四动作、Reopen/进程重启与脱敏审计。
4. **B6不抢占**：受控用户字体导入继续等待 B1～B5 收敛或创始人范围裁决。

## 需要同事配合

- **员工01**：复核两项新增跨模块断言、Discussion commit identity/启动收敛和 Delete 邻居重投影；通过后关闭 B2/B3 并纳入 B1 冻结。
- **员工05**：把 `deletedCandidateVaultLinks=0`、两类 pending journal 为零、Discussion 索引重启收敛加入 B5 同一 Release 矩阵；使用 README 的只读命令保存脱敏 JSON。
- **员工03/04**：无需重做 UI；若遇到 Discussion 索引提交后错误或决定重放，只消费 SQLite 查询终态，不另建前端事实源。

## 下一步 WIP（最多两项）

1. 配合员工01完成 `B2/B3-REV-01`、`B3-REV-02` 复评，只修复评审新增的确定性问题。
2. 配合员工05在唯一 clean Release 执行 B5 脱敏只读核对，不发布个人构建、不抢占 B6。
