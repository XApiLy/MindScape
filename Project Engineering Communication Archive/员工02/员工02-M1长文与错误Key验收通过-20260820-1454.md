# 员工02｜M1长文与错误Key验收通过

> 提交时间：2026-08-20 14:54（UTC+8）  
> 覆盖任务：DATA-015、PROV-014/016联调  
> 状态：长文正文、错误Key与数据一致性通过；网络超时/断流和clean release待关闭

## 相比上一份的变化

- 创始人使用员工05空正文修复正式Release完成真实长文复验，正文正常完整输出。
- 错误Key连接测试能正确显示安全鉴权错误，不回显Key、不静默回退Mock。
- SQLite只读复核确认最新运行唯一completed、正文3084字符、用量落库且无永久pending。

## 已阅读与落实

- 已读取PEC控制文件及员工01～06当前报告，重点消费员工05`DeepSeek空正文网关修复`的Build ID、终态映射和安全边界。
- 本轮不修改产品代码；窗口证据确认用户体验，SQLite聚合确认运行和数据事实。
- 只读取状态、计数、序列、长度与用量，不读取正文、请求JSON、事件正文、内部ID或API Key。

## 本次证据

- 证据：`docs/engineering/evidence/employee02/prov-014-long-output-auth-20260820/README.md`。
- Build ID：`20260820-142838-prov-empty-response-fix-0cb1c0053b5a-dirty`。
- SHA-256：`03B9B611511FEC24035D41F0A4F72F8F12BB3BAAD2377C0F81F7159958CEEC0A`。
- 长文：最新运行`completed`，序列1648，正文长度3084，`finishReason=stop`，input/output Token为22/1649。
- 错误Key：设置页显示Provider拒绝凭据的安全提示；测试后没有新增鉴权失败ModelRun，符合连接测试零运行副作用。
- 数据库：18 Run/18 Node，pending/streaming为0，`integrity_check=ok`，外键违规0。

## 契约、数据、安全与恢复影响

- 未修改Frozen V1、SQLite schema、Provider或凭据接口。
- 真实证据确认员工05的`thinking disabled`和空正文失败门禁不会妨碍正常长文completed。
- 错误Key不进入SQLite运行事实、不回显、不写入报告；历史运行未被连接测试改写。

## 验收结果

- DeepSeek不少于3000字的正常正文输出：通过。
- 空正文问题真实复验：通过，未再出现completed零正文。
- 错误Key分类、安全提示、不回显、无Mock降级与零ModelRun副作用：通过。
- PEC留存检查与`git diff --check`在本报告替换后执行。

## 风险与剩余No-Go

- 模型不存在、发送前断网、流式中断网、连接/首Token/空闲/总超时、异常SSE断流仍待验收。
- 日志、崩溃报告、证据与数据库导出的秘密扫描仍待最终签署。
- 当前Build来自dirty工作树，最终Go必须使用clean commit统一Release复跑关键路径。

## 下一步 WIP（最多两项）

1. 配合员工04/05完成模型不存在、断网、超时与异常断流矩阵，复核唯一终态、部分内容和无pending。
2. 执行日志/崩溃/证据脱敏扫描，并参加最终clean release数据门禁。

## 需要同事配合

- **员工01**：将长文与错误Key真实证据纳入统一验收表并签署对应门禁。
- **员工04/05**：继续网络、超时、异常断流矩阵，共用同一正式Build和脱敏证据。
- **创始人**：长文和错误Key无需重复；下一轮只配合断网或超时等不可替代操作。
