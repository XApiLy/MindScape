# 员工02｜M1取消与视口落库复核

> 提交时间：2026-08-19 15:28（UTC+8）  
> 覆盖任务：DATA-014/015、PROV-013联调、CAN-014联调  
> 状态：取消与视口数据库事实复核通过；强退/启动恢复仍为No-Go

## 相比上一份的变化

- 复核员工04/05同一正式验收证据：真实DeepSeek停止后唯一`cancelled`、无终态后delta、部分内容保留、单卡呈现和Node/Run一致均通过。
- 重新只读检查正式Tauri SQLite：数据库已有12个运行，其中9个completed、3个cancelled；无pending/streaming候选、重复幂等键或孤儿运行。
- 员工03双会话视口验收后的数据库已出现3条按会话唯一视口记录，schema v5、完整性和外键检查继续通过。

## 已阅读与落实

- 已重读`docs/engineering/m1-pec-review-and-closeout-20260818.md`、验收程序构建规则、PEC控制文件及员工01/03/04/05/06最新报告。
- 仅采信`artifacts/acceptance/versions/<build-id>/`正式release证据；员工03旧target/debug视口截图仅作为补充窗口记录，不把它当作正式Provider验收程序。
- SQLite查询使用只读URI与`query_only`，只读取状态、序列、长度、计数和视口坐标；未读取正文、请求JSON、事件正文、Key或内部ID到报告。
- Review Lab继续不作为运行或数据事实源；本轮无可判断的产品界面修改，不创建新的Review Lab版本。

## 本次真实验收与证据

- 员工05证据：`docs/engineering/evidence/employee05/prov-013-default-selection-cancel-20260819/README.md`，Build ID `20260819-150500-prov-013-mock-default-fix-0cb1c0053b5a-dirty`。
- 取消证据事实：`cancelled`运行的Node/Run一致，部分内容长度193字符，唯一终态序号104，终态后`text_delta=0`，SQLite完整性通过。当前数据库追加复核还发现另两条取消运行，3条取消运行部分内容长度分别为193、196、230，均为唯一cancelled终态。
- 当前真实SQLite聚合：schema v5；6会话、12节点、12 ModelRun、650事件；状态为`completed:9`、`cancelled:3`；pending/streaming候选0；重复幂等键0；孤儿运行0；外键违规0；`integrity_check=ok`。
- 员工03视口证据：`docs/engineering/evidence/employee03/can-014-20260818/README.md`；双会话切换、正常关闭与重启恢复通过。当前`canvas_viewports`有3条会话记录，按conversation主键隔离。

## 契约、数据、安全与恢复影响

- 未修改Frozen V1、SQLite schema v5、Provider协议或恢复代码；本轮仅复核真实事实并更新PEC。
- 取消运行未产生重复Node/Run；历史completed运行保持存在，未被取消流程改写。
- 视口只保存有限坐标和正缩放，不进入ConversationGraph语义；报告不暴露会话正文或凭据。

## 验证结果

- 取消：正式release窗口、SQLite唯一终态、无后续delta、部分内容和单卡证据通过（PROV-013）。
- 视口：双会话隔离、切换、正常关闭/重启和节点/边稳定通过（CAN-014）。
- 当前数据库：`integrity_check=ok`、外键0、无永久pending、无重复幂等键、无孤儿运行。
- 本轮无代码改动；PEC留存检查与`git diff --check`在报告替换后执行。

## 风险与剩余No-Go

- 流式中强制退出、启动扫描、`application_interrupted`收口及历史无损恢复尚未有真实证据。
- 无效Key、模型不存在、断网、连接/首Token/空闲超时、异常断流和日志/崩溃脱敏矩阵仍未关闭。
- 当前专项release来源树为dirty；M1最终Go仍需clean commit统一release Build ID。

## 下一步 WIP（最多两项）

1. 配合创始人执行流式中强退，复核重启后`application_interrupted`、无永久pending、部分内容与历史节点不变。
2. 配合员工01/04/05关闭无效Key、超时、异常断流及日志/崩溃脱敏矩阵，并将SQLite证据纳入统一验收表。

## 需要同事配合

- **创始人**：使用统一正式release执行不可替代的流式中强退和重启动作。
- **员工01**：将PROV-013、CAN-014及本次SQLite复核纳入统一M1验收表，继续主持No-Go签署。
- **员工03**：取消/失败/强退状态窗口出现后复核画布唯一节点、边和视口不跳变。
- **员工04/05**：继续提供错误、超时、异常断流及脱敏矩阵的同Build证据。
