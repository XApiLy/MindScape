# 员工02｜M2 知识实体、关系与证据表结构

时间：2026-08-25 18:25（Asia/Shanghai）  
状态：本推进单元完成表结构首切片

## 已阅读与落实

- 已阅读最新 PEC：员工01 FocusFrame 查询门禁、员工03/04 FocusFrame 消费边界、员工05 Markdown 分块索引交接，以及上一份员工02 FocusedContext 持久化报告。
- 按员工05要求，SQLite 只负责结构化持久化与作用域索引，不在存储层重算分块、排序或检索语义。

## 本次推进

- SQLite schema v9 新增 `knowledge_entities`、`knowledge_relations`、`evidence_refs` 三张表。
- Entity/Relation 以稳定 ID 为主键，保存完整版本化 JSON、conversation scope、revision 与更新时间。
- EvidenceRef 独立保存完整目标 JSON 与创建时间，支持后续 MessageBlock / ImportContent 等多态证据目标扩展。
- 为实体和关系增加会话更新时间索引，为后续统一查询 DTO、失效和重建提供数据库基础。

## 验证与边界

- Rust 测试：114 项通过。
- `cargo fmt`：通过。
- 本轮只完成迁移与表结构，尚未宣称 Entity/Relation/EvidenceRef IPC 或 RAG 查询闭环完成。
- 现有 FocusedContext、导入恢复和 FocusFrame 查询路径不变。

## 下一步协作（最多两个 WIP）

1. 员工01评审 scope、revision、EvidenceRef 外键/删除策略，确认实体与关系查询 DTO 字段。
2. 员工05提供 chunk/index provenance 到 EvidenceRef 的映射样例；随后补统一 upsert/query、失效和重建事务。

