# 员工02｜M2 知识实体、关系、证据统一落库

时间：2026-08-25 18:55（Asia/Shanghai）  
状态：数据层推进完成，进入 M2 联调验收

## 已阅读与落实

- 已阅读最新 PEC：员工01生命周期查询门禁、员工03分支继承查询、员工04 FocusedContext 投影消费、员工05 Markdown 分块索引，以及上一份员工02实体关系证据表结构报告。
- 按协作边界，存储层只保存版本化领域 JSON 与作用域元数据，不执行检索排序或分块语义。

## 本次完成

- SQLite schema v9 新增三类知识数据表后，补齐 Entity、Relation、EvidenceRef 的 upsert 数据层入口。
- Entity/Relation 支持按会话查询，按 `updated_at` 与稳定 ID 确定性排序；EvidenceRef 支持幂等更新。
- 写入前拒绝空会话作用域和空稳定 ID，保留完整 JSON 以避免多态契约字段丢失。
- 与 FocusedContext v8、导入恢复、FocusFrame 生命周期共存，迁移按版本顺序执行。

## 验证

- Rust 全量测试：114 项通过。
- `cargo fmt`：通过。
- schema 迁移和现有生命周期/导入/FocusedContext 回归均通过。

## M2 验收状态与剩余边界

- 已具备：通用导入原文、ParseReport、FocusFrame 生命周期、FocusedContext 重启查询、Entity/Relation/EvidenceRef 数据表和落库入口。
- 尚未宣称最终验收：统一查询 IPC、FTS/VectorIndex 重建、真实 Tauri Release 保存→重启→查询样本仍需联调；本 PEC 不伪造验收证据。

## 下一步 WIP（最多两个）

1. 员工01/05：确认 Entity/Relation 查询 DTO 与 chunk provenance→EvidenceRef 字段映射，完成统一查询和索引失效事务。
2. 员工03/04：在真实 Release 样本中复核 FocusedContext 三态、来源证据展示和重启恢复；员工02配合修复数据边界问题。

