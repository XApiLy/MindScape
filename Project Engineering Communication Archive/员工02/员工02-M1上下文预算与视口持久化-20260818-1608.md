# 员工02｜M1上下文预算与视口持久化

> 提交时间：2026-08-18 16:08（UTC+8）  
> 覆盖任务：CORE-014/015、DATA-006/014/015、CAN-014接口  
> 状态：可信上下文预算与CanvasViewport持久化底座完成；待真实窗口联调  
> 基线提交：`f54363e`（共享工作树；Git元数据只读，未创建Draft PR）

## 相比上一份的变化

- `start_model_run`从Provider Registry读取可信模型能力，以`contextWindowTokens - maxOutputTokens`得到输入预算；零余额或非法预算在创建Node/Run前拒绝。
- 补充预算化快照不修改历史，以及预算失败不残留pending Node/ModelRun的自动化证据。
- SQLite schema升级至V5，增加按conversation唯一保存的CanvasViewport（x、y、zoom）及稳定保存/读取命令和前端类型映射。

## 已阅读与落实

- 开工前已阅读PEC控制文件和员工01～05最新快照；报告前补读员工01的上下文预算不变量要求和员工03的共享运行快照报告。
- 仅推进M1 Real Chat Alpha，不扩展导入、多Provider、Harness、Skill/MCP或视觉范围。
- 上下文上限只信任Provider注册信息，UI不提交、不覆盖；未知上限不猜测裁剪，零可用输入预算直接拒绝。
- 应用中断仍使用Frozen V1的`failed + application_interrupted`，未增加平行终态。

## 本次完成与证据

- `desktop/src-tauri/src/adapters/provider/registry.rs`：暴露与实际运行同源的模型能力查询。
- `desktop/src-tauri/src/commands.rs`、`application/mod.rs`：统一运行接入可信预算；先编译上下文再持久化Node/Run；增加无残留回归测试。
- `desktop/src-tauri/src/domain/conversation.rs`、`adapters/sqlite.rs`：新增CanvasViewport领域输入/状态、V5迁移、按会话upsert与读取、往返更新测试。
- `desktop/src-tauri/src/lib.rs`：注册`save_canvas_viewport`和`get_canvas_viewport`命令。
- `desktop/src/domain/commands.ts`、`src/app/kernelClient.ts`：暴露同名类型安全客户端接口，员工03可直接接入。

## 契约、数据和安全影响

- Runtime/Context Frozen V1字段、事件判别器和终态语义未变；只将已冻结的可信能力真正接入运行前预算。
- SQLite schema由V4升级至V5，新增`canvas_viewports`表；conversation删除时级联清理，zoom要求大于0，坐标与缩放必须为有限数。
- 未改变凭据与网络边界；前端仍接触不到密钥，错误仍只返回安全投影。

## 验证结果

- `cargo test --offline --no-fail-fast`：41/41通过，包含预算失败零残留、预算快照历史不变、视口往返更新、启动恢复和事件幂等。
- `cargo clippy --offline --all-targets --all-features -- -D warnings`：通过。
- 本轮接口变更后的Rust代码已执行`rustfmt --edition 2024`。
- 前序同一推进单元中`pnpm test:chat` 10/10、`pnpm test:canvas` 9/9、`pnpm build`（1822模块）均通过。

## 风险与阻塞

- V5迁移需员工01复核版本策略，并由员工03完成真实窗口的按会话读取、节流保存和重启恢复验收。
- 带真实DeepSeek Key的成功、取消、厂商失败、应用中断与重启矩阵尚未完成，M1 Alpha仍不可放行。
- `.git`只读，无法执行`git mv`或创建Draft PR；旧员工02报告已安全移动到归档目录，后续暂存需确认rename识别。

## 下一步 WIP（最多两项）

1. 配合员工03接入CanvasViewport命令，完成多会话隔离、节流保存与重启恢复矩阵。
2. 配合员工04/05在Windows Tauri真实窗口完成DeepSeek终态、部分内容保留和启动恢复联调。

## 需要同事配合

- **员工01**：请复核V5迁移与“预算拒绝发生在Node/Run创建前”的证据，确认M1契约门禁。
- **员工03**：请使用`saveCanvasViewport/getCanvasViewport`接入CAN-014，不再等待或使用localStorage替代。
- **员工04**：请覆盖`contextBudgetInvalid`安全错误呈现，并参与真实窗口终态/重启矩阵。
- **员工05**：请持续维护可信`contextWindowTokens`来源，并配合真实DeepSeek运行矩阵。
