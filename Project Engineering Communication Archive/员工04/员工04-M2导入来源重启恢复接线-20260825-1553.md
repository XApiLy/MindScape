# 员工04｜M2 导入来源重启恢复接线

> 提交时间：2026-08-25 15:53（UTC+8）  
> 覆盖任务：`CHAT-M2-005`、`CHAT-M2-006` 前端恢复切片  
> 状态：Intake 已接入实时导入与重启查询，等待真实样本窗口验收

## 已阅读与落实

- 已重新阅读 PEC 控制窗口及员工02最新《M2 通用导入闭环与重启查询》（17:00）；确认 `list_import_sources(conversationId)` 与 `get_import_bundle(sourceId)` 已成为导入恢复的唯一 typed IPC 入口。
- 保持既有边界：前端只消费结构化 `ImportSource`、`ImportBundleQueryProjection`、`ParseReport`，不访问 SQLite/raw storage，不调用模型，不执行导入历史内容，不记录或回显 Key。

## 本次完成

- `desktop/src/App.tsx`：会话恢复时并行读取导入来源列表；切换会话/预览模式清空来源状态；导入成功后更新来源列表；新增按 source ID 查询完整 Bundle 的安全回调。
- `desktop/src/components/ChatWorkspace.tsx`：把来源列表、加载态和 Bundle 查询回调单向传入 Intake，不从 graph/坐标猜测导入来源。
- `desktop/src/components/ImportIntakeDialog.tsx`：
  - 增加“本地已导入来源”恢复区，展示安全文件名与数量；
  - 支持点击来源恢复最新 revision、消息计数、warnings/errors 和字段恢复状态；
  - 增加恢复中、空列表、错误和成功摘要状态；不渲染 raw 正文；
  - 保留实时导入的文件/粘贴、重复、解析错误与重试路径；粘贴以内部 `pasted-session.txt` 名称匹配后端 TXT 解析器。
- `desktop/src/styles.css`：增加来源历史、恢复加载、空态与 Bundle 摘要样式，保持 UI-HANDOFF-06 可替换边界。

## 验证结果

- `desktop/npm run test:chat`：38/38 通过。
- `desktop/npm run test:canvas`：20/20 通过。
- `desktop/npm run test:canvas:perf`：单独重跑 1/1 通过；CAN-019 200 节点基线 `initialProjectionMs=2.888`。
- `desktop/npm run build`：通过，Vite 1831 modules。
- `desktop/src-tauri cargo test --all-targets --locked`：103 passed / 0 failed。
- `node scripts/check-pec-retention.mjs`：通过；`node scripts/check-secrets.mjs`：1405 个文本文件通过；`git diff --check`：通过。

## 契约、安全、恢复与未完成边界

- 导入 raw 内容寻址、解析、Bundle 事务、启动临时文件清理与孤儿 raw 风险仍由员工02后端负责；本次没有复制或重算领域语义。
- 页面恢复只显示结构化来源/报告摘要；完整 ImportedMessage 仅作为下一步原样续接输入，当前不会自动创建会话图节点或调用模型。
- 真实 Markdown/TXT/JSONL 样本和新窗口重启验收尚未完成，因此 `CHAT-M2-005/006` 暂不宣称完整通过。

## WIP 与需要配合（最多两个）

1. **员工02**：提供真实样本矩阵与 raw orphan 引用扫描结果；确认 Bundle 查询在强杀边界后的可解释状态。
2. **员工01/03**：确认导入来源与 FocusFrame/原样续接的挂接边界；FocusedContext 快照持久化完成后再接入下一层消费。

