# 员工04｜M2 画布问答字体作用域修正

时间：2026-08-30 14:56（Asia/Shanghai）  
覆盖任务：`CHAT-M2-008～010` / 创始人画布字体现场验收反馈  
状态：画布问答摘要字体与受控字号接线完成；新标准 Tauri Release 已启动，等待创始人复验

## 已阅读与落实

- 已重新读取 PEC 当前七文件窗口、M2 字体派单和《M2 Markdown 原生阅读与字体偏好规格》；继续遵守 404×286 固定节点、200 节点性能、内容字体与 UI 状态字体分层。
- 创始人截图证明画布问题已使用所选字体，但回答摘要仍为系统字体；本轮把该截图作为作用域缺口，而不是字体资源加载失败处理。
- 使用前端设计规范维持现有画布信息层级，使用 React 性能规范仅增加稳定 class/CSS 变量，不引入节点状态或额外渲染；Tauri 仍只通过批准脚本发布。

## 相比上一份 PEC 的变化

- 根因：`.canvas-node-question strong` 已继承 `--reading-font-family`，但 `.canvas-node-answer p` 仍固定为 `10.5px/1.62` 且未声明阅读字体。
- 为真实回答新增 `canvas-node-answer-preview` 内容类；回答摘要现在继承阅读字体、font-synthesis 和字间距。
- 画布问答字号和行高随阅读偏好受控缩放：问题 12～18px，回答 10.5～16px；节点几何尺寸、截断行数和轻量纯文本预览不变。
- 失败、等待、模型名、终态、按钮等继续使用 UI/等宽字体，避免把运行状态误当阅读正文。
- 排版规格 6.1 已补充“画布问答摘要”和固定节点内安全缩放边界。

## 验证与交付

- Chat 56/56；Canvas 34/34；Canvas 性能 2/2；TypeScript/Vite production 2096 modules。
- 200 节点样本：初始投影约 2.56ms、5000 次平移约 1.13ms、5000 次缩放约 0.79ms、50 次流式投影约 105.58ms。
- Playwright 真实 Chromium：小赖字体 + 24px 设置下，画布问题计算为小赖字体/18px，回答摘要为同一字体/16px；“已完成”仍为 JetBrains Mono/Consolas。
- 秘密扫描 1621 个文本文件和 `git diff --check` 通过。
- Build ID：`20260830-145519-m2-canvas-reading-fonts-f5307ad71b7f-dirty`
- 程序：`D:\Project\MindScape\artifacts\acceptance\versions\20260830-145519-m2-canvas-reading-fonts-f5307ad71b7f-dirty\mindscape-desktop.exe`
- SHA-256：`44F04C3F3BD8BE0E0CD0CF2783D6C208937BCDB30869F11FCE3B4949E78477CF`
- 来源：`f5307ad71b7f` / `codex/m1-integration-recovery`；共享工作树 dirty 已写入 manifest。
- 启动前 `1420/1428` 无监听；验收目录程序已启动，窗口标题 `MindScape`。

## 契约、安全、恢复与风险

- 不改变 Canvas 投影、节点几何、SQLite、IPC、会话或 Provider；只修正内容层 CSS 作用域。
- 不读取、打印或持久化 Key；字体偏好仍仅按本地 workspace 保存，不进入会话或模型请求。
- 大字号会减少固定节点内可见摘要行数，完整内容仍通过聚焦阅读器查看；这是维持大画布密度的有意边界。
- 当前为多人共享 dirty 集成快照，只供本轮现场验收，不构成最终 clean M2 Go。

## 需要配合 / 未关闭项

1. **创始人**：在新窗口回到同一真实会话，确认画布卡片的用户问题和回答摘要现在都随字体设置变化。
2. **员工03/05**：在最终 clean Release 复核大字号下节点截断、200 节点缩放/拖拽和聚焦阅读入口，不扩大固定节点几何尺寸。

## 下一步 WIP（最多两个）

1. 根据创始人本轮画布复验结果收口字体作用域。
2. 等员工02用户字体 typed IPC 后接入受控导入、删除和失败回退。
