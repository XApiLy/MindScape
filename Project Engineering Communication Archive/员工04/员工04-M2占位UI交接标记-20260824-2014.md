# 员工04｜M2 占位 UI 交接标记

> 提交时间：2026-08-24 20:14（UTC+8）  
> 覆盖任务：`CHAT-M2-001`、`CHAT-M2-003`、`CHAT-M2-004` 交接治理切片  
> 状态：功能占位 UI 已补齐 `UI-HANDOFF-06` 交接信息，等待员工06后续视觉/交互接管；业务链路未改变

## 已阅读与落实

- 已阅读最新 `PEC-提交归档与时效规则.md`、`docs/design/ui-placeholder-and-employee06-handoff.md`，并复读员工01～03、05～06当前 PEC。
- 规则要求占位 UI 在 TSX/CSS 入口附近说明位置、用途、真实数据/IPC、正常/加载/空白/错误/停止/恢复状态、交互约束和可替换范围；本次已落实到运行档案、上下文账单、Provider 能力快照和导入 Intake 四处入口。
- 继续遵守员工06由创始人直接安排的边界：本次只补工程交接信息，不向 PEC 派发视觉任务，不引入新设计语言或复杂效果。

## 相比上一份的变化

- 运行档案入口 `ChatWorkspace.tsx` 增加交接注释，标明 `profileDraft`、能力快照和 `startModelRun.effectiveRunProfile` 的数据路径，以及冲突阻塞、停止/恢复语义。
- 上下文账单 `ContextDialog.tsx` 增加交接注释，明确 `get_context_snapshot` → `projectContextBill` 纯投影边界、空白和错误态。
- Provider 能力快照 `SettingsDialog.tsx` 增加交接注释，明确 `listProviders`/`providerCatalog` 来源、无 Key 禁用条件和 `details/summary` 键盘语义。
- 导入 Intake `ImportIntakeDialog.tsx` 增加交接注释，明确本地预检函数、ParseReport IPC 未接入时预览保持禁用、原文不执行边界。
- 对应四组 CSS 入口增加同名短注释，限定员工06可替换的表现层范围。

## 交付物与可核验位置

- `desktop/src/components/ChatWorkspace.tsx`
- `desktop/src/components/ContextDialog.tsx`
- `desktop/src/components/SettingsDialog.tsx`
- `desktop/src/components/ImportIntakeDialog.tsx`
- `desktop/src/styles.css`
- 旧快照已移至 `Project Engineering Communication Archive/员工04/员工04-M2运行档案真实UI接线-20260824-1919.md`；当前窗口保持 7 个 Markdown 文件。

## 契约、数据、安全与恢复影响

- 仅新增注释，不修改 Rust 权威类型、SQLite schema、IPC、Provider 映射、运行/取消/恢复状态或测试选择器。
- 没有读取、记录或持久化 API Key；没有调用真实模型或产生费用。
- 注释明确了未接入能力仍保持禁用/待内核接入，避免把占位 UI 误认为已完成能力。

## 验证结果

- `desktop/npm run test:chat`：31/31 通过。
- `desktop/npm run test:canvas`：17/17 通过。
- `desktop/npm run test:canvas:perf`：1/1 通过。
- `desktop/npm run build`：通过，Vite 1830 modules。
- `node scripts/check-pec-retention.mjs`：通过，当前窗口 7 文件。
- `node scripts/check-secrets.mjs`：通过，1310 个文本文件。
- `git diff --check`：通过。
- 本轮无视觉改动，未重复发布 Tauri 验收程序；此前运行档案 UI 的 Mock/窄屏证据仍在 `output/playwright/employee04-m2-run-profile-ui/`。

## 风险与阻塞

- 员工02已完成 `SqliteStore::persist_import_bundle` 原子事务写入，但通用导入命令与 ParseReport 查询 IPC 仍未交付，故 Intake 预览继续保持禁用。
- 员工01尚未冻结 FocusFrame 生命周期查询与恢复后的运行档案投影，`focusNew` 和重启后档案重试仍不可宣称完成。
- 员工05的 reasoning 终态事件矩阵尚待跨员工真实 Tauri/DeepSeek 验收；本轮未调用真实 Key。

## 下一步 WIP（最多两个）

1. 与员工01/05在统一验收窗口复核运行档案真实发送、InvalidRequest 安全提示和重启后档案投影。
2. 员工02交付 ParseReport 结构化 IPC 后，接入 Intake 预览账单和错误/恢复状态。

## 需要同事配合

- **员工06**：由创始人直接安排后，按各入口 `UI-HANDOFF-06` 说明替换表现层；不可改变领域字段、IPC、测试选择器和安全边界。
- **员工01**：冻结 FocusFrame 生命周期与 `effectiveRunProfile` 查询投影。
- **员工02**：在已完成 `persist_import_bundle` 的基础上提供通用导入命令、查询 IPC 与 ParseReport 稳定 DTO。
- **员工05**：协助真实模型运行档案和 reasoning 终态验收，保持 Provider 映射唯一归属。
