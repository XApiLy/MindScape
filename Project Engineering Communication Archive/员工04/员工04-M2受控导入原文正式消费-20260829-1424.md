# 员工04｜M2 受控导入原文正式消费

时间：2026-08-29 14:24（Asia/Shanghai）  
覆盖任务：`IMP-M2-004～008` / `CHAT-M2-001～010` 前端联调  
状态：typed IPC 消费与工程门禁完成；等待统一 clean Release 真实原文样本

## 已阅读与落实

- 已读取 PEC 控制文件及员工01《回流候选生命周期门禁》、员工02《受控原文与回流候选查询》、员工03《回流候选正式 UI 消费》、员工05《检索与长会话性能优化》当前报告。
- 员工02提供 `getRawImportContent(sourceId)`，只接受 SQLite 已登记来源并返回内容、完整字节数、哈希和截断状态；员工04因此只透传稳定 source ID，不接收、展示或拼接 `storageRef`/本地路径。
- 员工01已复核 Markdown Entity/FTS/Vector/Projection 单事务代码边界通过；回流候选进一步收紧为仅 Closed 非主线分支可见。员工03已正式完成该候选 UI，本报告只记录共享 prop 接线复核，不重复声明其交付。
- 使用现有视觉 token、React 懒加载/并行请求规范、Tauri typed IPC 错误边界与 Playwright 浏览器回归；所有入口保留 `UI-HANDOFF-06`。

## 相比上一份 PEC 的变化

- 导入预览从“安全渲染 / 内容块 Markdown”两层扩展为三层：安全渲染、内容块 Markdown、受控源文件原文。
- 源文件视图明确显示完整/截断、可见字节数/完整字节数和内容指纹；截断时复制动作明确为“复制当前预览”，不冒充复制完整原文。
- `getImportBundle` 与 `getRawImportContent` 并行启动；原文失败隔离显示，不吞掉 ParseReport 或已解析消息预览。
- 导入面板改为按需懒加载并在关闭时卸载，真实清除临时粘贴文本和预览状态。

## 本次完成

- App → ChatWorkspace → ImportIntakeDialog 接入 `RawImportContentProjection` typed callback；浏览器预览环境保持 unavailable，不尝试降级读取文件。
- 前端在展示前再次校验 source ID、64 位 SHA-256 形状、声明完整字节数与可见 UTF-8 字节数；不一致进入独立错误态并可重试。
- 新增受控原文 unavailable/loading/ready/error/empty/truncated 状态、完整性元数据、键盘可滚动文本区和对应复制语义。
- ImportIntakeDialog 拆为独立动态 chunk；关闭面板即卸载，修复“关闭后不保留”文案与此前组件状态实际保留不一致。
- 员工03并发完成正式回流候选 UI；共享树中补齐并复核 `ChatWorkspace → ConversationCanvas → CanvasFocusReader` callback 全链路，候选语义和归属以员工03报告为准。

## 验证证据

- Chat：51/51；新增受控来源、哈希和字节边界回归。
- Canvas：34/34；Canvas 性能：2/2。
- TypeScript/Vite production build：2095 modules，通过；主入口 `487.71 kB`，ImportIntakeDialog 独立 chunk `16.59 kB`，不再触发 500 kB chunk 警告。
- Playwright 浏览器：动态面板正常打开；粘贴临时文本后关闭并重新打开，textarea 值恢复为空；dialog 唯一。控制台无新增功能错误，只有既有 `/favicon.ico` 404。
- Secret scan：1581 个文本文件通过；`git diff --check` 通过。

## 契约、安全、恢复与发布边界

- 不改变 RawImportContentProjection、ImportBundle、ParseReport、FocusPromotionCandidateSet、SQLite、Vault、FTS、VectorIndex 或知识状态机。
- 原文只作为本地只读事实展示；不执行 Markdown/HTML/历史指令，不传给模型，不显示 Key、Authorization、Provider 原始响应、reasoning 或本地路径。
- 前端完整性复核是显示门禁，不替代员工02的内容寻址读取、尺寸上限、哈希和 UTF-8 校验；任何失败不自动降级。
- 本轮没有包含最新 typed IPC、生命周期门禁、员工03候选 UI及本轮受控原文 UI 的统一 clean Release，因此不能声明真实原文或重启验收通过；M2 仍为 **No-Go**。

## 下一步 WIP（最多两个）

1. **统一真实样本**：员工02准备完整/超过预览上限/篡改失败来源，员工05在代码冻结后发布唯一 clean Release；员工04在同一 Build 验证三视图、截断、复制、重试、关闭重启和不泄露路径。
2. **阅读偏好收口**：受控本地字体继续保持禁用，等待员工01/02冻结字体目录、格式/大小/失败回退 typed IPC；员工04不在前端自行接受路径、文件名或 CSS。
