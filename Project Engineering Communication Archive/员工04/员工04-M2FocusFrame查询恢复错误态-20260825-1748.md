# 员工04｜M2 FocusFrame 查询恢复错误态

> 提交时间：2026-08-25 17:48（UTC+8）  
> 覆盖任务：`CHAT-M2-002`、`CHAT-M2-010` 前端恢复切片  
> 状态：FocusFrame 查询失败不再伪装为未绑定，已提供显式重试；等待真实 Tauri 窗口验收

## 已阅读与落实

- 已重新阅读当前 PEC 控制窗口、员工01《M2 FocusedContext 持久化查询校验》、员工02《M2 通用导入恢复与孤儿引用回收》、员工03《M2 FocusFrame 会话列表接入画布》、员工05最新《RAG-M2 Markdown 分块索引首切片》及 M2 契约基线。
- 针对员工01的协作要求，前端保持 `undefined`（尚未绑定/未投影）、`null`（明确无 FocusFrame）和查询错误三者分离；错误时显示恢复失败，不从 UI 状态补造 FocusedContext。
- 员工02的导入恢复与孤儿引用回收不改变前端 IPC 边界；员工05的 Markdown chunk 仍是后端派生索引输入，前端不解析、重算或排序。

## 本次完成

- `desktop/src/App.tsx`：
  - 新增 `focusFrameQueryError` 状态；会话级 FocusFrame 列表读取失败时保留安全错误，而不是清空后显示“未绑定”；
  - 新增 `reloadFocusFrames`，按当前会话重新调用 `list_focus_frames` 并更新显式 parentNode 索引；
  - 将错误与重试回调传递到 ChatWorkspace/ConversationCanvas。
- `desktop/src/components/ChatWorkspace.tsx`：扩展 FocusFrame 查询错误和重试回调的只读传递，不复制领域状态规则。
- `desktop/src/components/ConversationCanvas.tsx`：
  - 阅读器 FocusFrame 控制区增加查询失败态与“重试读取 FocusFrame”；
  - 重试期间展示 loading，失败保留错误，成功后回到真实 active/closed/未绑定投影；
  - 保持创建、关闭、重开操作的 revision 与 typed IPC 边界。

## 契约、安全、恢复与未完成边界

- FocusedContextSnapshot 仍由内核持久化/查询负责；前端只消费非空投影，不自行生成快照、知识引用、Token 或分块结果。
- `list_focus_frames` 是当前会话 FocusFrame 发现入口；重试只使用当前会话 ID，不从坐标、标题或 selectedNodeId 推断 ID。
- 真实 Tauri 新窗口仍需验证查询失败恢复、创建→关闭→重开、会话切换隔离及重启恢复；M2 仍保持 No-Go，不宣称统一发布验收。

## 验证结果

- `desktop/npm run test:chat`：38/38 通过。
- `desktop/npm run test:canvas`：21/21 通过（包含最新 FocusFrame 多查询索引回归）。
- `desktop/npm run test:canvas:perf`：1/1 通过；CAN-019 基线保持在门限内。
- `desktop/npm run build`：通过，Vite 1831 modules。
- `desktop/src-tauri cargo test --all-targets --locked`：113 passed / 0 failed。
- `git diff --check`：通过；PEC retention 与 secret scan 待本报告落盘后复跑。

## WIP 与需要配合（最多两个）

1. **员工01/02**：完成 FocusedContextSnapshot SQLite 持久化、查询和重启 round-trip 后，我继续接入 Chat 侧真实快照状态与错误恢复，不复制领域编译规则。
2. **员工03**：请复核查询错误/重试和 active/closed/reopen 在阅读器中的占位交互；确认主线 FocusFrame 不绑定任意节点的语义保持不变。

