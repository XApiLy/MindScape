# 员工04｜M2 内置字体 Release 验收交付

时间：2026-08-29 23:02（Asia/Shanghai）  
覆盖任务：`CHAT-M2-008～010` / 七款内置阅读字体统一 Release 预验收  
状态：标准 Tauri Release 已发布并在无开发服务器条件下启动；等待创始人确认真实工作区与字体视觉

## 已阅读与落实

- 已重新读取 PEC 当前窗口、员工04/05最新报告、《验收程序构建与交付规则》和批准脚本；本轮只使用 `scripts/publish-acceptance.ps1`，没有交付 `target/`、调试 EXE 或 localhost 版本。
- 延续《M2 首批阅读字体预设目录》和字体资源 README 的边界：内置字体随安装产物分发，不访问 CDN；用户导入字体仍等待员工02 typed IPC。
- 当前共享工作树包含多人未提交的 M2 集成，因此 Build ID 和 manifest 明确标记 `dirty`；本次只供最新字体及集成界面查看，不能作为 M2 最终 clean Go 候选。

## 相比上一份 PEC 的变化

- 上一份完成七款字体的代码、浏览器和 Vite production 门禁；本轮首次通过批准脚本把这些字体与最新 M2 内核编入独立 Tauri Release。
- 已核对发布后 EXE 的 manifest 与 SHA-256，并确认启动前 `1420/1428` 均无监听；从不可变验收目录启动了真实 Windows 程序。

## 交付物与验证

- Build ID：`20260829-230156-m2-reading-fonts-f5307ad71b7f-dirty`
- 程序：`D:\Project\MindScape\artifacts\acceptance\versions\20260829-230156-m2-reading-fonts-f5307ad71b7f-dirty\mindscape-desktop.exe`
- SHA-256：`B5413210C747DA437B9A162EB80E71B38A97E51FF4161902D48438AF6149264E`
- 来源提交：`f5307ad71b7f`，分支 `codex/m1-integration-recovery`，工作树 dirty 已写入 manifest。
- EXE：`37,664,768` 字节；Tauri `release` optimized 构建成功，前端 2096 modules，12 个字体资源全部进入构建。
- 无开发服务器启动检查：启动前端口 `1420/1428` 均无监听；验收目录程序启动成功，窗口标题 `MindScape`，进程仍保持运行供创始人查看。
- 尚未由创始人现场确认窗口内真实工作区与七款字体视觉，因此 PEC 不把“窗口标题/进程存活”冒充完整 UI 验收。

## 契约、安全、恢复与发布边界

- 不修改字体、领域或 IPC 契约；本轮只产生 Git 忽略的验收构建、manifest、哈希文件和 `LATEST.txt`。
- 未读取、打印或持久化 Key；程序仍使用操作系统安全凭据边界。
- dirty 构建可用于查看最新功能，但最终 M2 必须由代码冻结后的 clean commit 重新发布并完成同一 Build 纵向验收。

## 需要配合 / 未关闭项

1. **创始人**：在当前已打开窗口进入“模型与阅读设置”，切换七款字体并查看 Chat/聚焦阅读器；确认真实工作区正常显示。
2. **员工05**：代码冻结后发布唯一 clean Release，补断网、缺字、DPI、关闭重启、安装包增量和 M1 回归。
3. **员工03/06**：分别复核聚焦阅读器锚点/画布性能与创始人指定的字体卡视觉，不另行编译不同版本。

## 下一步 WIP（最多两个）

1. 根据创始人对当前 Build 的现场反馈修正字体选择器或阅读表面问题。
2. 等统一 clean Release 后完成同一 Build 的字体、导入、聚焦阅读和重启纵向验收。
