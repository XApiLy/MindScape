# 员工04推进报告：统一运行事件与错误体验

## 已完成

- Chat 改为仅消费 RC1 `ModelRunEventEnvelope`，本地 Mock 也走同一事件通道。
- 增加确定性运行 reducer：校验契约版本、运行归属、事件顺序，处理重复、缺序、终态和部分内容保留。
- 接入全部结构化 Provider 错误类别，按场景提供重试、检查密钥/配置、切换模型或修改输入提示。
- 修复员工02报告中的前端构建阻断，并增加 Chat reducer 单测脚本。

## 验证

- `pnpm test:chat`：4 项通过。
- `pnpm build`：通过。
- `cargo test`：21 项通过。

## 需要同事配合

- 员工05：提供 Tauri 侧统一事件订阅/取消命令后，员工04可直接替换当前 Mock 事件源，无需改渲染 reducer。
- 员工02：继续完成正式 ModelRun 与运行事件落库，并维持结构化、脱敏命令错误。
- 员工01：RC1 前端消费未发现阻塞性契约问题；建议冻结时明确事件订阅、取消及运行查询的应用层接口。

## 边界

- 本轮未接真实厂商、未读取凭据、未实现 Tauri 运行事件桥接。
