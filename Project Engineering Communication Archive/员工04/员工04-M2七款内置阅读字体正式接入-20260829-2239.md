# 员工04｜M2 七款内置阅读字体正式接入

时间：2026-08-29 22:39（Asia/Shanghai）  
覆盖任务：`CHAT-M2-008～010` / Markdown 原生阅读与字体偏好联调  
状态：七款内置预设的前端接线、回退、持久化与浏览器验证完成；等待统一 clean Tauri Release 验收

## 已阅读与落实

- 已重新读取 PEC 当前七文件窗口及 2026-08-29 更新的《PEC 提交、归档与时效规则》；控制文件明确把 Caveat、Nunito、Lato、Cormorant、寒蝉半圆体、小赖字体和得意黑纳入员工04现有“阅读偏好收口”WIP，本轮没有新增并行 WIP。
- 已精读 `desktop/src/assets/fonts/README.md`、《M2 首批阅读字体预设目录》和《M2 Markdown 原生阅读与字体偏好规格》；落实为稳定 preset ID、自定义 family alias、中英数标点真实预览、明确中文回退、Regular 字体禁止伪合成，以及只影响 SafeMarkdown 阅读表面。
- 已复核员工01～06当前 PEC：员工03的聚焦阅读器继续消费共享 SafeMarkdown，员工05尚未发布包含最新代码的 clean Release；本轮不复制员工03领域投影、不生成验收程序。
- 未发现内置预设与现有领域契约冲突。用户导入字体仍由员工02后续 typed IPC 负责，设置内入口继续诚实禁用；内置字体不经过任意路径或用户导入 IPC。

## 相比上一份 PEC 的变化

- 上一份报告完成受控导入原文消费；本轮转入控制文件已正式派发的七款内置字体接线。
- 原有系统无衬线/衬线/高可读三项保留，新增七个稳定白名单 ID，并继续使用同一版本化 workspace localStorage 偏好信封。
- 设置页从简单字体按钮扩展为按“清晰正文 / 轻松与个性阅读 / 标题与氛围”分组的预设卡，展示中文、英文、数字、标点、用途、覆盖、字重和加载/回退状态。
- 字体资源现在真实进入 Vite 生产产物；README 的“尚未接线”状态已更新为“前端接线完成、统一 Release 待验收”。

## 本次完成

- 新增 `readingFonts.ts` 受控目录：七个稳定 ID、显示元数据、固定 `MindScape …` alias、回退说明、字体加载检测及无 FontFace/解码失败安全回退。
- 在 `styles.css` 注册 12 个本地 `@font-face`，全部使用相对打包资源与 `font-display: swap`，不访问 Google Fonts、CDN 或任意远程 URL。
- 为 Caveat/Nunito/Lato/Cormorant 配置明确中文回退；寒蝉半圆体、小赖字体、得意黑只有 Regular 时使用 `font-synthesis: none`，不生成伪粗体/伪斜体。
- Chat 回答、导入 Markdown 预览和画布聚焦阅读器继续通过共享 `--reading-font-family` / SafeMarkdown 生效；用户问题、按钮、状态、导航以及行内代码/代码块保持 UI/等宽字体。
- 选择器打开后并行调用 `document.fonts.load`，区分“正在验证字体 / 内置字体可用 / 使用安全回退”；加载失败不阻塞选择和正文显示。
- 新增 preset 完整性、稳定 alias、无路径/URL、成功加载、缺失/拒绝回退、系统字体不绕行内置加载器、七款 workspace 持久化测试。
- `desktop/src/assets/fonts/README.md` 记录生产输出：12 个字体文件合计 `32,755,706` 字节；这不是安装包增量或 Release 结论。

## 验证证据

- Chat：55/55；其中新增字体与持久化回归 7/7。
- Canvas：34/34；Canvas 性能：2/2；200 Markdown 节点仍走轻量摘要路径。
- TypeScript/Vite production build：2096 modules；主 JS `492.07 kB`（gzip `151.86 kB`），CSS `71.91 kB`（gzip `13.61 kB`），无 500 kB chunk 警告。
- Vite 输出 12 个字体文件，合计 `32,755,706` 字节；最大单项为 Xiaolai `22,220,806` 字节，保留为统一 Release 包体风险。
- Playwright 真实浏览器：七张卡全部为“内置字体可用”；网络清单只含 localhost 与 `src/assets/fonts`，没有远程字体请求。
- Playwright 选择小赖字体后，预览与真实聚焦阅读器计算样式均为 `MindScape Xiaolai → MindScape ChillRoundM → 系统无衬线` 且 `font-synthesis: none`；页面 reload 后保持选中；行内代码仍为 `JetBrains Mono / Consolas / monospace`。
- 浏览器无新增功能错误；仅保留项目既有 `/favicon.ico` 404。

## 契约、数据、安全、恢复与发布边界

- 不改变 SQLite、FocusFrame、ModelRun、Provider、RAG、Vault、ImportSource 或 raw Markdown；字体偏好仍只在本地 workspace 表现层持久化。
- 字体选择只接受编译期白名单 ID，不接收文件路径、文件名、URL、CSS family 字符串或外部内容；不读取、显示或记录 Key。
- 内置资源加载失败只改变状态并进入同类安全回退，不阻塞 Chat、导入或聚焦阅读；真实缺字覆盖仍需统一 Release 的中英文长文样本。
- 本轮只完成浏览器与 Vite production 工程门禁；没有通过批准脚本发布 Tauri Release，不能声明断网安装包、DPI、系统重启或安装包体积验收完成。M2 仍为 **No-Go**。

## 需要配合 / 未关闭项

1. **员工01**：评审七个内置 preset ID 与后续用户导入 font ID 的命名隔离，以及字体偏好不进入领域对象/模型请求的边界。
2. **员工03**：在统一 Release 复核聚焦阅读器切换字体时的阅读锚点与画布 200 节点无重排回归；画布卡片继续保持轻量摘要。
3. **员工05**：代码冻结后通过批准脚本发布唯一 clean Release，记录安装包实际增量，并执行断网、加载失败/缺字、中英长文、125%/150% DPI、关闭重启和 M1 回归。
4. **员工06**：只在创始人直接安排下评审字体卡片、配对与最终视觉；可替换 `UI-HANDOFF-06` 表现层，不改变白名单、加载/回退状态或持久化语义。

## 下一步 WIP（最多两个）

1. 在统一 clean Release 上完成 Chat、导入预览、聚焦阅读器、断网、回退、重启、DPI 与安装包体积的同一 Build 纵向验收。
2. 等员工02正式提供受控用户字体 typed IPC 后，再消费 TTF/OTF/WOFF2 导入、删除和恢复状态；接口未到前不自行开放文件路径。
