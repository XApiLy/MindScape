# 员工04｜M2 导入 Intake 首切片

> 提交时间：2026-08-24 17:59（UTC+8）  
> 覆盖任务：`CHAT-M2-004`  
> 状态：文件导入入口前端切片完成；本地解析、预览和保存等待数据 IPC

## 相比上一份的变化

- M1 已 Go，员工04从真实取消收口正式切换到 M2 `CHAT-M2-001～010`。
- 完成 `CHAT-M2-004` 的选择、拖放、粘贴、取消和元信息预检界面；保持 `CHAT-M2-005/006` 不越权实现。
- 原 `PROV-013` 当前报告已移入员工04归档；因受管环境禁止写 `.git/index.lock`，使用核验后的无覆盖文件移动，后续集成者需确认 Git rename 检测。

## 已阅读与落实

- 已阅读当前七份 PEC、M2 派单、V1 范围/产品共识/总路线、Core Kernel、前五员工职责、RC1、威胁模型、Git/PEC/验收构建规则，以及本地实体/RAG、Markdown Vault/分支隔离、模型控制/FocusFrame 三份 M2 方案。
- 已读取员工01最新 M2 契约与员工02/03/05首切片：前端只负责来源 intake；不读取 SQLite、不解析文件正文、不生成 `ParseReport`，也不把员工02的原文内容寻址存储误写成可用导入事务 IPC。
- React 约束落实为纯预检函数与派生 memo；视觉继续使用现有 MindScape 深色/金色语言和渐进披露，没有启动 VIS-009 或新的视觉生产架构。
- Tauri 边界落实为本轮无 Rust、命令、权限或文件系统能力新增；导入正文仍是不可信数据，不执行历史指令、不自动调用模型。

## 本次交付

- `desktop/src/app/importIntake.ts`：识别 Markdown/JSONL/TXT、空内容/未知格式/调用方大小策略和 UTF-8 粘贴字节数，仅检查元信息。
- `desktop/src/app/importIntake.test.ts`：5 项 intake 状态机回归。
- `desktop/src/components/ImportIntakeDialog.tsx`：文件选择、拖放、粘贴、来源账单、隐私说明、取消/Escape 和阻塞态预览。
- `desktop/src/components/ChatWorkspace.tsx`：顶部与空状态导入入口接线。
- `desktop/src/styles.css`、`desktop/package.json`：现有样式系统适配、明确禁用主操作、Chat 测试纳入新用例。
- 浏览器验收证据：`output/playwright/employee04-m2-import-intake/`；桌面与 `820×720` 视口均可用，0 个页面异常、0 个非预期 console error，开发服务器默认 favicon 404 已单列。

## 契约、数据、安全与恢复影响

- 没有修改 Rust 权威契约、Frozen V1、SQLite schema、Provider、凭据或恢复语义。
- 文件对象和粘贴正文仅在当前 React 面板内短暂存在；关闭后不持久化。本轮不输出正文到日志/PEC，不发送模型。
- “生成本地预览”在 `DATA-M2-006 / DATA-M2-009` 解析事务 IPC 接入前保持禁用，不伪造成功、不静默降级到浏览器解析。
- 支持大小上限由调用方传入；前端不与员工02暂定的 128 MiB 建立第二事实源。

## 验证结果

- `npm run test:chat`：23/23 通过（含 intake 5/5）。
- `npm run test:canvas`：14/14 通过；`npm run test:canvas:perf`：1/1 通过（200 节点、19 个导入来源节点）。
- `npm run build`：通过，Vite 生产构建 1828 个模块。
- Playwright 等价引擎验收：入口、隐私提示、粘贴预检、IPC 前禁用、窄视口、Escape 关闭全部通过。
- 本轮不发布 Tauri 验收程序；尚未形成可验收的真实导入闭环。

## 风险与阻塞

- 员工02当前只完成原始内容寻址存储；`ImportRevision / ImportedMessage / ParseReport` 的事务命令和查询尚未冻结，不能推进真实预览、保存和原样继续。
- 员工01的 FocusFrame / Effective Run Profile 已有权威类型，但生命周期 IPC 尚未交给前端；本切片不复制继承、排除或能力判断算法。
- 当前共享工作树包含员工01/02/03/05/06并行改动，员工04未覆盖其文件，也不声明整体 M2 已通过。

## 下一步 WIP（最多两项）

1. 等员工02交付 `DATA-M2-006 / DATA-M2-009` 解析事务 IPC 后，推进 `CHAT-M2-005`：真实 ParseReport、消息预览、警告、重复和发送范围。
2. 与员工01/05评审运行档案与能力快照的前端消费边界，为 `CHAT-M2-001` 做只读能力状态接线，不自行推断 Provider 支持项。

## 需要同事配合

- **员工02**：请冻结通用导入命令、取消/失败返回、大小策略与 `ParseReport` 查询；前端只消费结构化结果。
- **员工01**：请评审本切片未越过导入/FocusFrame 权威边界，并明确 `CHAT-M2-001/002` 可消费的生命周期命令。
- **员工03**：当前文案统一为“导入原文/非本地生成”和“原样继续”；待真实来源投影接入后共同复核 Chat/Canvas 单一含义。
- **员工05**：请在能力映射冻结后通知员工04；UI 将显示支持/不支持，不会静默删除 reasoning 或生成参数。
