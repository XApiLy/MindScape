# 员工04｜M2 导入 IPC 客户端接线

> 提交时间：2026-08-25 14:56（UTC+8）  
> 覆盖任务：`CHAT-M2-004`、`IMP-M2-004` 前端接口切片  
> 状态：已接入员工02已注册的 `persist_import_bundle` typed kernel client；原始文件保存/解析与 Intake 预览仍保持禁用

## 已阅读与落实

- 已读取最新 PEC 控制文件、M2 派单、UI 占位交接规范，以及员工01《M2 FocusFrame 查询投影契约》、员工02《M2 FocusFrame 生命周期 SQLite 接入》、员工03《M2 知识上下文占位 UI 与 IPC 阻塞》、员工05《M2 DeepSeek 真实 Key 验收通过》和员工06当前报告。
- 同时复核员工02已归档的《M2 导入 Bundle 命令 IPC 接线》：`persist_import_bundle` 已注册，参数为 `ImportSource`、`ImportRevision`、`ImportedMessage[]`、`ParseReport`，错误沿安全 `CommandError` 返回；raw 保存/解析尚未完成。
- 落实边界：前端只消费结构化 Bundle，不读取 SQLite/raw storage，不从候选元数据伪造 ParseReport，也不提前开启“生成本地预览”。

## 相比上一份的变化

- `desktop/src/domain/commands.ts` 新增 `PersistImportBundleInput`，复用冻结 Import 契约类型，避免在 Chat 层复制 Rust DTO。
- `desktop/src/app/kernelClient.ts` 新增 `kernelClient.persistImportBundle()`，调用真实 Tauri `persist_import_bundle` 命令并保持 `CommandResult<void>` 语义。
- 未修改 `ImportIntakeDialog` 的预览禁用门禁：在 raw 原子保存、通用格式解析和 ParseReport/source 查询完成前，按钮仍明确显示“等待本地解析命令接入”。

## 交付物与可核验位置

- `desktop/src/domain/commands.ts`
- `desktop/src/app/kernelClient.ts`
- 旧快照已归档至 `Project Engineering Communication Archive/员工04/员工04-M2运行档案错误提示与跨团队联调-20260825-1038.md`。

## 契约、数据、安全、恢复与跨团队接口

- 新增的是客户端调用封装，不改变 Rust 命令、Import 契约、SQLite v7、FocusFrame 生命周期或 Provider 运行语义。
- Bundle 输入只允许结构化来源、revision、消息和 ParseReport；不把原始文本放进日志或 PEC，不接触 Key，不执行导入内容。
- `persistImportBundle` 目前是后续解析管线的唯一客户端入口；在 raw 保存/解析命令到位前，没有任何 UI 路径调用它，因此不会产生半成品导入。

## 验证结果

- `desktop/npm run test:chat`：32/32 通过。
- `desktop/npm run test:canvas`：18/18 通过。
- `desktop/npm run build`：通过，Vite 1830 modules。
- `desktop/src-tauri cargo check --locked`：通过。
- `node scripts/check-pec-retention.mjs`、`node scripts/check-secrets.mjs`、`git diff --check`：待本报告落盘后执行并记录。
- 未执行 Tauri Release；本轮没有新的视觉改动、真实模型请求或验收 Build。

## 风险与阻塞

- 员工02当前最新工作已转向 FocusFrame SQLite 生命周期底座；通用 raw 保存/解析、ParseReport/source 查询命令仍未交付，Intake 不能进入真实预览。
- 员工01已冻结 FocusFrame 查询投影，但 close/reopen/query 命令和启动恢复 IPC 尚未接入；Chat/Canvas 暂不推断 Active/Closed。
- 员工05真实 DeepSeek 验收通过，Provider 适配进入冻结状态；后续仅在新协议差异出现时联调。

## 下一步 WIP（最多两个）

1. 等员工02交付 raw 原子保存/通用解析命令和 ParseReport/source 查询后，接入 Intake 的真实提交、加载、失败、停止、恢复状态。
2. 等员工01/02交付 FocusFrame close/reopen/query IPC 后，评审 ContextDialog 与画布阅读器的职责边界，只消费 `FocusFrameQueryProjection`。

## 需要同事配合

- **员工02**：提供 raw 保存/通用解析命令、ParseReport/source 查询 DTO，以及 FocusFrame 生命周期命令名称；前端已准备好 Bundle 调用封装。
- **员工01**：确认 FocusFrame 查询返回的 `focusedContext: null` 语义和恢复状态错误映射。
- **员工03**：FocusFrame 查询 IPC 到位后继续通过 `knowledgeContextByNodeId` 消费投影，不在 Chat 端复制知识编译规则。
- **员工05**：保持已验收的 Provider 错误码和运行档案字段稳定，若真实协议变化再通知前端。
