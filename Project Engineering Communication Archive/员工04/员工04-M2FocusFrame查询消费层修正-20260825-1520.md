# 员工04｜M2 FocusFrame 查询消费层修正

> 提交时间：2026-08-25 15:20（UTC+8）  
> 覆盖任务：`CHAT-M2-002`、`CHAT-M2-003`、`CHAT-M2-010` 前端消费切片  
> 状态：已消费共享 FocusFrame typed client 与员工03规范三态投影；Chat 文案层不再复制画布/领域状态规则，等待当前 FocusFrame 发现与快照持久化

## 已阅读与落实

- 已复读最新 PEC 控制文件，并读取员工01《M2 FocusFrame 生命周期闭环回归》、员工02《M2 FocusFrame 生命周期命令查询接线》、员工03《M2 FocusFrame 查询投影与三态修正》、员工05《M2 向量不可用时全文/关系降级》和员工06当前报告。
- 员工01/02已完成 FocusFrame 创建、查询、关闭、恢复 IPC；员工03已完成 `CanvasFocusFrameQueryProjection` 规范三态：`unavailable`、`availableWithoutKnowledge`、`availableWithKnowledge`。
- 本次按协作边界将 Chat 消费层包装在员工03的规范投影之上，不再从 `focusedContext` 或 React/Canvas 状态自行推断生命周期和知识状态。

## 相比上一份的变化

- 新增 `desktop/src/app/focusFramePresentation.ts`：仅将规范投影转换为 Chat 文案标签；状态判定由 `canvasM2Projection.projectFocusFrameQuery` 唯一负责。
- 新增 `desktop/src/app/focusFramePresentation.test.ts`：覆盖 active + unavailable、closed + revision/closedAt、availableWithoutKnowledge 三态。
- `desktop/package.json` 将该测试纳入 `test:chat`；共享 `kernelClient` 四命令封装和 `commands.ts` 类型镜像已核验兼容，没有重复定义 IPC。

## 交付物与可核验位置

- `desktop/src/app/focusFramePresentation.ts`
- `desktop/src/app/focusFramePresentation.test.ts`
- `desktop/src/app/kernelClient.ts`、`desktop/src/domain/commands.ts`（共享 FocusFrame typed client/输入输出镜像）
- `desktop/src/canvas/canvasM2Projection.ts`（员工03规范投影消费入口）
- 上一快照已归档至 `Project Engineering Communication Archive/员工04/员工04-M2FocusFrame查询状态投影-20260825-1515.md`。

## 契约、数据、安全、恢复与跨团队接口

- 未修改 Rust 命令、SQLite、FocusFrame 状态机、Provider、凭据、导入原文或 M1 终态。
- Chat 只消费结构化查询投影，不从画布坐标、选中节点或 React 状态推断 active/closed，不重算继承、排除、检索排序或 Token 预算。
- `focusedContext: null` 继续显示“上下文快照待内核接入”；仅当规范投影明确有快照时，才显示“暂无知识引用”或“已生成知识上下文”。

## 验证结果

- `desktop/npm run test:chat`：35/35 通过。
- `desktop/npm run test:canvas`：19/19 通过。
- `desktop/npm run build`：通过，Vite 1830 modules。
- `desktop/src-tauri cargo check --locked`：通过。
- `node scripts/check-pec-retention.mjs`：待本报告落盘后复跑。
- `node scripts/check-secrets.mjs`、`git diff --check`：待本报告落盘后复跑。
- 未执行 Tauri Release、真实知识快照查询或视觉验收；员工05真实 DeepSeek 验收保持通过。

## 风险与阻塞

- 目前没有可靠的当前会话 FocusFrame 发现/列表查询，不能从会话或节点自行猜测 FocusFrame ID；实际创建/切换入口需员工03与员工04共同确认。
- FocusedContext 快照仍未持久化，真实知识账单和来源引用不能宣称完成。
- 导入 raw 保存、通用解析和 ParseReport/source 查询仍未交付，`CHAT-M2-005/006` 继续保持禁用预览。

## 下一步 WIP（最多两个）

1. 与员工03确认当前 FocusFrame ID 的发现、从节点创建和切换路径后，把查询投影接入 ChatWorkspace/ConversationCanvas 的真实 loading、active、closed、recovered、error 状态。
2. 员工02交付导入解析/ParseReport 查询后，接入 `CHAT-M2-005/006` 真实预览、报告和原样继续。

## 需要同事配合

- **员工03**：确认 FocusFrame 创建/切换入口与稳定 ID 传递；继续保持画布只投影，不让 Chat 复制领域规则。
- **员工01/02**：交付 FocusedContext 快照持久化/查询和当前 FocusFrame 发现边界。
- **员工02**：交付 raw 导入保存、通用解析、ParseReport/source 查询 IPC。
- **员工05**：保持统一运行事件与 Provider 错误契约稳定，reasoning 分层 UI 待事件字段冻结。
