# 员工04｜M2 用户输入排版与自定义控制

时间：2026-08-30 13:22（Asia/Shanghai）  
覆盖任务：`CHAT-M2-008～010` / 创始人字体作用域与自定义排版验收反馈  
状态：功能、自动化、真实浏览器与标准 Tauri Release 均已完成；等待创始人现场视觉确认

## 已阅读与落实

- 已重新读取 PEC 当前七文件窗口、M2 字体派单、《M2 Markdown 原生阅读与字体偏好规格》及验收程序规则。
- 创始人明确要求“自己的输入也使用所选字体、字号可调并增加自定义项”，该直接产品反馈覆盖旧规格中“用户问题保持 UI 字体”的约定；规格 6.1 已同步修订，界面标签、按钮、状态和代码仍维持 UI/等宽语义。
- 使用现有占位视觉和 `UI-HANDOFF-06` 边界扩展设置，不建立新设计语言；用户字体文件导入继续等待员工02受控目录与 typed IPC，不接受任意路径或 CSS。

## 相比上一份 PEC 的变化

- 阅读字体现在覆盖输入框草稿、已发送问题、画布问题摘要和聚焦阅读器问题，回答正文及 Markdown 预览继续共用同一字体变量。
- 阅读偏好升级至 v2：新增安全范围内的字号、行高、字间距、段落间距和阅读宽度数值；旧 v1 工作区偏好可迁移并保持原有预设视觉值。
- 设置面板新增五个即时滑杆与“你的问题 + Markdown 正文”双预览；小/标准/大/特大等快捷预设仍可一键恢复对应数值。
- 真实浏览器验证发现旧 `.answer-text` 规则会把回答锁回 13px，已修正为统一阅读字号与行高。

## 交付物与验证

- 主要代码：`desktop/src/app/readingPreferences.ts`、`desktop/src/App.tsx`、`desktop/src/components/SettingsDialog.tsx`、`desktop/src/styles.css`、`desktop/src/components/safeMarkdown.css`、`desktop/src/components/conversationCanvas.css`。
- 偏好测试：覆盖 v2 自定义值持久化、七款字体、异常范围安全回退、v1 迁移和存储失败；Chat 56/56。
- Canvas 34/34；200 节点性能 2/2；TypeScript/Vite production 2096 modules；秘密扫描 1614 个文本文件；`git diff --check` 通过。
- Playwright 真实 Chromium：小赖字体 + 24px 在输入框、已发送问题、回答正文和聚焦阅读器即时一致；刷新后 v2 localStorage 恢复；行内代码保持 JetBrains Mono/Consolas 回退。
- Build ID：`20260830-132120-m2-input-reading-customization-f5307ad71b7f-dirty`
- 程序：`D:\Project\MindScape\artifacts\acceptance\versions\20260830-132120-m2-input-reading-customization-f5307ad71b7f-dirty\mindscape-desktop.exe`
- SHA-256：`7CD5C1A157595DEC785C4F07A4D9A1A1B8C8F4487CD41BFBB80874D48CD383B1`
- 来源：`f5307ad71b7f` / `codex/m1-integration-recovery`；共享工作树 dirty 已写入 manifest。
- 启动时 `1420/1428` 无监听；验收目录程序已启动，窗口标题为 `MindScape`，不依赖开发服务器。

## 契约、安全、恢复与风险

- 偏好只按本地 workspace 持久化，不进入会话、Markdown、FocusFrame、RAG、ModelRun、Provider 请求或 SQLite；不触发模型重跑。
- 数值经过固定白名单范围归一化：字号 12～24px、行高 1.4～2.2、宽度 52～110ch、字间距 -0.02～0.08em、段落间距 0.5～1.8em。
- 不读取、打印或持久化 Key；不接受远程字体 URL、任意 CSS 或本地文件路径。
- 当前 Release 为多人共享 dirty 集成快照，只供本轮 UI 验收；不能替代最终 clean 统一 Release，也不构成 M2 Go。

## 需要配合 / 未关闭项

1. **创始人**：在已打开的新窗口选择字体并调整滑杆，确认真实会话中的问题、回答、输入框和聚焦阅读器视觉符合预期。
2. **员工03**：在统一 Release 复核自定义字号/宽度下聚焦阅读器重排和 200 节点画布摘要密度；画布摘要保留固定紧凑字号，仅继承字体与字间距。
3. **员工05**：代码冻结后把本轮偏好 v2 纳入唯一 clean Release 的重启、DPI、长会话滚动与 M1 回归。
4. **员工02**：用户字体导入 typed IPC 就绪后通知员工04消费；当前禁用入口不伪装已完成。

## 下一步 WIP（最多两个）

1. 根据创始人对当前新窗口的现场反馈微调安全范围或控件密度。
2. 等员工02用户字体 typed IPC 后接入受控导入、删除、失败回退和重启恢复。
