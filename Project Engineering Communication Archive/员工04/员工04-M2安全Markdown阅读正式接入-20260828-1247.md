# 员工04｜M2 安全 Markdown 阅读正式接入

时间：2026-08-28 12:47（Asia/Shanghai）  
覆盖任务：`CHAT-M2-001～010`（Markdown 阅读、流式呈现与复制分层子项）  
状态：安全 renderer 与 Chat/聚焦阅读接线完成；字体偏好、导入预览和新版统一 Release 待完成

## 已阅读与落实

- 已重读 PEC 当前控制文件、员工01《Markdown 修订契约与迁移复核》、员工02《Markdown Vault 投影与修订落库》、员工03《画布 Markdown 摘要与聚焦阅读》、员工05《向量持久化降级重建与 Release》，并精读视觉中心《05 Markdown 原生阅读与字体偏好规格》。
- 对本轮的直接影响：raw Markdown 保持不可变；Chat 回答和聚焦阅读只消费派生 renderer；画布节点继续使用员工03的有界纯文本摘要；复制明确区分正文、Markdown 和代码。
- 与员工01/02/03/05当前契约无冲突。本轮不读取 Vault 文件、SQLite、VectorIndex 或 Key，不改变 Message、ModelRun、FocusFrame、KnowledgeEntity 和 MarkdownProjection。
- 使用 `frontend-design`、`vercel-react-best-practices`、`tauri-development` 和 `playwright` 约束功能占位、React 更新成本、桌面安全边界与真实浏览器验收；入口均保留 `UI-HANDOFF-06`。

## 相比上一份 PEC 的变化

- 上一版完成统一检索 UI；本版转入 PEC 新增的 Markdown 原生阅读门禁。
- 新增统一 `SafeMarkdown`：采用 `react-markdown@10.1.0` + `remark-gfm@4.0.1`，官方仓库许可证均为 MIT；不使用 `dangerouslySetInnerHTML`。
- Chat 已落库回答、实时流式回答、画布聚焦阅读器和导入原文阅读共用同一 renderer；画布节点不加载完整 Markdown AST。

## 本次完成

- 支持标题、段落、强调/删除线、行内代码、列表/任务项、引用、分隔线、链接、围栏代码、语言标签、独立代码复制和 GFM 表格横向滚动。
- `skipHtml` 禁止 raw HTML；链接仅允许 `http`、`https`、`mailto` 和页内锚点；`javascript:`、`data:`、`file:` 与相对执行入口失效；所有 Markdown 图片只显示本地占位，不发起远程加载。
- `blocksToMarkdown()` 从不可变内容块重建展示文本，代码围栏长度按正文反引号安全选择，不把 renderer DOM 写回领域对象。
- 流式正文使用 memoized renderer 与 `useDeferredValue`；完成、取消和失败仍由原运行状态表达，部分正文可继续阅读。
- 修复浏览器 Mock 分块丢换行问题：dotAll Unicode 分块现在完整保留 Markdown 换行和 Unicode，使 Mock 能真实覆盖结构化流式路径。
- 复制语义已拆分为“复制正文”“复制 Markdown”“复制代码”；复制正文排除代码工具栏，复制 Markdown 只复制 raw 原文，代码复制不包含围栏。

## 验证证据

- Chat：43/43；Canvas：26/26；Canvas 性能：2/2。
- TypeScript/Vite production build：2089 modules，通过；`git diff --check` 通过。
- Secret scan：1535 个文本文件通过。
- Playwright 真实浏览器：标题、任务列表、代码块、表格和 HTTPS 链接具有正确语义；危险链接退化为不可点击文本，`<img onerror>` 不进入 DOM；三种复制按钮均可执行。
- Renderer 交互无新增控制台错误；浏览器预览仍有既存 `/favicon.ico` 404，与 Markdown 安全链路无关。
- 视觉证据：`output/playwright/employee04-markdown-20260828/rendered-answer-safe-markdown.png`。

## 安全、性能与恢复边界

- raw Message/Run 内容仍是事实源；DOM、纯文本复制和画布摘要均为可重建派生层。
- 不执行 HTML、脚本、事件属性、iframe、代码、命令或工具调用；不加载远程图片，不显示或持久化 Key、Authorization、reasoning 和 Provider 原始响应。
- 200 节点热路径继续只消费有界 preview；完整 AST 仅在 Chat 阅读视图或用户打开聚焦阅读器时构建。
- 员工05的 Build `20260828-123243-m2-vector-rebuild-fallback-b38dd59de8cf-dirty` 早于本次 UI 改动，只能作为上一状态证据，不能用于本版 Markdown 验收；需重新发布统一 Release。

## 需要配合 / 下一步 WIP（最多两个）

1. **员工04主 WIP**：接入阅读字体、字号、行高和宽度的本地偏好占位，并把同一 renderer 接入导入预览 / MarkdownProjection 只读查询；偏好不得进入会话或模型请求。
2. **员工01/02/03/05与创始人**：代码冻结后用唯一批准脚本发布包含本次 UI 的新版统一 Tauri Release；在同一 Build ID 下验证真实 Chat、聚焦阅读、导入预览、Vault revision、检索失效、重启和 M1 终态回归。M2 当前仍为 No-Go。
