# 员工04｜B5 正式候选选择入口接入

时间：2026-09-01 15:53（Asia/Shanghai）  
关闭项：`B5-UI` 正式候选选择入口工程链  
状态：**React → typed IPC → SQLite v17 已接通；B5 整体仍待 clean Release 真实纵向验收**

## 已阅读与落实

- 已读取控制 PEC 顶部“2026-08-30：M2 Go 阻断收敛”和员工直接执行表，以及员工01《B5候选生成重放与接入审计》、员工03《B5正式候选选择入口前端闭环》、员工05《B5真实样本入口阻断》。
- 据此不再重复字体/阅读局部切片；本轮只关闭 `B5-UI` 跨模块入口，并保留“工程门禁不等于 B5/M2 Go”的发布边界。
- Tauri/Rust 审计采用 typed `Result`、权威实体重载、IMMEDIATE 原子事务和稳定幂等键；没有让 React、SQLite 手工编辑或测试夹具代替领域终态。

## 本次关闭的 B 编号

- 关闭 `B5-UI`：Active FocusFrame 新增正式候选多选、空态、选择计数、pending/error/success、同一 `generationId` 重试；Closed 状态继续使用既有四动作。
- 选择只展示同 FocusFrame、`candidate/inferred` 且带 EvidenceRef 的内核实体；该筛选仅用于呈现，Kernel 会在事务内重新加载并验证全部实体。
- typed input 携带 expected memory/lifecycle version；成功后重新读取权威 FocusFrame query，前端不自行增版、不伪造终态。

## 跨模块证据

- `ConversationCanvas → ChatWorkspace → App → kernelClient → Tauri command → KernelService → SQLite v17 IMMEDIATE transaction → FocusFrame query refresh` 已完整接通。
- SQLite 同事务写入 generation receipt 与 FocusFrame/memory/lifecycle；覆盖跨重启恢复、精确重放不二次增版、复用 ID 冲突、stale 全回滚。
- Rust：`223 passed / 0 failed / 1 ignored`，B5 audit `4/4`；候选生成切片 `15/15`；Clippy `-D warnings`、fmt 通过。
- 前端：Chat `63/63`、Canvas `34/34`、Canvas 性能 `2/2`、TypeScript/Vite `2098 modules`、`git diff --check` 通过。
- dirty debug 原生构建成功：`desktop/src-tauri/target/debug/mindscape-desktop.exe`；仅用于联调，不计 Acceptance Release。

## 剩余阻断

1. `B5` 尚未关闭：需形成新的 clean commit，由员工05发布唯一新 Acceptance Release，并在同一真实样本执行“生成 → 丢响应同 ID 重试 → Close → 四动作 → Reopen → 重启”。
2. 当前候选列表来自会话实体库存的前端展示筛选；员工01仍需裁决是否新增 Kernel 专用“可选候选清单”只读查询，减少无效库存暴露。
3. 未覆盖真实窗口点击与正式数据库/Vault 证据；不得把本次工程门禁或 dirty debug 构建写成 M2 Go。

## 下一步 WIP（最多两个）

1. 配合员工01复评是否需要 Kernel 专用可选候选查询；若冻结，只替换数据来源，不复制第二套状态机。
2. 等员工05发布新的唯一 clean Acceptance Release 后，复核真实 Active 多选 → Close 四动作 → Reopen/重启状态，不再生成个人 Release。
