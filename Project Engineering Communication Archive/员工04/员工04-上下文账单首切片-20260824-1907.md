# 员工04｜上下文账单首切片

> 提交时间：2026-08-24 19:07（UTC+8）  
> 覆盖任务：`CHAT-M2-003`（真实 ContextSnapshot 审计子切片）  
> 状态：已完成；运行预算等待 Effective Run Profile 查询投影

## 相比上一份的变化

- 在模型能力摘要基础上，扩展真实 `ContextSnapshot` 检查器为可审计上下文账单。
- 新增当前输入、历史消息、外部来源、显式约束、排除项和上下文 token 估算；不再只显示消息与排除原因。
- 员工05已在本轮完成 `EffectiveRunProfile` 真实发送接线，下一工作单元可正式推进 `CHAT-M2-001` 可写控制联调。

## 已阅读与落实

- 开工前重新读取 PEC 当前窗口，并核验实际工程：员工01完成知识状态/分支检索纯规则，员工02仍停在 SQLite v6 与存储底座，员工03提供 M2 纯投影，员工05随后发布运行档案真实发送接线。
- 复核 M2 派单、`CHAT-M2-003`、ContextSnapshot/EvidenceRef 与威胁模型；因此只消费冻结快照，不从 UI 重新计算继承、排除或知识状态。
- 前端设计规范落实为“冻结上下文清单”：五格账单后按输入、消息、来源、约束、排除顺序展开；React 规范落实为纯 `projectContextBill` 投影，组件不保存第二份领域状态。

## 本次交付

- `desktop/src/app/contextBill.ts`：ContextSnapshot 到审计账单的纯投影，明确四种 EvidenceRef 来源，不合成预算或来源。
- `desktop/src/app/contextBill.test.ts`：3 项回归，覆盖完整/空快照、四种证据目标和预算缺失边界。
- `desktop/src/components/ContextDialog.tsx`：显示本轮输入、账单指标、来源摘录、约束确认状态、排除原因和预算边界。
- `desktop/src/styles.css`：现有 MindScape 黑金样式下的账单清单；560px 视口改为两列，无横向溢出。
- `desktop/package.json`：Context bill 测试纳入 Chat 门禁。
- UI 证据：`output/playwright/employee04-m2-context-bill/`。

## 契约、数据、安全与恢复影响

- 未修改 Rust、SQLite、IPC、Provider、M1 终态/取消/恢复或凭据边界。
- 只显示后端已冻结并由用户主动打开的 ContextSnapshot；不读取文件/SQLite，不调用模型，不把候选或导入内容当作新事实。
- 当前快照不含输出、费用和超时预算，界面明确标为“等待 Effective Run Profile 接线”，不伪造数值。
- EvidenceRef 只显示结构化目标和已有摘录；未读取 raw storage，也未把证据内容写入日志或 PEC。

## 验证结果

- `npm run test:chat`：27/27 通过。
- `npm run test:canvas`：17/17 通过；`npm run test:canvas:perf`：1/1 通过。
- `npm run build`：通过，Vite 生产构建 1829 个模块。
- Playwright 等价引擎：桌面/560px 账单、导入来源、约束确认、预算边界通过；0 个页面异常、0 个非预期 console error。
- 临时组件挂载页已删除；产品树只保留真实组件、纯投影和测试。
- 本轮不发布 Tauri 验收程序；尚未形成包含 Effective Run Profile 查询的完整上下文账单。

## 风险与阻塞

- ContextSnapshot 尚无运行档案/预算字段；实际 reasoning、输出/费用/超时预算需要独立 Effective Run Profile 查询或 ModelRunProjection 加法投影。
- 员工05已把可选 `EffectiveRunProfile` 接入真实发送，但员工04尚未构造非空档案；当前真实运行继续保持 M1 默认行为。
- 导入 ParseReport IPC 仍未交付，`CHAT-M2-005/006` 继续等待。

## 下一步 WIP（最多两项）

1. 消费员工05已接线的 `EffectiveRunProfile`，推进 `CHAT-M2-001` 模型/reasoning/预算/上下文/工具控制与发送前错误展示。
2. 等员工02导入事务 IPC 完成后，推进 `CHAT-M2-005` 真实 ParseReport 预览。

## 需要同事配合

- **员工05**：真实发送接线已收到；请协助核对 Off/Standard/Deep 与不支持参数的真实 IPC 错误形状。
- **员工01**：后续运行档案查询投影需保持创建时不可变，避免上下文账单被当前设置回写。
- **员工02**：导入 IPC 冻结后通知员工04；当前 UI 不访问 v6 表或 raw storage。
- **员工03**：Context bill 的来源/排除含义保持与 Canvas M2 投影一致，不从坐标推断上下文。
