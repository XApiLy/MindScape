# 员工04｜M2 FocusFrame 查询状态投影

> 提交时间：2026-08-25 15:15（UTC+8）  
> 覆盖任务：`CHAT-M2-002`、`CHAT-M2-003`、`CHAT-M2-010` 前端消费切片  
> 状态：已核验并消费 FocusFrame 四命令 typed client，新增生命周期/上下文可用性纯投影；实际 Chat/Canvas 按节点创建 FocusFrame 的 UI 入口仍待共同确认

## 已阅读与落实

- 已读取最新 PEC 控制文件、M2 派单、UI 占位交接规范，以及员工01《M2 FocusFrame 命令查询 IPC 接线》、员工02《M2 FocusFrame 生命周期命令查询接线》、员工03《M2 知识上下文占位 UI 与 IPC 阻塞》、员工05《M2 向量不可用时全文/关系降级》和员工06当前报告。
- 员工01/02已将 `create_focus_frame`、`get_focus_frame_query`、`close_focus_frame`、`reopen_focus_frame` 注册到共享内核；共享工作树已同步 `kernelClient` 与命令镜像，本次没有重复定义或改写其 IPC。
- 落实关键语义：生命周期由内核/SQLite 权威返回；`focusedContext: null` 只表示上下文尚未编译/持久化，不能在 UI 显示成“没有知识”。

## 相比上一份的变化

- 新增 `desktop/src/app/focusFramePresentation.ts`：把 `FocusFrameQueryProjection` 纯投影为 UI 可消费的 lifecycle/context 状态，不读取 Canvas、React 临时状态或 SQLite。
- 新增 `desktop/src/app/focusFramePresentation.test.ts`：覆盖 active + 未编译、closed + revision/closedAt 恢复、available focusedContext 三种情况。
- 将新测试纳入 `desktop/package.json` 的 `test:chat` 门禁；共享 `kernelClient` 已提供四个 FocusFrame 命令的 typed 封装，本次仅验证其输入/输出与镜像契约兼容。

## 交付物与可核验位置

- `desktop/src/app/focusFramePresentation.ts`
- `desktop/src/app/focusFramePresentation.test.ts`
- `desktop/src/app/kernelClient.ts`（共享工作树已接入四个 FocusFrame 命令，本次完成消费审查）
- 旧快照已归档至 `Project Engineering Communication Archive/员工04/员工04-M2导入IPC客户端接线-20260825-1456.md`。

## 契约、数据、安全、恢复与跨团队接口

- 未修改 Rust 命令、SQLite schema、FocusFrame 状态机、Provider、凭据或 M1 运行/取消/恢复语义。
- 投影只读取结构化 `FocusFrameQueryProjection`；不从画布坐标、选中节点或 React 状态推断 active/closed，不重算知识选择、排除或 Token 预算。
- 不持久化 raw 内容、Key 或完整 reasoning；`focusedContext: null` 和真实 `FocusedContextSnapshot` 的可用状态在前端保持可区分。

## 验证结果

- `desktop/npm run test:chat`：35/35 通过。
- `desktop/npm run test:canvas`：19/19 通过（包含共享 FocusFrame 生命周期投影测试）。
- `desktop/npm run build`：通过，Vite 1830 modules。
- `desktop/src-tauri cargo check --locked`：通过。
- `node scripts/check-pec-retention.mjs`：通过，当前窗口保持 7 个 Markdown 文件。
- `node scripts/check-secrets.mjs`：通过，1388 个文本文件。
- `git diff --check`：通过。
- 本轮未执行 Tauri Release、真实 Provider 请求或视觉验收；员工05的真实 DeepSeek 验收保持已通过状态。

## 风险与阻塞

- FocusFrame 查询 IPC 已可消费，但当前 graph/node 契约没有稳定的 FocusFrame ID 映射；在员工03确认“从节点创建/切换 FocusFrame”的入口前，不自动创建或猜测 FocusFrame。
- FocusedContext 快照仍未持久化，查询返回 `focusedContext: null` 时不能展示知识空结果；真实知识账单和画布引用仍待后续数据切片。
- 导入 raw 保存/通用解析/ParseReport 查询仍未完成，Intake 预览和原样继续保持禁用。

## 下一步 WIP（最多两个）

1. 与员工03确认 FocusFrame ID 的创建/切换来源后，把 typed query 状态接入 ChatWorkspace/ConversationCanvas 的 loading、active、closed、recovered、error 入口。
2. 员工02交付 raw 导入解析和 ParseReport/source 查询后，接入 `CHAT-M2-005/006` 的真实预览、报告和原样继续。

## 需要同事配合

- **员工03**：确认从节点创建/切换 FocusFrame 的 UI 入口和稳定 ID 传递，不让 Chat 端从坐标推断关系。
- **员工01/02**：继续冻结 FocusedContextSnapshot 持久化/查询 DTO，明确 revision 与恢复语义。
- **员工02**：交付 raw 导入保存、通用解析及 ParseReport/source 查询 IPC。
- **员工05**：保持统一运行事件与 Provider 错误契约稳定；reasoning 分层 UI 待事件字段正式冻结。
