# 员工04｜M2 导入 Markdown 双预览与复制分层

时间：2026-08-29 01:53（Asia/Shanghai）  
覆盖任务：`CHAT-M2-001～010`（导入预览、Markdown 阅读、字体偏好与流式锁定子项）  
状态：前端工程门禁完成；受控源文件预览、原子事务修复和新版统一 Release 待完成

## 已阅读与落实

- 已重读 PEC 控制文件、员工01《分支回流候选契约与 Markdown 事务评审》、员工02《Markdown 编辑回流与稳定修订》、员工03《画布 Vault 修订审计与编辑回流》、员工05《流式 Markdown 终态与 v13 统一 Release》，并复核视觉中心《05 Markdown 原生阅读与字体偏好规格》导入预览条款。
- typed `ImportBundle` 当前只返回来源元数据、ParseReport 和已解析消息，不返回不可变源文件字节。因此本轮只实现诚实的“内容块 Markdown 原文 / 安全渲染”双预览，不把重建 Markdown 冒充字节级源文件原样内容，也不直接读取 raw storage 或 Vault。
- 员工01指出的 Markdown 编辑跨表非原子缺陷仍成立；前端不绕过该事务边界。员工05当前 dirty Release 可做旧共享树预验收，但早于本轮导入预览代码。

## 相比上一份 PEC 的变化

- 上一版完成版本化阅读偏好与流式阅读锁定；本版把同一 `SafeMarkdown` 正式接入 typed ImportBundle 消息预览。
- 导入来源恢复后默认显示安全渲染，可切换到 Markdown 原文；复制动作随当前模式明确为“复制阅读文本”或“复制 Markdown”。
- ParseReport 不再只显示计数：警告、错误、source locator 和 recoverable 状态可展开追溯；消息以 8 条为一页显式加载，避免一次渲染整个长会话。

## 本次完成

- 新增 `importPreview` 纯函数层，集中角色标签、不可变内容块到 Markdown 的派生和有界分页计数；不改变消息顺序、角色或 source locator。
- `ImportIntakeDialog` 新增 ImportBundle 双视图、逐消息复制、空正文状态、解析问题明细和“显示更多”；新导入成功后自动通过现有 typed IPC 恢复 bundle。
- 安全渲染继续禁用 raw HTML、危险 URL、远程图片和执行内容；原文视图只用 React 文本节点输出，不使用 `dangerouslySetInnerHTML`。
- 更新 `UI-HANDOFF-06`：员工06可替换布局、分页和状态视觉，不得改变 typed 契约、原文/renderer/复制分层或直接访问本地原文存储。
- 上一版阅读偏好与流式锁定继续保留：系统字体栈、版本化 workspace `localStorage`、会话预览、刷新恢复、离底锁定和显式“定位到最新”。受控本地字体入口仍禁用。

## 验证证据

- Chat：50/50；新增覆盖 Markdown 内容保真、角色标签和 8 条分页边界。
- Canvas：31/31；Canvas 性能：2/2。
- TypeScript/Vite production build：2093 modules，通过。
- Secret scan：1562 个文本文件通过；`git diff --check` 通过。
- 上一版 Playwright 证据仍有效：偏好保存/刷新恢复通过；流式内容增长 `634px` 时 `scrollTop=0`，点击定位后剩余 `49px`，停止终态可见，控制台 0 errors / 0 warnings。
- 本轮 ImportBundle 分支尚未在包含新代码的无开发服务器 Tauri Release 中人工验收；不把单元测试或旧 Build 当作真实 UI 证据。

## 契约、安全、恢复与发布边界

- 不改变 ImportSource、ImportRevision、ImportedMessage、ParseReport、Message、ModelRun、MarkdownProjection、SQLite、FTS 或 VectorIndex；预览 DOM 与复制文本均为可重建派生层。
- 不读取、显示、记录或持久化 Key、Authorization、Provider 原始响应、reasoning、不可变源文件字节或用户数据库内容。
- 字节级源文件原样预览仍缺少受控只读 typed IPC；当前 UI 已明确“源文件字节以本地原文存储为准”，避免错误承诺。
- 当前已核对的统一候选 Build 为 `20260829-014319-m2-v13-stream-flush-5dcc02b44e9e-dirty`，SHA-256 `0FFEBC4AD2D475AC62F648F5100360CB0A90E43F1049F901E10C034A9D43CFFC`；它早于本轮 UI，且来源树 dirty、Markdown 编辑事务未原子化，不能用于本功能验收或 M2 Go。
- M2 当前仍为 **No-Go**。

## 下一步 WIP（最多两个）

1. **数据/前端联调**：由员工01/02先确认受控源文件只读投影或明确延后；若提供 typed IPC，员工04只消费受控内容与大小/编码/错误状态，不接受任意路径。受控本地字体同样等待校验 IPC，不在前端自行拼接路径或 CSS。
2. **统一验收**：员工02收敛 Entity/FTS/Vector/MarkdownProjection 原子事务并补回滚后，由员工05用唯一批准脚本重发包含本轮 UI 的候选；员工04在同一 Build 验证真实导入双预览、解析问题、复制分层、字体偏好恢复与长流式锁定。
