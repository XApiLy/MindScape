# 员工04｜M2 知识查询失败恢复入口

时间：2026-08-26 15:43（Asia/Shanghai）  
覆盖任务：`CHAT-M2-007`、`CHAT-M2-010` 前端恢复切片  
状态：代码切片完成；M2 统一 Release 仍为 No-Go

## 已阅读与落实

- 已阅读当前 PEC 控制文件、员工01《M2 统一检索投影契约》、员工02《M2 知识查询 IPC 与验收接线》、员工03《M2 分支焦点与知识实体可读投影》、员工05《RAG-M2 检索候选 Provenance 桥接》及 M2 派单。
- 员工03已将 FocusFrame 当前状态、目标 tooltip 和实体名称回退接入画布；员工05已保留候选 provenance 并等待统一查询 DTO。本次只补前端查询失败恢复，不改变这些领域语义。

## 本次完成

- 在 `App.tsx` 新增 `reloadKnowledge`，重新并行读取当前会话 Entity/Relation typed IPC。
- 在 `ConversationCanvas` 状态条增加“重试知识查询”入口，具备 loading/disabled/error 状态；查询失败不会阻断会话图，也不会把失败误显示成空知识。
- 重试时成功的一侧结果会更新，失败的一侧保留此前已显示的结构化结果；错误继续通过安全文案和工作区 notice 呈现。
- 保持 `UI-HANDOFF-06` 约束：仅使用已有状态条样式，不执行检索、不重排候选、不确认/否决实体，不访问 SQLite、Markdown 或 VectorIndex。

## 验证结果

- `desktop/npm run test:chat`：40/40 通过。
- `desktop/npm run test:canvas`：21/21 通过。
- `desktop/npm run test:canvas:perf`：1/1 通过，CAN-019 200 节点基线通过。
- `desktop/npm run build`：通过，Vite 1832 modules。
- `git diff --check`：通过。

## 契约、数据、安全与恢复边界

- 未改变 FocusFrame、FocusedContext、KnowledgeEntity、Relation、EvidenceRef 或 Tauri IPC 契约；仅增加查询失败后的再次调用。
- 不读取或记录 Key、Authorization、原文、向量或 reasoning；错误只使用安全错误文案。
- 未将员工05的源级失效策略提前投影为“已删除/已否决”；前端仍只显示内核返回的事实。

## 需要其他员工配合 / 未完成边界

1. **员工02**：接入员工01最新 Entity/Relation/EvidenceRef `validate()` 门禁，并提供同一 Tauri Release 的失败不落库与重启 round-trip 证据。
2. **员工05/02**：按员工01已冻结的 `mindscape.knowledge-retrieval.v1` 接入 Vector/FTS/Relation 查询与事实源过滤后，员工04再把 candidates/omitted/notice 来源摘要接入 FocusedContext 阅读器。
3. M2 仍未通过统一门禁：SQLite 事务与 VectorIndex 失效重建、Markdown Vault/讨论日志、检索闭环和无开发服务器 Tauri Release 的 Build ID/SHA-256 证据仍待完成。
