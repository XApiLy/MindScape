# 员工04｜M2 阅读偏好与流式阅读锁定

时间：2026-08-29 01:46（Asia/Shanghai）  
覆盖任务：`CHAT-M2-001～010`（Markdown 阅读、字体偏好与流式阅读锁定子项）  
状态：代码与真实浏览器切片通过；统一 dirty Release 已发布，真实跨模块验收与原子事务修复待完成

## 已阅读与落实

- 已重读 PEC 控制文件、员工01《分支回流候选契约与 Markdown 事务评审》、员工02《Markdown 编辑回流与稳定修订》、员工03《画布 Vault 修订审计与编辑回流》、员工05《流式 Markdown 终态与 v13 统一 Release》及员工04上一快照；继续遵守视觉中心《05 Markdown 原生阅读与字体偏好规格》的 raw / renderer / 偏好分层和流式阅读锁定边界。
- 员工02的 schema v13、稳定 projection ID 与显式 `import_markdown_entity_edit` 不改变本轮偏好契约；员工03新增 typed 投影消费与本轮共享 `SafeMarkdown` 兼容。并发开发导致的 `MarkdownProjection` props 缺口已做机械补线，不在前端复制 revision 规则。
- 使用 `frontend-design`、React 性能规范、`tauri-development` 与 Playwright：只复用现有样式 token，保持偏好本地化、流式增量可读和 `UI-HANDOFF-06` 可替换边界。

## 相比上一份 PEC 的变化

- 阅读字体、字号、行高和宽度从“待接入”变为可用的版本化本地工作区偏好，并支持“仅本次会话预览 / 保存到本地工作区”。
- 修复 Chat 在每个流式增量到达时强制滚到底部的问题：用户离开底部后锁定当前位置，仅显示显式“定位到最新”入口。
- 导入预览和受控本地字体仍未伪装为完成；字体导入入口保持禁用并明确“待接入”。

## 本次完成

- 新增 `readingPreferences` 纯函数层：只接受 `sans / serif / accessible`、四档字号、三档行高和两档宽度；按 workspace ID 写入版本化 `localStorage`，损坏、未来版本、越界值或写入失败均安全回退。
- 设置面板新增语义化选项组、实时 `SafeMarkdown` 预览、会话级预览、工作区保存和恢复默认；偏好通过 `.app-shell` data attributes 与 CSS 变量覆盖 Chat、聚焦阅读和代码外正文，不进入 Message、Run、模型请求或 Vault。
- 字体只使用系统字体栈；不接受任意 URL、CSS、字体路径或文件名。受控本地字体需等待受限目录与校验 IPC，当前入口禁用。
- 新增 `shouldFollowLatest()` 与回归测试；流式时仅在用户位于底部阈值内跟随，离开后保持阅读位置，点击“定位到最新”才恢复跟随。
- 补齐员工03并发新增的 MarkdownProjection props 从 App 到 CanvasFocusReader 的机械传递，未改变投影排序、修订或文件读取语义。

## 验证证据

- Chat：48/48；Canvas：31/31；Canvas 性能：2/2。
- TypeScript/Vite production build：2092 modules，通过；`git diff --check` 通过。
- Secret scan：1558 个文本文件通过。
- Playwright 隔离浏览器：会话级选择 `serif / xlarge / spacious / wide` 即时生效；保存后刷新仍恢复同一组合。
- 流式锁定实测：用户滚到顶部后，内容高度从 `28708px` 增至 `29342px`，`scrollTop` 保持 `0`；“定位到最新”可见。点击后剩余底部距离 `49px`，入口自动消失；停止后显示“已停止”，停止按钮清零。
- 控制台：0 errors / 0 warnings。视觉证据：`output/playwright/employee04-reading-preferences-20260828/session-reading-preferences.png`。

## 契约、安全、恢复与协作边界

- 本轮不改变 Message、ModelRun、FocusFrame、KnowledgeEntity、MarkdownProjection、SQLite、FTS 或 VectorIndex；偏好只是可重建的本地表现层。
- 不读取、显示、记录或持久化 Key、Authorization、Provider 原始响应、reasoning 或用户数据库内容；浏览器验收只使用隔离预览工作区和 Mock。
- 员工05已发布统一候选 Build `20260829-014319-m2-v13-stream-flush-5dcc02b44e9e-dirty`；程序为 `D:\Project\MindScape\artifacts\acceptance\versions\20260829-014319-m2-v13-stream-flush-5dcc02b44e9e-dirty\mindscape-desktop.exe`，SHA-256 为 `0FFEBC4AD2D475AC62F648F5100360CB0A90E43F1049F901E10C034A9D43CFFC`。该 Build 包含本轮 48 项 Chat 基线，但阅读偏好/滚动锁定尚未在该无开发服务器程序中人工复验。
- 员工01发现 `import_markdown_entity_edit` 的 Entity/FTS/Vector 与 Projection 仍是分步提交，第二步失败可能形成跨表不一致；该缺陷不在前端绕过，需员工02收敛为单事务并重发候选。
- 当前 Build 来源树为 dirty，且真实确认/否决/删除/降级/重启/分支作用域样本未完成，因此 M2 仍为 **No-Go**；本报告只声明工程门禁和隔离浏览器交互通过。
- PEC retention 在员工01/05并发更新后已恢复并通过：员工01～06各一份当前报告及一份控制文件。

## 下一步 WIP（最多两个）

1. **员工04主 WIP**：在导入 Intake 中接入 raw / 安全渲染双预览与明确复制语义，继续消费 typed ImportBundle，不直接读取 Vault 或解释 revision。
2. **跨团队联调**：先在员工05当前统一 Build 对阅读偏好、刷新恢复、长流式锁定/定位/停止做无开发服务器预验收；员工02完成 Markdown 原子事务修复后，由员工05用唯一批准脚本重发可追溯候选，再由员工01/02/03/04/05与创始人在同一 Build ID 下完成真实纵向证据复核。
