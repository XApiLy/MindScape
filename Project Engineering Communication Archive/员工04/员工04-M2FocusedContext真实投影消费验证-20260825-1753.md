# 员工04｜M2 FocusedContext 真实投影消费验证

> 提交时间：2026-08-25 17:53（UTC+8）  
> 覆盖任务：`CHAT-M2-003`、`CHAT-M2-010` 前端消费验证切片  
> 状态：已接入真实 FocusedContext 查询投影语义，等待统一 Tauri 保存→重启→查询验收

## 已阅读与落实

- 已阅读最新 PEC 控制窗口、员工01《M2 FocusFrame 查询生命周期门禁》、员工02《M2 FocusedContext 快照持久化与查询》、员工03《M2 分支继承与 FocusFrame 查询稳定性》、员工05《RAG-M2 Markdown 分块索引首切片》及 M2 契约基线。
- 员工02已交付 `focused_context_snapshots` V8 持久化、`list_focus_frames/get_focus_frame_query` 非空查询和 `save_focused_context_snapshot` IPC；前端继续只消费查询，不主动保存或构造快照。
- 员工01要求的三态已保持：缺失快照为 `unavailable`，真实快照但无知识为 `availableWithoutKnowledge`，带知识选择为 `availableWithKnowledge`；查询错误走单独恢复失败态。

## 本次完成

- 新增 `desktop/src/app/focusFramePresentation.test.ts` 回归：用真实 FocusedContext 形状验证带知识投影显示为“已生成知识上下文”，不在前端重新排序、筛选或重算 Token。
- 既有 `ConversationCanvas` 阅读器已消费员工02返回的非空 `focusedContext`：展示 snapshot、selected/omitted memory、知识引用、EvidenceRef 摘要、检索版本和 token 预算；查询失败显示“恢复失败”并可重试。
- 保持生命周期控制与查询恢复边界：创建/关闭/重开使用 typed IPC，查询投影由内核返回，主线 `parentNodeId: null` 不绑定到任意节点。

## 契约、安全、恢复与未完成边界

- 前端不调用 `save_focused_context_snapshot`，不伪造编译结果；快照写入、校验、SQLite 外键和重启恢复归员工02/领域层负责。
- 不读取 SQLite/raw/Markdown/VectorIndex，不执行导入内容，不接触 Key；知识展示只消费 `EvidenceRef` 和安全摘要字段。
- M2 共同门禁仍未关闭：需要统一 Tauri Release 真实执行保存→重启→查询样本，并继续完成实体、Relation、EvidenceRef、FTS/VectorIndex 接线。

## 验证结果

- `desktop/npm run test:chat`：39/39 通过。
- `desktop/npm run test:canvas`：21/21 通过。
- `desktop/npm run test:canvas:perf`：1/1 通过。
- `desktop/npm run build`：通过，Vite 1831 modules。
- `desktop/src-tauri cargo test --all-targets --locked`：113 passed / 0 failed（共享工作树最终状态）。
- `git diff --check`：通过；PEC retention 与 secret scan 待本报告落盘后复跑。

## WIP 与需要配合（最多两个）

1. **员工01/02**：提供 FocusedContext 保存→重启→查询真实 Tauri 样本和 schema/revision 结论；如字段保持稳定，前端无需新增领域推断。
2. **员工03/05**：等待实体/Relation/EvidenceRef 与检索 DTO 冻结后，确认画布/Chat 来源摘要和降级 notice 字段，不在 UI 重算检索结果。

