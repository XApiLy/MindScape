# 员工04｜真实取消证据与重复投影修复

> 提交时间：2026-08-19 11:54（UTC+8）  
> 覆盖任务：CHAT-012、CHAT-015、PROV-013  
> 状态：真实取消数据不变量通过；阅读视图重复卡片已修复，等待新 Windows 窗口单卡复验

## 相比上一份的变化

- 创始人已在真实 DeepSeek 流式回答中完成停止操作；界面进入“已停止”，保留部分内容并显示重试入口。
- 只读 SQLite 聚合核对证明唯一 `cancelled` 终态、终态后无 delta、部分内容保留、单一 Node/Run 与数据库完整性通过。
- 原截图暴露同一取消运行被 `NodeCard` 与 `ActiveRunCard` 重复渲染；新增统一时间线投影并补回归测试。
- 验收脚本 4 更新为“部分通过”，不以旧窗口截图冒充修复后的 UI 通过证据。

## 已阅读与落实

- 本轮重新读取 PEC 控制文件、M1 审阅收口文件、统一验收表，以及员工01、02、03、04、05、06当前报告。
- 延续既有 V1 产品范围、Core Kernel、Frozen V1 契约、安全与 Git 规则；只处理 M1 真实取消和工作区前端投影，不进入 M2、多 Provider、Harness、Skill/MCP 或新画布能力。
- 落实“UI 非真相”原则：截图负责交互证据，SQLite 只读聚合负责运行事实；重复卡片明确判定为前端投影缺陷，不改领域数据。

## 本次完成与证据

- `desktop/src/app/chatTimeline.ts`：将会话节点与当前/恢复运行投影为唯一阅读时间线；同 `nodeId` 原位替换，未落图运行才追加。
- `desktop/src/app/chatTimeline.test.ts`：覆盖已落库节点不重复，以及瞬态运行仍正常追加。
- `desktop/src/components/ChatWorkspace.tsx`：阅读视图统一消费时间线投影，保留取消状态、重试和设置回退能力。
- `docs/engineering/evidence/employee04/prov-013-real-cancel-20260819/`：保存原始界面截图、脱敏聚合事实和验收边界。
- `docs/engineering/m1-alpha-acceptance-matrix-20260818.md`：脚本 4 调整为部分通过。

## 契约、数据、安全与恢复影响

- 未修改 Frozen V1、SQLite schema、Tauri 命令、Provider 取消协议或持久化语义。
- 只读核对未读取或保存消息正文、请求/事件正文、数据库路径、内部 ID 或 API Key；证据只保留聚合值和 12 位哈希指纹。
- 截图未展示设置内容或 Key。数据库只有一个 Node 和一个 Run，重复现象仅来自前端投影。
- `git mv` 因当前环境无法创建 `.git/index.lock` 未执行成功；已用核验过的绝对路径将旧 PEC 安全移动到员工04归档目录，文件未删除。

## 验证结果

- Chat 自动测试：16/16 通过，其中新增时间线测试 2/2。
- TypeScript 与 Vite 生产构建：通过，1825 个模块。
- 标准目录 Windows 调试程序：使用本机 Rust 缓存离线构建通过；路径为 `desktop/src-tauri/target/debug/mindscape-desktop.exe`，构建时间 2026-08-19 12:04，SHA-256 `1ACC36F5CDC09C37AE9CECBEBE96B7FC02555DAD348677382F6B15D3B5EBCAFB`。
- 真实取消数据不变量：通过；停止后 10 秒无新增 delta，终态与节点均为 `cancelled`。
- 新 Windows 窗口单卡复验：待完成，因此 PROV-013 / 脚本 4 尚未完全通过。

## 风险与剩余 No-Go

- 当前截图来自修复前窗口，能够证明真实取消，也同时证明重复投影缺陷；不能证明新代码的实际窗口效果。
- 无效 Key、断网/超时、异常断流、强退恢复、完整脱敏矩阵仍未闭环，M1 继续 No-Go。

## 下一步 WIP（最多两项）

1. 构建并启动包含修复的新 Windows 调试版本，重新打开该会话，确认阅读视图只有一张已停止卡片。
2. 与员工05继续执行无效 Key、断网/超时和异常断流矩阵，保持同一套脱敏证据。

## 需要同事配合

- **创始人**：关闭旧窗口，启动本轮新调试版本并重新打开“新会话 3”，确认已停止内容只出现一次。
- **员工02/03**：基于同一取消证据复核唯一 Node/Run，以及画布与阅读投影稳定。
- **员工05**：签署真实网络取消事实，并继续错误、超时和用量矩阵。
