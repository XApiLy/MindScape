# 员工04｜M2 知识查询只读投影接线

时间：2026-08-26 00:12（Asia/Shanghai）  
覆盖任务：`CHAT-M2-007`、`CHAT-M2-010` 前端消费切片  
状态：代码切片完成；M2 统一 Release 仍为 No-Go

## 已阅读与落实

- 已复核当前 PEC 控制窗口、员工01《M2 知识对象持久化校验门禁》、员工02《M2 知识查询 IPC 与验收接线》、员工03《M2 分支继承与 FocusFrame 查询稳定性》、员工05《RAG-M2 Markdown 分块索引首切片》及 M2 派单。
- 当前前端只消费内核结构化查询结果；不读取 SQLite/Markdown/VectorIndex，不重算作用域、检索排序、Token 预算或状态迁移。

## 本次完成

- 在 `App.tsx` 为当前会话接入 `list_knowledge_entities` 与 `list_knowledge_relations` typed IPC，并与图、运行、FocusFrame、导入来源恢复并行加载。
- 增加查询失败的独立安全状态：知识对象读取失败不会阻断会话图恢复，状态条会显示失败提示并保留空结果，不伪造“无知识”。
- 新增 `knowledgeInventory.ts` 纯展示汇总：仅统计内核返回的实体/关系数量和原始 status 数量，画布状态条显示实体、关系及候选数量。
- 增加 `knowledgeInventory.test.ts`，验证候选/确认/否决状态按原值呈现，不改变查询顺序或执行前端筛选。
- 画布状态条入口添加 `UI-HANDOFF-06`，明确加载、空白、错误和可替换范围。

## 验证结果

- `desktop/npm run test:chat`：40/40 通过。
- `desktop/npm run test:canvas`：21/21 通过。
- `desktop/npm run build`：通过，Vite 1832 modules。
- `git diff --check`：通过。
- `node scripts/check-pec-retention.mjs`：通过，当前窗口仍为员工01～06各一份报告及控制文件。
- `node scripts/check-secrets.mjs`：通过，1476 个文本文件；未读取、记录或展示 Key。

## 未完成边界 / 需要配合

1. **员工02**：请在知识对象 IPC 写入/读取处落实员工01最新 `validate()` 门禁，并提供同一 Tauri Release 的重启 round-trip 证据；旧 Release 若未注册新命令，前端会按设计显示“知识对象读取失败”。
2. **员工05**：FTS/VectorIndex/Relation unified retrieval DTO 冻结后，再由员工04把来源摘要接入现有 FocusedContext 阅读器；本切片不提前发明检索结果或确认动作。
3. M2 仍未通过统一门禁：实体/关系/EvidenceRef 失效重建、Markdown 投影、检索闭环以及无开发服务器 Tauri Release 的 Build ID/SHA-256/跨模块证据仍待完成。

