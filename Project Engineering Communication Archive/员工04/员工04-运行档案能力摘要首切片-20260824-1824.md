# 员工04｜运行档案能力摘要首切片

> 提交时间：2026-08-24 18:24（UTC+8）  
> 覆盖任务：`CHAT-M2-001`（只读能力摘要子切片）  
> 状态：能力事实可见；运行档案可写控制等待员工01生命周期契约与员工05映射

## 相比上一份的变化

- 在已完成 `CHAT-M2-004` 导入 Intake 的基础上，接入员工05 Provider 能力目录的 TypeScript 镜像。
- 模型设置现在可以展开查看 reasoning、结构化输出、工具闭环、流式输出和生成参数能力快照。
- 能力摘要是只读派生展示，不新增参数写入、不改变旧运行、不把厂商能力冒充为本地工具闭环。

## 已阅读与落实

- 本轮开工前重新读取 PEC 当前控制文件及员工01～06最新报告；重点确认员工01的 Effective Run Profile 尚未接入 `ModelRunRequest`，员工02已完成 SQLite v6 导入表但导入事务/`ParseReport` IPC 尚未冻结，员工03只消费明确来源字段，员工05要求员工04只读消费能力快照。
- 复读 M2 派单、`v1-work-breakdown` 的 `CHAT-M2-001～010`、V1 产品/内核/契约/威胁模型/验收构建规则；因此本轮只做 `CHAT-M2-001` 的安全子切片，不实现 reasoning 请求映射、预算持久化或上下文策略算法。
- React 性能规范落实为纯 `describeModelCapabilities` 派生函数，设置面板只在模型能力对象变化时由渲染路径消费；前端设计规范落实为默认收起的“能力快照”渐进披露，不增加新视觉架构。

## 本次交付

- `desktop/src/app/providerCatalog.ts`：新增 `describeModelCapabilities` 和明确的 supported / limited / unavailable 标签。
- `desktop/src/app/providerCatalog.test.ts`：新增能力快照回归，验证 `temperature/top_p` 的 `nonReasoningOnly` 和工具闭环未接入不会被误报为可用。
- `desktop/src/components/SettingsDialog.tsx`：每个模型增加可展开只读能力摘要；未配置 Key 的模型仍可查看能力事实，但不能选择或调用。
- `desktop/src/styles.css`：能力标签使用现有深色/金色系统，支持/受限/未声明三种状态。
- UI 证据：`output/playwright/employee04-m2-capability-summary/01-capability-summary.png`。

## 契约、数据、安全与恢复影响

- 未修改 Rust 权威契约、SQLite schema、Provider 请求、模型运行终态、取消/恢复或导入数据。
- 不读取、回显、记录或持久化 API Key；预览 Provider 不显示凭据输入，安全说明继续保留。
- `toolCalling=false` 明确显示为“工具闭环未接入”，不等同于厂商没有原生能力；`nonReasoningOnly` 明确显示限制，不允许 UI 静默删参数或改变 reasoning 模式。
- 能力快照不产生网络请求、不调用模型、不写入旧运行；真正的运行档案控制仍必须等员工01/05冻结创建前预检和生命周期命令。

## 验证结果

- `npm run test:chat`：24/24 通过。
- `npm run test:canvas`：17/17 通过（包含当前共享树 M2 画布投影回归）。
- `npm run test:canvas:perf`：1/1 通过；200 节点、19 个导入来源节点基线保持通过。
- `npm run build`：通过，Vite 生产构建 1828 个模块。
- Playwright 等价引擎验收：Settings 打开、能力摘要默认收起/展开、工具边界和凭据边界均通过；0 个页面异常、0 个非预期 console error。
- `node scripts/check-secrets.mjs`：通过；`git diff --check`：通过。
- 本轮不发布 Tauri 验收程序；能力摘要尚不构成完整 M2 运行控制闭环。

## 风险与阻塞

- 员工01的 Effective Run Profile 尚未挂入 `ModelRunRequest`，所以本轮不能提供 reasoning/预算/上下文/工具的可写控制，也不能声称每次 ModelRun 已不可回写。
- 员工05已完成 `PROV-M2-002` 的 DeepSeek 纯映射和不支持参数预检，但尚未接入真实发送链路；本轮只显示目录快照，不绕过运行时实际校验。
- 员工02的 `DATA-M2-006 / DATA-M2-009` 尚未提供导入事务 IPC，`CHAT-M2-005/006` 继续等待，当前 Intake 预览按钮保持禁用。

## 下一步 WIP（最多两项）

1. 等员工01将 Effective Run Profile 挂入 `ModelRunRequest`、员工05接入映射后，继续 `CHAT-M2-001` 的创建前预检和统一 ProviderError 展示。
2. 等员工02交付解析事务 IPC 后，推进 `CHAT-M2-005` 的真实 `ParseReport` 预览、警告、重复和发送范围。

## 需要同事配合

- **员工01**：请评审能力摘要与 Effective Run Profile 的字段边界，并提供创建前预检/生命周期命令；员工04不会复制运行档案规则。
- **员工05**：纯映射/预检已收到；请在接入发送链路后提供统一不支持参数错误码与 ProviderError 形状，UI 将保持显式限制，不做静默降级。
- **员工02**：当前无需 UI 接入；解析事务、取消/失败恢复和 `ParseReport` 查询冻结后请通知员工04。
- **员工03**：能力摘要不改变“导入原文/非本地生成”来源语义；待真实导入来源查询接入后继续共同复核 Chat/Canvas 文案。
