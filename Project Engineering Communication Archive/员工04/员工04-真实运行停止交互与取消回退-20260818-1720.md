# 员工04｜真实运行停止交互与取消回退

> 提交时间：2026-08-18 17:20（UTC+8）  
> 覆盖任务：CHAT-012、CHAT-015、PROV-013～017 前端验收入口  
> 状态：停止交互与取消终态渲染已就绪；等待真实 DeepSeek cancelled 窗口证据

## 相比上一份的变化

- 默认画布视图的底部输入区新增真实“停止生成”入口，不再要求先切到阅读视图，也不再使用“停止模拟”措辞。
- 新增 `cancelRequested` / `cancelErrorMessage`：点击后显示“正在停止”并防止重复取消；命令拒绝或失败会恢复入口并给出安全提示。
- `cancelled` 事件会清除停止中状态，继续保留部分文本、保留策略说明与重试入口；画布仍消费同一共享运行投影。

## 已阅读与落实

- 开工前已阅读 PEC 控制文件及员工01～06当前报告；本轮仅推进 M1 Real Chat Alpha 的真实停止窗口。
- 已消费员工05“真实连接与首次 completed 生成通过”的交接，并对齐员工02的唯一终态/无重复运行要求。
- 未读取、回显或持久化 Key；未触碰导入、多 Provider 扩张、Harness、Skill/MCP 或整体视觉重构。

## 本次完成与证据

- `desktop/src/components/ChatWorkspace.tsx`：画布/阅读共用输入区在运行中切换为停止按钮；阅读运行卡同步展示停止中、停止失败及 cancelled 结果。
- `desktop/src/App.tsx`：取消命令加入运行ID守卫、防重复请求、拒绝回退与安全错误呈现。
- `desktop/src/app/chatRunState.ts`：补齐取消请求的确定性 UI 状态；终态统一清理临时取消状态。
- `desktop/src/app/chatRunState.test.ts`：新增重复取消抑制、命令拒绝恢复及 cancelled 清理断言。

## 验证结果

- `pnpm test:chat`：14/14通过。
- `pnpm test:canvas`：9/9通过；`pnpm test:canvas:perf`：1/1通过。
- `pnpm build`：通过，生产构建1823个模块。
- `pnpm tauri build --debug --no-bundle`：通过；新窗口产物为 `desktop/src-tauri/target/debug/mindscape-desktop.exe`。
- 本轮尚未把真实 DeepSeek cancelled 标记为通过：必须由新窗口中的真实长回答停止操作提供证据。

## 风险与阻塞

- 真实网络取消、部分内容保留和 cancelled 唯一终态仍需人工窗口验收；自动状态测试不能替代该证据。
- 超时、异常断流、用量展示与应用重启恢复仍未完成，M1 Alpha 尚不可放行。
- `git mv` 已按制度尝试；源报告未被 Git 跟踪，故改用已校验绝对路径的安全移动归档。

## 下一步 WIP（最多两项）

1. 与员工05执行真实 DeepSeek 长回答立即停止，核对画布/阅读、部分内容、cancelled 投影和重试入口。
2. 继续补齐 completed usage 的可见证据，并参与取消后重启恢复矩阵。

## 需要同事配合

- **用户/员工05**：请用新调试窗口发起明显长回答并点击底部红色停止按钮，记录 cancelled、部分内容保留及无后续 delta 的证据。
- **员工02/03**：请复核取消后仅一个稳定 Node/Run，且画布与阅读视图共享同一 cancelled 投影。
- **员工06**：请评审停止中、停止失败、已停止三态的视觉层级，不改变冻结运行语义。
