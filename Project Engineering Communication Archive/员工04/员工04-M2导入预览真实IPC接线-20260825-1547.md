# 员工04｜M2 导入预览真实 IPC 接线

> 提交时间：2026-08-25 15:47（UTC+8）  
> 覆盖任务：`CHAT-M2-005`、`CHAT-M2-006` 前端接线切片  
> 状态：Intake 已接入 `import_generic_file`，等待真实样本窗口验收与后续来源查询

## 已阅读与落实

- 已重新阅读当前 PEC 控制窗口、员工01《M2 FocusFrame 会话列表查询接线》、员工02《M2 通用导入命令 raw/解析/事务接线》、员工03《M2 FocusFrame 会话列表接入画布》、员工05《M2 Provider 与索引脱敏边界》及 M2 任务拆解。
- 员工02已交付 `import_generic_file(conversation_id, original_file_name, payload)`，返回 `source`、`revision`、`report`、`duplicate`；本次按该 DTO 接通前端，不新增或复制领域解析规则。
- 保持导入原文只进入本地内核；前端不读 SQLite/raw storage，不调用模型，不执行历史内容，不记录或回显 Key。

## 本次完成

- `desktop/src/domain/commands.ts`：补齐 `ImportGenericFileInput` 与 `GenericImportCommandResult` 类型镜像。
- `desktop/src/app/kernelClient.ts`：新增 typed `importGenericFile` IPC 封装。
- `desktop/src/components/ImportIntakeDialog.tsx`：
  - 文件选择/拖放读取 `File` bytes，粘贴内容编码为 UTF-8 bytes；
  - 粘贴入口以内部 `pasted-session.txt` 契约名提交，匹配后端 TXT 解析器且不改变用户展示文案；
  - 仅候选预检通过且处于 Tauri 回调时启用“生成本地预览”；
  - 增加本地保存/解析 loading、空内容、错误重试、重复来源和 ParseReport 结果摘要；
  - 结果展示 revision 状态、消息/警告/错误计数和来源文件名，不展示原文正文。
- `desktop/src/components/ChatWorkspace.tsx`、`desktop/src/App.tsx`：通过 App 单向传递会话 ID，真实 Tauri 模式调用 `kernelClient.importGenericFile`；浏览器预览仍保持禁用，不伪造成功结果。
- `desktop/src/styles.css`：补充真实 IPC 的 loading/成功/重复/错误视觉状态，并保留 UI-HANDOFF-06 可替换边界。

## 验证结果

- `desktop/npm run test:chat`：38/38 通过。
- `desktop/npm run test:canvas`：20/20 通过。
- `desktop/npm run test:canvas:perf`：1/1 通过；CAN-019 200 节点基线保持可重复。
- `desktop/npm run build`：通过，Vite 1831 modules。
- `desktop/src-tauri cargo test --all-targets --locked`：102 passed / 0 failed。
- `node scripts/check-pec-retention.mjs`：通过（员工01～06各一份最新报告及控制文件）。
- `node scripts/check-secrets.mjs`：通过，1403 个文本文件；`git diff --check`：通过。

## 契约、安全、恢复与未完成边界

- `import_generic_file` 的 raw 内容寻址、解析、Bundle 事务和失败清理仍由员工02后端负责；前端只传 bytes 并投影安全摘要。
- 当前命令返回结果可用于本次预览，但尚未提供 source/ParseReport 查询或进程级中断恢复；页面关闭后不自行重建导入记录，也不把导入结果伪造为会话图节点。
- 真实 Markdown/TXT/JSONL 样本的 Tauri 新窗口验收仍待执行；在该验收前不宣称 `CHAT-M2-005/006` 完整通过。

## WIP 与需要配合（最多两个）

1. **员工02**：交付 source/ParseReport 查询与进程中断恢复扫描后，我继续接入导入结果恢复、来源列表和可解释错误回显。
2. **员工01/03**：确认 FocusFrame 创建/切换入口与导入节点挂接边界；在不从坐标猜 ID 的前提下，再接入导入内容的原样续接路径。
