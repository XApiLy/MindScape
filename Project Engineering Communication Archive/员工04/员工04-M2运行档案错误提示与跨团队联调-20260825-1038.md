# 员工04｜M2 运行档案错误提示与跨团队联调

> 提交时间：2026-08-25 10:38（UTC+8）  
> 覆盖任务：`CHAT-M2-001`、`PROV-M2-003` 联调切片  
> 状态：Provider 无效请求已映射为安全、可执行的运行档案提示；导入与 FocusedContext 真实 IPC 仍待同事交付

## 已阅读与落实

- 已读取最新 PEC 控制文件、`docs/design/ui-placeholder-and-employee06-handoff.md`，以及员工01《M2 导入 Bundle 语义校验与上下文边界》、员工02《M2 导入预检与父子事务落库》、员工03《M2 知识上下文占位 UI 与 IPC 阻塞》、员工05《M2 DeepSeek 流式事件夹具回归》和员工06当前报告。
- 员工05已冻结离线 SSE reasoning/content/usage 夹具，但真实 Key/Tauri 验收仍未完成；因此本次只消费统一 `providerInvalidRequest` 错误码，不调用云端、不读取或记录 Key。
- 员工02已完成领域预检、父先子排序和事务落库；命令层/ParseReport 查询仍未交付。员工03已交付知识上下文占位阅读器；FocusedContextSnapshot 生命周期查询/恢复 IPC 仍未冻结。

## 相比上一份的变化

- `desktop/src/app/commandErrorPresentation.ts` 新增 `providerInvalidRequest` 的安全提示：引导用户检查 reasoning、temperature/top_p、输出格式或预算，再返回运行档案重试。
- `desktop/src/app/commandErrorPresentation.test.ts` 新增回归，确认无效请求不把 Provider 底层英文/请求细节直接暴露给用户，同时保留未知安全消息透传行为。
- 该提示沿用 App 现有 `safeErrorMessage` → `notice-toast` 路径；未创建额外状态、未改变运行终态和重试契约。

## 交付物与可核验位置

- `desktop/src/app/commandErrorPresentation.ts`
- `desktop/src/app/commandErrorPresentation.test.ts`
- 上一份占位 UI 交接报告已归档至 `Project Engineering Communication Archive/员工04/员工04-M2占位UI交接标记-20260824-2014.md`。

## 契约、数据、安全、恢复与跨团队接口

- 只增加前端错误码到安全中文提示的展示映射，不修改 Rust ProviderError、运行终态、IPC 字段、SQLite、凭据或恢复逻辑。
- `providerInvalidRequest` 只显示可操作的参数检查方向；不回显 Provider 响应正文、请求 JSON、Key 或完整 reasoning。
- 未接入导入命令、ParseReport/source 查询或 FocusedContextSnapshot 查询；Intake 预览和知识真实数据继续保持明确禁用/待内核接入状态。

## 验证结果

- `desktop/npm run test:chat`：32/32 通过。
- `desktop/npm run test:canvas`：18/18 通过。
- `desktop/npm run test:canvas:perf`：1/1 通过。
- `desktop/npm run build`：通过，Vite 1830 modules。
- `desktop/src-tauri cargo test --all-targets --locked`：85 passed / 0 failed。
- `node scripts/check-pec-retention.mjs`：通过，当前窗口保持 7 个 Markdown 文件。
- `node scripts/check-secrets.mjs`：通过，1352 个文本文件。
- `git diff --check`：通过。
- 本轮未执行 Tauri Release、真实 Provider Key 或浏览器视觉验收，未生成可供创始人验收的 Build ID。

## 风险与阻塞

- 员工05离线 SSE 夹具已通过，但真实 DeepSeek 仍需统一窗口验证 Off/Standard/Deep/Custom、usage、空正文、取消和断网恢复。
- 员工02尚未交付通用导入命令与 ParseReport/source 查询 IPC；员工04暂不能启用“生成本地预览”。
- 员工01尚未冻结 FocusedContextSnapshot 生命周期/恢复查询 DTO；员工03的知识阅读器目前只能显示待接入占位态。

## 下一步 WIP（最多两个）

1. 与员工05及用户在统一验收窗口验证真实 Provider 终态和运行档案错误提示；前端只核对事件投影，不复制 Provider 解析。
2. 员工02命令/查询 IPC 冻结后，确认 Intake 输入输出 DTO，接入 ParseReport 预览和错误/恢复状态。

## 需要同事配合

- **员工05**：提供真实 DeepSeek/Tauri 验收窗口及无效档案、usage、空正文、取消样本；保持 `providerInvalidRequest` 安全错误语义稳定。
- **员工02**：提供通用导入命令、ParseReport/source 查询 DTO 和失败清理/重试结果；前端不会直接读 SQLite/raw storage。
- **员工01**：冻结 FocusedContextSnapshot 查询/恢复 DTO，明确 knowledgeContext selected/omitted 与上下文账单的只读边界。
- **员工03**：在查询 DTO 到位后，继续通过现有 `knowledgeContextByNodeId` 输入消费结构化投影，不在 Chat 侧重复计算知识规则。
