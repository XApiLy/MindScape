# 员工04｜B5 导入弹窗可视高度修复

时间：2026-09-02 14:35（UTC+8）  
状态：Ready for Review

## 已阅读与落实

- 已复读当前 `PEC-提交归档与时效规则.md` 顶部“2026-08-30：M2 Go 阻断收敛”和“员工直接执行表”，并检查员工01～05最新 PEC。按要求本轮只推进 B5 的可观察 UI 证据，不重复已通过的单元切片。
- 已落实《视觉与交互设计中心》及《M2 Markdown 原生阅读与字体偏好规格》的内容优先、状态诚实、内容区独立滚动和占位 UI 交接边界；没有改变领域契约或导入 IPC。

## 本轮推进与 B 编号

- **关闭 B5 中“导入弹窗在全屏/矮窗口不可完整操作”的局部阻断**：`desktop/src/styles.css` 将导入对话框改为接近视口高度的固定框架，header/footer 固定，`.import-content` 独立滚动；压缩粘贴区、拖拽区和来源历史区，增加矮窗口媒体规则。
- B5 总体验收仍未关闭；本轮不宣称统一 Release 或真实样本纵向验收完成。

## 跨模块证据

- `ImportIntakeDialog.tsx` 的导入、预览、提案审核 DOM 结构保持不变，样式仅改善可视容器，故不改变 `importSourceId/importRevisionId`、重启发现和审核状态投影。
- `ChatWorkspace.tsx` / `App.tsx` 继续消费同一导入入口；前端构建产物包含字体和导入 chunk，未引入新的 IPC 或数据字段。
- Review Lab 员工确认主线 preview：`20260902T063249Z-b5-导入弹窗可视高度修复-d39b`（ready）。观察重点：全屏/矮窗口下标题与底部操作可见，内容区可滚动，导入按钮不被截断。

## 验证与风险

- `pnpm test:canvas`：34/34 通过。
- `pnpm build`：TypeScript/Vite 通过，2100 modules；仅有既存 chunk size warning。
- `pnpm test:chat`：69/69 通过；`pnpm test:canvas:perf`：2/2 通过。
- 实际窗口检查：在 1024×650 的矮窗口打开“粘贴会话文本”，对话框高度为 634px，底部操作栏保持在可视区；较长预览/审核内容由内容区滚动承载。
- `node scripts/check-pec-retention.mjs`、`node scripts/check-secrets.mjs`、`git diff --check`：通过。
- 风险：Review Lab preview 不是 Acceptance Release；真实 Tauri clean 构建、DPI/重启/长文现场证据仍待员工05统一执行。

## 剩余阻断与下一步（最多两个 WIP）

1. **B1** clean 集成基线与唯一 Acceptance Release 仍由员工01/05负责；当前共享工作树 dirty，不能用本次 preview 代替正式 Build。
2. **B5** 仍需员工05在同一 clean Release 串起真实导入→FocusFrame→检索→回答→候选决策→重启纵向样本；我可配合复验导入/阅读 UI 状态，不新增功能 WIP。

契约、数据、安全、恢复和跨团队接口：本轮无变化。
