# 员工04｜B5 工作区消费与落点隔离

> 提交时间：2026-09-02 12:14（UTC+8）  
> 覆盖阻断：`B2/B5-UI` 导入知识提案在 Chat 工作区的正式消费与 scope 落点隔离  
> 状态：**员工04工作区消费子项已关闭；B5 / M2 仍为 No-Go，等待权威重启发现、员工01评审和唯一 clean Release 纵向验收**

## 已阅读与落实

- 已重读控制 PEC 顶部“2026-08-30：M2 Go 阻断收敛”和员工直接执行表，并读取员工01《B2B5实体生产前半段契约冻结》、员工02《B5导入实体提案原子持久化》、员工03《B5导入知识建议审核入口》、员工05《B5建议Producer与Request链闭环》。本轮不重复员工03的审核界面，只收口员工04负责的 ChatWorkspace 权威落点投影和跨模块消费验证。
- 已落实 `mindscape.import-knowledge-proposal.v1`：React 只提交选中的 ImportedMessage、目标 scope 与允许修改的审核字段；不构造 EvidenceRef、entity ID、status、revision、generator，也不触发导入后自动分析。
- 已按现有 UI 语言做最小接线修正，没有引入新设计语言或大范围样式。本轮对正常界面没有新增视觉形态，只移除不应出现的 stale/cross-conversation/Closed/mainline 落点，因此不重复发布员工03已有的 Review Lab 主线 `20260901T155050Z-b5-导入知识建议与审核入口-f995`。

## 本次关闭的 B 编号

### B2/B5-UI：关闭员工04负责的 Chat 工作区知识落点投影缺口

- 新增 `importKnowledgeProposalTargets.ts`，把“当前会话 + 当前会话内 Active 非 mainline FocusFrame”收敛为单一、可测试的工作区投影；Conversation 始终为首选，FocusFrame 按权威 query 投影去重。
- 显式过滤跨会话残留 query、Closed FocusFrame 和 mainline，防止会话切换或异步刷新期间把无效 scope 暴露给导入审核界面。内核仍进行最终校验，前端门禁不替代领域事实。
- `ChatWorkspace → ImportIntakeDialog` 已改为消费该投影；员工03已有 request/query/review/list UI、员工05 request producer、员工02 v18 receipt/review/Vault 原子链保持不变。

## 跨模块证据

1. 正式调用链已核验：`ChatWorkspace target projection → ImportIntakeDialog message selection/review → App safe handler → kernelClient typed invoke → Tauri commands → KernelService → SQLite v18 / deterministic producer / Markdown Vault`。
2. 新增 3 项工作区投影回归：当前会话与 Active 分支正常出现；跨会话、Closed、mainline 全部排除；同一 FocusFrame 的重复节点投影只显示一次。
3. 前端门禁：Chat `69/69`、Canvas `34/34`、Canvas 性能 `2/2`、TypeScript + Vite production build `2100 modules` 全部通过。
4. 跨内核门禁：`cargo test --all-targets --all-features --locked` 为 library `243 passed / 0 failed / 1 ignored`，B5 audit example `4/4`；真实回归覆盖 request 精确重放、ImportContent EvidenceRef、FocusFrame candidate、Confirm/Reject、FTS/vector、Vault 与重启恢复。

## 契约、数据、安全与恢复影响

- 不修改领域契约、SQLite schema、Provider 或 Vault；只把现有 kernel query 投影变成可验证的 UI 落点列表。
- 不读取或输出导入正文、绝对路径、完整稳定 ID 或 Key；测试使用固定虚构 ID。
- 响应丢失后的 requestId/decisionId 重试仍由员工03 UI 与 v18 receipt 保证；本轮没有新增浏览器缓存或本地伪恢复。

## 剩余阻断与协作

1. **B5 仍未关闭**：当前 `get_import_knowledge_proposal_batch(requestId)` 需要调用方已知 requestId；缺少按 import source/revision 权威发现既有 request/batch 的查询，应用重启后不能依赖 React 临时状态恢复提案。请员工01/02冻结并实现该入口，员工03/04随后消费。
2. **评审与唯一发布**：请员工01评审 v18 原子链、Producer 与本次 scope 投影后形成 clean 集成提交；由员工05发布唯一 Acceptance Release 并从全新数据库执行完整 B5。共享树仍 dirty，本报告不宣称 B1、B5 或 M2 Go，也不发布员工04个人 Release。

## 下一步 WIP（最多两个）

1. 消费按 source/revision 发现既有 request/batch 的 typed query，补齐导入详情重启恢复状态。
2. 配合员工05在唯一 clean Release 中复验“导入 → 建议 → FocusFrame candidate → 候选选择 → Close 四动作 → Reopen/重启”。
