# 员工06｜原仓效果独立对照台

> 提交时间：2026-08-20 15:20（UTC+8）  
> 覆盖任务：VIS-008、D-046视觉路线复核  
> 状态：原仓独立对照台已完成并可评审；尚未选择生产方案  
> 对照地址：`http://127.0.0.1:4190/`  
> 基线：共享 dirty worktree，未创建个人提交

## 相比上一份的变化

- 撤销上一份报告把 Specy、Glacé 等“融合效果”作为玻璃评审基线的口径；创始人指出该实现并不等同于原仓实际效果，旧 `tools/visual-prototype` 不再作为玻璃材质通过证据。
- 新建 `tools/source-reference-lab`，把 Glacé、Shuding Liquid Glass、Ybouane LiquidGlass、React Weather Effects、Drei、Three.js 与 Joshbrew Realtime Clouds 分别装入独立 frame；各实现不共享 Shader、参数、样式或运行时。
- 对照台只提供切换、同屏、重载、单独打开和来源说明，不重画原仓效果，也不把不同路线混称为同一种材质或体积云。
- 本轮未修改 `desktop/`，没有开始生产 UI 重构。

## 已阅读与落实

- 继续遵守 Frozen V1、M1收口派单、Git/PEC留存规范、契约基线、威胁模型和视觉交互职责边界。
- 已读取员工01当前集成交接：M1仍由员工01统一决定 Go/No-Go；本轮视觉对照不扩大M1范围。
- 已读取员工02长文/错误Key证据、员工04取消闭环与员工05网络超时门禁状态；本轮没有触碰 Provider、Chat、SQLite、取消、超时或凭据契约。
- 按创始人纠偏，先忠实运行原仓，再讨论选型；在选型前不把参考实现写入 MindScape 正式界面。

## 本次完成与证据

- 玻璃材质三路独立对照：
  - Glacé `c8f5a363ab2b`：原仓站点，SVG尺寸位移、边缘折射、色差、轻模糊和 sheen。
  - Shuding Liquid Glass `a2d2e847f793`：隔离测试页直接加载原始 `liquid-glass.js`，保留其 SVG filter 透镜行为。
  - Ybouane LiquidGlass `5ebda520bebd`：原仓 WebGL 多通道折射、圆角/Z曲率、边缘高光和 Fresnel playground。
- 天气两路独立对照：React Weather Effects `8326628e18cf` 原仓 Rain/Snow/Fog，以及 Three.js 官方 WebGPU Compute Rain/Snow。
- 云层三路独立对照：Drei Cloud `ffa15b956e32` 广告牌云、Three.js dev `09860b8ff9f8` 体积纹理 Raymarching 示例、Joshbrew `3863234f739d` WebGPU Compute 体积云。
- 原仓来源、commit、许可、技术机制与边界均在对照台左侧明示；第三方说明位于 `tools/source-reference-lab/THIRD_PARTY_NOTICES.md`。
- 关键截图位于 `output/playwright/source-reference-lab/`：玻璃三路同屏、雨景、Drei Cloud、Three Volume Cloud 与 Joshbrew Realtime Clouds。

## 契约、数据、安全与恢复影响

- 未修改 Frozen V1、IPC、SQLite schema、Provider、Chat、Canvas、凭据或运行终态。
- 对照台是开发辅助工具，不读取 Prompt、会话、API Key 或用户数据库；没有任意第三方 Shader 执行入口。
- 本地原仓只绑定 `127.0.0.1:4190～4194`；Drei 与 Three.js 官方页面需要联网。
- 生产回退策略仍保持 `Classic Opaque`，但本轮尚未生成任何生产材质实现。

## 验证结果

- `tools/source-reference-lab`：`pnpm build` 通过。
- Glacé site、React Weather Effects 与 Joshbrew Realtime Clouds 原仓构建通过；Ybouane 原仓站点可直接运行。
- HTTP实测：4190对照台、4191 Glacé、4192 Ybouane、4193 `/rain` `/snow` `/fog`、4194 Joshbrew 全部返回 200。
- 真实浏览器 1600×1000：三套玻璃可同屏区分；Drei、Three Volume Cloud、Joshbrew 三套云均真实出图。
- 真实浏览器 1280×720：对照导航、来源信息和主要 frame 可用；页面允许纵向滚动，没有把 frame 压缩成静态缩略图。
- 对照台自身无浏览器错误；天气原仓产生的 WebGL attribute 与图片比例警告已如实保留并写入 README。

## 风险与阻塞

- `VIS-008`仍需要创始人逐项观看并选择玻璃、天气和云路线；当前只完成事实对照，不能自行宣称方案冻结。
- Ybouane 包元数据声明 MIT，但当前仓库根目录没有独立 `LICENSE` 文件，正式移植前需要再次确认。
- React Weather Effects 原仓生产模式的 `/snow` 与 `/fog` 返回 500，因此对照台使用原仓开发服务器；雨景还存在原仓 WebGL attribute 警告。
- Joshbrew 首次初始化需要数秒并依赖 WebGPU/OffscreenCanvas；Drei 与 Three.js 在线页面受网络可用性影响。

## 下一步 WIP（最多两项）

1. 创始人在 4190 对照台逐项选择或排除玻璃、天气与云实现，员工06只记录视觉结论。
2. 选型后制作精确的视觉令牌、性能档与员工03/04工程交接；在范围门禁通过前仍不修改 `desktop/`。

## 需要同事配合

- **创始人**：优先评审“玻璃材质 → 同屏对照”，明确 Glacé、Shuding、Ybouane 哪一路作为后续目标；云与天气可分开决定。
- **员工01**：继续独立推进M1 Go/No-Go，本对照台不是M1验收Build的一部分。
- **员工03/04**：暂不移植参考仓代码；等待创始人选型和员工06交接包。
