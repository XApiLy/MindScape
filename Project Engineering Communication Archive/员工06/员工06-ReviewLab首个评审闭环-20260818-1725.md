# 员工06｜Review Lab 首个评审闭环

> 提交时间：2026-08-18 17:25（UTC+8）  
> 覆盖任务：UX-003～006、UX-008～009（设计评审基础设施）  
> 状态：首个可用闭环完成，等待创始人实际评审  
> 基线提交：`f54363e`（共享工作树）

## 相比上一份的变化

- 员工06已由创始人明确承接，建立持续沟通、版本展示、反馈记录和跨员工转派的工作方式。
- 新增独立开发工具 `tools/review-lab/`，不集成进 MindScape 产品，也不占用 M1 产品功能范围。
- 已完成首个真实预览版本归档，验证人工标记、构建、历史保留、并排比较、构建日志和版本评审记录闭环。

## 已阅读与落实

- 已精读产品共识、V1范围、视觉与交互岗位职责、总路线、M1派单、PEC规则和员工06上一份当前报告。
- 已精读员工03当前“画布共享运行快照与终态一致性”和员工04当前“真实运行停止交互与取消回退”，并扫读员工01、02、05当前状态。
- 具体落实：Review Lab 只展示和评审界面，不成为 Node、ModelRun、取消或恢复的事实源；后续停止中、停止失败、已停止三态规格必须消费员工04现有状态，且不得把员工02定义的幂等重放呈现成新运行。
- 员工05已取得真实 DeepSeek completed 证据，但取消、超时、断流和重启仍待验收；本轮没有用 Mock 预览替代真实 Tauri Alpha 门禁。

## 本次完成与证据

- `tools/review-lab/server.mjs`：仅监听本机的版本 API、构建队列、源码指纹、静态归档、评审写入、日志读取和完整版本启动入口。
- `tools/review-lab/public/`：版本时间线、内嵌交互预览、双版本比较、构建状态、版本元数据和确认/修改/否决/后置评审界面。
- `tools/review-lab/lib.mjs`、`lib.test.mjs`：输入收敛、版本 ID、评审结论和路径相关基础约束测试。
- `tools/review-lab/README.md`：启动方式、构建等级、数据安全边界、限制和后续接入说明。
- 首个成功归档：`Review Lab 可用原型`，预览页面和版本化 JS 资源均从独立历史路径返回 HTTP 200；源码在构建期间未变化。
- 首次 Windows `npm.cmd` 启动失败也被保留为失败版本，并追加“需要修改”自检记录；修复后第二次构建通过，没有覆盖失败历史。

## 契约、数据和安全影响

- 未修改 `desktop/` 产品代码、Frozen V1领域契约、SQLite schema、Provider或凭据边界。
- Review Lab 固定执行 `npm run build` 和可选 `npm run tauri -- build`，不暴露任意命令执行接口，只监听 `127.0.0.1`。
- 所有版本产物位于被 Git 忽略的 `tools/review-lab/runtime-data/`；静态预览使用现有 Mock Preview，不读取或复制真实 API Key。
- 每次构建记录 Git提交、脏文件数量和源码指纹；构建期间源码变化会标记为风险版本，不能伪装成可复现快照。

## 验证结果

- `node --test`：4/4通过。
- `node --check server.mjs`、`node --check public/app.js`：通过。
- `git diff --check -- tools/review-lab`：通过。
- Review Lab 首页、`/api/system`、`/api/versions`、版本预览、归档 JS 资源和评审写入接口均完成本机实际请求验证。
- Review Lab 触发 `desktop` 前端生产构建：通过；首个成功版本状态为 `ready`。

## 风险与阻塞

- 当前评审意见绑定到版本，尚不能直接点击界面坐标生成批注。
- 完整 Windows Tauri 构建入口已实现但本轮未额外执行正式 bundle；真实应用验收仍按 M1 统一脚本完成，不能由浏览器预览替代。
- 当前共享工作区存在多名员工未提交改动，工具会如实记录 dirty 状态；严格发布候选仍应从已提交 SHA 构建。
- Review Lab 当前为本机归档，尚未提供团队共享存储和权限控制。

## 下一步 WIP（最多两项）

1. 与创始人在 Review Lab 中完成第一次真实体验评审，记录保留、修改、否决和后置结论。
2. 使用当前 Chat/画布版本审阅流式、停止中、停止失败、cancelled和恢复表现，形成 UX-009 状态规格并转交员工03、04。

## 需要同事配合

- **创始人**：启动 Review Lab 并确认版本时间线、交互预览、双版本比较和评审记录是否符合持续沟通方式。
- **员工03**：后续可评审画布推进请明确标记版本，并提供节点、终态、视口和真实性能的重点确认项。
- **员工04**：请与员工06共同审阅停止中、停止失败、已停止三态；交付状态事实，员工06负责视觉层级和动效规格。
- **员工02/05**：取消、幂等、恢复和Provider事实继续由领域/运行时提供，Review Lab只记录其界面验收结果。
