# 员工06｜分层视觉系统完整执行计划

> 提交时间：2026-08-19 15:51（UTC+8）  
> 覆盖任务：VISUAL-FOUNDATION-001、VIS-001～020  
> 状态：执行计划已建立；设计/原型轨可开始，生产实现等待门禁  
> 基线提交：`0cb1c00` 后的当前共享工作区

## 相比上一份的变化

- 将分层视觉与氛围方向从设计基线扩展为完整执行计划，建立 `VIS-001～020`、唯一负责人、依赖、阶段出口、证据、回滚和完成定义。
- 明确两条执行轨：员工06可以立即做现状审计和独立原型；员工02/03/04的生产任务必须等待 M1、创始人原型签字和正式范围变更。
- 为原型设定可调整的默认值：云雾/雨/光影、无声音、壁纸推荐但可覆盖、区域覆盖先到L2、轻柔模式不穿过工作内容。
- 明确未来生产架构的状态边界、Wallpaper Engine安全边界、性能/可访问性硬门禁和统一 Tauri验收方式。

## 已阅读与落实

- 已阅读 `docs/engineering/v1-master-roadmap-and-ownership.md` 与当前 M1派单，落实为“计划可发布但整体生产视觉重构不能在M1静默开工”。
- 已阅读 `docs/v1-product-scope.md`，确认现有V1只把主题微调列为P1；完整背景/材质/氛围系统进入生产前必须完成范围变更或排到V1后。
- 已阅读 `docs/v1-work-breakdown.md`，沿用单一直接负责人、S/M/L、依赖、Definition of Done和UX/CAN/CHAT/QA/REL协作方式；专项编号使用`VIS-*`避免改写既有任务语义。
- 已阅读 `docs/engineering/acceptance-build-policy.md`，最终视觉验收只接受同一个正式Tauri Build ID，不以原型、Review Lab或浏览器Preview替代。
- 已复核 `desktop/src` 当前结构与测试入口，使计划落点对应真实 `App`、WorkspaceSidebar、ChatWorkspace、ConversationCanvas、SettingsDialog和现有Node测试，而非旧原型。

## 本次完成与证据

- 新增 `docs/engineering/layered-visual-system-execution-plan-20260819.md`，包含目标、非目标、任务表、七阶段、架构建议、Wallpaper适配、安全、测试、PR/PEC、回滚、风险和完成定义。
- 更新设计基线至v0.2并挂接执行计划，区分最终产品待确认项与原型执行默认值。
- 更新 `docs/decisions.md`，新增D-045，记录独立原型与生产范围门禁。
- 更新 `docs/discussion-log.md`，记录计划形成和未向员工02/03/04发出生产开工指令的原因。

## 契约、数据和安全影响

- 当前仍是文档与计划变化，没有修改Frozen V1契约、数据库、Provider、凭据、恢复逻辑或正式UI代码。
- 未来视觉偏好计划使用独立带版本的本地配置，不能进入会话图、ContextSnapshot或ModelRun。
- Wallpaper Engine首批只计划只读发现与安全图片/视频兼容分类；不执行Scene/Application/Web脚本壁纸，不复制用户订阅内容到仓库或证据。
- 所有生产任务仍需在实际实现前经过对应负责人设计评审，计划中的建议目录不是未经评审的冻结接口。

## 验证结果

- 计划已覆盖项目现有的范围门禁、单一负责人、个人最多两个WIP、阶段出口、真实证据、统一验收程序和PEC制度。
- 已逐项映射员工02、03、04与06的所有权；员工01只在领域越界时评审，员工05无本计划直接任务。
- 已建立原型评审脚本和背景/材质/区域/业务状态/专注状态/设备策略/尺寸验收矩阵。
- `node scripts/check-pec-retention.mjs` 已通过：员工01～06各一份当前报告，另有一份控制文件，当前窗口保持七份。

## 风险与阻塞

- 完整视觉系统超过当前V1“主题微调”范围；原型确认后仍需要创始人/产品负责人决定纳入V1还是V1后实施。
- Wallpaper Engine Scene/Application/Web类型兼容性存在技术和安全不确定性，计划已限定首批承诺，不以设想冒充支持。
- 透明材质、视频背景、200节点画布和氛围叠加的性能只能在真实Tauri与目标设备上证明，原型流畅不算生产通过。
- 生产轨目前按规则阻塞，设计与原型轨无阻塞。

## 下一步 WIP（最多两项）

1. 执行 `VIS-001`：形成当前正式界面保留/修改/删除/后置审计。
2. 执行 `VIS-002`：形成关键业务状态、视觉组合和专注衰减场景矩阵，然后进入独立原型。

## 需要同事配合

- **创始人**：在独立原型生成后按统一脚本确认或修改执行默认值，并决定生产范围门禁。
- **员工03、员工04**：现阶段只需在审计时提供当前行为和工程约束，不开始视觉生产重构。
- **员工02**：现阶段不开始Wallpaper扫描；生产范围通过后再负责本地发现、安全与偏好持久化。
