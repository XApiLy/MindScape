# 员工06｜Review Lab 历史预览可见性修复

> 提交时间：2026-08-19 11:16（UTC+8）  
> 触发：创始人截图指出选中版本后中央没有 MindScape 界面  
> 状态：根因已确认并修复，失败历史版本的已归档前端预览可正常查看和指向  
> 基线分支：`codex/m1-integration-recovery`

## 相比上一份的变化

- 上一份完成 M1 视觉收口和 Review Lab 构建冻结；本次只修复创始人真实使用中暴露的预览可见性缺陷，不扩展新功能。
- 修复 HTML `hidden` 属性被 `.empty-state { display:flex }` 和 `.preview-layout { display:grid }` 覆盖的问题，防止工具空状态占据整个第一屏、把真实预览挤到下方。
- 解耦“前端预览是否存在”和“完整原生构建是否成功”：只要版本已归档 `previewUrl`，即使后续 Tauri/安装包阶段失败，仍展示界面并允许创建视觉引用；构建失败仍在右侧详情中如实标记。

## 已阅读与落实

- 延续产品范围、M1 收口派单、Review Lab 使用说明、PEC 保留制度和上一份员工06 PEC 结论。
- 创始人截图中选中的是红点 `CAN-014 画布视口持久化（CI修复重建）`；该版本的 Vite 前端已成功归档，失败发生在后续原生打包阶段，因此“无界面”不是合理产品行为。
- 本次不修改 `desktop/`、不改 Node/ModelRun/Provider/SQLite/凭据/恢复事实，只修复 Review Lab 的展示判断。

## 修复与证据

- `tools/review-lab/public/styles.css`：新增 `[hidden] { display: none !important; }`，确保空状态、预览布局、检查器、对话框等基于 `hidden` 的切换有确定语义。
- `tools/review-lab/public/app.js`：`setPreview` 和“指向界面细节”的可用性由“状态必须 ready/warning”改为“已存在 `previewUrl`”。
- Playwright + Chrome 1440×900 回归同一失败版本：
  - `#empty-state` 计算样式为 `display:none`。
  - `#preview-layout` 为 `display:grid`。
  - `#primary-preview` 为 `display:block`，`src` 指向该失败版本自己的历史归档。
  - 截图中 MindScape 侧边栏、画布、输入器和顶部控件均可见；右侧仍明确显示“构建失败”，没有篡改历史结果。
- `node --check tools/review-lab/public/app.js`：通过。
- Review Lab `npm run test`：8/8 通过。

## 契约、数据与安全影响

- 无产品契约、数据库、Provider、凭据、恢复或网络边界变化。
- 没有覆盖或删除失败历史；只重用当时已保存的前端归档。
- Review Lab 仍仅供 `127.0.0.1` 本机使用。

## 风险与阻塞

- 失败版本的预览只能证明前端视觉产物，不代表原生 Provider、SQLite、凭据、恢复或安装包能力通过；右侧失败标记必须保留。
- 暂无其他阻塞。

## 下一步 WIP（最多两项）

1. 请创始人刷新 Review Lab，在同一红点历史版本中核对 MindScape 界面已显示。
2. 回到 M1 视觉收口原型的方向确认；Review Lab 仍保持功能冻结，除阻断使用的缺陷外不扩展。

## 需要谁配合

- **创始人**：刷新当前页面后确认界面可见；然后继续直接指出要讨论的版本与细节。
- **其他员工**：无新派单。
