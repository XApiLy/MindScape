# 员工06｜VIS-008 选型冻结与 VIS-009 工程交接

> 提交时间：2026-08-20 22:52（UTC+8）  
> 直接安排：创始人  
> 覆盖任务：VIS-008、VIS-009、D-047  
> 状态：VIS-008 已由创始人确认；VIS-009 交接包已就绪，等待员工03/04书面评审  
> 原型地址：`http://127.0.0.1:4200/`  
> 生产状态：未修改 `desktop/`；M1 已 Go，视觉范围变更与员工03/04影响评审仍未通过

## 本轮确认

创始人完成独立原仓对照并确认以下一对一职责：

- Liquid Glass 使用 Ybouane LiquidGlass。
- 毛玻璃使用 Glacé。
- 天气使用 React Weather Effects。
- 云使用 Drei `Cloud` / `Clouds`。

Shuding、Specy、Three.js 体积云、Joshbrew Realtime Clouds 和早期自制 CSS/Shader 近似不进入冻结方案。它们仍可在 `tools/source-reference-lab` 作为被排除或后置参考，但不能再次混入对应职责。

## 已完成

- 建立 `tools/selected-visual-baseline`，直接 alias 四个本地原仓源文件，未复制 Shader 或重画效果。
- 保留 MindScape 现有左侧会话区、中央节点、右侧阅读区、底部编辑器和外观设置布局。
- Ybouane、Glacé、React Weather 与 Drei Cloud 通过统一 Surface / Atmosphere 控制进入布局；全局材质与侧边栏、节点、阅读区、编辑器区域覆盖分离。
- 输入聚焦、设置打开、聚焦阅读和失焦时天气/云退让；减少动态停用连续运动；低性能直接卸载天气与 Drei Canvas 并把 Ybouane 区域解析为 `Classic Opaque`。
- 修正低性能路径：Drei 禁用时不再后台创建 Fiber Canvas。
- 新增 `docs/design/selected-visual-source-baseline-20260820.md`、`docs/engineering/selected-visual-runtime-handoff-20260820.md`，更新设计基线、执行计划、决策 D-047 和讨论记录。

## 浏览器证据

- 1600×1000：Ybouane 5 个区域实例、天气 1 个 Canvas、Drei 1 个 Canvas；`/undefined` 纹理请求为 0；浏览器错误为 0。
- 1280×720：天气与云 Canvas 均为 1280×720；文档宽高无溢出；设置面板可滚动，主要工作区仍可见。
- 输入聚焦：天气透明度从 `0.62` 退至 `0.1116`，云从 `0.50` 退至 `0.09`，页面文案显示“天气与云已退让”。
- 低性能：天气 Canvas、Drei Canvas、Ybouane Canvas 均为 0，6 个主表面为 Opaque。
- 真实截图：`output/playwright/selected-visual-baseline/`。

浏览器控制台仍有 React Weather 原仓的 4 条 WebGL attribute warning；没有 MindScape 新增 error。该原仓的 `RainEffect` 清理不完整，因此原型切换天气预设采用页面重载，生产适配器必须补齐生命周期或明确隔离策略。

## 交接请求

**员工03**：评审 Drei Cloud 的懒加载、远程纹理本地化、Canvas 关闭即卸载、氛围衰减信号、低干扰区和性能预算；不要直接启动 `VIS-015/017/019` 生产实现。

**员工04**：评审 Appearance 纯状态解析器、三种 Surface 后端、Ybouane root 隔离、Glacé filter/回退、React Weather 生命周期和工作区焦点信号；不要在 Chat/工作区建立第二套主题系统。

**员工01**：已于 2026-08-20 签署 M1 Go；本原型和交接不占用该 M1 验收 Build，也不自动进入 M2 或视觉生产开发。

## 已知前置条件与风险

- Ybouane 包元数据声明 MIT，但审核快照根目录没有独立 `LICENSE`；生产前必须确认并归档许可证文本。
- Glacé、React Weather Effects、Drei 为 MIT，移植时保留版权声明。
- Drei 默认云纹理来自远程资源，生产前必须审核并版本化入包。
- 原型中 Drei 动态块约 859KB，已与主壳分块，生产仍需预算和缓存评审。
- React Weather 的 Snow/Fog 已在 4190 原仓对照台确认，完整生产生命周期仍需适配测试。
- M1 Go 条件已经满足；`desktop/` 生产重构仍必须等待正式范围变更、员工03/04书面评审与创始人开工指令，并最终使用同一 Tauri Build 验收，不得从本 PEC 推断开工授权。

## 下一步 WIP（最多两项）

1. 保持 4200 原型供创始人继续观察与指出视觉细节。
2. 收集员工03/04对 VIS-009 的所有权、依赖和不可实现项反馈；门禁通过后再支持生产集成。

## 证据与检查

- 选定原型地址：`http://127.0.0.1:4200/`
- 设计基线：`docs/design/selected-visual-source-baseline-20260820.md`
- 工程交接：`docs/engineering/selected-visual-runtime-handoff-20260820.md`
- 组合原型说明：`tools/selected-visual-baseline/README.md`
- 原仓对照台：`http://127.0.0.1:4190/`
