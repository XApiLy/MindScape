# 员工01｜M2 FocusFrame 查询生命周期门禁

时间：2026-08-25 17:48（Asia/Shanghai）  
状态：查询投影门禁完成；FocusedContext SQLite 接线已完成切片，等待统一真实样本验收

## 已阅读与落实

- 已阅读最新 PEC 控制文件（2026-08-25 M2 状态复审）、员工02《M2 FocusedContext 快照持久化与查询》、员工03《M2 分支继承与 FocusFrame 查询稳定性》、员工04《M2 FocusFrame 查询恢复错误态》、员工05《RAG-M2 Markdown 分块索引首切片》及员工06当前报告。
- 控制文件明确 M2 仍为 No-Go，第一共同门禁是 FocusedContextSnapshot SQLite 持久化、查询和重启恢复；本轮不宣称 M2 验收，只补查询边界并评审员工02已出现的 V8 接线。
- 按“生命周期是状态权威、focusedContext 可为空”的契约，查询校验现在先拒绝非法生命周期，再校验可选 FocusedContext，不从 UI 或空值推断状态。

## 本次推进（CORE-M2-001/002 支撑）

- 为 `FocusFrameLifecycleSnapshot` 新增 `validate()`：校验生命周期契约版本、稳定 FocusFrame、revision>0、更新时间，以及 active/closed 与 `closedAt` 的一致性。
- `validate_focus_frame_query_projection` 复用生命周期校验；增加 active 携带 `closedAt` 的回归测试，避免重启后投影出不可能状态。
- 更新 M2 契约基线，冻结查询投影的生命周期字段门禁。
- 评审员工02当前共享工作树：`focused_context_snapshots` V8 表、upsert/get 查询和应用层保存/列表恢复接线已完成；写入与读取均调用本轮 FocusedContext 校验，员工02报告已给出 113 项 Rust 回归和 Clippy 证据。共同门禁 1 的代码切片已具备，仍需统一 Tauri Release 的真实保存→重启→查询样本。

## 交付物与验证

- `desktop/src-tauri/src/domain/focus_lifecycle.rs`
- `desktop/src-tauri/src/domain/focus_query.rs`
- `docs/contracts/m2-contract-baseline.md`
- Rust：113 passed / 0 failed（共享工作树最终状态）
- Clippy：`-D warnings` 通过
- `cargo fmt --all -- --check`、`git diff --check`、PEC retention：通过

## 契约、数据、安全与恢复影响

- 只新增纯领域查询门禁，不改变 V1 ContextSnapshot 语义、Provider、凭据或原始导入内容。
- FocusedContext 快照仍可为 `null`；持久化读取失败必须返回错误，不得伪造“无知识”。
- 生命周期校验在查询边界执行，拦截 SQLite/重启恢复中的错误 revision、时间和 active/closed 元数据。

## 需要其他员工配合 / 阻塞

- **员工02**：补统一 Tauri Release 的保存→重启→查询真实样本，并确认损坏 JSON/跨会话记录在发布程序中仍安全失败；代码切片已完成，但共同门禁 1 在真实样本前保持未关闭。
- **员工03/04**：等待 FocusedContext 真实查询恢复后复核画布/Chat 三态，不把 `null` 显示为“无知识”。
- **员工05**：共享工作树的 Markdown chunking 新增 `flush` 参数后有一处调用遗漏，本轮已补齐 `&mut chunks` 以恢复编译；请将该修正纳入你的提交和 PEC，不要由员工01代收归属。

## 下一步（最多两个 WIP）

1. 评审员工02的 FocusedContext SQLite/restart 测试，补领域侧发现的跨会话或生命周期拼接回归。
2. 与员工05确认 VectorMatch → KnowledgeRetrievalCandidate 的 evidence/scope 边界，不进入 SQLite 字段实现。
