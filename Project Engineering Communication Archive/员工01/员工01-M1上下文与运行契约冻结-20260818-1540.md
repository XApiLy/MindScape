# 员工01｜M1上下文与运行契约冻结

> 提交时间：2026-08-18 15:40（UTC+8）  
> 覆盖任务：ARC-015、CORE-009～013、CORE-020  
> 状态：契约切片已完成，等待跨团队联调评审  
> 基线提交：`f54363e`（当前分支`docs/REL-002-pec-retention`；Draft PR受只读Git元数据限制尚未创建）

## 相比上一份的变化

- `mindscape.context.v1`、`mindscape.runtime.v1`及M1分支上下文语义由RC1进入Frozen V1；Import/Evidence继续保持RC1。
- 冻结单一`start_model_run`边界和completed/cancelled/failed/application_interrupted唯一语义。
- 上下文编译新增可选预算、最旧完整问答裁剪和当前输入超限显式失败。
- Rust测试由23项增至29项，完成全量工程验证。

## 已阅读与落实

- 已完整阅读V1范围、2026-08-17产品共识、总路线、M1派单、内核总览、前五人责任书、契约、安全、Git、PEC制度、架构走查及员工02～05当前PEC；员工02、05在本轮更新PEC后已重新读取。
- 具体落实：UI不得再串联`append_turn → run_model → complete_turn`；进程中断采用`failed + providerCode=application_interrupted`，不增加平行`interrupted`枚举。
- 员工02～05对M1 Context/Runtime无契约阻断；M2导入契约未获得专项消费方，不提前冻结。

## 本次完成与证据

- `desktop/src-tauri/src/domain/context.rs`：覆盖根、继续、深入、发散、换角度和预算规则。
- `desktop/src-tauri/src/domain/contracts/runtime.rs`：统一事件到RunState映射及本地中断构造器。
- `desktop/src-tauri/src/adapters/sqlite.rs`：消费统一状态映射，并验证schema v4完整冻结请求往返。
- `desktop/src/domain/runtime.ts`：同步运行投影与中断代码镜像。
- `docs/contracts/v1-contract-baseline.md`、`docs/architecture/v1-architecture-walkthrough.md`：记录冻结边界、终态表和消费方结论。
- `docs/decisions.md`新增D-041，工程入口和讨论日志同步更新。

## 契约、数据和安全影响

- Frozen序列化字段和枚举未发生破坏性变化；新增Rust辅助方法、TypeScript常量和加法查询投影。
- schema保持v4；完整ModelRunRequest、事件序号、幂等键、部分内容和终态可无损保存。
- ModelRunRequest仍不携带明文Key；Provider只消费冻结快照，UI与Provider不得自行选择历史。
- 冻结后的字段、枚举或含义变化必须先提交ADR并说明迁移与回滚。

## 验证结果

- `cargo test --no-fail-fast`：29/29通过。
- `cargo clippy --all-targets --all-features -- -D warnings`：通过。
- `pnpm test:chat`：4/4通过。
- `pnpm test:canvas`：3/3通过。
- `pnpm build`：通过。
- `pnpm tauri build --debug --no-bundle`：通过，生成Windows调试应用。
- `git diff --check`与`node scripts/check-pec-retention.mjs`：通过。

## 风险与阻塞

- 员工04尚未切换统一启动命令，旧前端三段式编排仍是M1阻断。
- 员工05真实DeepSeek请求、错误矩阵和真实取消尚未完成，不能宣称真实Chat Alpha通过。
- 上下文预算算法已完成，但模型`contextWindowTokens`到编译器预算的可信接线仍需员工02/05联调，当前统一启动路径默认不裁剪。
- 工作区包含多人并行未提交变化；`.git`只读导致无法创建分支、Draft PR或执行`git mv`，提交时必须确认PEC归档识别为rename。

## 下一步 WIP（最多两项）

1. ARC-015联调评审：审查员工02协调器、员工04命令切换和员工05真实Provider是否遵守Frozen V1。
2. CORE-012/020：接入可信模型上下文窗口预算，补上下文属性测试和真实分支快照验收。

## 需要同事配合

- **员工02**：在统一启动路径接收可信模型上下文窗口并传入编译器；保持Node/ModelRun终态同事务。
- **员工03**：按运行查询投影验证停止、失败、中断后画布与阅读视图一致。
- **员工04**：移除三段式前端编排，消费`start_model_run`、`list_model_runs`及`application_interrupted`提示。
- **员工05**：从注册表提供模型上下文窗口，并完成DeepSeek成功、取消、鉴权、超时、用量和脱敏矩阵。
