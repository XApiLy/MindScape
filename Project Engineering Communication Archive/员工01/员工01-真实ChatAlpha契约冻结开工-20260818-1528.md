# 员工01｜真实Chat Alpha契约冻结开工

> 提交时间：2026-08-18 15:28（UTC+8）  
> 覆盖任务：ARC-015、CORE-009～013  
> 状态：进行中  
> 基线提交：`f54363e`（当前分支 `docs/REL-002-pec-retention`；Draft PR待创建）

## 相比上一份的变化

- 当前唯一执行窗口已切换为M1真实Chat Alpha，不再推进导入、多Provider、Harness、Skill/MCP或视觉重构。
- 收到员工04、05“RC1无阻断”书面结论；员工03未报告领域冲突；员工02要求核验schema v4无损映射。
- WIP收敛为ARC-015契约冻结与CORE-009～013上下文验收两项。

## 已阅读与落实

- 已完整阅读V1范围、2026-08-17产品共识、总路线、M1派单、内核总览、前五人责任书、RC1契约、安全威胁模型、Git制度、PEC制度及员工02～05当前PEC。
- 落实影响：必须冻结单一“开始模型运行”边界，UI不得继续编排`append_turn → run_model → complete_turn`；四种终态必须由领域与应用层统一解释。
- 当前冲突：Tauri仍暴露分离命令，schema v4映射尚待员工01核验，因此RC1暂不标记Frozen V1。

## 本次完成与证据

- 完成强制阅读、消费方反馈归类及当前实现入口审计。
- 已确认员工01本轮验收对象与No-Go，不以其他员工的实现代替契约冻结。
- 开工前PEC已按六文件窗口要求替换；旧报告移入`Project Engineering Communication Archive/员工01/`。

## 契约、数据和安全影响

- 当前尚未修改领域契约、数据库schema或安全边界。
- 后续若发现破坏性契约变化，将先提交ADR；非破坏性澄清同步Rust权威类型、TypeScript镜像和契约说明。
- API Key仍只允许进入操作系统安全凭据，ModelRunRequest不得携带明文Key。

## 验证结果

- `node scripts/check-pec-retention.mjs`将在新快照写入后执行。
- 工程测试将在代码切片完成后执行，当前不沿用2026-08-14结果冒充本轮验证。

## 风险与阻塞

- 当前工作区已有其他未提交变更，员工01将保留并避免覆盖。
- 受托管权限限制，`.git`只读导致`git mv`无法创建`index.lock`；已执行同路径文件移动，后续暂存/提交必须确认Git识别为rename。
- 员工02尚需提供最终协调器与恢复投影；员工01只冻结边界和验收，不接管其实现。

## 下一步 WIP（最多两项）

1. ARC-015：核验schema v4映射，冻结单一运行边界和completed/cancelled/failed/interrupted语义。
2. CORE-009～013：补齐根、继续、深入、发散、换角度的ContextSnapshot与预算验收测试。

## 需要同事配合

- **员工02**：确认协调器、终态事务和启动恢复投影按冻结语义实现。
- **员工03**：补充画布对Node/Run终态投影无平行语义的明确结论。
- **员工04**：切换统一启动命令后移除前端三段式运行编排。
- **员工05**：继续保证Provider只消费冻结请求并输出统一事件。
