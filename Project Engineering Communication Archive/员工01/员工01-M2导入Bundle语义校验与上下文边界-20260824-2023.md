# 员工01｜M2 导入 Bundle 语义校验与上下文边界

> 提交时间：2026-08-24 20:23（UTC+8）  
> 覆盖任务：`IMP-M2-001～003` 评审切片、`CORE-M2-004` 联调边界、`ARC-M2-001` 评审  
> 状态：通用导入 bundle 领域预检完成；知识上下文与运行档案消费边界继续等待命令/查询 IPC 联调

## 已阅读与落实

- 已阅读最新 PEC 控制文件、M2 派单、V1 产品范围/路线、Core Kernel、前五员工职责、RC1、威胁模型、架构走查、Git/PEC/验收制度，以及本地实体/RAG、Markdown Vault/分支隔离、模型控制与聚焦连续性方案。
- 已重新读取员工02～06当前报告：员工02已完成 `persist_import_bundle` 原子事务并等待命令层；员工03已完成 `KnowledgeContextSelection` 画布投影；员工04已补齐运行档案、上下文账单和 Intake 的 `UI-HANDOFF-06` 占位交接；员工05已完成 reasoning/content/usage/终态回归矩阵；员工06仍为创始人直管视觉席位。
- 具体落实：本次只补领域级导入预检和契约文档，不改员工02的 SQLite 实现、不接管员工03/04 UI、不复制员工05 Provider 逻辑，也不触碰员工06原型。

## 相比上一份 PEC 的变化

- 新增 `desktop/src-tauri/src/domain/imports.rs` 的 `validate_import_bundle`，并从 `domain/mod.rs` 导出为命令/存储共用的纯语义入口。
- 预检覆盖：source/revision/report 引用一致性、必填稳定 ID、message revision、一致的 ParseReport message count、message ID 唯一、source locator 唯一、父消息存在性、自引用和父图环路。
- 更新 `docs/contracts/m2-contract-baseline.md`：明确导入预检是单一领域语义，并记录员工04功能占位 UI 已能构造 EffectiveRunProfile；真实 Key、不可变查询/重启投影和知识生命周期仍未完成。

## 交付物与验证证据

- 代码：`desktop/src-tauri/src/domain/imports.rs`、`desktop/src-tauri/src/domain/mod.rs`。
- 契约：`docs/contracts/m2-contract-baseline.md`。
- Rust 单测：`cargo test --all-targets --locked`，82 passed / 0 failed；新增有效 bundle、重复 locator、缺失父节点、环路和计数不一致测试。
- Rust 门禁：`cargo fmt --all -- --check`、`cargo clippy --all-targets --all-features --locked -- -D warnings` 通过。
- 前端门禁：`pnpm exec tsc --noEmit` 通过；`pnpm test:chat` 31/31；`pnpm test:canvas` 18/18；`pnpm test:canvas:perf` 1/1；`pnpm build` 通过（1830 modules）。
- 仓库门禁：`git diff --check`、`node scripts/check-pec-retention.mjs`、`node scripts/check-secrets.mjs` 通过。

## 契约、数据、安全、恢复与接口影响

- 新增纯领域预检，不改变 M1 `ContextSnapshot`、runtime、SQLite schema 或 Provider 请求；不读取 raw storage、密钥或用户原文日志。
- 校验允许 batch 中子消息出现在父消息之前，语义上与输入顺序无关；存储层在立即外键约束下仍必须按父先子后插入，或使用等价的事务内延迟策略。
- 预检失败发生在事务前，不产生半个 bundle；原始字节与结构化投影的原子关系仍由员工02命令层负责。
- `KnowledgeContextSelection`、画布投影和 EffectiveRunProfile UI 均保持单向消费；前端不得重算状态、排序、预算或 Provider 参数。

## 已知风险与阻塞

- 员工02的 `persist_import_bundle` 尚未调用本预检，也尚未提供通用导入命令/ParseReport 查询 IPC；接线时需处理父消息插入顺序和失败后的 raw orphan 清理/重试策略。
- `FocusedContextSnapshot` 的生命周期查询/恢复 IPC 尚未冻结，员工03画布投影和员工04账单暂不能接入真实知识快照。
- 员工05的 Provider 终态矩阵尚未进入真实 DeepSeek Key/Tauri 验收；本次不宣称 M2 Go。

## 下一步 WIP（最多两个）

1. **IMP-M2-004 / DATA-M2-006**：与员工02将 `validate_import_bundle` 接入导入命令前置校验，冻结安全 ParseReport 错误 DTO、父消息批量写入顺序和重启重试语义。
2. **CORE-M2-004 / ARC-M2-001 联调**：与员工02/03/04冻结 FocusedContextSnapshot 查询/恢复 DTO，核对 `knowledgeContext.selected/omitted`、EffectiveRunProfile 和上下文账单的不可变投影。

## 需要同事配合

- **员工02**：消费 `validate_import_bundle`，在 SQLite 事务前拒绝非法 bundle；补父消息顺序、重复 hash、失败回滚和 raw 清理/重试测试。
- **员工03**：继续只投影结构化 `KnowledgeContextSelection`，不从画布重算候选状态或 Token 预算。
- **员工04**：消费结构化导入/FocusedContextSnapshot/EffectiveRunProfile 查询，不在占位 UI 复制领域规则；保持错误、空白、停止和恢复状态可见。
- **员工05**：提供真实 reasoning/content/usage/终态验收结果，保持 Provider 映射和错误语义唯一归属。

