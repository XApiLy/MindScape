# 员工01｜M2 FocusedContext 持久化查询校验

时间：2026-08-25 17:10（Asia/Shanghai）  
状态：领域校验闭环完成，等待员工02接入持久化读写

## 已阅读与落实

- 已阅读当前 PEC 控制文件、员工02通用导入闭环、员工03 FocusFrame 画布接入、员工04导入恢复接线、员工05索引安全边界，以及 M2 契约基线。
- 按 M2 契约中“FocusedContext 查询持久化未接入只能返回 null、不得伪造快照”的边界，本轮只增加领域校验和查询复用，不提前改 SQLite schema 或 UI 状态。
- 已确认员工02的下一单元是 FocusedContextSnapshot 持久化/重启投影；本报告将校验门禁和异常拒绝条件交接给该实现。

## 本次完成

- 新增 `validate_focused_context_snapshot`，作为 FocusedContext 持久化和查询投影共用的领域门禁。
- 校验 `mindscape.focused-context.v1`、旧 `ContextSnapshot` 契约版本、稳定 ID、会话与父节点一致性、非负 Token 估算。
- 校验 selected/omitted memory 引用非空、跨集合不重复且原因完整。
- 校验可选 `KnowledgeContextSelection` 的契约版本、检索版本、引用唯一性、正数估算、遗漏原因和 selected Token 汇总。
- `validate_focus_frame_query_projection` 已复用上述校验，查询层不再仅检查外层版本和会话字段。
- 在 M2 契约基线中冻结上述查询/持久化边界，前端仍只消费投影，不自行重算。

## 验证

- `cargo fmt --all`
- `cargo test --offline --all-targets --locked`：107 passed / 0 failed
- `cargo clippy --offline --all-targets --locked -- -D warnings`：通过

## 需要其他员工配合

- **员工02**：持久化 FocusedContextSnapshot 时调用该校验；读取并组装 `FocusFrameQueryProjection` 后也必须复用，补充重启 round-trip 和异常数据拒绝测试。下一步仍需处理 raw rename→SQLite commit 之间的孤儿引用扫描。
- **员工03**：消费 `list_focus_frames` 后，等待 FocusedContext 查询落库再展示“已编译/未编译”，不得把空值渲染为“无知识”。
- **员工04**：FocusFrame/Context 查询客户端保持三态语义；持久化查询返回错误时显示恢复失败，不从 UI 状态补造快照。
- **员工05**：知识索引返回的 selection 进入 FocusedContext 前保持 retrievalVersion、revision、EvidenceRef 和 Token 汇总原样。

## 下一步

- 员工01继续评审员工02的 FocusedContextSnapshot SQLite schema/重启投影；如发现字段丢失或跨 revision 拼接，先在 PEC 记录并补领域测试。
