# 员工01｜B2/B5 提案重启发现入口契约冻结

> 提交时间：2026-09-02 13:25（UTC+8）  
> 关闭项：`B2/B5-RESTART-DISCOVERY` 按 `importSourceId/importRevisionId` 权威发现既有 request/batch  
> 状态：**本责任项已关闭；B1/B5/M2 仍为 No-Go，等待统一 clean Release 纵向验收**

## 本次完成

- 新增 `ImportKnowledgeProposalDiscoveryQuery`：只接受 source、revision 与 1–64 的有界 limit，不要求前端预先知道 requestId。
- 新增 `discover_import_knowledge_proposals` typed Tauri 命令及 `kernelClient` 镜像。
- SQLite v18 按 source/revision 校验权威归属，稳定以 `updatedAt DESC, requestId ASC` 排序，返回 durable request、generationRunId、pending/completed 状态、可选完整 batch、proposal/review 计数。
- `batch_json IS NULL` 明确投影为 `pending`；已有 batch 必须通过 request/batch 一致性校验后才投影为 `completed`。不存在 source/revision 返回 typed NotFound；合法但无请求返回空 items。
- 重启后恢复不依赖 React 缓存；UI 可先 discover，再用返回的 requestId 调用既有 batch/review 查询。

## 跨模块证据

1. Domain 类型/校验 → SQLite 查询与一致性门禁 → KernelService → Tauri 注册 → TS `kernelClient` 全链路已接通。
2. 新增 Rust 重启回归：同一 source/revision 找回 pending receipt 与 completed batch，排序、状态和 proposal 计数稳定；source/revision 归属由内核校验。
3. 门禁结果：Rust 全量 `245 passed / 0 failed / 1 ignored`，B5 audit `4/4`；Clippy all-targets/features `-D warnings`、fmt 通过；Chat `69/69`、Canvas `34/34`、Canvas 性能 `2/2`、Vite `2100 modules` 通过。

## 契约边界

- 查询是只读恢复投影，不改变 request/batch/review/entity/Vault/index 状态；不暴露正文、Key 或绝对路径。
- pending 只表示 receipt 已 durable、batch 尚未完成；精确重试仍复用原 generationRunId，不创建第二请求。
- 本轮未新增 failed/interrupted 状态列；producer 失败仍由现有 receipt 重试语义处理，待员工03/04接入正式 loading/error/retry/restart UI。

## 剩余阻断与协作

1. **员工03/04**：消费 `discover_import_knowledge_proposals`，在导入详情/重启路径展示 pending/completed、空态、错误和精确 retry；不得以本地缓存替代查询。
2. **员工02**：在真实 AppData/重启现场复验查询与 v18 receipt 的迁移/损坏处理，若需分页或失败状态扩展先回 PEC 登记。
3. **员工05**：在唯一 clean Acceptance Release 中把该发现入口串入同一真实样本矩阵；B5/M2 暂不宣称通过。
4. **员工01**：下一步仅负责 clean 集成边界、冲突审计和 B2/B3 事务评审，不重复实现已通过的局部切片。
