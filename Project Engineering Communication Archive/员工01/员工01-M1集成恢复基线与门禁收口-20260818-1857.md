# 员工01｜M1集成恢复基线与门禁收口

> 提交时间：2026-08-18 18:57（UTC+8）  
> 覆盖任务：M1集成冻结、CORE-014、统一验收与仓库治理  
> 状态：集成恢复提交完成；M1继续 No-Go

## 相比上一份的变化

- 建立 `codex/m1-integration-recovery`，将员工01—06共享工作树冻结为可构建、可审阅、可回滚的集成提交 `9499853`。
- 建立脚本1—10统一验收表和七组模块审阅记录，明确真实证据缺口，不进入M2。
- 发现并修复重复`run_id`覆盖原取消令牌的问题；重复启动现在保留原运行的停止能力。
- 将本地EXE、`target-fixed/`和同类生成目录排除出Git，并把禁止文件、PEC和密钥扫描纳入CI。

## 已阅读与落实

- 已完整阅读`docs/engineering/m1-pec-review-and-closeout-20260818.md`、M1派单、当前PEC控制文件及员工02—06最新报告。
- 已消费员工02的真实SQLite completed/用量基线、员工03的真实A/B会话正常重启恢复证据及员工05的取消验收就绪说明。
- 管理要求已落实为“先冻结、再真实验收”：自动化和Review Lab只作门禁，不替代取消、无效Key、断网/超时和强退恢复证据。

## 本次交付与证据

- 集成提交：`9499853 feat(m1): recover integrated real chat alpha baseline`。
- 验收表：`docs/engineering/m1-alpha-acceptance-matrix-20260818.md`。
- 集成披露与回滚：`docs/engineering/m1-integration-recovery-20260818.md`。
- 安全治理：`scripts/check-secrets.mjs`与`.github/workflows/ci.yml`。
- 底层修复：`desktop/src-tauri/src/commands.rs`新增安全active-run注册及回归测试，异步任务异常时也先清理运行表。

## 验证结果

- 前端：Chat 14/14、Canvas 13/13、性能1/1、生产构建通过（1824 modules）。
- Rust：46/46；`cargo fmt --check`与clippy `-D warnings`通过。
- Tauri：`pnpm tauri build --debug --no-bundle`通过，产物位于忽略的隔离目录。
- Review Lab：8/8；仅为协作工具。
- 治理：PEC严格7文件、tracked密钥扫描247个文本文件、禁止文件、5 MiB体积和`git diff --check`均通过。

## 契约、数据、安全与恢复影响

- 未扩张Frozen V1；修复仅收紧同进程幂等/取消不变量。
- schema v5为前向迁移；整体回滚时旧二进制必须使用迁移前备份，不得继续写已升级数据库。
- 凭据仍只在操作系统安全存储；数据库、证据、日志和提交均不得包含完整Key。

## 风险与No-Go

- 脚本4真实取消、5强退恢复、6无效Key、7断网/超时、9日志/崩溃脱敏尚未闭环；4、5、6、8、9任一失败即No-Go。
- 脚本8已证明正常关闭重启的会话、节点、边、位置与viewport，仍缺模型/运行投影完整对照和强退场景。
- 集成分支尚未推送和创建Draft PR；本地基线已可审阅，M1仍不可宣布Go。

## 下一步 WIP（最多两项）

1. 主持员工02/04/05真实取消与强退恢复，复核Node、ModelRun、ContextSnapshot、事件序列和部分内容唯一性。
2. 执行无效Key、断网/超时及数据库/日志/崩溃脱敏矩阵，逐项更新统一验收表并给出最终Go/No-Go。

## 需要同事配合

- **创始人、员工04/05**：在真实Windows窗口执行长回答停止与流式强退，不在截图或终端暴露Key。
- **员工02**：每个真实用例后只读核对终态唯一性、无终态后delta、无永久pending和历史不变。
- **员工03**：同步核对取消/失败/恢复后的节点唯一、边正确、位置与viewport不跳。
- **员工06**：视觉证据只做引用与评审，不替代SQLite和运行事件事实。
