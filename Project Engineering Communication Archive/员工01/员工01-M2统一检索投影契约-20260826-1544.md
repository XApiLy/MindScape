# 员工01｜M2 统一检索投影契约

时间：2026-08-26 15:44（Asia/Shanghai）  
状态：领域/前端契约冻结，等待员工05接入 FTS/Vector/Relation，员工02提供事实源过滤

## 已阅读与落实

- 已阅读最新 PEC 控制文件、M2 派单、员工02《M2 知识查询 IPC 与验收接线》、员工03《M2 分支焦点与知识实体可读投影》、员工04《M2 知识查询失败恢复入口》、员工05《RAG-M2 检索候选 Provenance 桥接》及员工06当前报告。
- 当前计划将下一道共同门禁定为实体/关系/EvidenceRef 失效闭环与 FTS/VectorIndex/Relation 可重建查询；本轮不实现索引算法，只冻结三类索引汇合后的结构化候选边界。
- 员工02/04已完成知识对象 IPC 与只读展示，员工05明确等待 `evidence/scope/reason` DTO；本轮交付 `mindscape.knowledge-retrieval.v1`，让消费端不再自行解释降级或来源。

## 本次推进（RAG-M2-003 / CORE-M2-004 支撑）

- 新增 Rust `KnowledgeRetrievalProjection` 契约及 `validate()`：
  - 固定 `retrievalVersion`、候选 Entity、EvidenceRef、Token 估算和整数检索分数；
  - 每个候选必须注明 Vector / FullText / Relation 一个或多个来源；
  - `selected/omitted` 引用不得重复，候选必须经过知识对象校验；
  - `RetrievalNotice` 明确 vector 可用性、fallback 和安全提示，禁止“向量不可用却标记已回退”的矛盾状态。
- 新增前端 TypeScript 对应类型，保持 Tauri JSON 的 camelCase 结构一致。
- 更新 [M2 契约基线](D:/Project/MindScape/docs/contracts/m2-contract-baseline.md)，明确该投影只是候选，不等于确认事实，仍需 FocusFrame 状态/scope/预算编译。

## 交付物与验证

- `desktop/src-tauri/src/domain/knowledge_retrieval.rs`
- `desktop/src-tauri/src/domain/mod.rs`
- `desktop/src/domain/knowledge.ts`
- `docs/contracts/m2-contract-baseline.md`
- 新增有效投影、重复候选、缺来源和错误 fallback notice 回归测试。
- Rust：127 passed / 0 failed
- Clippy：`-D warnings` 通过
- `cargo fmt --all`：通过
- TypeScript：通过；Chat 40/40、Canvas 21/21、Canvas 性能 1/1、生产构建 1832 modules：通过

## 契约、数据、安全、恢复与跨团队接口

- 纯领域加法，不改变 V1 ContextSnapshot、FocusedContext、导入原文、Provider 或凭据边界。
- 投影可携带 EvidenceRef 和作用域，但不携带原文正文、Prompt、Key、完整 reasoning 或向量正文。
- FTS/Vector/Relation 只能产生候选；删除、否决、过期及跨作用域过滤仍由 SQLite/领域层再次校验，索引不可成为事实源。

## 需要其他员工配合 / 阻塞

- **员工05**：按该 DTO 映射 VectorMatch/FTS/Relation 命中，保留来源、版本与降级 notice；不得把 `f32` NaN、向量正文或索引状态直接暴露给 UI。
- **员工02**：在统一查询组装前调用 Entity/Relation/EvidenceRef `validate_for_conversation()`，并对删除/否决/过期后的候选做事实源过滤；补同一 Release 的重建与失效样本。
- **员工03/04**：等待员工05接入后只消费该投影的 candidates/omitted/notice，不在画布或 Chat 重算排序、scope、预算或状态。

## 下一步（最多两个 WIP）

1. 评审员工05的 unified retrieval DTO 接线，补 Vector 不可用、FTS/Relation fallback 和 EvidenceRef 保真测试。
2. 与员工02确认删除/否决/过期后的事实源过滤顺序，并为 FocusedContext 编译补一条候选→selected/omitted 回归。
