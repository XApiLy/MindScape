# 员工01｜B5 候选生成重放与接入审计

> 提交时间：2026-09-01 15:42（UTC+8）  
> 覆盖阻断：`B5` 正式候选生成的幂等重放、SQLite/IPC/UI 接入评审  
> 状态：**重放歧义已关闭；SQLite/Tauri 命令尚未接入，当前前端 WIP 构建失败，B5 仍为 No-Go**

## 本次推进的 B 编号

### B5：关闭成功响应丢失后的二次增版歧义

- 新增 `validate_focus_promotion_candidate_generation_replay`：`generationId` 固定为不可变幂等键；候选引用按集合比较、顺序不敏感，其余 typed command 字段必须完全一致。
- 同 ID 同请求必须直接返回既有 receipt，不再读取并修改可变 FocusFrame；同 ID 不同请求必须返回 integrity conflict。
- 冻结落库顺序：先查 receipt，再加载 lifecycle/entity；receipt、FocusFrame JSON、memory version 与 lifecycle revision 必须在同一 `IMMEDIATE` 事务提交，失败整体回滚。
- 明确员工05必须验证一次“服务端已成功、客户端未收到响应、使用同一 generationId 重试”，且 memory/lifecycle 不得二次增版。

## 跨模块证据

- Rust 候选生成直接回归：`11 passed / 0 failed`，包含顺序无关的精确重放和复用 ID 改变 lifecycle revision 的冲突；完整 library 为 `219 passed / 0 failed / 1 ignored`。
- `cargo fmt --all -- --check` 与 `cargo clippy --locked --all-targets --all-features -- -D warnings` 通过；重放校验已从统一 domain API 导出，未用 lint 豁免掩盖未接线状态。
- 员工03/04当前选择帮助函数直接测试：`2 passed / 0 failed`；UI 已按稳定顺序发送 candidate refs，失败重试复用原 input，并在成功后重新读取 Kernel FocusFrame query。
- 完整 `pnpm build` 当前失败：`ConversationCanvas.tsx` 引用 `focusPromotionSelection.ts` 尚未导出的 `selectableFocusPromotionEntities`，后续三个 `entity` 参数连带成为 implicit `any`。该 WIP 不可进入 clean commit。
- 代码扫描确认前端 `kernelClient` 已预留 invoke，但 Rust application/command/lib 尚无 `generate_focus_promotion_candidates` 实现与注册；调用会在运行时失败。
- 本轮只有领域规则与文档变化，无独立可见 UI 交付，因此不发布 Review Lab preview，不生成 Acceptance Build。

## 事务与接口评审结论

- 现有 schema v16 `focus_promotion_decisions` 已采用稳定 ID receipt、请求/投影双存、读取一致性校验和单事务提交；候选生成应复用这一模式，使用下一连续 schema 版本，不能只调用现有非原子 `update_focus_frame_lifecycle`。
- application 必须在事务内加载全部权威 `KnowledgeEntity`，调用领域 plan；前端展示过滤不替代 Kernel 对 conversation、FocusFrame scope、status、EvidenceRef 和 revision 的最终裁决。
- 成功后 Frame 仍为 Active；Close 后才展示四动作。重试返回旧 projection 时不得再次写 lifecycle 或改变时间戳。

## 剩余阻断与协作

1. **员工02**：新增 generation receipt migration、同事务 persistence/replay/integrity validation、KernelService 方法、Tauri command 和 `lib.rs` 注册；补成功、同 ID 重放、同 ID 冲突、stale rollback、重启恢复测试。
2. **员工03/04**：完成 `selectableFocusPromotionEntities` 导出或调整调用，恢复 TypeScript/Vite 构建；保持同 ID retry，并确保成功后的权威 query 刷新失败时用户有明确刷新路径。
3. **员工05**：只有上述接线进入新的 clean commit 后，才重新发布唯一 Acceptance Release；执行生成、响应丢失重试、Close、四动作、Reopen、重启的同一样本证据。

## 下一步 WIP（最多两个）

1. 评审员工02提交的 SQLite v17/KernelService/Tauri 命令是否逐项满足原子与幂等契约。
2. 复验员工03/04完整前端构建及 Active → Close 状态投影，再决定是否准入 clean 候选。
