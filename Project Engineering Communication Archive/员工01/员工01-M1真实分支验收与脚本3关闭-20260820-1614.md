# 员工01｜M1 真实分支验收与脚本 3 关闭

> 提交时间：2026-08-20 16:14（UTC+8）  
> 覆盖任务：M1-GATE-001、M1-DATA-003  
> 状态：脚本 3 通过；M1 整体仍因脚本 8 No-Go

## 相比上一份的变化

- 创始人在统一 clean Build 中完成真实“深入”和“发散”两种分支操作，并等待真实 DeepSeek 完成。
- SQLite 只读核对确认 Node、Edge、ContextSnapshot 三处类型一致，脚本 3 正式关闭。
- 脚本 8 进一步确认仍缺画布位置联合恢复证据；取消节点位置为 0，`application_interrupted` 节点位置为 0。

## 已阅读与落实

- 已复核 M1 派单、Core Kernel、RC1 契约、威胁模型、验收构建规则和员工02～06当前 PEC。
- 只读取分支类型、父子关系、条目数量、长度、状态、模型和完整性结果；不读取正文、请求 JSON 或凭据。
- 视觉对照实验仍未进入 M1 提交；本轮只关闭领域分支与 ContextSnapshot 验收。

## 本次证据

- Build ID：`20260820-155308-m1-clean-gate-98e12706c2f4`。
- 程序：`D:\Project\MindScape\artifacts\acceptance\versions\20260820-155308-m1-clean-gate-98e12706c2f4\mindscape-desktop.exe`。
- SHA-256：`6FE02B0BD551288805B8034AE39DB19528815F901F24D1C69471865985ABA59A`。
- 来源提交：`98e12706c2f4`；`sourceTreeDirty=false`。
- 证据：`docs/engineering/evidence/employee01/m1-real-branches-20260820/README.md` 与 `sanitized-branch-facts.json`。

## 验证结果

- Node 分支：`continues=23`、`deepens=1`、`diverges=1`。
- ContextSnapshot 分支：同样为 `continues=23`、`deepens=1`、`diverges=1`。
- `deepens` 选择 2 条消息、无省略；`diverges` 选择 3 条消息、1 条省略；两者各 1 条 incoming edge、1 个 ModelRun，均 `completed`。
- `PRAGMA integrity_check=ok`；外键违规 0；`pending/streaming=0`。

## 契约、数据、安全与恢复影响

- 未修改 Frozen V1、SQLite schema、Provider、IPC 或消息正文语义。
- 真实分支证据证明 ContextSnapshot 仍按分支类型保存，不以 UI 标签替代领域事实。
- 脚本 8 仍为硬阻塞：取消/失败/强退后的节点、边、位置、viewport 尚未在同一窗口证据中同时稳定。

## 下一步 WIP（最多两项）

1. 员工02/03在本 Build 上联合复核取消、失败、强退后的画布节点/边/位置/viewport，并保留脱敏证据。
2. 脚本 8 关闭后再执行一次统一 clean release 回放，更新矩阵并签署最终 Go/No-Go。

## 需要同事配合

- **员工02/03**：提供同一 Build 的画布恢复窗口和 SQLite 交叉核验。
- **员工04/05**：本轮分支已完成，无需重复真实 Provider 长文、错误 Key 或网络超时；如脚本 8 需要，协助定位失败/取消会话。
- **创始人**：下一步只需按员工02/03的简短步骤完成重启后画布检查，不需要再次执行分支。
