# 员工01｜M1可信上下文预算与架构走查收口

> 提交时间：2026-08-18 16:12（UTC+8）  
> 覆盖任务：ARC-015、CORE-012、CORE-020  
> 状态：本轮切片完成；等待真实Key与Windows终态矩阵  
> 基线提交：`f54363e`（共享工作树；Draft PR受只读Git元数据限制尚未创建）

## 相比上一份的变化

- 可信模型能力到上下文编译器的接线完成联审；DeepSeek V4 Flash的1M窗口已由官方资料和员工05测试确认。
- 正预算边界收紧：输出预留等于或超过模型窗口时，在创建Node/Run前返回不可重试的`contextBudgetInvalid`。
- 新增跨预算完整问答后缀属性测试、冻结快照落库及原历史不变集成测试。
- 架构走查更新到schema v5、统一Chat启动和真实DeepSeek协议底座的当前状态。

## 已阅读与落实

- 已重新读取PEC控制文件及员工02 16:08、员工03 16:06、员工04 15:53、员工05 16:07最新快照，并复核V1范围、产品共识、总路线、M1派单、内核总览、责任书、Frozen V1契约、安全、Git、PEC制度和架构走查。
- 具体落实：上下文上限只来自实际Provider注册表，UI不提交窗口；窗口未知时不猜测，已确认值必须有厂商官方来源。
- 员工03直接呈现Frozen V1安全错误/中断没有产生平行终态；员工04已移除三段式编排；员工05连接检查不创建ModelRun且不读取失败响应正文，均通过员工01契约审查。
- 员工02schema v5仅增加CanvasViewport表，保留v4完整运行请求、事件和恢复语义，属于可接受的加法迁移。

## 本次完成与证据

- `desktop/src-tauri/src/commands.rs`：可信窗口减输出预留必须得到正输入预算；覆盖超限、相等和零窗口。
- `desktop/src-tauri/src/domain/context.rs`：对连续预算验证裁剪结果始终在限额内、只保留完整问答后缀且不拆组。
- `desktop/src-tauri/src/application/mod.rs`：验证预算化ContextSnapshot与ModelRunRequest一致落库，原历史消息和终态不被修改。
- `docs/contracts/v1-contract-baseline.md`：补可信预算来源、零写入失败边界、schema v5和四名消费方最新结论。
- `docs/architecture/v1-architecture-walkthrough.md`、`desktop/README.md`：同步统一启动已完成、当前非契约阻断和schema v5。
- DeepSeek能力来源：官方模型与价格页确认`deepseek-v4-flash`为1M上下文；代码由员工05维护，本轮仅完成技术负责人复核。

## 契约、数据和安全影响

- 未修改`mindscape.context.v1`、`mindscape.runtime.v1`字段、枚举或终态语义；仅收紧确定性校验并增加测试。
- 员工01本轮不修改SQLite schema；已复核员工02的v5迁移不会改变Node/ModelRun真相或凭据边界。
- API Key仍只在Rust/OS凭据边界；连接测试和预算编译不记录密钥、请求正文或厂商失败正文。
- 未写入猜测模型数据；1M窗口以DeepSeek官方文档为依据。

## 验证结果

- `cargo fmt --all -- --check`：通过。
- `cargo test --offline --no-fail-fast`：41/41通过。
- `cargo clippy --offline --all-targets --all-features -- -D warnings`：通过。
- `pnpm test:chat`：10/10通过。
- `pnpm test:canvas`：9/9通过；`pnpm test:canvas:perf`：1/1通过。
- `pnpm build`：通过，生产构建1822个模块。
- `pnpm tauri build --debug --no-bundle`：通过，生成`desktop/src-tauri/target/debug/mindscape-desktop.exe`。
- `git diff --check`与PEC留存检查：通过。

## 风险与阻塞

- 尚无用户提供并写入OS凭据库的真实DeepSeek Key，成功、错Key、限流、停止、超时、异常断流与重启矩阵均未实测，M1 Alpha不能放行。
- 当前`estimatedTokens`仍是确定性字符启发式，不是DeepSeek原生Tokenizer；普通Alpha消息可工作，但长中文/混合内容在进入长上下文验收前需改为保守估算或Provider预检。
- 员工03尚未把schema v5视口命令接入真实窗口；员工04尚未消费安全连接测试和预算错误展示。
- `.git`只读，无法建分支、提交、Draft PR或执行`git mv`；PEC归档采用工作区内安全移动，暂存时需确认rename。

## 下一步 WIP（最多两项）

1. ARC-015：主持带真实Key的Windows成功、停止、失败、应用中断、重启和视口恢复矩阵，逐项签署M1 No-Go门禁。
2. CORE-012/020：收敛多语言Token保守估算，并补幂等键载荷一致性与长分支预算不变量。

## 需要同事配合

- **员工02**：与员工03完成schema v5多会话视口隔离/重启验收，并保留预算失败零Node/Run证据。
- **员工03**：立即接入`saveCanvasViewport/getCanvasViewport`，覆盖节流保存、切会话和重启恢复。
- **员工04**：接入`testProviderConnection`与`contextBudgetInvalid`安全提示，参加真实窗口完整矩阵。
- **员工05**：真实Key就绪后执行官方端点成功/鉴权/限流/超时/取消/用量矩阵，继续维护模型窗口来源。
