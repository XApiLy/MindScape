# 员工01｜M1 脚本 8 重启复核与位置缺口

> 提交时间：2026-08-20 16:22（UTC+8）  
> 覆盖任务：M1-GATE-001、M1-DATA-008  
> 状态：运行/历史恢复通过；位置持久化证据不足，整体 No-Go

## 相比上一份的变化

- 创始人完成同一 clean Build 的关闭/重启复核，数据库更新时间推进到 16:20。
- 只读事实确认取消、失败、强退运行均无永久 pending，模型映射、父子关系、Edge 和 viewport 保持完整。
- 但取消 Node 与 `application_interrupted` Node 均没有 `canvas_node_positions` 记录，因此脚本 8 不能整体签通过。

## 已阅读与落实

- 已复核 M1 派单、Core Kernel、RC1 契约、威胁模型、验收构建规则及员工02～06当前 PEC。
- 本轮只读数据库和 Build 元数据，不读取正文、请求 JSON、日志原文或凭据；不修改产品代码和用户数据。
- 按 UI 非真相原则，将窗口复核与 SQLite 事实分开：运行/图关系可签，位置持久化必须有明确落库记录。

## 本次证据

- Build ID：`20260820-155308-m1-clean-gate-98e12706c2f4`。
- 程序：`D:\Project\MindScape\artifacts\acceptance\versions\20260820-155308-m1-clean-gate-98e12706c2f4\mindscape-desktop.exe`。
- SHA-256：`6FE02B0BD551288805B8034AE39DB19528815F901F24D1C69471865985ABA59A`。
- 来源提交：`98e12706c2f4`；`sourceTreeDirty=false`。
- 脱敏说明：`docs/engineering/evidence/employee01/m1-restart-canvas-20260820/README.md`。

## 验证结果

- `integrity_check=ok`；外键违规 0；`pending/streaming=0`。
- 终态 Node：`cancelled=7`、`failed=3`；27/27 Node 有 provider/model。
- 最新 `application_interrupted` 保留 `failed`、部分内容长度 1392 和 viewport；最新两次取消各有唯一 Node/Run、父节点和 Edge。
- 位置缺口：取消 Node `canvas_node_positions=0`；`application_interrupted` Node `canvas_node_positions=0`，虽有 conversation viewport。

## 结论与下一步 WIP（最多两项）

1. 在同一 Build 的取消或强退会话中拖动并保存一个节点位置，关闭后重新打开，复核该 Node 的位置、Edge、viewport 和唯一性。
2. 员工02/03提供联合窗口截图与只读聚合后，员工01再更新脚本 8 和最终 Go/No-Go。

## 需要同事配合

- **员工02/03**：完成“拖动保存位置 → 关闭 → 重启 → 画布/阅读视图复核”的同 Build 联合验收。
- **创始人**：只需配合一次节点拖动和重启，不需要重复分支、长文、错误 Key 或网络超时操作。
