# 员工01｜M2 回流候选生命周期门禁

时间：2026-08-29 14:17（Asia/Shanghai）

覆盖任务：ARC-M2-002 / CORE-M2-002～003 / DATA-M2-010 评审

状态：正式回流候选生命周期门禁完成；等待员工03/04消费与 clean 统一 Release

## 已阅读与落实

- 已读取 PEC 控制文件及员工02～06当前报告，并追溯员工02《M2 Markdown 原子事务与统一 Release》证据。
- 使用 `rust-best-practices` 将生命周期判断放入领域快照，以 `KernelResult<Option<_>>` 表达非法快照与诚实空态；测试分别覆盖 Active 隐藏、Closed 暴露和应用查询恢复。
- 使用 `tauri-development` 保证 Tauri command 只经过 KernelService 消费领域门禁，前端不能自行从 `promoteRefs` 合成正式候选。

## 相比上一份 PEC 的变化

- 上一份指出 Entity/FTS/Vector/MarkdownProjection 跨提交风险；员工02现已实现 `persist_markdown_entity_revision` 单事务，应用层只调用该入口，Projection 冲突会在 commit 前回滚整笔事务。员工01代码边界复核通过，该项不再是代码阻塞。
- 复核员工02新 `get_focus_promotion_candidates` 时发现其忽略 lifecycle，Active 分支也会返回正式候选；本轮已收口。

## 本次完成

- 为 `FocusFrameLifecycleSnapshot` 新增正式候选查询门禁：只有 Closed 的任务、探索、复盘分支且声明非空时返回 `FocusPromotionCandidateSet`。
- Active、主线和空声明统一返回 `null`；Active 仍可在普通 FocusFrame 投影中展示 `promoteRefs` 声明，但不能呈现为可确认成果。
- 分支重新打开后正式候选再次隐藏；memory version 冲突仍在应用查询层显式失败。
- `KernelService::get_focus_promotion_candidates` 改为调用领域生命周期入口，不再在应用层复制 branch kind/空集合判断。
- 更新 M2 契约基线，冻结员工03/04消费语义。

## 验证证据

- 生命周期定向测试：7/7；候选应用查询：3/3。
- Rust 全量：169/169；Clippy `--all-targets --all-features --locked -D warnings` 通过；fmt 通过。
- Chat：51/51；Canvas：34/34；Canvas 性能：2/2。
- TypeScript/Vite production build：2094 modules，通过。
- 员工02原子事务定向测试复跑通过；事务函数代码确认 Entity、FTS、Vector、Projection 位于同一 SQLite transaction。

## 契约、数据、安全与恢复边界

- 不改变 typed IPC 形状：返回仍为 `FocusPromotionCandidateSet | null`，只收紧 `null` 的领域含义。
- 不修改 SQLite schema、Vault、原始导入内容、知识状态机或 Provider。
- 原子事务代码边界通过不等于真实重启验收；员工02测试目前直接断言 Entity revision 回滚，FTS/Vector/Projection 的直接状态断言仍建议补齐。
- 本轮未生成 Release；既有 dirty Build 早于员工02受控原文/候选查询和本轮生命周期门禁，不能作为最终证据。

## 需要配合 / 未关闭项

1. **员工03**：消费 `getFocusPromotionCandidates`；Active 显示声明或“分支尚未完成”，Closed 且非空才显示“待确认”，过期 memory version 显示冲突，不自动回流。
2. **员工04**：消费员工02 `getRawImportContent` 完成受控源文件原样预览；若展示回流候选，遵守同一 Active/Closed 语义。
3. **员工02**：补 FTS、Vector、Projection 冲突回滚的直接断言及关闭重启一致性样本。
4. **员工05**：代码冻结后才发布唯一 clean 统一 Release，纳入最新 typed IPC、生命周期门禁、性能优化和 M1 回归。

## 下一步（最多两个 WIP）

1. 复核员工03/04对新 typed IPC 和生命周期空态的 UI 消费，不允许前端复制领域判断。
2. 在 clean 统一 Release 上汇总导入原文、Vault 编辑、分支关闭候选、确认/否决/删除、索引降级重建、重启与性能证据。
