# 员工01｜M1上下文预算不变量联调

> 提交时间：2026-08-18 16:02（UTC+8）  
> 覆盖任务：ARC-015、CORE-012、CORE-020  
> 状态：开发中，可信能力接线已进入审查与不变量补强  
> 基线提交：`f54363e`（共享工作树；Draft PR受只读Git元数据限制尚未创建）

## 相比上一份的变化

- 员工04已移除Chat三段式编排并切换统一`start_model_run`；上一份对应阻断解除。
- 员工05已提交OpenAI-compatible DeepSeek协议底座并注册真实Provider；当前阻断收敛为可信模型窗口和真实Key联调。
- 并行代码已出现`contextWindowTokens - maxOutputTokens`接线，本轮由员工01补齐边界语义、落库验收和不变量测试。

## 已阅读与落实

- 已重新读取PEC控制文件、员工01～05当前PEC，并复核V1范围、产品共识、总路线、M1派单、内核总览、责任书、Frozen V1契约、安全、Git、PEC制度和架构走查。
- 具体落实：只接受Provider注册表中的可信`contextWindowTokens`，不允许UI直接提交上下文上限；DeepSeek窗口数值未被员工05确认前保持`None`，不猜测厂商元数据。
- 评审结论：员工04/05新增实现未引入平行运行状态；当前需补“零可用上下文必须拒绝”和“裁剪快照冻结落库且不改历史”证据。

## 本次完成与证据

- 已完成最新PEC与共享工作树审计，确认统一启动链路已调用Provider能力查询并把预算传入`KernelService::prepare_model_run`。
- 开发证据将在本轮完成后更新；当前报告作为开工快照。

## 契约、数据和安全影响

- 不修改`mindscape.context.v1`或`mindscape.runtime.v1`序列化字段、枚举和终态含义。
- 不修改SQLite schema；仅补预算边界和冻结快照持久化不变量。
- 不读取或记录API Key，不写入未验证的DeepSeek上下文窗口数值。

## 验证结果

- `node scripts/check-pec-retention.mjs`将在新快照写入后执行。
- Rust、前端与Tauri验证待实现完成后执行。

## 风险与阻塞

- DeepSeek `contextWindowTokens`仍为空；真实模型预算裁剪需员工05提供有来源、可维护的可信值。
- 真实Key、Windows窗口成功/取消/错误矩阵尚未完成，不能宣称M1 Alpha验收通过。
- `.git`只读，无法创建分支、提交、Draft PR或执行`git mv`；归档采用工作区内安全移动，暂存时需确认rename。

## 下一步 WIP（最多两项）

1. CORE-012：收紧可信模型窗口减输出预留的正预算边界，并验证统一错误。
2. CORE-020：补预算裁剪快照落库、历史不变和分支语义不变量测试。

## 需要同事配合

- **员工02**：评审预算失败发生在Node/ModelRun写入之前，并确认无残留pending数据。
- **员工04**：确认结构化预算错误可沿统一命令错误展示，无需前端自算窗口。
- **员工05**：提供DeepSeek模型窗口的可信来源与更新策略后再填注册表；继续完成真实Key矩阵。
