# 员工01｜B2/B3 评审关闭与 B1 冻结入口

> 提交时间：2026-08-31 22:04（UTC+8）  
> 覆盖任务：B2/B3 最终事务复评 / B1 集成冻结入口  
> 状态：**B2、B3 工程阻断均已关闭并纳入 B1 待冻结范围；B1/B5仍未关闭，M2仍为 No-Go**

## 本轮关闭的 B 编号

### B3：可读 Markdown Vault 最终事务/恢复评审通过

- `B3-REV-03` 已关闭：DiscussionLog 恢复按日志 ID 的 SQLite 最新 revision 判断，单调契约下 `journal.revision <= latest_revision` 均视为已提交；旧 journal 只清理，不会回滚更新 revision 的日志、Evidence 来源页或索引。
- `B3-REV-04` 已关闭：正式 `kernelClient.deleteKnowledgeEntity → Tauri command → KernelService` 路径已进入共享 Vault projection 锁、Vault-first pre-image journal、邻居重投影、SQLite transaction、失败补偿与启动收敛。
- 通用删除 journal 覆盖目标实体页、关系邻居实体页、邻居 Evidence 来源页和全局实体索引；SQLite 未提交时字节级恢复，SQLite 已提交时以目标实体不存在为事实，按当前实体/关系重建可读终态。
- 启动流程在 Tauri state 注入前恢复 focus、通用删除和 Discussion journals，再执行孤儿清理；所有生产错误均通过 `Result` 传播，未引入生产 `unwrap/expect` 或任意 Vault 路径访问。

### B2：维持上一轮工程评审通过

- Focus 四动作、create-only 幂等、SQLite v16、Vault journal、FTS/Vector/Relation 失效、正式 typed UI、同 ID 重试与重启历史已经完成；员工02本轮没有改变领域或 IPC 契约。
- B2/B3 现在可以一并进入 B1 集成冻结候选；正式用户验收仍属于 B5 唯一 clean Tauri Release，不在本报告中重复计功。

## 跨模块证据

- 员工01逐项复跑员工02新增的五项直接回归：`5 passed / 0 failed`。
- 连续 revision 回归覆盖“revision 1 已提交、索引失败留 journal → revision 2 成功 → 重启”，最新日志、来源页、索引与 SQLite revision 均保持 revision 2。
- 正式通用删除回归覆盖正常删除/重启、Vault 索引写失败补偿、SQLite 已提交但 journal 未清的启动收敛，以及适配器提交/未提交双终态。
- Rust 全量 `cargo test --all-targets --all-features --locked --quiet`：library `205 passed / 0 failed / 1 ignored`；B5 audit example `2 passed / 0 failed`。ignored 为既有固定生产语义模型质量用例，不计本轮 B3 证据。
- `cargo clippy --all-targets --all-features --locked -- -D warnings` 与 `cargo fmt --all -- --check`：通过。
- B5 脱敏审计已覆盖 focus、Discussion、通用删除三类 pending journal，并保留 Delete 悬空 Wikilink、SQLite relation/FTS/vector 与 Vault 文件一致性检查。
- 本轮无可见 UI 变化，因此不发布 Review Lab preview；未生成个人 Acceptance Build、Build ID、EXE 或 SHA-256。

## B1 冻结边界

- 可纳入：B2 领域/SQLite/Vault/typed IPC/UI、B3 实体/关系/EvidenceRef/DiscussionLog/外部编辑/三类 journal/启动恢复，以及已评审通过的 B4 真实语义接口和审计工具。
- 暂不能称为 clean：共享工作树仍约 231 项 dirty/untracked，包含员工02～05实现、历史 PEC 归档、证据和工具；必须先完成文件归属、生成物排除、全量门禁与单一集成提交。
- B1 clean commit 完成后只允许员工05发布一次 Acceptance Release；任何当前 dirty Build 或 Review Lab preview 都不能替代 B5。

## 剩余阻断与协作

1. **B1 未关闭**：员工01下一步清点共享变更、排除本地生成物、冻结单一可审查集成提交。
2. **B5 未关闭**：员工05在上述 clean commit 上执行同一真实样本，必须包含连续 revision、三类 journal 为零、通用删除故障恢复、四动作、Reopen、重启和 B4 语义召回。
3. **B2/B3/B4 工程阻断已关闭**：员工02～04仅修复集成或 B5 发现的确定性回归，不再扩展新能力。
4. **B6 未开始**：不得抢占 B1/B5；是否保留 M2 范围等待控制 PEC 或创始人裁决。

## 下一步 WIP（最多两个）

1. 执行 B1 共享树清点、生成物隔离和 clean 集成冻结。
2. 将唯一 clean commit 交给员工05执行 B5 Acceptance Release 与纵向真实样本。
