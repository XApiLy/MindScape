# 员工01｜M1多语言预算与幂等门禁收口

> 提交时间：2026-08-18 18:01（UTC+8）  
> 覆盖任务：ARC-015、CORE-012、CORE-020  
> 状态：本轮内核切片完成；等待真实取消与恢复矩阵  
> 基线提交：`f54363e`（共享工作树，尚未形成独立员工01提交）

## 相比上一份的变化

- 完成员工02幂等载荷与单进程并发准备锁的Frozen V1评审，确认同载荷唯一Node/Run、异载荷拒绝零副作用。
- Token估算从统一“字符数除四”升级为多语言保守规则，并加入每条Chat消息封装开销。
- DeepSeek真实连接、首次V4 Flash生成和completed终态正式进入已通过门禁，不再列为阻断。
- schema v5视口客户端已进入共享树；修复Node 24测试语法兼容后，按会话节流、隔离和失败续写自动测试通过。

## 已阅读与落实

- 已完整读取最新PEC控制文件，以及员工02 17:18、员工03 16:06、员工04 17:20、员工05 17:11、员工06 17:58当前报告；员工06更新后已重新读取。
- 幂等落实：比较会话、父节点、分支、标题、提示词、Provider、Model、能力和预算；固定错误不回显正文。共享锁只覆盖本地准备阶段，不阻塞网络运行或取消。
- 上下文落实：ASCII按每4字符1 Token向上取整；非ASCII按UTF-8字节数保守计入；当前输入和每条选中消息各增加4 Token封装开销。真实用量仍只采信Provider事件。
- Review Lab落实：本轮主要是后端不变量与等价测试兼容修复，没有新的可判断界面体验，因此不创建版本；真实取消/恢复界面证据应使用`full`。
- 员工03的Frozen终态呈现仍无平行语义；其当前PEC已落后于实际视口接入，需本人下一份报告纠正。

## 本次完成与证据

- `desktop/src-tauri/src/domain/context.rs`：新增`TokenEstimate`，覆盖ASCII、中文、Emoji、Chat封装和长分支完整问答后缀。
- `desktop/src-tauri/src/application/mod.rs`：同步预算化快照集成测试，继续验证落库快照与运行请求一致且历史不变。
- `desktop/src/canvas/canvasViewportPersistence.ts`：仅将TypeScript参数属性改为等价显式字段，修复Node 24 strip-only测试执行，不改变视口业务语义。
- `docs/contracts/v1-contract-baseline.md`：冻结多语言估算、完整幂等载荷与单进程锁边界，更新员工02～05最新证据。
- `docs/architecture/v1-architecture-walkthrough.md`：同步真实completed、视口自动测试及剩余No-Go。
- `docs/decisions.md`新增D-043/D-044；`docs/discussion-log.md`记录评审依据与当前门禁。

## 契约、数据和安全影响

- 未修改`mindscape.context.v1`、`mindscape.runtime.v1`序列化结构、事件枚举、终态语义或SQLite schema v5。
- 估算算法仅影响新ContextSnapshot的`estimatedTokens`与裁剪结果；历史快照不重算、不回写。
- 幂等错误不包含新旧提示词、上下文或Provider敏感信息；Key边界未改变。
- 视口兼容修复是TypeScript语法等价改写，不改变数据来源、保存节流或失败语义。

## 验证结果

- `cargo fmt --all -- --check`：通过。
- `cargo test --offline --no-fail-fast`：45/45通过。
- `cargo clippy --offline --all-targets --all-features -- -D warnings`：通过。
- `pnpm test:chat`：14/14通过。
- `pnpm test:canvas`：12/12通过；`pnpm test:canvas:perf`：1/1通过。
- `pnpm build`：通过，生产构建1824个模块。
- `pnpm tauri build --debug --no-bundle`：通过，生成`desktop/src-tauri/target/debug/mindscape-desktop.exe`。
- `git diff --check`与PEC七文件留存检查：通过。

## 风险与阻塞

- 真实网络停止/cancelled、部分内容保留、用量、超时、异常断流、应用中断和重启恢复仍未完成，M1 Alpha不可放行。
- 多语言估算是有意偏保守的确定性门禁，不是DeepSeek原生Tokenizer；可能提前裁剪，但不会冒充账单用量。
- 准备锁只保证单个桌面进程；未来多进程写同一数据库必须改为SQLite单事务准备。
- 员工03需更新过期PEC，并补真实窗口多会话和重启视口证据。
- 共享工作树混合员工01～06未提交变化；虽然当前Git已可写，但不能把全树直接归为员工01独立提交。

## 下一步 WIP（最多两项）

1. ARC-015：主持真实长回答停止、cancelled唯一终态、部分内容、用量、异常退出和重启恢复矩阵，逐项签署M1 No-Go。
2. CORE-020：复核员工03视口真实窗口证据及员工02运行恢复落库，形成M1内核不变量最终验收表。

## 需要同事配合

- **员工02**：配合取消、用量、异常退出和重启恢复落库核对；保持单进程准备锁不覆盖网络阶段。
- **员工03**：更新当前PEC并完成多会话切换、节流保存、真实重启视口恢复；保留本轮Node兼容修复。
- **员工04/05**：立即执行真实长回答停止，验证无后续delta、cancelled唯一终态、部分内容和重试入口，再推进用量/超时/断流。
- **员工06**：真实取消与恢复界面证据准备好后以Review Lab `full`版本建立视觉引用，不替代领域门禁。
