# 员工01｜M2 FocusFrame 查询投影契约

> 提交时间：2026-08-25 12:35（UTC+8）  
> 覆盖任务：`CORE-M2-001`、`CAN-M2-003`、`CHAT-M2-001` 查询边界切片  
> 状态：纯领域查询 DTO 已冻结；命令、SQLite 持久化和启动恢复仍待数据层接入

## 已阅读与落实

- 已阅读 PEC 当前控制文件、M2 派单、M2 契约基线、Core Kernel 总览、V1 产品范围/路线、RC1、威胁模型、架构走查、数据布局、Markdown/RAG 方案、Git/PEC/验收制度，以及员工02～05当前 PEC。
- 员工02已交付 `persist_import_bundle` Tauri 命令，员工05真实 DeepSeek 验收通过；员工03/04仍明确等待 FocusedContext 查询/恢复 DTO。本次将该阻塞收敛为可独立评审的只读投影，不提前改写 SQLite 或 UI。
- 落实影响：查询结果由生命周期快照提供 Active/Closed 权威状态，FocusedContext 允许为空表示“尚未编译/暂无可用历史快照”；前端不得以空值推断无知识或复制领域规则。

## 本次推进

- 新增 `mindscape.focus-query.v1` 的 `FocusFrameQueryProjection`：组合 `FocusFrameLifecycleSnapshot` 与可选 `FocusedContextSnapshot`，作为后续 Tauri 查询返回 DTO 的唯一内核形状。
- 新增 `validate_focus_frame_query_projection`：校验查询契约版本、非空 FocusFrame ID、FocusedContext 契约版本，以及 FocusFrame/会话一致性；跨 FocusFrame、跨会话拼接返回完整性错误。
- 增加 Rust 单测覆盖空 FocusedContext 合法、未知查询版本拒绝、空 FocusFrame ID 拒绝；增加 TypeScript 镜像类型供员工03/04消费。
- 更新 `docs/contracts/m2-contract-baseline.md`，明确生命周期是状态权威、FocusedContext 可选以及 UI 只读边界。

## 交付物与证据

- `desktop/src-tauri/src/domain/focus_query.rs`
- `desktop/src-tauri/src/domain/mod.rs`
- `desktop/src/domain/focus.ts`
- `docs/contracts/m2-contract-baseline.md`
- 本报告及上一份已原子归档至 `Project Engineering Communication Archive/员工01/`。

## 契约、数据、安全、恢复与跨团队接口

- 仅新增加法 DTO 和纯校验，不修改 M1 `ContextSnapshot`、Provider 请求、凭据边界或员工02导入事务。
- DTO 不携带 raw 内容、Key 或完整 reasoning；FocusedContext 只保留既有结构化快照和知识选择投影。
- 当前没有宣称查询 IPC、SQLite FocusFrame 表或重启恢复已完成；后续命令层必须先调用同一校验入口，再映射安全 `CommandError`。

## 验证结果、风险与阻塞

- `cargo test --offline --all-targets --locked`：91 passed / 0 failed。
- `cargo clippy --all-targets --all-features --locked --offline -- -D warnings`：通过。
- `pnpm exec tsc --noEmit`、`pnpm test:chat`（32/32）、`pnpm test:canvas`（18/18）、`pnpm test:canvas:perf`（1/1）、`pnpm build`（1830 modules）：通过。
- `node scripts/check-pec-retention.mjs`、`git diff --check`：通过。
- 阻塞仍是数据/命令接线：没有 SQLite 生命周期表、FocusFrame close/reopen IPC、查询/启动恢复实现，不能进入 M2 Go/No-Go。

## 下一步 WIP 与需要配合

1. **员工02**：基于本 DTO 评审并实现 FocusFrame 生命周期表、revision 冲突保护、查询/启动恢复读写；不得以画布选择推断状态。
2. **员工03/04**：确认 `focusedContext: null` 的加载/空白/错误展示语义；命令上线后只消费结构化投影，不在前端重算知识或生命周期。

