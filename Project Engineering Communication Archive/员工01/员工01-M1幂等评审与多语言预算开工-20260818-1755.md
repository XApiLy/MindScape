# 员工01｜M1幂等评审与多语言预算开工

> 提交时间：2026-08-18 17:55（UTC+8）  
> 覆盖任务：ARC-015、CORE-012、CORE-020  
> 状态：开发中；真实completed已通过，进入内核不变量补强  
> 基线提交：`f54363e`（共享工作树，尚未形成独立员工01提交）

## 相比上一份的变化

- PEC制度已扩展为员工01～06加控制文件的七文件窗口；员工06提供Review Lab，但纯后端不变量推进不单独标记界面版本。
- DeepSeek真实连接、首次生成和`completed`终态已通过，不再把“无真实Key”列为阻断。
- 员工02完成幂等异载荷拒绝与同进程并发准备锁，本轮由员工01履行Frozen V1评审并继续收敛多语言Token预算。

## 已阅读与落实

- 已完整读取最新PEC控制文件及员工02 17:18、员工03 16:06、员工04 17:20、员工05 17:11、员工06 17:43当前快照，并复核契约基线、架构走查和内核Token/幂等规则。
- 评审落实：员工02按完整执行输入比较幂等载荷，锁仅覆盖准备阶段且由所有`KernelService`克隆共享；单进程桌面模型下不产生重复Node/Run，符合Frozen V1。
- 范围落实：本轮不接管员工03视口UI、员工04取消交互、员工05真实可靠性矩阵或员工06视觉规格。
- 发现的后续边界：多进程写库需要SQLite单事务准备，但不属于当前M1单进程模型；字符数除四会明显低估中文/Emoji，需要员工01修正。

## 本次完成与证据

- 已完成员工02`validate_idempotent_replay`、共享准备锁及顺序/并发测试代码审查。
- 多语言预算实现与证据将在本轮完成后更新；当前报告作为开工快照。

## 契约、数据和安全影响

- 计划不修改`mindscape.context.v1`、`mindscape.runtime.v1`序列化结构或schema v5。
- Token变化只收紧确定性估算，不读取Provider数据库、凭据或用户界面状态。
- 幂等错误继续只返回安全固定消息，不回显新旧提示词、上下文或Provider数据。

## 验证结果

- PEC七文件窗口将在新报告写入后校验。
- Rust、前端和Tauri验证待实现完成后执行。

## 风险与阻塞

- 真实取消、超时、异常断流、用量和应用重启恢复仍未完成，M1 Alpha不可放行。
- 员工03当前报告已滞后于schema v5接口交付，仍需尽快接入CanvasViewport。
- 当前共享工作树混合多名员工未提交改动，不能把全树直接归为员工01提交。

## 下一步 WIP（最多两项）

1. CORE-012/020：实现含消息封装开销的多语言保守Token估算，并补中文、Emoji和长分支预算测试。
2. ARC-015：把幂等/并发评审与真实completed新状态同步进Frozen V1走查文档。

## 需要同事配合

- **员工02**：保留当前单进程锁边界；若准备事务继续演进，请先与员工01确认数据库原子性方案。
- **员工03**：立即消费schema v5视口命令并更新过期阻断说明。
- **员工04/05**：继续真实取消、用量、超时、断流和重启矩阵，不改Frozen终态。
- **员工06**：本轮无界面变化无需Review Lab；后续真实可靠性界面证据使用`full`版本归档。
