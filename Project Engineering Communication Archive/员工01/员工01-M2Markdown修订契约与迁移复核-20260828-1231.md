# 员工01｜M2 Markdown 修订契约与迁移复核

时间：2026-08-28 12:31（Asia/Shanghai）

覆盖任务：ARC-M2-004 / CORE-M2-003 / DATA-M2-010 评审

状态：Markdown 投影领域门禁完成；等待用户编辑回流与统一 Tauri Release 验收

## 已阅读与落实

- 已阅读当前 PEC 控制文件、员工02～06最新报告、M2 派单、Markdown Vault/分支日志架构与 M2 契约基线。
- 使用 `tauri-development` 与 `rust-best-practices` 约束本轮实现：Tauri IPC 前必须完成领域校验，Rust 入口使用 `KernelResult`，修订溢出和倒退显式失败，测试按单一行为命名，不用 panic/unwrap 处理生产路径。
- 员工02已并行完成 Vault 文件适配、schema v11 修订表和 typed IPC；本轮只补领域不变量和迁移评审，不替代数据层实现。

## 本次完成

- 为 `MarkdownProjection` 增加 `validate()`：拒绝未知契约、空 ID、零修订、空 hash/frontmatter/time，以及绝对路径、父级逃逸、反斜线、盘符和非 `.md` 路径。
- 增加 `next_revision()`：保持 projection/entity 稳定身份，projection revision 严格递增，允许安全改名，禁止 entity revision 倒退并拒绝 revision 溢出。
- 更新 M2 契约基线，明确 Markdown 是可重建投影，不是事件账本；用户编辑必须产生新修订，不能静默覆盖历史。
- 全量测试发现 schema v10 回填夹具未删除 v12 `knowledge_vector_records`；与员工02的 v11/v12 接线对齐后，夹具已真正模拟 v9，回填测试恢复通过。

## 验证证据

- MarkdownProjection 定向测试：7/7 通过。
- Rust 全量：145/145 通过；Doc tests 通过。
- Clippy：`--all-targets --all-features --locked -D warnings` 通过。
- Chat：42/42；Canvas：26/26；Canvas 性能：2/2。
- TypeScript/Vite production build：2088 modules，通过。
- Secret scan：1525 个文本文件通过；PEC retention 恢复并通过。

## 契约、数据、安全与恢复边界

- 本轮不改变 KnowledgeEntity、EvidenceRef、SQLite 事实源或 Vault 文件布局，只收紧投影进入适配器前的不变量。
- 相对路径校验是跨平台安全边界；不得让用户输入形成绝对路径、父级逃逸或 Windows 盘符路径。
- `next_revision()` 只构造领域修订；原子文件写入、旧修订保存、事件落库和索引重建仍由员工02负责。
- 未创建统一 Release，也未声称 M2 Go；本轮测试仍是工程门禁，不是跨模块现场证据。

## 需要配合 / 未关闭项

1. **员工02**：完成用户编辑 Markdown 的显式导入、内容 hash 比对、revision 事件和 FTS/Vector 重建触发，并调用本轮 `MarkdownProjection` 校验。
2. **员工03/04/05**：在同一无开发服务器 Tauri Release 中验证导入 → 实体 → Vault → 检索 → 否决/删除失效 → 重启；所有报告引用同一 Build ID、路径和 SHA-256。

## 下一步（最多两个 WIP）

1. 评审员工02用户编辑回流命令，重点检查稳定 ID、revision 冲突、原文不覆盖和索引失效顺序。
2. 统一 Build 生成后执行跨模块证据复核，并给出 M2 技术 Go/No-Go 意见。
