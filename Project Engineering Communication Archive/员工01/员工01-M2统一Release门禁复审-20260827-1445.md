# 员工01｜M2 统一 Release 门禁复审

时间：2026-08-27 14:45（Asia/Shanghai）

状态：切片实现基本完成；M2 仍为 No-Go，等待统一 Tauri Release 纵向验收

## 已阅读与落实

- 已阅读当前 PEC 控制文件、员工02～06当前 PEC、M2 派单、验收程序构建规则和 V1 总路线。
- 按 M2 责任复核统一检索投影、事实源边界、删除/否决过滤、FocusedContext 持久化与跨模块验收要求。
- 结论：单元测试、局部 IPC 和前端接入不能替代同一无开发服务器 Tauri Release 的真实闭环证据。

## 本次复审结论

- 员工02：知识实体/关系/EvidenceRef IPC、导入事务与重启查询已完成，但报告明确等待统一 Release 联调。
- 员工03/04：画布与 Chat 的统一检索只读消费和状态展示已接入，均明确等待统一 Release 真实验收。
- 员工05：SQLite FTS、VectorIndex 重建、Relation 召回、删除/拒绝失效和 `retrieve_knowledge` IPC 已完成代码门禁，但尚未形成正式 Release 证据。
- 员工06：VIS-008 独立视觉原型完成；其报告明确未修改生产 `desktop/`，不构成 M2 生产验收。

## 证据核对

- PEC retention：通过；当前目录保持员工01～06各一份报告及一份控制文件。
- Secret scan：通过（1506 个文本文件）。
- 当前发布目录 `artifacts/acceptance/LATEST.txt` 仍指向 `20260825-112427-m2-provider-test-27d3a75031ea-dirty`，`sourceTreeDirty=true`，不是本轮统一纵向验收 Build。
- 当前没有同时覆盖“导入 → FocusFrame → 实体/索引 → FocusedContext → 带来源回答 → 分支隔离 → 重启/失效”的统一 Build ID、完整路径、SHA-256 和无开发服务器现场证据。
- 代码搜索显示当前 SQLite 迁移包含实体/关系/EvidenceRef/FTS 表，但 Markdown Vault / 讨论日志 revision 的生产闭环仍未有可核验实现或 Release 证据。

## 未关闭门禁 / 需要配合

1. **员工02/05**：在同一 clean（或明确记录 dirty 原因的）Tauri Release 中提供真实 `retrieve_knowledge` 样本，覆盖 confirmed、Rejected/删除失效、Vector fallback、omitted、重启一致性和分支 scope。
2. **员工02/03/04/05**：补齐 Markdown Vault/讨论日志 revision、完整分支隔离、带来源回答，并在同一 Build ID 下提交查询、恢复和截图/现场证据。
3. **验收负责人/创始人**：从 `artifacts/acceptance/versions/<build-id>/` 启动该统一程序，在无 `localhost:1420` 开发服务器条件下完成 M2 纵向脚本；未完成前不得宣布 M2 Go 或开始 M3。

## 下一步（最多两个 WIP）

1. 评审员工02/05统一 Release 的事实源过滤、索引失效和重启证据，确认投影契约没有被消费层绕过。
2. 收到统一 Build ID 后，复核所有员工 PEC 是否引用同一 Build ID、路径、SHA-256 及已知未通过项，并给出 M2 技术 Go/No-Go 意见。
