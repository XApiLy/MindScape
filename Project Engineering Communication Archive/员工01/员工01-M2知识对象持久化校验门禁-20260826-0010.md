# 员工01｜M2 知识对象持久化校验门禁

时间：2026-08-26 00:10（Asia/Shanghai）  
状态：领域校验切片完成，等待员工02把门禁接入知识 IPC/SQLite 并进行真实 Release 联调

## 已阅读与落实

- 已阅读最新 PEC 控制文件、M2 派单，以及员工02《M2 知识查询 IPC 与验收接线》、员工03《M2 分支继承与 FocusFrame 查询稳定性》、员工04《M2 FocusedContext 真实投影消费验证》、员工05《RAG-M2 Markdown 分块索引首切片》和员工06当前报告。
- 当前计划明确 M2 仍为 No-Go，下一阶段从 FocusedContext 持久化转入实体/关系/EvidenceRef/索引失效闭环；本轮聚焦员工01主责 ARC-M2-003～005 与 CORE-M2-003/004，不实现 FTS/VectorIndex。
- 员工02已提供实体、关系、EvidenceRef 的 upsert/list IPC；本轮将“可进入事实源和派生索引”的校验规则冻结，避免 SQLite 与索引适配器各自解释对象语义。

## 本次推进

- 为 `EvidenceRef` 新增领域 `validate()`：校验稳定 ID、时间、可选哈希/摘录，以及 Message、Import、Attachment、ToolResult 四类 EvidenceTarget 的必填定位字段。
- 为 `KnowledgeScope`、`GeneratorRef`、`ScopedEvidenceRef` 新增校验：作用域、生成器、revision、EvidenceRef 和引用 ID 必须完整，引用 ID 不可重复。
- 为 `KnowledgeEntity` 新增校验：契约版本、ID/name、scope、revision、generator、时间戳和证据集合。
- 为 `KnowledgeRelation` 新增校验：契约版本、稳定 ID、非空且不同的 source/target、scope、revision、generator、时间戳和证据集合；拒绝自环关系。
- 为 Entity/Relation 增加 `validate_for_conversation()`，拒绝 Conversation/FocusFrame scope 与 IPC 会话参数不一致的跨会话写入，同时允许 workspace/project 级知识按设计复用。
- 更新 [M2 契约基线](D:/Project/MindScape/docs/contracts/m2-contract-baseline.md)，冻结对象进入 SQLite/FTS/VectorIndex 前的共同门禁。

## 交付物与验证

- `desktop/src-tauri/src/domain/contracts/evidence.rs`
- `desktop/src-tauri/src/domain/contracts/knowledge.rs`
- `docs/contracts/m2-contract-baseline.md`
- 新增 EvidenceRef、KnowledgeEntity、KnowledgeRelation 校验回归。
- Rust：120 passed / 0 failed
- Clippy：`-D warnings` 通过
- `cargo fmt --all`：通过

## 契约、数据、安全与恢复影响

- 只增加纯领域校验，不改变导入原文、V1 ContextSnapshot、Provider 或前端展示语义。
- 非法实体/关系/证据不得写入 SQLite，也不得产生 FTS/VectorIndex 派生记录；删除、否决、过期后的索引失效仍由员工02/05的数据与索引事务实现。
- EvidenceRef 仍只保存来源定位和可选摘要，不包含 API Key 或 reasoning 原文扩展。

## 需要其他员工配合 / 阻塞

- **员工02**：在 `upsert_knowledge_entity/relation/evidence` 写入前调用对应 `validate()`，读取后也校验；补充错误对象不落库、跨会话 scope 拒绝和重启 round-trip 测试。
- **员工05**：VectorMatch/FTS 适配器只消费已校验的 Entity/Relation/EvidenceRef；提供 unified evidence/scope DTO 前先确认这些字段不被压缩或重算。
- **员工03/04**：继续保持真实查询投影三态，不在前端重算关系、证据或检索排序；统一 Release 验收时记录来源与降级 notice。

## 下一步（最多两个 WIP）

1. 评审员工02的知识对象写入门禁和员工05的 FTS/VectorIndex/Relation 查询 DTO，补跨作用域和失效回归。
2. 设计 `KnowledgeRetrievalProjection` 的纯领域边界（selected/omitted/evidence/scope/reason），不进入底层索引实现。
