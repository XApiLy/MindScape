# 员工03｜M2 FocusFrame 查询投影与三态修正

时间：2026-08-25 15:16（Asia/Shanghai）  
状态：已完成第一刀，等待当前 FocusFrame 发现与快照持久化联调

## 已阅读与落实

- 已阅读最新 PEC 控制文件、M2 派单、员工01《M2 FocusFrame 生命周期闭环回归》、员工02《M2 FocusFrame 生命周期命令查询接线》、员工04《M2 FocusFrame 查询状态投影》、员工05《M2 向量不可用时全文关系降级》、员工06当前报告，以及 M2 契约基线、UI-HANDOFF-06 规范和本地实体/RAG方案。
- 员工01/02 已交付 `create_focus_frame`、`get_focus_frame_query`、`close_focus_frame`、`reopen_focus_frame`；本次按固定命令名接入前端 typed client，不推断生命周期、不复制状态机。
- 最新契约明确 `focusedContext: null` 表示快照未编译或暂不可用，不等于“没有知识”。本次修正画布占位 UI 的状态表达，避免把内核未接入误报为空知识。
- 员工04已完成 FocusFrame 查询状态纯投影并确认当前没有稳定的 graph/node → FocusFrame ID 映射；员工05已完成向量不可用时全文/关系降级边界；本次不修改导入、检索或 Provider 规则。

## 本次完成

- **CAN-M2-003：FocusFrame 生命周期 typed client**
  - `desktop/src/domain/commands.ts` 新增 `FocusFrameLifecycleCommandInput`、查询/创建结果类型镜像。
  - `desktop/src/app/kernelClient.ts` 新增 `createFocusFrame`、`getFocusFrameQuery`、`closeFocusFrame`、`reopenFocusFrame`，参数与 Tauri 命令边界保持一致。
- **CAN-M2-005：FocusFrameQueryProjection → 画布只读投影**
  - `desktop/src/canvas/canvasM2Projection.ts` 新增生命周期状态、revision、closedAt、FocusFrame 元数据和 FocusedContext 引用投影。
  - 明确 `focusedContextState` 三态：`unavailable`、`availableWithoutKnowledge`、`availableWithKnowledge`。
  - 复制 selectedMemoryRefs、omittedMemoryRefs 和知识引用数组，避免画布反向修改领域快照。
- **画布占位 UI 语义修正**
  - `ConversationCanvas` 可选消费 `focusFrameQueryByNodeId`。
  - 聚焦阅读器现在区分：尚未查询、节点无 FocusFrame、FocusFrame 已有但快照未编译、快照无知识、快照有 selected/omitted。
  - TSX/CSS 保留 `UI-HANDOFF-06` 说明；当前 App 尚未传入真实 map，因此不会伪造查询结果。
- 测试新增 FocusFrame lifecycle 与 FocusedContext 可用性边界，覆盖 active/closed、revision、`focusedContext: null` 和无知识快照。

## 交付物与验证

- `desktop/src/domain/commands.ts`
- `desktop/src/app/kernelClient.ts`
- `desktop/src/canvas/canvasM2Projection.ts`
- `desktop/src/canvas/canvasM2Projection.test.ts`
- `desktop/src/components/ConversationCanvas.tsx`
- `desktop/src/components/conversationCanvas.css`
- `pnpm test:canvas`：19/19 通过。
- `pnpm test:chat`：35/35 通过。
- `pnpm test:canvas:perf`：1/1 通过；200 节点基线保持通过。
- `pnpm build`：通过，Vite 1830 modules。
- `cargo test --offline --all-targets --locked`：99/99 通过。
- `git diff --check`：通过。
- 未执行 Tauri Release、真实知识快照查询或视觉验收；没有生成 Build ID。

## 契约、数据、安全、恢复与跨团队接口

- 仅接入既有 Tauri 命令客户端和前端纯投影，不修改 Rust、SQLite、Provider、凭据、导入原文或 M1 运行终态。
- 画布不直接访问 SQLite/raw storage/Markdown/向量索引；不计算继承、排除、检索排序或 Token 预算。
- `focusedContext: null` 保持“未编译/暂不可用”语义；仅在真正存在 `FocusedContextSnapshot` 且 `knowledgeContext` 为 null 时显示“无知识上下文”。
- 当前查询仍按 FocusFrame ID 工作；没有新增会话级 active FocusFrame 列表或恢复推断。

## 风险、阻塞与下一步（最多两个 WIP）

- **当前阻塞**：FocusFrame 生命周期命令已可用，但前端还没有可靠的“当前会话 active FocusFrame 发现/列表”查询，且 `focusedContext` 仍固定为 null，无法进行真实知识引用联调；员工04也确认当前 graph/node 没有稳定 FocusFrame ID 映射。
- **WIP1｜员工01/02**：确认 `list_focus_frames` 或 `get_active_focus_frame(conversationId)` 是否进入契约，并继续交付 FocusedContext 快照持久化/查询；前端保持只消费 projection。
- **WIP2｜员工04**：评审 FocusFrame typed client 的共享封装和 App 状态归属；接口确认后由员工03接入从节点创建、关闭/恢复和 `focusFrameQueryByNodeId`，补 Tauri 查询回归测试。
