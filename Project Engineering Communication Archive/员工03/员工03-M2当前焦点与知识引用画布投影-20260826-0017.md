# 员工03｜M2 当前焦点与知识引用画布投影

时间：2026-08-26 00:17（Asia/Shanghai）  
状态：CAN-M2-001/005 画布投影切片完成；M2 仍为 No-Go，等待统一 Tauri Release 联调

## 已阅读与落实

- 已阅读当前 PEC 控制文件、M2 派单、CAN-M2-001～008 计划，以及员工01《M2 知识对象持久化校验门禁》、员工02《M2 知识查询 IPC 与验收接线》、员工04《M2 知识查询只读投影接线》、员工05《RAG-M2 Markdown 分块索引首切片》最新报告。
- 员工02已提供 Entity/Relation/EvidenceRef 查询 IPC，员工04已将知识对象恢复结果接入工作区；因此本轮只做画布只读投影，不在前端重算作用域、排序、Token 预算或失效规则。
- 按 UI-HANDOFF-06 占位规则扩展现有状态条和聚焦阅读器，保持键盘语义、错误诚实和视觉替换边界，不建立新的视觉生产架构。

## 本次推进

- **CAN-M2-001｜主线/任务分支与当前 FocusFrame 标识**
  - 画布底部状态条新增当前选中节点的 FocusFrame 状态：活动、已关闭、未绑定、读取失败分别显示。
  - FocusFrame objective 作为只读 tooltip 来源；状态仍由 `list_focus_frames` 查询投影提供，不从坐标或节点标题推断。
- **CAN-M2-005｜知识引用投影**
  - 聚焦阅读器消费 `list_knowledge_entities` 返回的实体索引，将已选知识引用从裸 `entityId` 显示为实体名称，并保留稳定 ID、status、scope、revision、token 和 EvidenceTarget 类型。
  - 查询返回未知实体时回退显示稳定 ID，不伪造名称或来源。
- 保持原有 FocusedContext 三态、selected/omitted 字段和查询错误/重试语义不变。

## 交付物与验证

- `desktop/src/components/ConversationCanvas.tsx`
- `pnpm test:canvas`：21/21 通过。
- `pnpm test:chat`：40/40 通过（包含知识对象汇总测试）。
- `pnpm test:canvas:perf`：1/1 通过；200 节点 CAN-019 基线保持在门限内。
- `pnpm build`：通过，Vite 1832 modules。
- `node scripts/check-secrets.mjs`：通过，1477 个文本文件。
- `git diff --check`：通过。
- 旧员工03 PEC 已移至 `Project Engineering Communication Archive/员工03/`；PEC retention 将在本报告落盘后从仓库根目录复跑。

## 契约、数据、安全、恢复与跨团队接口

- 未改变 FocusFrame、FocusedContext、KnowledgeEntity、Relation、EvidenceRef 或 Tauri IPC 契约；画布只读消费 App 已查询的结构化结果。
- 不直接访问 SQLite、Markdown 或 VectorIndex，不执行检索、不重排候选、不把候选状态提升为确认事实。
- 查询失败仍单独显示错误；实体缺失时保留稳定 ID；导入来源和原文轨语义不变。

## 风险、阻塞与下一步（最多两个 WIP）

- **阻塞**：员工05尚未完成 FTS/VectorIndex/Relation unified retrieval DTO，CAN-M2-005 目前只能展示已返回的 FocusedContext 引用，不能提前展示未接入的检索来源或回流候选。
- **WIP1｜员工05/02**：冻结 unified evidence/scope/reason DTO 后，员工03接入来源摘要、排除原因和降级状态的画布只读投影。
- **WIP2｜员工01/02/04**：使用同一个无开发服务器 Tauri Release 验证导入→实体→FocusFrame→重启恢复→画布状态，并提供 Build ID、程序路径和 SHA-256；CAN-M2-003 分支切换/返回与 CAN-M2-006 回流候选仍待领域命令和验收样本。
