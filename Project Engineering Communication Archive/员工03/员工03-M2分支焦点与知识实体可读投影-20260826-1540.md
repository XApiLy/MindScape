# 员工03｜M2 分支焦点与知识实体可读投影

时间：2026-08-26 15:40（Asia/Shanghai）  
状态：CAN-M2-001/005 画布侧继续推进；M2 仍为 No-Go

## 已阅读与落实

- 已阅读当前 PEC 控制文件、M2 派单及员工01～05、员工06当前报告；重点落实员工02已提供 Entity/Relation/EvidenceRef 查询 IPC，员工04已提供知识查询只读投影，员工05的索引失效仍等待 SQLite 事务接线。
- 计划中的 CAN-M2-001/005 属于画布消费边界：本轮只显示内核已返回的 FocusFrame 和 KnowledgeEntity，不实现 FTS/VectorIndex 查询、失效判断或关系推理。
- 按 UI-HANDOFF-06 规则复用现有状态条和阅读器占位结构，保留错误、未绑定、已关闭和未知实体状态，不引入新的视觉语言。

## 本次推进

- **CAN-M2-001｜当前分支焦点标识**：画布状态条新增当前选中节点的 FocusFrame 状态和 objective tooltip，区分活动、已关闭、未绑定与读取失败；数据源为 `list_focus_frames` 的显式 parentNode 映射。
- **CAN-M2-005｜知识实体可读引用**：聚焦阅读器将 FocusedContext 的 selected 引用映射到已查询的 KnowledgeEntity 名称，同时保留稳定实体 ID、status、scope、revision、估算 Token 和 EvidenceTarget 类型；实体不在查询结果中时回退显示 ID。
- 修复后续查询仍保持只读：不从前端重算检索排序、作用域、知识状态、预算或来源有效性。

## 交付物与验证

- 代码：[desktop/src/components/ConversationCanvas.tsx](D:/Project/MindScape/desktop/src/components/ConversationCanvas.tsx)
- `pnpm test:canvas`：21/21 通过。
- `pnpm test:chat`：40/40 通过。
- `pnpm test:canvas:perf`：1/1 通过；200 节点 CAN-019 基线保持在门限内。
- `pnpm build`：通过，Vite 1832 modules。
- `node scripts/check-secrets.mjs`：通过，1477 个文本文件。
- `git diff --check`：通过。
- 旧员工03 PEC 已移至 `Project Engineering Communication Archive/员工03/`；报告落盘后复跑 PEC retention。

## 契约、数据、安全、恢复与跨团队接口

- 未改变 FocusFrame、FocusedContext、KnowledgeEntity、Relation、EvidenceRef 或 Tauri IPC 契约；只扩展画布表现层读取。
- 不访问 SQLite、Markdown 或 VectorIndex；未知实体和查询错误均显式保留，不伪造知识或恢复成功。
- 当前 FocusFrame 仍按显式 `parentNodeId` 投影；主线无 parent 不会绑定到任意节点。

## 风险、阻塞与下一步（最多两个 WIP）

- **阻塞**：统一检索 DTO、Markdown 投影来源路径、回流候选命令和实体失效事务尚未完成；CAN-M2-005 暂只能显示 FocusedContext 已选引用，不能提前展示未接入的检索结果。
- **WIP1｜员工01/02/05**：冻结 unified evidence/scope/reason DTO，并接通状态变更到 FTS/VectorIndex 的失效重建后，员工03接入来源摘要与降级状态投影。
- **WIP2｜员工01/02/04**：在同一无开发服务器 Tauri Release 完成导入→实体→FocusFrame→重启恢复的跨模块样本；CAN-M2-003 分支切换/返回和 CAN-M2-006 回流候选仍待命令与验收证据。
