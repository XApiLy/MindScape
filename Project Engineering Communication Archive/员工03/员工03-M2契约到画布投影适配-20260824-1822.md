# 员工03｜M2 契约到画布投影适配

时间：2026-08-24 18:22（Asia/Shanghai）  
状态：已完成一刀，等待生命周期查询边界联调

## 已阅读与落实

- 已阅读当前 PEC 控制文件、M2 派单、员工01《M2 契约与 FocusFrame 首切片》、员工02《M2 导入事务表与 v6 迁移》、员工04《M2 导入 Intake 首切片》、员工05《M2 DeepSeek 运行档案映射预检》（及其 Provider 能力目录首切片），以及本地实体/RAG、Markdown Vault/分支隔离和内核总览文档。
- 员工01 已冻结 `FocusFrame`、`KnowledgeEntity`、`ImportSource`、`ImportRevision`、`ParseReport` 的 TS 契约；本次只增加画布视图适配，不复制继承、排除优先级、知识状态转换或导入解析算法。
- 员工02 已完成 SQLite v6 表但尚未提供结构化导入查询 IPC；因此本次没有让 UI 直接读文件、SQLite 或 raw storage，也没有提前接入未冻结的生命周期命令。
- 员工04 的 Intake 仍处于元数据预检阶段；本次保留“导入原文 / 非本地生成”和“原样继续”的来源语义，不改变其交互入口。
- 员工05 当前只完成发送前的 DeepSeek 运行档案纯映射，尚未接入发送链路；本次不依赖 Provider 运行档案字段，因此无接口冲突。

## 本次完成

- **CAN-M2-005 / CAN-M2-004：契约到画布纯投影适配层**
  - 新增 `desktop/src/canvas/canvasM2Projection.ts`。
  - `projectFocusFrame` 映射 FocusFrame 的目标、分支类型、引用集合和版本；复制数组，保证画布投影不能反向修改领域对象。
  - `projectKnowledgeReference` 映射知识实体的生命周期状态、作用域类型、修订号与证据摘要/来源类型。
  - `projectImportSource` 映射来源、revision、分析策略、raw track 数量和 ParseReport 计数；无报告时保持 `null`，不合成解析结果。
- 新增 `desktop/src/canvas/canvasM2Projection.test.ts`，覆盖 FocusFrame 空值与引用隔离、知识 candidate/evidence 投影、导入 partiallyParsed/无 ParseReport 两种状态。
- 将适配层测试加入 `desktop/package.json` 的 `test:canvas`，保持适配逻辑与现有会话图坐标/边关系解耦。

## 交付物与验证

- 代码：`desktop/src/canvas/canvasM2Projection.ts`、`desktop/src/canvas/canvasM2Projection.test.ts`、`desktop/package.json`。
- `pnpm test:canvas`：17/17 通过。
- `pnpm test:canvas:perf`：1/1 通过；200 节点基线 `nodes=200, edges=199, importedNodes=19`，本次未改变坐标算法。
- `pnpm test:chat`：24/24 通过。
- `pnpm build`：TypeScript 与 Vite 构建通过（1828 modules）。
- `git diff --check`：通过。
- 未执行 Tauri Release 发布；本次没有可供创始人验收的 Build ID。浏览器自动化此前因本机 Playwright 包缓存/权限不可用，未宣称视觉验收通过。

## 契约、数据、安全、恢复与接口影响

- 仅新增前端纯函数投影，未修改 Rust、SQLite、IPC、M1 运行/取消/恢复或凭据边界。
- 画布不读取原文正文路径，不把导入文本当作指令，不引入自动分析或向量检索旁路。
- `FocusFrame` 的继承/排除语义仍由 Context Compiler 负责；当前适配层等待结构化查询结果后再接入组件。
- 上一份员工03 PEC 已移入 `Project Engineering Communication Archive/员工03/`；由于该文件在共享工作树中未被 Git 跟踪，`git mv` 无法执行，本次使用等价文件移动并保留历史文件。

## 风险、阻塞与下一步（最多两个 WIP）

- 当前阻塞：员工01/02 尚未交付 FocusFrame/ImportSource 的生命周期命令与结构化查询 IPC，故本刀只交付可测试适配层，不接入真实 UI 数据流。
- **WIP1｜员工01**：请确认 `canvasM2Projection` 的字段裁剪没有遗漏，并在 FocusFrame 查询边界冻结后提供稳定返回类型。
- **WIP2｜员工02/04**：完成导入事务写入与 ParseReport 查询 IPC 后，由员工04接 Intake 预览、由员工03接来源节点详情；双方继续保持前端不直接访问 SQLite/raw storage。
