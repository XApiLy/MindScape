# 员工03｜M2 回流候选正式 UI 消费

时间：2026-08-29 14:20（Asia/Shanghai）  
覆盖任务：CAN-M2-005 / CAN-M2-008 / ARC-M2-002 联调  
状态：`FocusPromotionCandidateSet` 前端消费完成；等待统一 clean Release 真实纵向验收

## 已阅读与落实

- 已读取 PEC 控制文件及员工01～06当前报告；重点落实员工02《受控原文与回流候选查询》和员工01《回流候选生命周期门禁》的最新 typed 契约。
- 员工02已提供 `getFocusPromotionCandidates(focusFrameId, expectedMemoryVersion)`；员工01进一步冻结：Active、主线、空声明统一返回 `null`，只有 Closed 的任务/探索/复盘分支才能返回正式候选，过期 memory version 显式冲突。
- 按 React 性能规范只在用户打开单节点聚焦阅读器时惰性查询；使用稳定 App callback 和 effect cleanup，避免全会话预取及旧节点异步响应覆盖新节点。

## 相比上一份 PEC 的变化

- 上一版把 typed 查询列为接口阻塞；本版确认该阻塞已解除并完成正式消费。
- 旧 UI 的“typed IPC 尚缺”提示已删除；`promoteRefs` 继续保留为声明层，正式“待确认”列表只使用内核返回的 `FocusPromotionCandidateSet`。
- 员工01已复核员工02 Markdown 原子事务代码边界通过；该项不再是代码阻塞，仍等待真实重启一致性证据。

## 本次完成

- App 接入 `kernelClient.getFocusPromotionCandidates`，始终携带当前 FocusFrame 稳定 ID 与 `expectedMemoryVersion`，结构化错误转为安全 UI 文案。
- `ChatWorkspace → ConversationCanvas → CanvasFocusReader` 完成 typed callback 透传；候选查询仅在聚焦阅读器存在且 FocusFrame 已绑定时启动。
- 正式区分 unavailable、loading、`null` empty、error/stale 和 ready；错误态支持重新查询及刷新 FocusFrame。
- Active 的 `null` 显示“分支尚未完成”，上方仍可审计 promoteRefs 声明；Closed 成功态显示 source frame、branch kind、memory version、候选引用和“待确认”。
- 候选引用若能匹配已加载 KnowledgeEntity，显示实体名称与稳定 ID；否则保留原始引用，不重排、不丢弃、不推断类型。
- 不提供确认、否决或合并按钮；UI 明确说明任何状态转换必须继续经过内核知识状态机。

## 验证证据

- Canvas：34/34；Chat：51/51；Canvas 性能：2/2。
- TypeScript/Vite production build：2095 modules，通过；主入口约 487.71 kB，ImportIntakeDialog 独立 chunk 约 16.61 kB。
- `git diff --check`：通过。
- 员工01接口门禁证据：生命周期定向 7/7、应用查询 3/3、Rust 169/169；本轮员工03未重复声明为 UI 真实验收。

## 契约、安全、恢复与发布边界

- 前端不从 `promoteRefs` 合成候选，不复制 Active/Closed 领域判断，不因坐标或显示名称改变引用身份。
- `null`、完整性冲突和成功集合保持不同状态；memory version 变化会触发新查询，已取消的旧响应不会落入当前阅读器。
- 候选只读，不修改 SQLite、Vault、FTS、VectorIndex、主线或项目知识；不显示 Key、Authorization、Provider 原始响应、reasoning 或向量正文。
- 既有 dirty Build 早于员工02新 IPC、员工01生命周期门禁和本轮 UI；本轮未生成 Release，M2 仍为 No-Go。

## 需要配合 / 未关闭项

1. 员工02补 FTS/Vector/Projection 冲突回滚直接断言及关闭重启一致性真实样本；员工01复核证据。
2. 员工05在代码冻结后发布包含员工01生命周期门禁、员工02 typed IPC、员工03本轮 UI、员工04最新导入 UI和员工05性能优化的唯一 clean 候选。

## 下一步 WIP（最多两个）

1. 在统一候选验证 Active 隐藏→Closed 暴露→Reopen 再隐藏、过期 memory version 冲突及关闭重启一致性。
2. 与员工04/05复核长会话 content-visibility 下的聚焦阅读、候选展开、滚动锚点和复制语义。
