# 员工03｜B2 正式动作与重启状态 UI 闭环

> 提交时间：2026-08-31 10:59（UTC+8）  
> 覆盖任务：B2 / CAN-M2-005 / B5 联调准备  
> 状态：**B2 工程实现已关闭；仍待 B1 唯一 clean Release 内的 B5 真实 Tauri 纵向验收，M2 仍为 No-Go**  
> 基线提交：`1eaf191b1a6c`（共享 dirty 集成工作树）

## 相比上一份的变化

- 上一份 PEC 记录的正式 typed command、持久化决定与重启查询三个前置阻断均已由员工01/02提供，本轮不再重复汇报只读候选列表。
- 员工03已把 `confirm / promote / reject / delete` 正式接入画布聚焦阅读器，并消费内核终态投影；失败、重试、刷新和重启历史均不以 React 临时状态冒充领域事实。
- 按 2026-08-31 Review Lab 规则，在归档旧 PEC 前发布员工确认主线 `preview`：`20260831T025812Z-b2-回流决策与重启历史正式-ui-00a1`，状态 `ready`。

## 已阅读与落实

- 已重新阅读控制 PEC 顶部“2026-08-30：M2 Go 阻断收敛”“员工直接执行表”和 Review Lab 发布规则；本轮只关闭 B2 正式 UI 与状态投影，不发布 Acceptance Release，不宣称 M2 Go。
- 已读取员工01《B2 原子决策领域契约》，严格镜像 create-only `decisionId`、memory/lifecycle/entity/decision optimistic revision、四动作及 conversation 目标作用域；React 不复制领域状态机。
- 已复核员工02共享树中的 SQLite v16 决策表、Vault 事务日志、FTS/Vector/Relation 失效、应用服务和重启查询；UI 成功态只由 `decide_focus_promotion` 返回值确定。
- 已复核视觉中心 `01/02/03/05` 当前边界：沿用正式产品现有组件与视觉语言；画布保持轻量摘要，候选动作与持久化历史放在聚焦阅读器，未改变 raw Markdown、字体或画布几何事实。

## 本轮关闭的 B 编号

### B2：工程实现阻断关闭

- 新增 TypeScript `FocusPromotionDecisionCommandInput/Projection` 与构造器，四动作精确携带员工01冻结的并发版本和用户 attribution。
- `App → ChatWorkspace → ConversationCanvas → kernelClient → Tauri command` 已形成正式调用链；Mock/Web 预览不会伪造原生决策能力。
- 每个候选提供“确认（仅分支）”“回流当前会话”“否决”“删除”；回流、否决、删除有二次确认，同一时刻只允许一个动作。
- 每个候选显示 pending/error/success；不确定失败后的同动作重试复用原始 `decisionId` 和完整输入，避免生成第二个决定。
- 持久化成功后即视为命令成功；知识/FocusFrame 后续刷新失败只显示警告，不把已提交动作误报成失败并诱导非幂等重试。
- 已接 `list_focus_promotion_decisions`：重启后显示不可变动作、决定修订、时间和 promoted entity；候选为空时明确显示“已全部处理”，不会把空集合误报为内核错误。

## 跨模块证据

1. 前端映射：`desktop/src/app/focusPromotionDecision.test.ts` 证明 confirm 无目标、promote 保留源候选并指向当前 conversation、reject/delete 不携带目标，且全部携带 frozen optimistic revisions。
2. 正式 IPC：`desktop/src/app/kernelClient.ts` 消费 `decide_focus_promotion`、`get/list_focus_promotion_decision(s)`；Tauri 命令与 `lib.rs` 注册已存在，TypeScript production build 通过。
3. 原子数据链：SQLite v16 在单事务中持久化 decision、源/目标实体及派生索引；confirm 幂等重启、stale 整笔回滚、promote 新实体、reject 检索失效、delete tombstone/关系/索引清理均有测试。
4. Vault 与恢复：application service 先写带 journal 的 Vault projection，再提交 SQLite；数据库冲突回滚所有 Vault 文件，启动时按已提交 decision 恢复或清理 journal。
5. 正式 UI 投影：命令终态触发知识与 FocusFrame 并行刷新，历史查询独立恢复；Reopen 不撤销决定，重新关闭后已处理 candidate 由内核过滤。

## 验证结果

- Chat：`60 passed / 0 failed`。
- Canvas：`34 passed / 0 failed`；Canvas 性能：`2 passed / 0 failed`。
- TypeScript/Vite production build：通过，`2097 modules transformed`。
- Rust 全量：`197 passed / 0 failed / 1 ignored`；ignored 为既有约 330 MB 固定生产语义模型下载质量用例。
- Rust Clippy `--all-targets --all-features -D warnings`、fmt：通过。
- Review Lab 最终 preview：`20260831T025812Z-b2-回流决策与重启历史正式-ui-00a1` / `ready`；浏览器控制台 `0 errors / 0 warnings`。重点观察四动作层级、逐候选状态、全部处理终态和重启决定记录。静态 preview 不替代 SQLite 或 Release 现场验收。

## 契约、数据、安全与恢复影响

- UI 不再通过通用 upsert/delete 组合动作，也不把 localStorage/组件状态作为决定事实；所有终态来自持久化 projection。
- 高影响动作二次确认；错误只显示安全命令信息，不展示数据库路径、原文、Key 或内部响应体。
- 回流创建新的 conversation-scoped confirmed entity，保留分支源实体和 EvidenceRef；确认仅分支，否决退出检索，删除保留 tombstone。
- 本轮未生成 Build ID、EXE 或 SHA-256，也未启动新的开发服务器；唯一正式 Release 仍由员工01冻结、员工05统一发布。

## 剩余阻断

1. **B1 未关闭**：当前仍是 `1eaf191b1a6c` 上的 226 项 dirty 共享集成快照，不能称为 clean candidate。
2. **B5 未关闭**：需员工05在同一 clean Tauri Release 内用真实样本逐一操作 confirm/promote/reject/delete，并跨 Reopen、进程退出/重启核对 UI、SQLite、Vault、EvidenceRef、FTS/Vector/Relation 一致。
3. Review Lab preview 是可见交互快照；其无持久样本的静态环境不能单独证明原生 SQLite/重启链路。

## 下一步 WIP（最多两项）

1. 配合员工01处理 B1 集成冲突和代码冻结，只修复 B2 合流阻断，不扩展新画布能力。
2. 配合员工05执行 B5 同一真实样本的四动作、Reopen 与重启窗口验收；证据进入唯一 Release 矩阵。

## 需要同事配合

- **员工01**：评审员工02最终 Vault journal/SQLite v16 与员工03 typed input 是否完全符合冻结契约，并将 B2 纳入 clean 集成入口。
- **员工02**：提交最新 B2 持久化/Vault/restart PEC，并在 B5 现场提供 decision、实体、Vault、FTS/Vector/Relation 的脱敏核对方式。
- **员工04**：如 Chat/阅读入口需要同一动作，只复用现有 callback；当前无第二套状态机阻断。
- **员工05**：在唯一 clean Release 执行并记录 B5 四动作与重启矩阵；不要把 Review Lab preview 计作原生验收。
