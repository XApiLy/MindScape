# 员工03｜B2 正式动作接口阻断收敛

> 提交时间：2026-08-30 23:28（UTC+8）
> 覆盖任务：B2 / CAN-M2-005 / CAN-M2-008 / ARC-M2-002 联调
> 状态：阻塞；B2 未关闭，M2 仍为 No-Go
> 基线提交：`f5307ad71b7f`

## 相比上一份的变化

- 不再把“正式候选只读查询已接入”重复报告为完成；控制 PEC 已明确只读候选列表不计 B2 关闭。
- 本轮完成 B2 从 React 正式入口、TypeScript client、Tauri command、application service、SQLite/Vault 到重启投影的接口审计，确认阻断不在员工03既有候选读取 UI，而在缺少内核拥有的候选决策命令和持久化结果。
- **本轮关闭的 B 编号：无。** B2 不能在没有 typed command、持久化决策和重启结果的情况下诚实关闭。

## 已阅读与落实

- 已重新阅读控制 PEC 顶部“2026-08-30：M2 Go 阻断收敛”和“员工直接执行表”；据此将员工03唯一 WIP 收敛为 B2 正式 UI 动作消费，不继续扩展画布、Markdown 阅读或视觉功能。
- 已阅读 M2 Go 阻断审计、V1 责任归属、员工01当前 FocusFrame 生命周期 PEC、员工02最新 B3 PEC及 PEC 留存制度。员工02已经证明否决/删除可使 Vault、FTS、Vector 失效，但这些通用知识写入能力不能替代“该 FocusFrame 候选已经被哪位用户作出哪种决定”的领域事实。
- 已按 React 组件边界复核 `App → ChatWorkspace → ConversationCanvas → CanvasFocusReader`；员工03会继续让 App 持有 IPC 编排、阅读器持有动作反馈，候选身份和状态只来源于内核 typed projection。

## 本次完成与跨模块证据

### B2：未关闭；已定位唯一前置接口阻断

1. `desktop/src/domain/focus.ts` 只有只读 `FocusPromotionCandidateSet`，字段止于 frame、branch kind、memory version 和 candidate refs；没有候选决策、目标作用域、决策修订或终态投影。
2. `desktop/src/app/kernelClient.ts` 和 `desktop/src-tauri/src/commands.rs` 只有 `get_focus_promotion_candidates`；不存在确认、否决、回流或候选删除的 typed command。
3. `desktop/src-tauri/src/application/mod.rs` 只根据已关闭 FocusFrame 返回声明集合；重启后会从同一 `promoteRefs` 再次返回全部候选，没有已确认/已否决/已删除的持久化判定。
4. 现有 `upsert_knowledge_entity` 是通用整对象写入，SQLite 当前没有 B2 专属的 expected entity revision、FocusFrame/memory version 绑定或幂等决策记录。前端直接拼装实体会允许并发覆盖，且无法证明用户动作属于哪个候选集合。
5. 现有 `delete_knowledge_entity` 能删除实体、Vault 文件和派生索引，但不会原子更新 FocusFrame 候选决策；删除后原 `promoteRefs` 仍可恢复成悬空候选。因此不能把通用删除按钮伪装为 B2 删除闭环。
6. React 当前只读投影已正确保持 Active 隐藏、Closed 暴露、Reopen 隐藏和 stale memory version 冲突。该部分是 B2 动作 UI 的已有入口，不作为本轮关闭证据重复计数。

以上证据跨越前端领域类型、IPC client、Tauri command、应用服务、SQLite/Vault 与重启读取路径，说明按钮缺失不是单纯视觉问题，必须先冻结领域命令。

## 契约、数据和安全影响

- 员工03不会使用前端临时状态或 localStorage 记录“已确认”，也不会直接改写候选实体来绕过内核；否则重启、并发窗口、Vault 与检索状态会分叉。
- 员工01需要冻结一个原子、可幂等的 B2 typed command。输入至少要绑定 `focusFrameId`、`candidateRef`、候选集合的 expected memory version、动作、目标主线/项目作用域，以及必要的 expected decision/entity revision；输出需要返回持久化后的候选决策终态和新版本，供 UI 直接投影。
- 领域契约必须明确“确认”和“回流”的关系、否决后能否恢复、删除是删除候选记录还是知识实体、Reopen 后决策是否保留；员工03不在 UI 内猜测这些语义。
- 员工02应让命令原子协调候选决策、KnowledgeEntity、Markdown Vault、FTS/Vector/Relation 失效或重建，并提供关闭/重启后查询同一终态的存储能力。

## 验证结果

- 本轮为跨模块接口审计和阻断收敛，没有新增工程代码，不重复运行或转述此前已通过的局部 Canvas/Chat 单元测试。
- 已逐层检索确认当前命令表只有候选查询和通用实体写入/删除，没有 B2 决策命令；没有生成 Build ID，也不声明 Release 或 M2 Go。

## 风险与阻断

1. **B2 仍阻断**：员工01尚未提供候选确认/否决/回流/删除的正式领域命令和持久化结果投影。员工03此时增加可点击按钮只会产生不可恢复的伪状态。
2. **B5 仍阻断于 B2**：没有决策命令便无法在同一 clean Release 证明 Closed 候选动作、Reopen、删除和重启一致性。
3. 共享工作树仍是 dirty 集成态；本轮没有发布候选，B1/B4/B5/B6 状态均不由本报告改写。

## 下一步 WIP（最多两项）

1. 员工01 typed command 与终态 projection 到位后，立即在现有 `App → ChatWorkspace → ConversationCanvas → CanvasFocusReader` 链路接入四类动作、逐候选 pending/error/success、并发防重、过期刷新、键盘焦点和安全确认。
2. 与员工02/05使用同一 clean Release 样本验证动作后知识/Vault/全文/向量/关系状态，以及 Reopen 和进程重启后的同一投影；证据进入 B5 矩阵，不另发第二个 Release。

## 需要同事配合

- **员工01（直接阻塞对象）**：冻结并实现上述 B2 决策 typed contract，特别是动作枚举、目标作用域、expected revisions、幂等键、删除语义、Reopen 语义和结果 projection；下一份 PEC明确该接口的命令名与状态机测试。
- **员工02**：在员工01契约下接入持久化/Vault/索引事务与重启恢复，避免前端通过通用 upsert/delete 组合出非原子流程。
- **员工04**：仅当同一动作还必须出现在现有 Chat/阅读入口时复用员工03 callback，不建立另一套状态判断。
- **员工05**：暂不将只读候选列入 B5 通过；待正式动作接口完成后，在唯一 clean Release 中执行同一真实样本。
