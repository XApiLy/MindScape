# 员工03｜M1画布共享运行快照与终态一致性

> 提交时间：2026-08-18 16:06（UTC+8）  
> 覆盖任务：CAN-012、CAN-014、CAN-019  
> 状态：CAN-012前端一致性切片完成；真实窗口矩阵和CAN-014重启持久化待联调  
> 基线提交：`f54363e`（共享工作树；Draft PR受只读Git元数据限制尚未创建）

## 相比上一份的变化

- 画布不再自行二次调用`list_model_runs`，改为消费`App`与阅读视图同批加载、同批刷新的`ModelRunProjection[]`。
- 实时画布节点不再写死Mock，直接显示本次运行真实`providerId/modelId`。
- failed、cancelled和`providerCode=application_interrupted`的安全提示、部分内容保留标记进入画布投影与聚焦阅读。
- 新增Frozen V1四类终态矩阵，验证稳定`nodeId`合并、无重复卡片和真实模型信息一致。

## 已阅读与落实

- 开工前已重读PEC控制文件和员工01～05当前快照；报告前补读员工01最新`M1上下文预算不变量联调`。
- 本轮只推进M1 Real Chat Alpha，不触碰导入、多Provider扩展、Skill/MCP和视觉重构。
- 员工02仍未提供CanvasViewport持久化命令/映射，因此CAN-014不使用localStorage伪装重启恢复。
- 终态仍严格使用Frozen V1：应用退出中断是`failed + application_interrupted`，没有新增平行状态。

## 本次完成与证据

- `desktop/src/App.tsx`：保存会话加载所得运行投影；启动成功时upsert，异常后与图一起重查，并下发共享快照。
- `desktop/src/components/ChatWorkspace.tsx`、`ConversationCanvas.tsx`：透传共享投影；实时卡片使用真实Provider/Model，显示结构化安全错误和取消保留状态。
- `desktop/src/canvas/graphProjection.ts`：从终态事件投影`runError/partialContentRetained`；删除独立查询Hook，消除双快照竞态。
- `desktop/src/canvas/graphProjection.test.ts`：覆盖completed、cancelled、provider failed、application interrupted四类终态。
- `desktop/src/app/chatRunState.ts`、`tsconfig.app.json`：修正新增运行常量在Node ESM测试中的显式TypeScript路径解析。

## 契约、数据和安全影响

- 未修改Runtime/Context Frozen V1字段、枚举或语义，未修改SQLite schema和网络/凭据边界。
- 画布只显示内核提供的`safeMessage`，不展示厂商原始敏感响应，也不自行推断终态。
- ConversationGraph仍是节点/边真相；ModelRunProjection只按稳定`nodeId`覆盖运行显示事实。

## 验证结果

- `pnpm test:canvas`：9/9通过。
- `pnpm test:canvas:perf`：1/1通过；200节点/199边计算门槛通过。
- `pnpm test:chat`：10/10通过。
- `pnpm build`：通过，生产构建1822个模块。
- `git diff --check`：通过；PEC留存检查在新快照写入后执行。

## 风险与阻塞

- 尚未在带真实Key的Windows Tauri窗口覆盖完成、用户停止、厂商失败、应用中断和重启矩阵，不能宣称M1 Alpha验收完成。
- CAN-014仍缺按conversation保存/读取`x、y、zoom`的正式内核接口，是当前No-Go项。
- 200节点结果仍是CPU计算基线，不等同真实窗口FPS/GPU/内存门禁。
- `.git`只读导致`git mv`无法创建`index.lock`；本轮已用同路径安全移动归档旧PEC，暂存时需确认Git识别为rename。

## 下一步 WIP（最多两项）

1. CAN-012：在真实Windows Tauri窗口完成成功、停止、失败、application_interrupted与重启的画布/阅读一致性矩阵。
2. CAN-014：员工02提供稳定视口接口后接入节流保存、按会话读取和重启恢复测试。

## 需要同事配合

- **员工02**：请提供CanvasViewport的正式保存/读取命令、迁移与失败语义。
- **员工04**：请在真实窗口评审同一运行快照下画布/阅读呈现，并覆盖停止、失败、中断、重启和无重复卡片。
- **员工01**：请确认画布直接呈现Frozen V1 `safeMessage`与`application_interrupted`没有产生平行终态语义。
- **员工05**：真实Key可用后请配合DeepSeek成功、取消、错误与重启矩阵。
