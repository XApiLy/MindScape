# 员工03｜M1画布运行一致性与200节点基准

> 提交时间：2026-08-18 15:48（UTC+8）  
> 覆盖任务：CAN-012、CAN-014、CAN-019  
> 状态：CAN-012切片与CAN-019首个基准完成；CAN-014重启持久化等待接口  
> 基线提交：`f54363e`（当前分支`docs/REL-002-pec-retention`；Draft PR受只读Git元数据限制尚未创建）

## 相比上一份的变化

- 从“画布只消费前端活动运行态”推进为同时消费员工02的`list_model_runs`恢复查询投影。
- 恢复后的failed/cancelled/completed状态和部分内容按稳定`nodeId`就地合并，不创建平行卡片。
- 新增流式内容增长位置不变回归测试，以及200节点固定样本和可重复计算基准。
- CAN-014仍只完成本次启动内的会话视口恢复；没有使用localStorage伪装重启持久化。

## 已阅读与落实

- 已完整阅读V1范围V1.0 Draft、2026-08-17产品共识、V1总路线1.0、M1派单、内核总览、前五人责任书、Context/Runtime Frozen V1契约、RC1威胁模型、Git V1制度和PEC-GOV-001。
- 已读取员工01当前`M1上下文与运行契约冻结`、员工02当前`真实Chat运行协调与启动恢复`、员工04当前`统一运行事件与错误体验`及员工05当前`DeepSeek真实Provider开工`。
- 具体落实：本轮只做M1的CAN-012/014/019；画布不解释终态、不拼上下文，只以Frozen V1的`ModelRunProjection.nodeId/state/partialContent`覆盖显示投影。
- 冲突判断：员工02正在修改SQLite和命令层，本轮未触碰其高风险文件；正式视口表/命令缺失，因此CAN-014重启恢复保持阻塞而不越权落库。

## 本次完成与证据

- `desktop/src/canvas/graphProjection.ts`：按最新运行投影合并节点状态、Provider、模型和部分内容。
- `desktop/src/canvas/useConversationRunProjections.ts`：按会话读取恢复投影，浏览器预览失败时不伪造数据。
- `desktop/src/canvas/canvasViewport.ts`：抽取可测试的平移、缩放和定位数学。
- `desktop/src/canvas/graphProjection.test.ts`：覆盖最新运行选择、稳定nodeId合并、无重复节点和长流式内容位置稳定。
- `desktop/src/canvas/canvasPerformance.test.ts`与`docs/engineering/canvas-200-node-baseline.md`：建立200节点固定样本、命令和首轮记录。

## 契约、数据和安全影响

- 未修改Frozen V1字段、枚举或含义，未新增平行Node/Run类型。
- 未修改SQLite schema、凭据、日志和网络边界；画布只通过稳定Tauri查询客户端读取投影。
- `ModelRunProjection`只影响UI显示，节点关系仍完全来自ConversationGraph，坐标不参与语义推断。

## 验证结果

- `pnpm test:canvas`：8/8通过。
- `pnpm test:canvas:perf`：1/1通过；最终记录为初次投影2.850ms、5000次平移0.842ms、5000次缩放1.890ms、1000次选中扫描6.831ms、50次流式投影132.220ms。
- `pnpm test:chat`：5/5通过。
- TypeScript全量检查与`pnpm build`：通过，生产构建1821个模块。
- `git diff --check`与PEC六文件检查：通过（新PEC替换后再次执行）。

## 风险与阻塞

- CAN-014仍缺按会话保存/读取`x、y、zoom`的后端命令和SQLite映射，重启后视口恢复未完成，是M1 No-Go项之一。
- 当前基准是CPU计算基线，不等同于真实Tauri窗口的FPS、GPU合成和内存验收。
- 共享工作树包含员工01/02/04/05并行修改；`.git`只读使`git mv`无法创建`index.lock`，本轮使用同路径移动保留旧PEC，暂存时必须确认Git识别为rename。

## 下一步 WIP（最多两项）

1. CAN-014：员工02确认正式视口持久化接口后，接入保存、读取和重启恢复回归测试。
2. CAN-012联调：与员工04在真实Windows Tauri窗口验证停止、失败、application_interrupted和完成后画布/阅读视图一致。

## 需要同事配合

- **员工02**：请确认或提供按conversation保存/读取CanvasViewport的稳定命令、迁移和恢复测试边界。
- **员工04**：请评审恢复运行投影的共享呈现，并在真实窗口覆盖发送、停止、失败、中断、重启和无重复卡片。
- **员工01**：请确认画布仅以Frozen V1运行查询覆盖显示、没有平行终态语义。
