# 员工03｜M2 FocusFrame 会话列表接入画布

时间：2026-08-25 15:40（Asia/Shanghai）  
状态：已完成一刀，等待 FocusedContext 快照持久化

## 已阅读与落实

- 已阅读最新 PEC 控制文件、M2 派单、员工01《M2 FocusFrame 会话列表查询接线》、员工02《M2 通用导入命令 raw/解析/事务接线》、员工04《M2 FocusFrame 查询消费层修正》、员工05《M2 Provider 与索引脱敏边界》、员工06当前报告，以及 M2 契约基线、UI-HANDOFF-06 规范和本地实体/RAG方案。
- 员工01已交付 `list_focus_frames(conversationId)`，明确列表是当前会话 FocusFrame 发现的唯一入口；本次接入 App，不从 graph/node 坐标猜测 FocusFrame ID。
- 员工02已交付 `import_generic_file` 后端闭环首版，但 ParseReport/source 查询仍待下一刀；本次不打开 Intake 或直接访问 raw/SQLite。
- 员工04已确认 Chat 与 Canvas 共同消费员工03的三态投影；本次保持 lifecycle 与 focused-context 状态单向、只读传递。

## 本次完成

- **CAN-M2-003：会话级 FocusFrame 列表接入**
  - `desktop/src/app/kernelClient.ts` 新增 `listFocusFrames(conversationId)` typed client。
  - `App.tsx` 在 Tauri 会话加载时与会话图、ModelRun、画布视口并行请求 FocusFrame 列表；查询失败只提示状态，不阻塞会话图恢复。
  - 对每个查询调用 `projectFocusFrameQuery`，再通过 `upsertFocusFrameQueryByNodeId` 按内核返回的显式 `parentNodeId` 建立节点索引。
  - 切换会话、预览模式和空会话时清空索引，避免跨会话污染。
- **CAN-M2-008：画布集成消费边界**
  - `ChatWorkspace` 将只读 `focusFrameQueryByNodeId` 传给 `ConversationCanvas`。
  - 聚焦阅读器现在可以消费真实生命周期列表结果；没有 parentNodeId 的主线 FocusFrame 不被绑定到任意节点。
  - `focusFrameQueryLoader` 补充显式成功/失败 discriminated union，恢复 TypeScript 门禁并保持逐条错误隔离。

## 交付物与验证

- `desktop/src/app/kernelClient.ts`
- `desktop/src/App.tsx`
- `desktop/src/components/ChatWorkspace.tsx`
- `desktop/src/canvas/canvasM2Projection.ts`
- `desktop/src/canvas/canvasM2Projection.test.ts`
- `desktop/src/app/focusFrameQueryLoader.ts`
- `pnpm test:canvas`：20/20 通过。
- `pnpm test:chat`：38/38 通过。
- `pnpm test:canvas:perf`：1/1 通过；200 节点基线 `nodes=200, edges=199, importedNodes=19`。
- `pnpm build`：通过，Vite 1831 modules。
- `git diff --check`：通过。
- PEC retention：待本报告落盘后复跑；未执行 Tauri Release、真实 FocusedContext 查询或视觉验收，没有生成 Build ID。

## 契约、数据、安全、恢复与跨团队接口

- 仅新增既有 `list_focus_frames` 的前端调用和只读索引，不修改 Rust、SQLite、Provider、凭据、导入原文或 M1 运行终态。
- 列表查询与会话图/运行/视口请求并行，单独失败不会阻断其他恢复；错误只显示安全提示。
- 画布只使用 `lifecycle.frame.parentNodeId` 建立显式映射；不会从坐标、selectedNodeId、标题或分支按钮推断 FocusFrame ID。
- `focusedContext: null` 继续投影为未编译/暂不可用，不显示为“无知识”；真实知识来源仍等待快照持久化和查询。

## 风险、阻塞与下一步（最多两个 WIP）

- **当前阻塞**：FocusFrame 会话列表和画布消费已接通，但 `focusedContext` 仍固定为 null，且从节点创建/切换 FocusFrame 的交互入口与稳定 ID 传递尚未最终确认。
- **WIP1｜员工01/02**：交付 FocusedContext 快照持久化/查询，保持 revision、恢复和 `focusedContext` 三态契约；同时补 current active FocusFrame 的恢复语义。
- **WIP2｜员工04**：与员工03确认从节点创建/切换入口在 ChatWorkspace 与 ConversationCanvas 的职责边界；确认后接入 `createFocusFrame`、close/reopen 和真实 loading/error/recovered 状态。
