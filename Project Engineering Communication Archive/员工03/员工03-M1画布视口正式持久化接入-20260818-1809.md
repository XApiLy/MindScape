# 员工03｜M1画布视口正式持久化接入

> 提交时间：2026-08-18 18:09（UTC+8）  
> 覆盖任务：CAN-014、CAN-020；联调CAN-012  
> 状态：schema v5前端接入与自动验证完成；真实窗口重启体验证据待补  
> 基线提交：`f54363e`（共享工作树，尚未形成员工03独立提交）

## 相比上一份的变化

- 纠正上一份“等待视口接口”的过期阻塞：员工02已交付schema v5及`saveCanvasViewport/getCanvasViewport`，本轮已完成正式消费。
- Tauri打开会话时按conversation读取`x/y/zoom`；读取失败只回退默认视口并提示，不阻断会话图恢复。
- 新增每会话独立的250ms节流与串行写队列：首个变更立即保存、连续交互合并、切会话强制flush、失败后后续保存仍可继续。
- Preview继续使用本次启动内存，不使用localStorage冒充重启恢复。

## 已阅读与落实

- 已完整重读V1范围V1.0 Draft、2026-08-17产品共识、V1总路线1.0、M1派单、内核总览、前五人责任书、Frozen V1契约、RC1威胁模型、Git制度、PEC-GOV-001及员工06视觉交互职责。
- 已读取PEC控制文件和员工01～06当前快照；报告前再次读取员工01 18:01、员工06 17:58最新替换。
- 具体落实：视口只作为可恢复UI投影，经Tauri命令读写，不进入ConversationGraph语义、不影响Node/Edge/Context/Run；本轮不做视觉重构和复杂动画。
- 员工06流程落实：已提交Review Lab `full`版本；工具构建失败被保留为历史，未覆盖或伪报成功。

## 本次完成与证据

- `desktop/src/App.tsx`：会话图、运行投影和正式视口并行加载；Tauri模式创建保存调度器；会话切换/卸载flush；读写错误使用安全提示。
- `desktop/src/components/ChatWorkspace.tsx`：以内存最新值优先、schema v5恢复值兜底，并将每次平移/缩放交给应用层持久化。
- `desktop/src/canvas/canvasViewportPersistence.ts`：实现加载映射、每会话节流、写入串行化、flush和失败隔离。
- `desktop/src/canvas/canvasViewportPersistence.test.ts`：覆盖首写、快速变更合并、多会话隔离、切换flush、写失败后续写及新会话会话读取。
- 联调期间员工01将Node 24不支持的TypeScript构造器参数属性等价改为显式字段；本轮保留该兼容修复，业务语义未改变。

## 契约、数据和安全影响

- 未修改Frozen V1字段、终态语义或SQLite schema v5；只消费员工02已冻结的视口命令。
- 写入仅包含conversation ID、有限坐标和正缩放，不包含会话正文、Key、ContextSnapshot或Provider数据。
- 读取失败不会损坏领域状态；保存失败不会阻断画布继续操作，后续调度仍可重试最新位置。
- 坐标不用于推导父子关系、分支类型或运行状态。

## 验证结果

- `pnpm test:canvas`：13/13通过。
- `pnpm test:canvas:perf`：1/1通过；200节点/199边门槛通过。
- `pnpm test:chat`：14/14通过。
- `pnpm build`：通过，生产构建1824个模块。
- `cargo fmt --all -- --check`：通过。
- `cargo test --offline --no-fail-fast`：45/45通过，包含schema v5按会话视口往返/更新测试。
- `cargo clippy --offline --all-targets --all-features -- -D warnings`：通过。
- `pnpm tauri build --debug --no-bundle`：通过，产物`desktop/src-tauri/target/debug/mindscape-desktop.exe`。
- `git diff --check`：通过；PEC留存检查在本报告写入后执行。

## Review Lab

- 已提交`full`版本`CAN-014 画布视口持久化`，ID：`20260818T100802Z-can-014-画布视口持久化-3fbc`。
- 前端预览构建成功，但完整构建被Review Lab记录为`failed`：工具固定传入`CI=1`，当前Tauri CLI的`--ci`只接受`true/false`，退出码2。
- 同一源码的手工Tauri调试构建已通过；遵守制度保留失败版本，等待员工06修复工具后新建full版本，不覆盖旧记录。

## 风险与阻塞

- 自动测试已证明保存、隔离和加载语义，但尚缺真实窗口中“移动A、移动B、切换、关闭、重新打开”的人工证据，CAN-014仍不能签署最终验收。
- Review Lab full构建入口存在CI环境值兼容问题，暂时无法归档可启动的完整历史副本。
- 真实DeepSeek取消、用量、异常退出和运行恢复矩阵仍未完成，M1 Alpha不可放行。
- 共享工作树包含员工01～06并行修改；当前旧PEC未被Git跟踪，`git mv`无法处理，本轮使用已校验同路径移动归档。

## 下一步 WIP（最多两项）

1. CAN-014：在真实Windows窗口完成双会话平移/缩放、切换、关闭与重启恢复，并在Review Lab修复后重新提交full版本。
2. CAN-012联调：与员工02/04/05验证真实取消、应用中断和重启后仅一个稳定Node/Run，画布与阅读视图一致。

## 需要同事配合

- **员工02**：请共同核对真实窗口双会话隔离及数据库最终视口值，确认切换flush符合schema v5预期。
- **员工04/05**：请继续真实长回答停止和异常退出矩阵，员工03复核画布无重复节点与错边。
- **员工01**：请评审视口失败回退、节流与flush没有越过UI投影边界，并在真实窗口证据后签署CAN-014。
- **员工06**：请将Review Lab构建子进程的`CI=1`修为Tauri接受的布尔值，保留当前失败记录；修复后通知员工03重建full版本并创建正式视觉引用。
