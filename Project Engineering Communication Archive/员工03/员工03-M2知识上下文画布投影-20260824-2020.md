# 员工03｜M2 知识上下文画布投影

时间：2026-08-24 20:20（Asia/Shanghai）  
状态：已完成一刀，等待真实 FocusedContextSnapshot 查询联调

## 已阅读与落实

- 已阅读最新 PEC 控制文件，以及员工01《M2 知识上下文编译与运行档案评审》、员工02《M2 导入事务写入与真实样本校核》、员工04《M2 占位 UI 交接标记》、员工05《M2 推理终态与用量回归矩阵》、员工06当前报告。
- 员工01 已将 `KnowledgeContextSelection` 作为 `FocusedContextSnapshot.knowledgeContext` 的可选加法字段冻结；本次画布只消费 selected/omitted 的结构化结果，不重新检索、排序、门控或计算 Token 预算。
- 员工02 已完成 `persist_import_bundle` 原子事务，但通用导入命令和查询 IPC 尚未交付；员工04的 Intake 继续保持真实 ParseReport 预览禁用，因此本次不接入导入 UI 或直接访问 SQLite/raw storage。
- 员工04已补齐 UI-HANDOFF-06 占位交接注释，员工05已完成 Provider 终态回归矩阵；本次不修改 Chat、Provider、视觉表现层或 M1 运行/取消/恢复路径。

## 本次完成

- **CAN-M2-005：KnowledgeContextSelection → Canvas 投影**
  - 在 `desktop/src/canvas/canvasM2Projection.ts` 新增 `CanvasKnowledgeContextProjection` 与 `projectKnowledgeContext`。
  - 保留契约版本、检索版本、预算估算、selected 引用的状态/作用域/修订/证据来源类型/评分，以及 omitted 引用和排除原因。
  - 对 selected、evidence、omitted 数组建立新数组；画布视图不能反向修改领域快照。
  - `null` 快照保持 `null`，不合成“无知识”或默认检索结果。
- 在 `desktop/src/canvas/canvasM2Projection.test.ts` 增加 selected/omitted、`focusFrame` scope、`importContent` evidence 和引用隔离测试。

## 交付物与验证

- 代码：[desktop/src/canvas/canvasM2Projection.ts](../desktop/src/canvas/canvasM2Projection.ts)、[desktop/src/canvas/canvasM2Projection.test.ts](../desktop/src/canvas/canvasM2Projection.test.ts)。
- `pnpm test:canvas`：18/18 通过。
- `pnpm test:canvas:perf`：1/1 通过；200 节点基线 `nodes=200, edges=199, importedNodes=19`，本次未改变坐标与边投影。
- `pnpm test:chat`：31/31 通过。
- `pnpm build`：TypeScript 与 Vite 构建通过（1830 modules）。
- `git diff --check`：通过；PEC 当前窗口校验通过。
- 未执行 Tauri Release、真实 Provider Key 或浏览器视觉验收；没有生成可供创始人验收的 Build ID。

## 契约、数据、安全、恢复与跨团队接口

- 仅新增前端纯函数，不修改 Rust、SQLite、IPC、Provider、凭据和网络边界。
- `KnowledgeContextSelection` 的选择规则、排除原因、EvidenceRef 和预算值仍由员工01的领域编译器负责；画布不把知识候选当作事实重新判定。
- 证据仅投影为 id、摘录和 target type；不读取原文路径，不执行导入内容，不记录密钥或完整推理。
- 真实接线依赖 `FocusedContextSnapshot` 查询 DTO；在该边界冻结前不修改 ContextDialog，避免与员工04的上下文账单职责重叠。
- 上一份员工03 PEC 已移入 `Project Engineering Communication Archive/员工03/`；因共享工作树中的 PEC 未被 Git 跟踪，`git mv` 不可用，本次使用等价文件移动并保留历史记录。

## 风险、阻塞与下一步（最多两个 WIP）

- 当前阻塞：FocusedContextSnapshot 的生命周期查询/恢复 IPC 尚未交付，画布适配层暂未接入真实数据源。
- **WIP1｜员工01/02**：冻结并提供包含可选 `knowledgeContext` 的结构化查询 DTO；同时保证候选 revision、删除/否决失效和 EvidenceRef 与编译器契约一致。
- **WIP2｜员工04**：在真实查询 IPC 到位后确认 ContextDialog/工作区的上下文账单消费边界；员工03再接入画布来源/知识引用详情，不复制业务规则。
