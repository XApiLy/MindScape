# 员工03｜B5 正式候选选择入口前端闭环

> 提交时间：2026-09-01 15:51（UTC+8）  
> 覆盖阻断：`B5-UI` 正式候选选择入口  
> 状态：**员工03前端责任项与 generation 跨模块接线已关闭；剩真实 Tauri 同样本与唯一 clean Release 验收，B5整体仍为 No-Go**

## 本次关闭项

- 关闭 `B5-UI`：Active FocusFrame 已接入正式多选区，展示内核实体库存、空态、选择计数、pending、失败、同一 receipt 重试和成功版本回执；Closed 状态继续复用既有四动作区。
- 前端提交 `mindscape.focus-promotion-generation.v1` typed input，稳定排序 `candidateRefs`，携带 expected memory/lifecycle version；失败重试复用同一 `generationId`，不在 React 自增版本。
- `App → kernelClient → generate_focus_promotion_candidates` 接线完成；成功后重新读取权威 FocusFrame query，刷新失败时明确提示“已保存、不要重复提交”。
- 保留 `UI-HANDOFF-06`，冻结数据来源、状态、键盘焦点、幂等重试和员工06可替换边界。

## 跨模块证据

- 领域：消费员工01冻结的 generation/replay 契约与 TypeScript 镜像；员工01直接回归 `11/11`。
- React/IPC：`ConversationCanvas → ChatWorkspace → App → kernelClient` typed 链路已编译；选择辅助直接测试 `2/2`。
- SQLite/Tauri：已消费员工02落盘的 schema v17 receipt、`IMMEDIATE` 事务、KernelService、Tauri command 与 `lib.rs` 注册；generation 领域+SQLite `15/15`，覆盖重启恢复、同 ID 精确重放、复用 ID 冲突和 stale 整体回滚。
- Rust 全量：library `223 passed / 0 failed / 1 ignored`；Clippy all targets/features/locked `-D warnings` 与 rustfmt check 通过。
- 前端门禁：Chat `62/62`、Canvas `34/34`、Canvas 性能 `2/2`、TypeScript/Vite build `2098 modules`、`git diff --check` 均通过。
- 视觉证据：Review Lab 员工主线 `preview` 已 ready，ID `20260901T074440Z-b5-正式回流候选选择入口-9ac0`；920×720 浏览器复核无布局/交互异常，控制台仅 Review Lab `favicon.ico` 404。静态 preview 不伪造 SQLite/Active FocusFrame 数据。

## 剩余阻断与协作

1. **员工02**：接线和直接测试已落盘，请发布新的个人 PEC，明确 schema v17、原子/幂等/重启证据与剩余阻断，供员工01正式复评。
2. **员工01**：复评已落盘的同 generationId replay、冲突和 stale rollback；确认普通实体库存是否需要新增“可选候选清单”只读查询，避免 UI 暴露最终会被内核拒绝的实体。
3. **员工05**：仅在新 clean commit/唯一 Acceptance Release 中执行真实“生成 → 响应丢失重试 → Close → 四动作 → Reopen → 重启”样本；本 preview 不构成 Release 通过。

## 下一步 WIP（最多两个）

1. 等员工01复评后完成真实 Tauri Active/Closed 状态验收。
2. 根据员工01对候选只读查询的复评收紧库存展示，再交员工05发布唯一 clean Release。
