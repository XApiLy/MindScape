# 员工03｜M2 FocusFrame 显式节点索引与查询门禁

时间：2026-08-25 15:36（Asia/Shanghai）  
状态：已完成一刀；等待当前 FocusFrame 发现与创建入口确认

## 已阅读与落实

- 已阅读最新 PEC 控制文件、M2 派单、员工01《M2 FocusFrame 生命周期闭环回归》、员工02《M2 通用导入解析器首切片》、员工04《M2 FocusFrame 查询消费层修正》、员工05《M2 Provider 与索引脱敏边界》、员工06当前报告，以及 M2 契约基线、UI-HANDOFF-06 规范和本地实体/RAG方案。
- 员工01/02已完成 FocusFrame 生命周期创建、查询、关闭、恢复及统一校验；但 `focusedContext` 仍未持久化。员工04确认当前没有稳定的 graph/node → FocusFrame ID 映射，本次不猜测当前 FocusFrame，也不自动创建。
- 员工02已完成通用 Markdown/TXT/JSONL 纯解析器，但文件命令和来源/ParseReport 查询仍待下一刀；本次不提前接入导入 UI。
- 按员工04的协作边界，画布继续提供唯一的 FocusFrame 查询状态/三态投影入口，Chat 只消费该结构化结果，不复制状态规则。

## 本次完成

- **CAN-M2-003 / CAN-M2-008：显式 parentNodeId 画布索引适配器**
  - 在 `desktop/src/canvas/canvasM2Projection.ts` 新增 `upsertFocusFrameQueryByNodeId` 和 `removeFocusFrameQueryByNodeId`。
  - 仅使用内核返回的 `lifecycle.frame.parentNodeId` 建立查询结果到节点的索引；`parentNodeId: null` 的主线 FocusFrame 不绑定到任意节点。
  - 使用新 Map 返回值，不修改调用方状态，便于 React 状态更新和后续恢复/关闭投影。
- 在 `desktop/src/canvas/canvasM2Projection.test.ts` 增加：
  - 显式 parentNodeId 建立索引；
  - 关闭/移除不污染其他节点；
  - 主线无 parentNodeId 时不产生伪造节点映射。
- 修正共享 FocusFrame 查询加载器的异步结果判别类型，使成功/失败结果成为显式 discriminated union，解除 TypeScript 构建门禁；不改变查询命令或错误语义。

## 交付物与验证

- `desktop/src/canvas/canvasM2Projection.ts`
- `desktop/src/canvas/canvasM2Projection.test.ts`
- `desktop/src/app/focusFrameQueryLoader.ts`
- `pnpm test:canvas`：20/20 通过。
- `pnpm test:chat`：38/38 通过。
- `pnpm test:canvas:perf`：1/1 通过；200 节点基线保持通过。
- `pnpm build`：通过，Vite 1830 modules。
- `git diff --check`：通过。
- 未执行 Tauri Release、真实 FocusedContext 查询或视觉验收；没有生成 Build ID。

## 契约、数据、安全、恢复与跨团队接口

- 仅新增前端纯投影/索引函数，不修改 Rust、SQLite、Tauri 命令、Provider、凭据、导入原文或 M1 运行终态。
- 画布不从坐标、selectedNodeId 或 React 临时状态推断 FocusFrame ID；只接受调用方已查询的结构化 projection。
- 不读取 SQLite/raw storage/Markdown/向量索引，不执行导入文本，不改变 `focusedContext: null` 的“未编译/暂不可用”语义。
- `focusFrameQueryLoader` 只加载调用方提供的 ID，去重、过滤空 ID，并隔离单个查询失败；不会发现或猜测 ID。

## 风险、阻塞与下一步（最多两个 WIP）

- **当前阻塞**：没有会话级 active FocusFrame 发现/列表查询，graph/node 也没有稳定 FocusFrame ID 来源；`focusedContext` 仍固定为 null，真实知识引用无法联调。
- **WIP1｜员工01/02/04**：共同确认 `list_focus_frames` / `get_active_focus_frame` 或等价稳定边界，并确定从节点创建、切换和重启恢复的 ID 传递路径。
- **WIP2｜员工02**：完成 FocusedContext 快照持久化/查询及导入文件命令、ParseReport/source 查询后，员工03接入真实 map 和画布来源详情；继续保持前端不直接访问本地存储。
