# 员工03｜B5 实体生产入口阻断确认

> 提交时间：2026-09-01 22:29（UTC+8）  
> 覆盖阻断：`B5` 全新数据库无法产生首条 KnowledgeEntity  
> 状态：**关闭 B5 实体入口归因审计；B5 本体仍为 No-Go，等待员工01/02冻结并实现生产契约**

## 本次关闭的 B 编号

### B5：关闭“候选选择 UI 空态属于员工03展示故障”的误判

- 已读取员工05《B5候选生成复验与实体入口阻断》、员工02数据接入和员工01重放契约，并复核正式调用图。
- `ConversationCanvas → generate_focus_promotion_candidates → SQLite v17` 已接通；新库为空不是选择器或四动作 UI 故障。
- 生产前端没有 `upsertKnowledgeEntity` 调用方；现有 ImportIntake 只导入原文/Bundle，不创建 candidate/inferred KnowledgeEntity。
- 内核虽有 `transition_knowledge` 纯领域规则和底层 `upsert_knowledge_entity`，但没有“真实 ImportContent → 候选建议 → 用户确认/保存”的 typed product command。React 若直接拼 KnowledgeEntity，会复制 scope/status/revision/generator/EvidenceRef 规则并绕过领域入口，因此本轮未增加直写表单或测试后门。

## 跨模块证据

- UI：正式选择器、同 generationId 重放和 Closed 四动作已存在，但在 `knowledgeEntities=[]` 时诚实显示空态。
- IPC：`kernelClient.upsertKnowledgeEntity` 只有定义，无 `App.tsx` 或正式组件调用；ImportIntakeDialog 无实体创建/确认命令。
- Domain/Data：candidate/inferred 必须是同 FocusFrame scope、带可解析 EvidenceRef；员工02 schema v17 generation 事务只消费已有权威实体，不负责生产第一条实体。
- 因此链路断点固定为：`ImportSource/Revision/Message → [缺失的实体建议/确认 command] → KnowledgeEntity → 已完成的选择/生成/四动作`。

## 需要协作与剩余阻断

1. **员工01**：冻结实体生产契约，明确建议生成者（用户/规则/模型）、candidate 与 inferred 的初始语义、ImportContent EvidenceRef 绑定、用户确认/否决、expected revision、幂等键及 FocusFrame scope；不得复用裸 `upsert_knowledge_entity` 作为产品命令。
2. **员工02**：按冻结契约实现原子持久化与 typed Tauri command，事务内解析 source/revision/locator 并生成权威 ID/revision/generator；失败不得留下实体、EvidenceRef、FTS/vector 或 Vault 部分写入。
3. **员工03/04**：命令落地后在导入/知识库存 UI 消费 proposal、loading/empty/error/confirm/reject/retry 状态，再把生成实体送入现有 FocusFrame 选择器。
4. **员工05**：仅在新 clean Release 从全新数据库执行真实导入到实体创建的用户动作；不得手工写 SQLite/Vault 或用 fixture 关闭 B5。

## 下一步 WIP（最多两个）

1. 消费员工01冻结的实体建议/确认 typed contract，完成员工03正式 UI。
2. 联调员工02持久化后发布新 Review Lab preview，并交员工05重跑全新数据库 B5。
