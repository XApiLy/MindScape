# 员工03｜M2 分支记忆作用域审计

时间：2026-08-29 02:16（Asia/Shanghai）  
覆盖任务：CAN-M2-003 / CAN-M2-005 / CAN-M2-008 / ARC-M2-002 联调  
状态：分支 scope 与 omitted 前端审计完成；正式回流候选查询和统一干净 Release 待完成

## 已阅读与落实

- 已阅读 PEC 控制文件及员工01《分支回流候选契约与 Markdown 事务评审》、员工02/04/05/06当前报告，复核 M2 派单、M2 契约基线和 Markdown Vault/分支日志架构。
- 员工01新增 `FocusPromotionCandidateSet` 仅存在 Rust 领域层，当前无 Tauri command、TypeScript 类型或查询投影；因此 UI 只展示 `promoteRefs` 声明并明确“等待 typed 候选投影”，不伪造确认/合并能力。
- 按 React 性能规范把审计视图做成独立模块级组件和纯投影；完整引用仅在用户打开单节点聚焦阅读器时渲染，不进入 200 节点画布热路径。

## 相比上一份 PEC 的变化

- 上一版完成 Vault 投影审计与显式编辑回流；本版继续补齐 FocusFrame 分支类型、memory version、四组记忆声明和冻结上下文实际 selected/omitted 的可读审计。
- 新增正式回流候选接口阻塞提示；未把 `promoteRefs` 改名为已生成候选，也未增加确认按钮。
- 员工01发现 Markdown 编辑的 Entity/FTS/Vector 与 Projection 仍可能跨事务半提交；本轮前端不绕过该缺陷，现有 dirty Release 不作为放行证据。

## 本次完成

- 新增 `branchMemoryAudit` 纯投影，逐字段复制 branch kind、context policy、memory version、inherit/local/exclude/promote 和冻结 selected/omitted，不执行继承、去重、排序或回流规则。
- 聚焦阅读器新增“分支记忆作用域”区：四组声明各自保留空态和数量，冻结快照实际选中/排除与原因独立展示。
- `FocusedContext` 未编译时明确显示“不能从声明推断实际继承结果”；主线、探索、任务、复盘使用领域 branch kind 显示。
- `promoteRefs` 非空时显示契约提示：正式 `FocusPromotionCandidateSet` 尚未通过 typed IPC 提供，当前不可确认或合并。
- 补充 `UI-HANDOFF-06`、键盘可聚焦的原生 details/summary 和窄窗单列布局。

## 验证证据

- Canvas：34/34；新增声明保真、冻结 selected/omitted 分离、无 promoteRefs 不伪造候选三项回归。
- Chat：50/50；Canvas 性能：2/2。
- TypeScript/Vite production build：2094 modules，通过。
- `git diff --check`：通过。

## 契约、安全、恢复与发布边界

- 本轮只消费现有 `get_focus_frame_query` typed 投影，不修改 FocusFrame、FocusedContext、KnowledgeEntity、SQLite、Vault、FTS 或 VectorIndex。
- UI 不让坐标决定分支，不从声明集合重算冻结上下文，不自动把分支结果写回主线。
- `FocusPromotionCandidateSet` 缺少查询 IPC 是明确接口阻塞；员工01/02提供前，员工03不会自行从 `promoteRefs` 合成该领域对象。
- Build `20260829-014319-m2-v13-stream-flush-5dcc02b44e9e-dirty` 早于本轮 UI，且 Markdown 编辑事务未原子化；本轮未生成新 Release，M2 仍为 No-Go。

## 需要配合 / 未关闭项

1. 员工01/02提供只读 `FocusPromotionCandidateSet` typed 查询/IPC，并保持来源 FocusFrame、会话、branch kind 与 memory version；员工03只负责展示。
2. 员工02先修复 Markdown 编辑原子事务并补回滚/重启测试，员工05再发布包含员工03/04最新 UI 的干净统一候选版。

## 下一步 WIP（最多两个）

1. typed 回流候选查询可用后接入聚焦阅读器，只展示“待确认”及来源，不在前端执行状态转换。
2. 在统一干净 Release 中验证任务分支 scope、实际 omitted、Vault 新 revision 与重启恢复使用同一 Build ID。
