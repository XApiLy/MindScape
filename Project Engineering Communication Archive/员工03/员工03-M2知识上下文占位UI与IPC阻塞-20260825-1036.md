# 员工03｜M2 知识上下文占位 UI 与 IPC 阻塞

时间：2026-08-25 10:36（Asia/Shanghai）  
状态：已完成一刀；FocusedContextSnapshot 查询/恢复 IPC 仍未交付

## 已阅读与落实

- 已阅读最新 PEC 控制文件、员工01《M2 导入 Bundle 语义校验与上下文边界》、员工02《M2 导入预检与父子事务落库》、员工04《M2 占位 UI 交接标记》、员工05《M2 DeepSeek 流式事件夹具回归》、员工06当前报告，以及 M2 派单、UI 工程先行占位规范和本地实体/RAG方案。
- 员工01最新报告明确：`FocusedContextSnapshot` 生命周期查询/恢复 IPC 尚未冻结；员工02最新报告明确：父先子事务落库已完成，但通用导入命令与 ParseReport/source 查询 IPC 仍未交付。基于此，本次没有宣称真实知识数据或导入预览已接通。
- 按 UI-HANDOFF-06 规则，新增区域只验证状态、语义、键盘和可替换边界；不建立新视觉语言，不改领域契约，不让 UI 重算检索、排序或预算。

## 本次完成

- **CAN-M2-005 / CAN-M2-008：画布聚焦阅读器知识上下文占位区**
  - `ConversationCanvas` 新增可选 `knowledgeContextByNodeId` 输入，未来可直接消费结构化 `CanvasKnowledgeContextProjection`；当前未提供该输入时明确显示“待内核查询接入”。
  - 聚焦阅读器区分三种状态：
    - `undefined`：FocusedContextSnapshot 查询/恢复 IPC 尚未接入；
    - `null`：已取得快照且本节点没有知识引用；
    - 有值：展示 selected、revision、scope、证据 target type、Token 估算、检索版本和 omitted 排除原因。
  - selected/omitted 仅只读展示，不从画布重算知识状态、检索排序、排除规则或 Token 预算。
  - 在 TSX 与 CSS 入口补齐 `UI-HANDOFF-06` 交接说明，员工06可替换表现层但不得改变状态区别、来源语义和安全边界。

## 交付物与验证

- `desktop/src/components/ConversationCanvas.tsx`
- `desktop/src/components/conversationCanvas.css`
- 复用 `desktop/src/canvas/canvasM2Projection.ts` 中的结构化知识投影，不新增领域模型。
- `pnpm test:canvas`：18/18 通过。
- `pnpm test:canvas:perf`：1/1 通过；200 节点基线 `nodes=200, edges=199, importedNodes=19`，新增内容仅在聚焦阅读器打开时渲染，不改变画布坐标投影。
- `pnpm test:chat`：31/31 通过。
- `pnpm build`：通过，Vite 1830 modules。
- `git diff --check`：通过。
- 未执行 Tauri Release、真实 Provider Key 或浏览器视觉验收；没有生成 Build ID。

## 契约、数据、安全、恢复与跨团队接口

- 仅新增可选前端视图输入和占位展示，不修改 Rust、SQLite、IPC 命令、Provider、凭据、运行终态或 M1 恢复语义。
- 不直接读取 SQLite/raw storage/原文路径；不执行导入内容；不记录密钥或完整 reasoning。
- 当前真实数据接线仍阻塞于员工01/02的结构化查询 DTO；未提供时 UI 明确显示待接入，不伪造“无知识”成功结果。
- 上一份员工03 PEC 已移入 `Project Engineering Communication Archive/员工03/`；由于共享工作树中的 PEC 未被 Git 跟踪，`git mv` 无法执行，本次使用等价文件移动并保留历史记录。

## 风险、阻塞与下一步（最多两个 WIP）

- **明确阻塞**：员工01尚未冻结 FocusedContextSnapshot 生命周期查询/恢复 IPC；员工02虽已完成预检与父先子落库，但尚未交付通用导入命令与 ParseReport/source 查询 IPC，因此当前只能完成占位 UI，不能进行真实来源/知识联调。
- **WIP1｜员工01/02**：提供结构化查询 DTO 与命令名称，至少包含 snapshot 状态、`knowledgeContext.selected/omitted`、ImportSource/ParseReport 的加载/空白/错误/恢复语义。
- **WIP2｜员工04**：确认 ContextDialog 与画布阅读器的职责分界；IPC 到位后由员工03接入 `knowledgeContextByNodeId`，并补真实查询回归测试。
