# 员工03｜M2 阻塞与跨员工协作清单

时间：2026-08-29 13:41（Asia/Shanghai）  
覆盖任务：CAN-M2-003 / CAN-M2-005 / CAN-M2-008 / ARC-M2-002 / ARC-M2-004 联调  
状态：画布 scope、omitted、Vault 修订审计已完成；等待回流候选 typed 查询与统一 Release 纵向验收

## 已阅读与落实

- 已读取 PEC 控制文件及员工01～06当前报告，重点复核员工01回流候选契约、员工02 Markdown 原子事务与新版 Release、员工04导入双预览、员工05检索/长会话性能优化。
- 状态校正：上一份 PEC 所述 Markdown 跨表非原子问题，员工02已用 `persist_markdown_entity_revision` 单事务修复并补冲突回滚；不再列为“待开发阻塞”，改列为“待员工01复核及真实纵向验收”。
- 员工02已发布 `20260829-021706-m2-v13-markdown-atomic-5dcc02b44e9e-dirty`，可做同 Build 预验收；其 source tree 仍为 dirty，且早于员工05后续性能优化，不能作为最终 clean 候选或 M2 Go 证据。

## 员工03当前完成面

- 画布节点保持有界 Markdown 摘要；聚焦阅读使用共享安全 renderer，不把渲染结果写回事实源。
- 已消费 FocusFrame 生命周期、分支路径、branch kind、memory version、inherit/local/exclude/promote 声明及冻结 selected/omitted。
- 已消费 Vault MarkdownProjection 历史，显示相对路径、entity/projection revision、content hash；支持显式 `import_markdown_entity_edit` 回流，不直接读取或执行文件。
- 回流后清空前端旧检索投影；`promoteRefs` 仅显示为声明，不冒充正式回流候选，不提供自动合并。
- 当前工程门禁：Canvas 34/34、Chat 50/50、Canvas 性能 2/2、production build 2094 modules。

## 当前阻塞与协调责任

| 阻塞 | 当前事实 | 需要协调的同事 | 需要交付 | 解除条件 |
| --- | --- | --- | --- | --- |
| 正式回流候选无法接入 UI | `FocusPromotionCandidateSet` 仅在 Rust 领域层；没有 Tauri command、TypeScript 类型或只读查询投影 | 员工01、员工02；员工04同步消费边界 | 员工01冻结查询投影、主线/空集合/过期 memory version 语义；员工02提供 typed IPC 与持久/恢复查询；员工04确认 Chat 只展示“待确认” | 前端可按稳定 FocusFrame ID 查询集合，返回 source frame、conversation、branch kind、memory version、refs，并具备 loading/empty/error/restart 测试 |
| Markdown 原子事务尚未完成跨角色复核 | 员工02已完成单事务和冲突回滚测试，但员工01当前报告尚未给出修复复核结论 | 员工01、员工02 | 员工01复核事务边界；员工02提供冲突回滚及关闭重启后的 Entity/FTS/Vector/Projection 一致性证据 | 员工01书面确认领域/事务边界通过，同一真实样本重启前后 revision 与检索结果一致 |
| 最终统一验收程序尚未冻结 | 当前原子事务 Build 为 dirty，且早于员工05后续 top-K / 长会话优化；员工04最新导入双预览也需同 Build 实测 | 员工05牵头，员工02/03/04配合，员工01最终复核 | 员工05在代码冻结后用批准脚本发布唯一候选；员工02准备 Vault/删除/重启样本；员工03验证 scope/omitted/Vault UI；员工04验证导入双预览/长会话锚点；员工01汇总 Go/No-Go | 同一 Build ID、完整路径、SHA-256、可追溯 source state；无开发服务器完成纵向样本及 M1 回归 |
| 真实数据量与长会话性能证据不足 | 单元/合成性能通过，但尚无 1k+ 持久知识记录、长 Markdown 会话、峰值内存和可见跳动现场证据 | 员工05、员工03、员工04 | 员工05采集检索延迟/内存/帧稳定性；员工03复核聚焦阅读和 200 节点；员工04复核滚动锁定、定位最新、复制语义 | 统一候选中记录样本规模、P95/峰值、窗口尺寸和操作证据，无内容丢失或可见跳动 |

## 安全、契约与发布边界

- 员工03不会从 `promoteRefs` 自行合成 `FocusPromotionCandidateSet`，不会复制知识状态转换，也不会把分支结果自动写回主线。
- UI 不接受 Vault 任意路径，不读取 SQLite/VectorIndex，不显示 Key、Authorization、Provider 原始响应、完整 reasoning 或向量正文。
- 当前 Build `20260829-021706-m2-v13-markdown-atomic-5dcc02b44e9e-dirty` 仅用于纵向预验收：程序路径 `D:\Project\MindScape\artifacts\acceptance\versions\20260829-021706-m2-v13-markdown-atomic-5dcc02b44e9e-dirty\mindscape-desktop.exe`，SHA-256 `834CE0193C4746723D9E75D2E1F3E42E4015F863B74A23B200F874FE4DB77BA2`。
- 本轮只更新协作状态，没有新增代码或新 Release；M2 仍为 No-Go。

## 下一步 WIP（最多两个）

1. 等员工01/02提供 `FocusPromotionCandidateSet` typed 查询后，员工03接入聚焦阅读器只读“待确认”列表及来源/版本状态。
2. 与员工02/04/05在下一统一候选完成 branch scope、omitted、Vault 编辑新 revision、检索、重启与长会话可见性验证，由员工01复核结论。
