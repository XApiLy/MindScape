# 员工03｜M2 画布 Vault 修订审计与编辑回流

时间：2026-08-28 17:42（Asia/Shanghai）  
覆盖任务：CAN-M2-005 / CAN-M2-008 / ARC-M2-004 联调  
状态：前端消费闭环完成；等待统一 Tauri Release 真实样本

## 已阅读与落实

- 已阅读 PEC 控制文件、M2 派单、视觉中心《05 Markdown 原生阅读与字体偏好规格》及员工01/02/04/05/06当前报告；继续落实“节点轻摘要、聚焦阅读消费 typed 投影、UI 非真相源”。
- 开发中复读员工02最新《Markdown 编辑回流与稳定修订》；新增接口与本轮无冲突，已接入 `import_markdown_entity_edit`，且未扩张为编辑器或任意路径读取。
- 使用 React 性能规范约束加载：实体清单返回后立即并行请求各实体投影，单实体失败隔离，不形成逐实体串行瀑布。

## 本次完成

- App 接入 `list_markdown_projections`，按实体缓存完整修订历史；会话切换清空缓存，知识刷新时同步刷新投影。
- 聚焦阅读器在冻结知识引用与统一检索候选中展示最新 Vault 相对路径、projection/entity revision、完整 content hash 和历史数量。
- 完整区分加载、无投影、未纳入查询、单实体失败与成功；画布状态条汇总投影数量、失败数量并提供重试。
- 接入“同步 Vault 编辑”显式按钮：只调用 typed `import_markdown_entity_edit`，不接受路径输入、不直接读文件；`changed=false` 不制造空修订，`changed=true` 合入新修订、刷新实体清单并清除旧检索投影。
- 新增并发、ID 去重、空清单、单实体失败隔离和修订去重回归；补充 `UI-HANDOFF-06` 数据、状态与安全边界。

## 验证证据

- Canvas：31/31；Canvas 性能：2/2；Chat：48/48。
- TypeScript/Vite production build：2092 modules，通过。
- `git diff --check`：通过。

## 契约、安全、恢复与协作边界

- 不重排、不解释后端投影历史；最新项沿用 IPC 返回顺序，UI 不从文件名、坐标或内容推断领域事实。
- 不打开、解析或执行 Vault 文件；回流路径、稳定 ID、frontmatter、大小与 revision 安全继续由员工01/02领域和数据层校验。
- 回流后清空前端旧检索投影，避免继续展示过期候选；SQLite/FTS/Vector 原子更新仍以员工02/05实现为权威。
- 未生成统一 Release，未声称 M2 Go。员工05现有 dirty Build 早于 v13 编辑回流及本轮 UI，不能作为本功能验收证据。

## 需要配合 / 未关闭项

1. 员工05发布包含 v13 与本轮前端的统一无开发服务器 Tauri Release，并提供 Build ID、路径和 SHA-256。
2. 员工01/02/04/05与员工03在同一 Build 验证投影→外部编辑→显式回流→新 revision→检索命中→重启一致性，以及 rejected/delete/omitted/分支 scope。

## 下一步 WIP（最多两个）

1. 在统一 Release 中完成真实 Vault 编辑回流与聚焦阅读证据，记录同一 Build ID。
2. 复核分支知识引用、omitted 与回流候选在画布中的作用域显示，不在前端复制继承规则。
