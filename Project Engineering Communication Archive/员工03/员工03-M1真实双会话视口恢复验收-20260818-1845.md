# 员工03｜M1真实双会话视口恢复验收

> 提交时间：2026-08-18 18:45（UTC+8）  
> 覆盖任务：CAN-014；联调 CAN-012  
> 状态：CAN-014 真实窗口验收通过；CAN-012 仅成功/正常重启状态通过，取消、失败与强退恢复待联合证据  
> 基线：共享工作树集成冻结中，未创建个人提交

## 相比上一份的变化

- 在真实 Windows Tauri 窗口完成会话 A/B 独立平移缩放、切换、正常关闭和重新启动。
- A 从 86% 调整并恢复为 106%，保持 2 个节点、1 条边；B 从 86% 调整并恢复为 66%，保持 1 个节点、0 条边。
- 只读核对 SQLite 中两会话的 `x/y/zoom`、节点数和边数，结果与重启后窗口一致。
- 未新增画布功能、复杂动效或领域契约；只新增脱敏验收截图和记录。

## 已阅读与落实

- 推进前重读 PEC 当前控制文件、M1 PEC 审阅与收口派单、PEC 提交归档规则及员工01～06当前报告；报告生成后又补读员工02于 18:38 更新的真实 SQLite 基线报告。
- 落实控制要求：不进入 M2、不扩展画布能力，先核对集成冻结与禁止提交产物；`desktop/MindScape-DeepSeek-Fixed.exe` 和 `desktop/src-tauri/target-fixed/` 已被忽略规则命中。
- 落实 UI 非真相原则：窗口截图与 SQLite 只读事实交叉核对，未用 Preview/localStorage 冒充应用重启恢复。
- 当前无契约冲突；唯一未闭环项是员工02/04/05尚未交付取消、失败和强退恢复的同一批真实运行证据。

## 本次证据

- 完整步骤、数据库事实与边界：`docs/engineering/evidence/employee03/can-014-20260818/README.md`。
- 截图 01～07：覆盖初始 A、A 调整、创建 B、B 调整、切回 A、重启恢复 B、重启切回 A。
- 成功状态检查：A 的 2 个完成节点、1 条父子边在切换和重启后保持稳定；B 只有本次验收生成的 1 个完成节点，无重复节点。

## 验证结果

- `pnpm test:canvas`：13/13 通过。
- `pnpm test:canvas:perf`：1/1 通过；200 节点基准通过。
- `git diff --check`：通过。
- `node scripts/check-pec-retention.mjs`：将在本报告替换后再次执行。
- 真实 Windows：双会话视口隔离、切换、正常关闭与重启恢复通过。
- 启动观察：重启进程先出现短暂无窗口阶段，随后正常创建主窗口；本次未形成崩溃或永久挂起，不冒充强退恢复证据。

## 契约、数据与安全

- 未修改 Frozen V1、SQLite schema、Tauri 命令或前端实现。
- 新增 B 会话的一次本地 Mock 完成运行只用于稳定节点验证；未使用真实 Provider、未消耗额度。
- 数据库只读查询未读取消息正文、ContextSnapshot、Provider 请求或凭据；所有截图均未打开设置页或展示 Key。
- 构建产物忽略规则已生效，但共享混合工作树仍需由集成收口负责人建立可构建、可回滚基线。

## 剩余 No-Go

- 缺少真实取消、失败、异常断流与强制退出恢复后“唯一稳定节点、正确边、位置不跳”的联合窗口证据。
- 当前分支仍为 `docs/REL-002-pec-retention`，跨员工改动尚未形成可审查的 M1 集成恢复 PR。
- Review Lab `CI=1` 兼容修复后的 full 版本尚未取得，不影响本次直接 Windows 证据，但仍影响历史可启动副本归档。

## 下一步 WIP（最多两项）

1. CAN-012：收到员工02/04/05的取消、失败和强退恢复用例后，在同一窗口证据中复核节点唯一、边正确、位置不跳。
2. 配合集成恢复审查 Canvas 分组，确保 CAN-014 代码、测试和本次证据进入可构建、可回滚基线；不新增功能。

## 需要同事配合

- **员工01**：请依据本次真实证据签署 CAN-014，并将其加入统一 M1 验收表。
- **员工02**：其报告采集时 `canvas_viewports=0`；本次操作后已产生 A/B 两条最终值，请按其派单复查唯一性，并继续提供强退/重启恢复的脱敏 SQLite 事实。
- **员工04/05**：请提供真实取消和失败矩阵窗口，员工03同步检查节点、边和视口稳定性。
- **员工06**：Review Lab 修复后通知员工03重建 full 版本；当前不派发视觉重构。
