# 员工03｜M2 画布 Markdown 摘要与聚焦阅读

时间：2026-08-28 12:33（Asia/Shanghai）  
覆盖任务：CAN-M2-005 / CAN-M2-008  
状态：本切片完成；等待 Vault 投影消费与统一 Tauri Release

## 已阅读与落实

- 已阅读 PEC 当前窗口、M2 派单及视觉中心《05 Markdown 原生阅读与字体偏好规格》；落实“节点只显示轻量摘要、聚焦阅读保留完整 Markdown、raw/renderer/字体偏好分层”。
- 已读取员工01《Markdown 修订契约与迁移复核》和员工02《Markdown Vault 投影与修订落库》；画布不读取 Vault 文件、不解释 revision，只等待 typed IPC 投影。
- 无契约冲突：本轮只增加前端派生字段，不改变 ModelRun、FocusFrame、KnowledgeEntity 或 MarkdownProjection。

## 本次完成

- `CanvasNodeProjection` 增加 `questionPreview` / `answerPreview`；raw `question` / `answer` 保持不变，继续供复制、恢复和聚焦阅读使用。
- 新增有界 `markdownToCanvasPreview()`：最多读取 8192 字符、输出 280 个 Unicode 字符；轻量处理标题、引用、列表、任务项、链接、代码围栏和 HTML 标记，不构建 Markdown AST。
- 节点卡片及瞬态流式节点只消费 preview；聚焦阅读器接入共享 `SafeMarkdown`，导入原文与回答使用安全 DOM renderer。
- “复制全文”改为诚实的“复制 Markdown”；保留独立代码块复制，不把渲染 DOM 写回原文。
- 增加 raw 保真、危险 HTML 降级、链接去 URL、Unicode 截断、长流式文本和 200 Markdown 节点性能回归。

## 验证证据

- Canvas：26/26；Canvas 性能：2/2。
- 200 节点基线：`initialProjectionMs=4.379`，`streaming50ProjectionMs=139.140`；200 个长 Markdown 节点摘要投影 `44.223ms`。
- Chat：42/42；Vite production build：2088 modules，通过。
- Playwright：画布将 `# 验收计划` 显示为“验收计划”，聚焦阅读器保留原始标题并显示“复制 Markdown”；控制台 0 errors / 0 warnings。
- 截图：`output/playwright/canvas-markdown-reader-20260828.png`。

## 安全、性能与协作边界

- 原始 Markdown 不可变；preview 和 renderer DOM 都是可重建派生层。HTML/链接不会在画布摘要执行，完整安全 URL/HTML 边界由共享 renderer 负责。
- 画布热路径不引入 `react-markdown` AST；完整解析只发生在用户打开聚焦阅读或阅读视图时。
- 本轮未生成正式 Tauri Release，不能替代同一 Build ID 下的 Vault、检索、失效与重启验收。

## 下一步 WIP（最多两个）

1. 员工03：消费员工02的 `list_markdown_projections` typed IPC，在聚焦阅读器只读显示实体对应 Vault 路径、revision 与 content hash；不直接读文件。
2. 员工01/02/03/04/05：在同一无开发服务器 Tauri Release 中验证导入 → 实体 → Vault → 检索 → 分支隔离 → 失效/重启，并共用 Build ID、路径和 SHA-256。
