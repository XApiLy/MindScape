# 员工03｜M2 统一检索消费与分支路径导航

时间：2026-08-27 01:03（Asia/Shanghai）  
状态：CAN-M2-003/005/008 重切片完成；等待知识检索 IPC 与统一 Tauri Release

## 已阅读与落实

- 已阅读最新 PEC 控制文件、M2 派单、员工01《M2 统一检索投影契约》、员工02《M2 知识持久化校验与检索联调准备》、员工04《M2 知识查询失败恢复入口》、员工05《RAG-M2 统一检索投影适配》和员工06当前报告。
- `mindscape.knowledge-retrieval.v1` 已冻结且员工05已完成 Provider/Index 到统一投影的安全适配；本轮员工03只建立画布消费适配器，不新增查询 IPC、不重排候选、不重算 scope/预算/失效。
- CAN-M2-003 仍不能伪造领域级 FocusFrame 切换，因此本轮实现会话图显式 parentNodeId 路径导航：可回到任一祖先并同步定位，但不把坐标或 UI 选择解释为知识关系或主线 FocusFrame。

## 本次推进

- **CAN-M2-005 / CAN-M2-008｜统一检索画布消费适配**
  - `canvasM2Projection.ts` 新增 `projectKnowledgeRetrieval` 和 `CanvasKnowledgeRetrievalProjection`。
  - 原样保留候选顺序、整数分数、Token、Vector/FullText/Relation 多来源、EvidenceRef、embedding provenance、omitted 和 fallback notice。
  - 所有数组和嵌套对象复制为画布只读形状，避免消费层修改领域投影；未知/降级信息不被提升为确认事实。
  - 新增混合来源、embedding provenance、孤儿 omitted、顺序保持和不可变复制回归。
- **CAN-M2-003｜分支路径与返回交互**
  - 新增 `canvasBranchNavigation.ts`，只按显式 `parentNodeId` 投影入口→当前节点路径。
  - 缺失父节点或异常环会安全停止，不猜测缺失祖先；导入来源类型保持原值。
  - `ConversationCanvas` 增加左上角当前路径导航，点击任一祖先节点会同步选择 Composer 父节点并居中画布；当前项使用 `aria-current`。
  - 新增 UI-HANDOFF-06 注释和最小样式，移动端保留横向滚动，不建立新视觉系统。

## 交付物与验证

- `desktop/src/canvas/canvasM2Projection.ts`
- `desktop/src/canvas/canvasM2Projection.test.ts`
- `desktop/src/canvas/canvasBranchNavigation.ts`
- `desktop/src/canvas/canvasBranchNavigation.test.ts`
- `desktop/src/components/ConversationCanvas.tsx`
- `desktop/src/components/conversationCanvas.css`
- `desktop/package.json`
- Canvas：24/24 通过；新增 3 项统一检索/路径回归。
- Chat：40/40 通过。
- Canvas 性能：1/1 通过；200 节点 `initialProjectionMs=3.993`、`streaming50ProjectionMs=89.817`，保持门限内。
- Rust：130/130 通过（共享树统一检索适配已进入当前状态）。
- TypeScript/Vite：通过，1833 modules。
- Secret scan：1496 个文本文件通过；`git diff --check` 通过。
- Playwright 真实浏览器：创建入口节点和发散子节点后，路径显示“会话入口 / 发散”；点击入口后 Composer 返回入口节点并同步居中。应用控制台无新增错误，仅既存 `favicon.ico` 404。
- 截图：`output/playwright/canvas-branch-trail-two-level-20260827.png`。

## 契约、数据、安全、恢复与跨团队接口

- 未改变 FocusFrame、FocusedContext、KnowledgeRetrieval、KnowledgeEntity、SQLite 或 Tauri IPC；画布只复制已验证 DTO。
- 不访问 SQLite、Markdown、VectorIndex 或向量正文；不执行候选排序、事实确认、排除算法或降级推断。
- 路径导航只消费会话图父子关系，画布坐标仍只负责布局；点击祖先不会创建、关闭或重开 FocusFrame。

## 风险、阻塞与下一步（最多两个 WIP）

- **阻塞**：SQLite 中真实 FTS/Vector/Relation 执行、统一 retrieval 查询 IPC、删除/否决/过期后的同事务失效重建仍未完成；因此适配器已有自动测试但生产 UI 不能提前伪造候选数据。
- **WIP1｜员工02/05/01**：统一查询 IPC 落地后，员工03把 `projectKnowledgeRetrieval` 接入聚焦阅读器，展示 sources、omitted、fallback notice 和 provenance 摘要。
- **WIP2｜员工01/02/04**：在同一无开发服务器 Tauri Release 复核路径返回、FocusFrame 三态、来源展示和重启隔离；CAN-M2-006 回流候选仍待领域命令与数据契约。
