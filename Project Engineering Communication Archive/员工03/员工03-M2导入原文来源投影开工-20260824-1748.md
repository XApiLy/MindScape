# 员工03｜M2 导入原文来源投影开工

> 提交时间：2026-08-24 17:48（UTC+8）  
> 覆盖任务：CAN-M2-004；为 CAN-M2-007 增补来源标记基准  
> 状态：进行中；首个不依赖新后端契约的安全切片完成  
> 基线提交：`bd3f159`；当前为多人共享 dirty 工作树，尚未形成员工03独立提交或 PR

## 相比上一份的变化

- M1 已由员工01签署 Go，员工03正式切换到 M2 `CAN-M2-001～008` 主交付。
- 首先完成 CAN-M2-004 的前端投影切片：领域图已明确标记为 `importedFrom` 或消息角色为 `imported` 时，画布将其显示为“导入原文”，不再表现为本地模型新生成内容。
- FocusFrame、主线/任务/探索/复盘分支尚未编码；当前员工01未发布权威 M2 契约实现，员工03不在前端复制继承和记忆规则。

## 已阅读与落实

- 已读取最新七份 PEC、M2 本地知识与聚焦连续性派单、V1 总路线、V1 范围、产品共识、内核总览、前五人责任书、RC1 契约、威胁模型、Git/PEC/验收构建制度；报告写入后又补读员工02 17:45 与员工05 17:46 的 M2 最新报告。
- 已读取本地实体与混合 RAG、Markdown Vault 与分支记忆隔离、模型控制与 FocusFrame、V1 工作拆分中的 `CAN-M2-001～008`。
- 具体落实：来源判断只消费领域的 `branchType` / `MessageRole`；坐标、视觉连线、模型字段和文本内容都不用于推断来源或知识关系。
- 员工06的 VIS-009 仍只是书面评审请求，不是视觉生产开工指令；本轮沿用现有样式系统，没有引入 Three/Fiber、天气、玻璃或新视觉架构。
- React 性能约束落实为：来源事实在纯 `projectConversationGraph` 中一次投影，卡片与阅读视图复用结果；状态栏来源计数按 `visibleNodes` memoize，不新增全局监听或重复 IPC。

## 本次完成与证据

- `desktop/src/canvas/graphProjection.ts`：新增 `CanvasNodeOriginProjection`，将明确导入语义投影为 `importedSource`，本地/流式节点保持 `localRun`。
- `desktop/src/components/ConversationCanvas.tsx`：导入节点在卡片、聚焦阅读、无障碍名称和状态栏中显示“导入原文 / 非本地生成”；模型位置改为“外部会话原文”。
- `desktop/src/components/conversationCanvas.css`：增加与现有分支色彩一致的导入来源边框、标签和阅读说明，不改变布局架构。
- `desktop/src/canvas/graphProjection.test.ts`：验证 `importedFrom` 与 `role=imported` 两个明确领域信号，且节点位置、边语义和数量不变。
- `desktop/src/canvas/canvasPerformance.test.ts`：200 节点固定样本加入 19 个导入来源节点并校验投影数量。

## 契约、数据和安全影响

- 未修改 Rust 权威契约、Frozen V1、SQLite、Tauri IPC、导入文件、ContextSnapshot 或 Provider。
- 只读取已存在的明确领域字段，不建立前端导入事实源，也不接触 Markdown、向量库、文件路径或 API Key。
- 外部导入仍按不可信数据处理；本轮只标记其来源，不执行内容、不发送模型、不生成知识实体。
- 当前共享工作树包含员工05 Provider 与员工06视觉/文档改动；员工03只修改五个 Canvas 文件和本报告，未覆盖其他同事文件。

## 验证结果

- `pnpm test:canvas`：14/14 通过。
- `pnpm test:canvas:perf`：1/1 通过；200 节点、199 边、19 个导入来源节点，初始投影约 `3.566ms`，50 次流式投影约 `125.488ms`。
- `pnpm test:chat`：18/18 通过，M1 Chat 状态未退化。
- `pnpm build`：通过，Vite 生产构建 1826 个模块。
- `git diff --check`：通过。
- Playwright CLI：当前环境无 `@playwright/cli` 缓存且网络受限，未执行成功；未将此项写成 UI 通过。真实导入样本视觉验收待导入查询接入后使用正式 Tauri Release。

## 风险与阻塞

- 员工01尚未发布 `ARC-M2-002/003/004` 权威实现和画布查询投影，员工03不能安全实现主线/任务/探索/复盘分支、FocusFrame 当前态、实体引用、排除范围与回流候选。
- 员工01已在共享树中开始加入 Focus/Knowledge 类型，但当前尚无冻结 PEC，且 `focused_context.rs` 被员工05报告为仍有编译错误；员工03不会消费未冻结且未通过门禁的中间契约。
- 员工02已完成 Markdown/JSONL/TXT 原始内容寻址存储第一刀，并明确要求员工03当前无需接入；ImportSource/ImportRevision/ImportedMessage 事务与 ParseReport 尚未冻结，所以本轮只能显示“导入原文”，不能显示平台、文件、修订、定位或解析警告。
- 当前分支仍是 `codex/m1-integration-recovery` 且多人改动未清；`.git` 在本次执行环境中只读，`git mv` 无法创建 `index.lock`，旧 PEC 已用无覆盖文件移动完成工作树替换，后续提交者需在可写 Git 环境中确认 rename 检测。
- 本轮没有正式验收 Build ID，不向创始人交付程序。

## 下一步 WIP（最多两项）

1. CAN-M2-001～003：收到员工01 FocusFrame/分支查询契约与员工02持久化查询后，接入当前焦点、分支类型、返回主线和切换恢复；前端只消费结果。
2. CAN-M2-004/007：用员工02真实 Markdown/JSONL/TXT 导入样本完成来源详情、200 节点稳定性和正式 Tauri Release 视觉验收。

## 需要同事配合

- **员工01**：请冻结并交付画布可消费的 FocusFrame、分支作用域、知识引用/排除项及回流候选查询契约，明确稳定 ID 与状态枚举。
- **员工02**：原始内容寻址存储第一刀已知悉；待其明确通知 UI 可接入后，请提供按 conversation/node 查询的来源投影，至少包含 source ID、platform、原文件显示名、revision、locator 与 ParseReport 摘要；不要让前端读文件或 SQLite。
- **员工04**：请评审“导入原文 / 非本地生成”文案与从导入节点原样续接入口，避免 Chat 与 Canvas 对同一来源使用两套含义。
- **员工06**：本轮不启动 VIS-009 生产实现；待 M2 核心来源/分支交互稳定后再共同评审视觉表现和性能预算。
