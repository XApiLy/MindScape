# 员工05｜PROV-013真实选择与取消通过

> 提交时间：2026-08-19 15:14（UTC+8）  
> 覆盖任务：PROV-013、CHAT-003/006/015联调  
> 状态：真实Provider默认选择、流式取消与数据不变量通过；继续推进错误和超时矩阵  
> 基线提交：`0cb1c0053b5a`（专项release来源树dirty，manifest已披露）

## 相比上一份的变化

- 创始人在修复版正式release中确认下一次运行实际使用`deepseek-v4-flash · deepseek`，启动选择不再静默停留于Mock。
- 真实长回答可正常停止：界面显示“已停止”，保留部分内容、保留重试入口，底部下一次模型仍为DeepSeek。
- SQLite只读复核证明最新运行只有一个`cancelled`终态，终态后无delta，Node/Run状态一致且数据库完整。
- PROV-013由“修复版待真实发送确认”更新为通过；M1仍因错误、超时、强退恢复与泄漏矩阵保持No-Go。

## 已阅读与落实

- 推进前重新读取PEC七文件，重点消费员工04的PROV-013真实取消闭环、员工02真实SQLite基线、员工01集成分支交接和最新验收程序规则。
- 具体落实：同一Build ID同时核对窗口和SQLite事实；不把截图单独当作网络终止证明，也不读取正文、请求JSON、内部ID或Key。
- 历史Mock节点继续保留Mock归属；本次新DeepSeek运行独立记录真实Provider/Model，不改写历史。

## 本次完成与证据

- 证据：`docs/engineering/evidence/employee05/prov-013-default-selection-cancel-20260819/README.md`。
- Build ID：`20260819-150500-prov-013-mock-default-fix-0cb1c0053b5a-dirty`。
- 程序SHA-256：`B0F424B5C55E1FEDC1E49577DA8001751F79A352E629366AC45436997C415606`。
- 脱敏运行指纹：`a16ba48ffc4a`；Run/Node均`cancelled`，部分内容193字符，104事件，唯一终态序号104，终态后delta为0。
- SQLite：`integrity_check=ok`，外键违规0。

## 契约、数据和安全影响

- 未修改Frozen V1、SQLite schema、Provider协议、凭据接口或终态语义。
- 只读核对未输出会话正文、请求、事件正文、数据库路径、内部ID或API Key。
- 本次专项Build为dirty来源，不替代最终clean集成release门禁。

## 验证结果

- 真实Provider启动默认选择：通过。
- DeepSeek真实流式与用户取消：通过。
- 唯一`cancelled`终态、终态后delta为0：通过。
- 部分内容、重试入口、Node/Run一致：通过。
- SQLite完整性与外键：通过。
- PROV-013 / 统一验收脚本4：员工05签署通过，等待员工01纳入最终门禁表。

## 风险与阻塞

- 无效Key、模型不存在、断网、连接/首Token/空闲超时、异常断流、用量重启投影和泄漏检查仍为M1 No-Go。
- 强制退出和启动恢复属于员工02主责，员工05需配合确认Provider中断不被伪装为完成。
- 最终Go版本必须来自可追溯clean commit的统一release Build ID。

## 下一步 WIP（最多两项）

1. PROV-014：执行无效Key、模型不存在、断网、连接/首Token/空闲超时与异常断流矩阵。
2. PROV-016/017：复核用量重启投影及数据库、日志、崩溃信息的Key与正文脱敏。

## 需要同事配合

- **员工01**：将本次同Build窗口与SQLite证据纳入统一验收脚本4并签署关闭。
- **员工02/03/04**：复用本运行核对重启后取消投影、唯一节点/边和阅读视图一致性。
- **创始人**：PROV-013无需重复操作；下一轮只需配合无效Key、断网/超时或强退等不可替代的Windows动作。
