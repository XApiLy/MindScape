# 员工05｜RAG-M2 混合检索 SQLite 闭环

时间：2026-08-27 14:24（Asia/Shanghai）  
覆盖任务：`RAG-M2-003`、`RAG-M2-004` 生产接线  
状态：代码与工程门禁完成；等待统一 Tauri Release 消费验收

## 已阅读与对齐

- 已阅读 PEC 控制文件及员工01统一检索契约、员工02持久化联调准备、员工03统一检索消费、员工04失败恢复报告。
- 继续以 SQLite/领域对象为事实源；FTS、Vector、Relation 只产生候选，不得确认事实或绕过 scope/status/revision 校验。

## 本次完成

- SQLite schema 升至 v10，新增持久化 FTS5 索引；v9 升级会校验并回填既有 Confirmed Entity，迁移前备份规则保持不变。
- Entity 写入与 FTS 更新在同一事务完成：Confirmed 建索引；Candidate/Inferred/Rejected/Superseded/Stale 均从 FTS 移除。
- 新增生产混合检索：SQLite FTS + 确定性本地向量重建 + Confirmed Relation 邻居召回，合并为冻结的 `mindscape.knowledge-retrieval.v1`。
- 本地 VectorIndex 每次只从当前已校验 Confirmed 事实重建，保留模型、维度、source hash、index version；不调用云端 Embedding，不持久化向量正文。
- 新增 `retrieve_knowledge` Tauri IPC 与前端 `kernelClient.retrieveKnowledge()`；默认 limit 12，服务端强制 1～50。
- 新增 `delete_knowledge_entity`：同事务清除 Entity、FTS 与引用该实体的 Relation；共享 EvidenceRef 不被误删。
- 孤儿 FTS 命中进入 omitted，且不得成为关系扩散种子；非法 scope、损坏事实、NaN/Infinity、非法投影均在 IPC 前拒绝。

## 验证

- Rust 全量：134/134 通过。
- Clippy：`--all-targets --all-features --locked -D warnings` 通过；Rust fmt 通过。
- 新增验证：三路混合来源、重启一致性、Rejected 失效、删除后 Relation/召回清理、schema v10 FTS 回填。
- TypeScript/Vite：1833 modules，生产构建通过。
- Chat：40/40；Canvas：24/24；Canvas 性能：1/1。
- Secret scan：1497 个文本文件通过；`git diff --check` 通过。

## 安全、数据与恢复边界

- API Key、Authorization、Provider 原始响应、reasoning、向量正文均不进入 SQLite FTS 或统一投影。
- 查询只水合已校验的当前事实；旧索引、跨会话对象和非 Confirmed 状态不能进入 candidates。
- 本次未编译正式 Release，不能以单元测试替代真实 Tauri UI 的来源、omitted、删除和重启验收。

## 需要配合 / 下一步（最多两个 WIP）

1. 员工01/02：复核 schema v10、迁移备份、整数分数、事实源过滤与删除事务；若认可，本检索底座可进入统一 Release。
2. 员工03/04：消费 `retrieveKnowledge()`，显示 sources/omitted/fallback/provenance，并在同一无开发服务器 Release 验证查询、拒绝、删除和重启。

