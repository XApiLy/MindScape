# 员工05｜Windows调试版编译与真实Key联调准备

> 提交时间：2026-08-18 16:31（UTC+8）  
> 覆盖任务：PROV-005～008、PROV-013～017  
> 状态：Windows Debug可执行文件已编译，等待用户本机启动并输入真实Key

## 相比上一份的变化

- 员工04已将`test_provider_connection`接入设置页，真实Key联调所需UI已具备。
- 执行`pnpm tauri build --debug --no-bundle`，生成最新Windows可执行文件`desktop/src-tauri/target/debug/mindscape-desktop.exe`。
- Codex受限进程尝试启动时因无权写入Windows AppData而在Tauri setup阶段退出；该限制不是编译失败，需用户在桌面会话中直接启动。

## 已阅读与落实

- 编译前已重读PEC控制文件及员工01～06最新报告，确认当前唯一目标仍是M1真实Chat Alpha。
- 具体落实：不在命令行、环境变量或源码中接收Key；用户必须从设置页写入Windows凭据库，先手动测试连接，再执行真实生成。
- 与员工04契约无冲突：设置页只调用Tauri脱敏命令，不直连厂商端点。

## 本次完成与证据

- 编译命令：`pnpm tauri build --debug --no-bundle`。
- 前端构建：1823模块转换完成。
- Rust/Tauri编译：`Finished dev profile`，退出码0。
- 产物：`D:/Project/MindScape/desktop/src-tauri/target/debug/mindscape-desktop.exe`。
- 启动诊断：Codex受限进程返回`Failed to setup app: 拒绝访问 (os error 5)`，未进入凭据读取或网络阶段。

## 契约、数据、安全与恢复影响

- 本轮无代码、契约或schema变更，仅生成调试编译产物。
- 未读取、传输、记录或保存任何真实Key；启动失败发生在Tauri setup的本地数据目录准备阶段。

## 验证结果

- Debug编译：通过。
- 可执行文件输出：成功。
- PEC七文件窗口检查：本报告替换后执行。

## 风险与阻塞

- Codex工具进程无法写入用户AppData，因此不能代替用户保持桌面窗口；用户直接启动EXE时应使用正常Windows会话权限。
- 真实Key尚未完成连接成功、错Key、生成、取消、超时、用量与断流矩阵，M1 Alpha仍不可放行。
- `.git`只读仍阻止提交与Draft PR。

## 下一步 WIP（最多两项）

1. 用户在本机启动EXE，从设置页安全保存DeepSeek Key并点击“测试连接”。
2. 连接成功后执行首次真实发送、流式停止和错误/用量矩阵，并记录脱敏结果。

## 需要同事配合

- **用户**：请直接启动编译产物，只在设置页输入Key；连接测试后反馈成功或脱敏错误文案。
- **员工01～04**：连接成功后共同完成真实窗口终态、预算、画布与恢复矩阵。
