# 员工05｜RAG-M2 Markdown 分块索引首切片

> 提交时间：2026-08-25 17:44（UTC+8）  
> 覆盖任务：RAG-M2-001  
> 状态：纯分块适配器完成；等待 SQLite/FTS/Relation 查询接线

## 已阅读与落实

- 已读取最新 PEC 控制文件及员工01～04、06当前报告。
- 员工02已完成通用导入闭环、重启查询和孤儿引用回收，但 VectorIndex/FTS/Relation 查询 DTO 尚未冻结；因此本次只提供 Provider/Index 可消费的纯分块输出，不改导入解析器、SQLite 或 UI。
- 按 RAG 方案保留原文与来源元数据，分块仅是派生索引输入；不执行代码块、不调用模型、不把分块结果当作事实。

## 本次完成与证据

- 新增 `desktop/src-tauri/src/adapters/provider/chunking.rs`：
  - `chunk_markdown` 保留 Heading、Paragraph、CodeBlock 三类块。
  - 每块记录 `sourceId`、`sourceHash`、可选 `branchId`、稳定 ordinal/id、heading path、chunk hash 和 `chunkVersion`。
  - 支持未闭合代码围栏保真保留，不丢失原文。
  - 输出稳定、可重复，可作为 Embedding/VectorIndex 的 `IndexInput` 上游。
- `desktop/src-tauri/src/adapters/provider/mod.rs` 导出分块边界。

## 契约、数据、安全和恢复影响

- 不修改原始导入内容，不执行导入中的命令或工具调用。
- chunk hash 只用于派生索引去重/失效检测，不替代 source hash、EvidenceRef 或 KnowledgeEntity 状态。
- branch/source metadata 随块传递，后续查询必须继续执行作用域过滤。

## 验证结果

- 新增分块测试覆盖 heading path、段落、代码块、来源/分支 provenance、确定性 hash 和未闭合代码围栏。
- 已执行 `cargo fmt --all`。
- 共享 Rust 定向/全量测试当前被员工01并行 FocusFrame 接线阻塞：`application/mod.rs:141` 中 `lifecycle` 移动后再次借用；本次未把阻塞误报为通过。
- 阻塞解除后需复跑 `cargo test --all-targets --locked`、Clippy、TypeScript、Chat/Canvas、秘密扫描和 PEC 检查。

## 风险与阻塞

- 分块尚未接入 SQLite FTS 或 VectorIndex 持久化；不能宣称 RAG-M2-001 完整闭环。
- 当前实现按通用 Markdown 基础语法切块，平台专用导入和复杂 AST 解析不在 M2 范围。

## 下一步 WIP（最多两项）

1. 等员工01修复共享 Rust 编译阻塞后复跑门禁，并将 chunk 输出与 `IndexInput` 做字段级回归。
2. 与员工02评审 chunk metadata 到 FTS/VectorIndex 的持久化和重建触发条件，推进 RAG-M2-003。

## 需要同事配合

- **员工01**：修复 `lifecycle` 移动/借用错误，确认分支和 FocusFrame 作用域过滤入口。
- **员工02**：确认 source/revision/branch/chunk metadata 的 SQLite/FTS 映射，不在存储层执行分块语义。
- **员工03/04**：等待结构化检索查询 DTO 冻结后消费来源和降级状态，不在前端重算分块或排序。
