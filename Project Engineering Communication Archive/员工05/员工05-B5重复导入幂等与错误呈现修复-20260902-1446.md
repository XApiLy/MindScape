# 员工05｜B5 重复导入幂等与错误呈现修复

> 提交时间：2026-09-02 14:46（UTC+8）  
> 覆盖：`B5` 真实导入重复内容失败、结构化错误显示为 `[object Object]`  
> 状态：**修复已完成并通过回归；当前 Acceptance Build 未覆盖本轮改动，等待员工01重新冻结后再发布**

## 已阅读与落实

- 已重新读取当前 PEC 顶部 B1/B5 规则和员工01 clean 冻结、员工04导入恢复报告。
- 按 Tauri/Rust 与 React 安全边界，保留内核 CommandError.safeMessage，不把结构化错误对象直接转换为字符串；不泄漏 Key、正文或绝对路径。

## 本次推进

- 定位截图中的真实根因：ImportStorage 先按内容哈希去重，但 SQLite `import_sources.content_hash` 唯一约束下仍重复插入新来源，造成导入失败。
- 同一会话重复导入相同内容现在幂等返回已有 `ImportSource/Revision/ParseReport`；跨会话重复明确提示切换原会话或修改内容，不会把来源错误挂到新会话。
- `ImportIntakeDialog` 的导入、来源恢复、提案请求、审核和历史读取统一使用 `commandErrorMessage`，消除 `[object Object]`。

## 跨模块证据

1. `ImportStorage duplicate → SqliteStore find_import_source_by_content_hash → KernelService → import_generic_file typed command → ImportIntakeDialog` 已接通。
2. Rust 全量：`245 passed / 0 failed / 1 ignored`，B5 audit `4/4`；Clippy 与 fmt 通过。
3. 前端：Vite `2100 modules`、Chat `69/69` 通过；Review Lab preview `20260902T063907Z-导入错误提示可读化-5525` 为 `ready`。
4. 未修改当前已发布 Build `20260902-141043-m2-b5-provider-bd2ab97f7b7b` 的文件；该 Build 的 SHA-256 仍为 `406A8861A18457FAF9B2E2DDE101449BC04ED3EA0D5D16B209426AEB94BE360E`。

## 剩余阻断与协作

1. 本轮 Rust/React 修复位于共享工作树，不能冒充当前 clean Acceptance Build；请员工01重新冻结后，再由员工05按唯一 Release 规则发布新 Build。
2. 在新 Build 发布前，当前窗口请避免重复点击“生成本地预览”导入同一内容；正确流程是复用已显示的导入来源，继续勾选消息并请求知识建议。
3. B5 仍需在新 clean Build 中完成真实导入 → 建议 → Confirm/Reject → FocusFrame 回流 → Close/Reopen/重启，并运行脱敏审计。

## 用户操作指引

1. 点击“导入已有 AI 会话”。
2. 选择“粘贴会话文本”或“选择或拖放文件”。
3. 确认下方出现“本地导入预览”后，点击其中一条或多条消息进行选择。
4. 选择目标会话或 Active FocusFrame。
5. 点击“请求知识建议”，然后对每条建议选择 Confirm 或 Reject。
6. 如果已看到相同的 `pasted-session.txt` 导入记录，不要重新导入；直接打开该来源继续操作。新 Build 会将重复导入安全地视为已存在记录。
