# 员工05｜M2 VectorIndex 快照恢复契约

> 提交时间：2026-08-25 17:08（UTC+8）  
> 覆盖任务：PROV-M2-005、RAG-M2-003 接线前置  
> 状态：本地索引快照/恢复完成，等待 SQLite 字段映射评审

## 已阅读与落实

- 已读取最新 PEC 控制文件及员工01～04、06当前报告。
- 员工02已完成通用导入闭环和重启查询，但尚未冻结 VectorIndex/FTS/Relation 查询 DTO；因此本次只强化 Provider/Index 适配器，不改 SQLite 和 IPC。
- 员工01已完成 FocusFrame 会话列表查询接线；本次继续保持 VectorIndex 只能产生候选，不能绕过 FocusFrame、scope、status、EvidenceRef 和 Context Compiler。

## 本次完成与证据

- `desktop/src-tauri/src/adapters/provider/embedding.rs`
  - 新增 `LocalVectorIndex::snapshot`，提供可持久化的 `EmbeddingRecord` 快照。
  - 新增 `LocalVectorIndex::restore`，恢复前严格校验模型版本、metadata 维度、向量长度、有限值和非零向量。
  - 新增 `VectorIndexRestoreError`，错误不携带原文、Key 或 Provider 响应。
  - 新增快照恢复与模型版本不匹配回归测试。
- `desktop/src-tauri/src/adapters/provider/mod.rs` 导出恢复错误类型，供后续数据层映射安全错误。

## 契约、数据、安全和恢复影响

- 快照只包含稳定 ID、向量和 provenance metadata，不包含原文正文、Prompt、Key 或 reasoning。
- 模型/分块版本不一致时拒绝恢复，避免新旧向量静默混用；失败不覆盖已有索引。
- 该边界支持 SQLite 后续以事务方式持久化与重建，但本次没有新增 schema。

## 验证结果

- `cargo test --manifest-path desktop/src-tauri/Cargo.toml --all-targets --locked`：104/104 通过。
- `cargo clippy --manifest-path desktop/src-tauri/Cargo.toml --all-targets --all-features --locked -- -D warnings`：通过。
- `cargo fmt --all`、`git diff --check`：通过。

## 风险与阻塞

- SQLite VectorIndex、FTS、Relation 的持久化/查询 DTO 仍未由员工02冻结，当前不能宣称 RAG-M2-003 完整接线。
- hash embedding 仍是离线工程验证实现，不代表生产语义质量。

## 下一步 WIP（最多两项）

1. 与员工02评审 `EmbeddingRecord` 到 SQLite 索引表的字段、事务、重建和失效映射。
2. 与员工01确认快照恢复后的候选如何进入 `KnowledgeRetrievalCandidate`，继续保持领域过滤唯一归属。

## 需要同事配合

- **员工02**：提供 VectorIndex/FTS/Relation 统一查询 DTO 和持久化字段映射。
- **员工01**：评审 VectorMatch→KnowledgeRetrievalCandidate 的分数、证据和 scope 边界。
- **员工03/04**：等待查询 IPC 冻结后再消费检索来源与降级 notice，不在前端重算。
