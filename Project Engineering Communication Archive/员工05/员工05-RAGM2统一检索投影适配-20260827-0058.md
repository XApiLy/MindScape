# 员工05｜RAG-M2 统一检索投影适配

时间：2026-08-27 00:58（Asia/Shanghai）  
状态：本推进单元完成，统一检索投影适配已通过工程门禁

## 已阅读与对齐

- 已阅读最新 PEC 控制文件及员工01统一检索投影契约、员工02知识持久化校验与检索联调准备、员工03/04消费侧报告。
- 以员工01冻结的 `mindscape.knowledge-retrieval.v1` 为唯一跨层契约；索引命中只作为候选，必须由 SQLite 校验后的知识事实源补全实体与证据。

## 本次完成

- 新增 `adapters/provider/retrieval.rs`，把 FTS/Vector/Relation 混合候选投影为冻结的 `KnowledgeRetrievalProjection`。
- 检索候选由单来源升级为多来源集合；相同实体命中会合并来源、保留最高分并补齐 embedding provenance。
- Vector 命中保留 embedding model/version/dimensions/source revision，向量不可用时保留全文/关系降级状态与 notice。
- 缺失于校验事实源的索引命中不进入结果，改写入 omitted；实体 ID 不一致、NaN/Infinity/越界分数、领域契约不合法均直接拒绝。
- 投影只携带已校验实体、EvidenceRef 和必要 provenance，不携带向量正文、API Key、provider 原始响应或推理内容。

## 验证

- 新增统一投影测试 3/3：混合来源与 provenance、孤儿命中排除、非有限分数拒绝。
- Rust 全量测试：130/130 通过。
- Clippy `-D warnings`：通过；Rust fmt：通过。
- TypeScript 检查：通过；Chat：40/40；Canvas：24/24。
- Secret scan：1488 个文件通过；`git diff --check`：通过。

## 当前 M2 状态与风险

- Provider、Embedding、内存 VectorIndex、Markdown 分块、失效/快照、混合候选和统一领域投影已经贯通。
- 尚未完成 SQLite 查询命令中的真实 FTS/Vector/Relation 执行与 projection 组装，也未形成删除/否决/过期后的同事务持久化索引重建。
- 当前实现不会把仅存在于索引、但不存在于知识事实源的记录误注入上下文；此安全边界必须保持。

## 下一步 WIP（最多两个）

1. 员工05 + 员工02：把统一投影接入 SQLite 会话事实源与知识查询 IPC，补齐 FTS/Vector/Relation 实际组装及删除/否决/过期失效事务。
2. 员工01复核来源、omitted、availability 与版本映射；员工03/04随后在同一正式 Tauri Release 验证消费、错误恢复和来源展示。

