# 员工05｜M2 DeepSeek 流式事件夹具回归

> 提交时间：2026-08-25 10:36（UTC+8）  
> 覆盖任务：PROV-M2-003  
> 状态：离线 SSE 夹具完成，等待真实 Key/Tauri 验收

## 已阅读与落实

- 已读取最新 PEC 控制文件及员工01～06当前报告。
- 员工04已完成 `effectiveRunProfile` UI→IPC 占位接线，但 PEC 明确真实 DeepSeek 验收尚未完成；因此本次不调用云端、不读取 Key，先补可重复离线门禁。
- 保持 Provider 唯一归属：前端只消费统一事件，不重算 reasoning/content/usage。

## 本次完成与证据

- `desktop/src-tauri/src/adapters/provider/openai_compatible.rs`
  - 新增 DeepSeek SSE chunk fixture：reasoning_content、visible content、usage 三类事件分别解析。
  - 断言 reasoning 不进入 content，usage 正确映射 input/output/cache token。
  - 与终态矩阵共同覆盖 reasoning-only、reasoning+content、empty、usage、Provider error。

## 验证结果

- `cargo test --all-targets --locked`：85/85 通过。
- `cargo clippy --all-targets --all-features --locked -- -D warnings`：通过。
- `cargo fmt --all`、`git diff --check`：通过。

## 风险与阻塞

- 尚未使用真实 DeepSeek Key 做 Tauri 联调；需验证真实网络下 Off/Standard/Deep/Custom、usage、空正文、超时、取消和断网恢复。
- 本切片不宣称 M2 Go 或真实 Provider 验收完成。

## 下一步 WIP（最多两项）

1. 配合用户/员工04进行真实 Key 验收，记录请求档案、终态事件和安全错误。
2. 若真实返回与 fixture 有差异，只在 Provider 适配层补兼容，不改变统一事件契约。

## 需要同事配合

- **用户/员工04**：提供真实 Key 验收窗口，按上述场景测试并回传截图/运行结果。
- **员工01**：复核真实终态事件与运行档案投影边界。
- **员工06**：等待事件字段稳定后再调整视觉表现，不改变事件语义。
