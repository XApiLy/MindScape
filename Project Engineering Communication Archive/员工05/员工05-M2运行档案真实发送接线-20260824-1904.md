# 员工05｜M2 运行档案真实发送接线

> 提交时间：2026-08-24 19:04（UTC+8）  
> 覆盖任务：PROV-M2-002  
> 状态：真实发送链路接线完成，等待跨员工 UI 选项联调

## 相比上一份的变化

- 上一切片只有 DeepSeek 纯映射/预检；本次已将 EffectiveRunProfile 贯穿 IPC 输入、应用服务、ModelRunRequest 和 OpenAI-compatible 发送体。
- 清除了员工01报告所述 Provider dead-code/Clippy 阻塞。

## 已阅读与落实

- 已读取最新 PEC 控制文件及员工01～06当前报告，重点核对员工01的契约状态与员工04能力摘要边界。
- 当前代码中 ModelRunRequest 仍无运行档案字段；依据员工01要求员工05完成真实发送接线，本次采用可选加法字段，未提供时保持 M1 行为。
- 使用 Rust/Tauri契约镜像原则：Rust 权威类型、serde 默认兼容、TypeScript 同步、发送前验证且不静默降级。

## 本次完成与证据

- `domain/contracts/runtime.rs` 与 `src/domain/runtime.ts`：ModelRunRequest 新增可选 EffectiveRunProfile。
- `domain/conversation.rs` 与 `src/domain/commands.ts`：StartModelRunInput 新增可选运行档案并通过 IPC 生命周期传递。
- `application/mod.rs`：运行档案进入持久化请求；幂等重放比较包含完整运行档案，防止同 key 偷换参数。
- `openai_compatible.rs`：发送前校验 contract/provider/model；映射 thinking、reasoning_effort、temperature/top_p、max_tokens、json_object；不支持组合返回统一 InvalidRequest。
- 新增请求 JSON 序列化和目标不匹配回归；原 M1 无档案请求继续默认禁用 thinking，保持可见正文行为。

## 契约、数据和安全影响

- 契约为可选加法字段，旧 IPC 调用反序列化为 None；没有 SQLite schema 变更。
- 校验发生在读取凭据和网络发送之前；错误不含 API Key、Provider 响应正文或完整推理。
- 不保存 reasoning 正文，不启用工具调用，不对不支持参数静默删除。

## 验证结果

- `cargo test --all-targets --locked`：75/75 通过。
- `cargo clippy --all-targets --all-features --locked -- -D warnings`：通过。
- `pnpm exec tsc --noEmit`：通过；`pnpm run test:chat`：27/27 通过。
- `node scripts/check-secrets.mjs`：1285 文件通过；`git diff --check`：通过。

## 风险与阻塞

- 后端接线已完成，但当前 UI 尚未构造非空 EffectiveRunProfile；真实参数选择仍需员工04接入并联调。
- 本切片不声明 M2 整体可验收，也未发布正式验收 Build。

## 下一步 WIP（最多两项）

1. 与员工04联调运行控制选择，验证 Off/Standard/Deep/Custom 和错误展示的真实 IPC 路径。
2. 推进 PROV-M2-003：reasoning-only/content/empty/usage/终态回归矩阵。

## 需要同事配合

- **员工04**：从能力快照构造 EffectiveRunProfile，并将后端 InvalidRequest 原样作为安全可执行提示展示；不得自行丢参。
- **员工01**：评审可选字段与幂等语义，确认后将契约基线标记为已接线。
- **员工02**：本次无 schema 变更；未来不可变档案持久化若落表，须保留现有请求字段和幂等一致性。
