# 员工05｜真实Provider默认选择修复

> 提交时间：2026-08-19 15:07（UTC+8）  
> 覆盖任务：PROV-013、CHAT-003/015联调  
> 状态：启动后错误保留Mock默认值已修复；正式release验收版已发布并确认DeepSeek为下一次运行模型  
> 基线提交：`0cb1c0053b5a`（共享工作树dirty，manifest已披露）

## 相比上一份的变化

- 创始人截图证明长回答实际由`mock-stream-v1`完成，原因不是DeepSeek请求失败，而是应用启动状态把Mock预置为“当前选择”。
- `refreshProviders`发现真实DeepSeek凭据后会保留已有选择；硬编码Mock因此被误判为用户明确选择，阻止真实Provider成为默认模型。
- 将启动模型选择改为`null`，待Tauri返回Provider注册表和凭据状态后，由现有选择策略优先选取可用真实模型；无Key时才使用Mock。
- 纠正上一份交付方式：根据新生效的验收程序规则，`target-fixed`调试程序不再作为验收事实，本轮只交付`artifacts/acceptance/versions/`中的Tauri release。

## 已阅读与落实

- 开工前读取PEC当前七文件，重点消费员工01集成分支交接、员工04 PROV-013闭环和更新后的PEC控制文件；完整读取`docs/engineering/acceptance-build-policy.md`。
- 落实影响：不覆盖历史节点的Provider/Model；旧Mock节点继续显示Mock，只有下一次发送采用DeepSeek，符合“模型切换不改写历史”不变量。
- 未读取、移动或回显系统凭据；没有自动发送真实请求，也没有增加自动重试。

## 本次完成与证据

- `desktop/src/App.tsx`：`selectedModel`启动值由硬编码Mock改为空，等待真实Provider与凭据探测完成后选择。
- Chat测试16/16通过；Provider选择专项测试4/4通过；TypeScript/Vite生产构建通过，1825模块。
- 正式验收Build ID：`20260819-150500-prov-013-mock-default-fix-0cb1c0053b5a-dirty`。
- 程序：`D:\Project\MindScape\artifacts\acceptance\versions\20260819-150500-prov-013-mock-default-fix-0cb1c0053b5a-dirty\mindscape-desktop.exe`。
- SHA-256：`B0F424B5C55E1FEDC1E49577DA8001751F79A352E629366AC45436997C415606`。
- 无`localhost:1420`监听时独立启动成功；离屏窗口核对底部为`deepseek-v4-flash`、`DeepSeek · 真实 API 可用`，右上角为“本地内核 · 真实 API”。临时截图核对后已删除，未作为长期证据。

## 契约、数据和安全影响

- 未修改Frozen V1、SQLite schema、Provider协议、凭据接口或运行终态。
- 历史Mock节点仍保留真实模型归属；不把既有Mock回答伪装成DeepSeek回答。
- release来源树为dirty，适用于本次专项复验，不作为最终clean里程碑Go版本。

## 验证结果

- 启动默认模型错误：已修复。
- 正式release无开发服务器独立启动：通过。
- 下一次运行模型显示DeepSeek真实API：通过。
- 真实DeepSeek下一次生成：等待创始人在已启动窗口发送确认。
- PEC七文件、`git diff --check`：本报告替换后执行。

## 风险与阻塞

- 画布上旧Mock回答不会自动变为DeepSeek回答；这是历史事实保护，不是残留缺陷。
- M1仍需完成无效Key、模型不存在、断网、连接/首Token/空闲超时、异常断流、用量重启投影和泄漏矩阵。
- 最终Go版本仍需来自可追溯clean commit的统一release Build ID。

## 下一步 WIP（最多两项）

1. 使用本Build ID确认下一次发送由DeepSeek执行，不再静默落入Mock。
2. 与员工04按固定脱敏脚本推进PROV-014/016/017错误、超时、用量投影与泄漏矩阵。

## 需要同事配合

- **创始人**：只使用本报告列出的新窗口；确认底部为`deepseek-v4-flash`后重新发送测试问题。
- **员工04**：复用同一Build ID继续真实错误与超时交互验收。
- **员工01/02**：后续将本修复与错误矩阵纳入clean集成release和SQLite复核。
