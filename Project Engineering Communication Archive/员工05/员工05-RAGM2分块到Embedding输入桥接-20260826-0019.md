# 员工05｜RAG-M2 分块到 Embedding 输入桥接

> 提交时间：2026-08-26 00:19（UTC+8）  
> 覆盖任务：RAG-M2-001、PROV-M2-005 接线前置  
> 状态：字段桥接完成，等待统一 FTS/VectorIndex/Relation DTO

## 已阅读与落实

- 已读取最新 PEC 控制文件、M2 派单及员工01～04、06当前报告。
- 员工02已完成知识对象 IPC 与验收接线，并明确下一步由员工05接入 FTS/VectorIndex/Relation；员工01已冻结 Entity/Relation/EvidenceRef 写入前校验。
- 本次只增加 `TextChunk` 到现有 `IndexInput` 的 Provider/Index 边界映射，不定义新的领域对象、不访问 SQLite、不在前端重算排序。

## 本次完成与证据

- `desktop/src-tauri/src/adapters/provider/chunking.rs`
  - 新增 `TextChunk::as_index_input()`。
  - 保留稳定 chunk ID、原文 chunk text、source hash、chunk version，作为 Embedding/VectorIndex 的统一输入。
- 新增回归确认标题/段落分块投影后仍保留 ID、正文、源哈希和版本。

## 验证结果

- `cargo test --manifest-path desktop/src-tauri/Cargo.toml --all-targets --locked`：121/121 通过。
- `cargo clippy --manifest-path desktop/src-tauri/Cargo.toml --all-targets --all-features --locked -- -D warnings`：通过。
- `pnpm --dir desktop exec tsc --noEmit`：通过。
- `cargo fmt --all`、`git diff --check`：通过。

## 契约、数据、安全和恢复影响

- 无 SQLite、IPC、网络、凭据或 UI 变更；分块仍是派生索引输入，不覆盖原文。
- branch/source/heading provenance 仍由 `TextChunk` 保留；`IndexInput` 只负责当前 Embedding 所需最小字段，后续统一检索 DTO 不得丢失来源元数据。

## 风险与阻塞

- VectorIndex 仍为本地适配器，FTS/Relation 查询和统一 provenance DTO 尚未接线；不能宣称 RAG-M2-003 完整完成。
- 当前没有将 chunk 自动写入 SQLite，避免越过员工02的数据事务边界。

## 下一步 WIP（最多两项）

1. 与员工02评审 `TextChunk`/`EmbeddingRecord` 到统一 FTS/VectorIndex/Relation 查询 DTO 的字段映射。
2. 在 DTO 冻结后补 RAG-M2-003 端到端候选合并、降级 notice、删除/否决失效回归。

## 需要同事配合

- **员工02**：提供统一检索查询 DTO 和 SQLite 索引持久化/重建入口。
- **员工01**：确认候选进入 `KnowledgeRetrievalCandidate` 前继续复用 Entity/Relation/EvidenceRef 和 FocusFrame 校验。
- **员工04**：DTO 到位后只消费来源、scope、evidence 和降级状态，不在 UI 重算检索。
