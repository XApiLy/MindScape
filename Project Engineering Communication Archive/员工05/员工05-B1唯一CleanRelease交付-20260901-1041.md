# 员工05｜B1 唯一 Clean Release 交付

时间：2026-09-01 10:41（Asia/Shanghai）  
覆盖阻断：`B1` 员工05统一回归与唯一 Release 责任  
状态：**唯一 clean Acceptance Build 已发布并独立启动；等待创始人确认真实工作区可见后关闭 B1 现场门禁，M2仍为 No-Go**

## 已阅读与落实

- 已读取员工01《B1文件归属审计与Clean冻结》、控制PEC的B1/B5执行表和Acceptance Build规则；只从员工01冻结的clean commit发布一次正式版本。
- Tauri发布规范落实为：标准脚本重新执行`pnpm tauri build --no-bundle`，从验收目录启动，不交付`target/`程序，不运行前端开发服务器。

## 本次推进的B编号

### B1：唯一clean Release已发布

- 来源commit：`e40dde0af587078df16b3f1a046d5016f6041a3f`，分支`codex/m1-integration-recovery`；构建前`git status`为空。
- Build ID：`20260901-104100-m2-b1-clean-release-e40dde0af587`。
- 程序：`D:\Project\MindScape\artifacts\acceptance\versions\20260901-104100-m2-b1-clean-release-e40dde0af587\mindscape-desktop.exe`。
- SHA-256：`3DF0F0141A998663C19E3202782FA3B34A138573FB15D00E014908E3BAB255C4`；manifest与发布后独立重算一致。
- 大小：`41,015,296` bytes；manifest `sourceTreeDirty=false`。
- 启动前后`localhost:1420/1428`均无监听；程序从验收目录启动，进程存活且窗口标题为MindScape。真实工作区视觉确认等待创始人当前窗口反馈。

## 跨模块与回归证据

- 冻结commit门禁：Rust library `205/205`、B5 audit example `2/2`、Clippy全targets/features/locked、fmt、Vite 2097 modules、Chat `60/60`、Canvas `34/34`、Canvas性能`2/2`。
- secret scan通过；发布脚本再次完成Vite内嵌前端和Tauri optimized Release构建，不依赖开发服务器。
- B2/B3/B4均来自同一个clean commit与同一个EXE，后续B5不得另行编译等价程序。

## 剩余阻断 / 需要配合

1. **创始人**：确认当前已打开窗口显示真实MindScape工作区、不是空白页或localhost错误；确认后B1现场门禁可关闭。
2. **B5仍未关闭**：必须使用本Build ID执行同一真实样本纵向验收；当前Review Lab没有Acceptance Build自动`full`登记接口，需保留“full登记待接线”缺口，不重新编译第二份EXE。
3. **B6未开始**：继续不抢占B5，等待范围裁决。

## 下一步WIP（最多两个）

1. 记录创始人对本clean Build真实工作区的启动确认。
2. 固定使用本Build ID执行B5真实样本、故障、重启和语义召回矩阵。
