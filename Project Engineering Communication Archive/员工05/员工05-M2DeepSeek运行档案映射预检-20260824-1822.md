# 员工05｜M2 DeepSeek 运行档案映射预检

> 提交时间：2026-08-24 18:22（UTC+8）  
> 覆盖任务：PROV-M2-002（纯映射/预检切片）  
> 状态：纯函数完成；等待 EffectiveRunProfile 挂入 ModelRunRequest 后接发送链路

## 已阅读与边界确认

- 已读取最新 PEC 控制文件、M2 派单及员工01/02/03/04/05 当前报告。
- 员工01当前报告明确：EffectiveRunProfile 尚未挂入 ModelRunRequest；因此本次只实现可复用的 Provider 映射与发送前校验，不改动员工01契约、不绕过运行时接线。
- 本次继续遵守 M2 边界：不改 SQLite、导入/Vault、UI、凭据和网络协议终态。

## 本次完成

- `desktop/src-tauri/src/adapters/provider/openai_compatible.rs`
  - 新增 `map_effective_deepseek_profile` 纯函数及参数快照类型。
  - `ReasoningMode::Off/Standard/Deep` 确定性映射到 DeepSeek `thinking.disabled/high/max`。
  - `Custom` 只接受 vendor parameter `reasoning_effort=high|max`。
  - 思考模式携带 `temperature/top_p`、任意 `seed` 或 token reasoning budget 时，返回明确 `InvalidRequest` 错误，不静默丢参。
  - 映射 `max_output_tokens`（优先 generation 参数，其次 budget envelope）和 JSON response format。

## 验证

- `cargo fmt --manifest-path desktop/src-tauri/Cargo.toml --all`：通过。
- `cargo test --manifest-path desktop/src-tauri/Cargo.toml --lib adapters::provider::openai_compatible::tests --locked`：通过。

## 需要同事配合

- **员工01**：将最终 EffectiveRunProfile 以稳定字段挂入 ModelRunRequest 后通知员工05；届时接入本纯函数并补请求 JSON/IPC 联调测试。
- **员工04**：确认运行控制在发送前调用映射错误并保持统一 ProviderError 展示，不静默删除不支持参数。
- **员工02**：无需数据库变更；运行档案持久化仍以员工01契约为准。

## 风险

- 当前纯函数尚未接入真实发送路径，不能宣称 PROV-M2-002 完整闭环或 M2 整体可验收。
