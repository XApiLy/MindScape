# 员工05｜M1 网络异常与用量门禁复核

时间：2026-08-20 15:00（UTC+8）  
任务：继续关闭 PROV-014/016/017 及 M1 真实 Provider 门禁

## 已阅读与落实

- 已重新阅读 `tauri-development`、`rust-best-practices`（代码风格、Clippy、错误处理、自动测试、注释章节）和 `vercel-react-best-practices`；本轮保持 Tauri release 验收边界、Result/安全错误契约、单行为回归测试和 React 生产构建不变。
- 已复核当前 PEC 七文件、`m1-alpha-acceptance-matrix-20260818.md`、`m1-pec-review-and-closeout-20260818.md` 与员工02当前真实证据；因此没有重复编译另一个验收版本，也没有把自动测试冒充真实网络证据。

## 本次推进

1. **真实长文/用量/事件复核：通过**
   - 同一 Build ID `20260820-142838-prov-empty-response-fix-0cb1c0053b5a-dirty` 的最新 DeepSeek 运行：`completed`、`finishReason=stop`、可见正文 3084 字符。
   - Usage：`input=22`、`output=1649`、`cachedInput=0`；`UsageUpdated` 与 `Completed.usage` 一致。
   - 事件 1648 条，序列连续，`started=1`、`usage_updated=1`、`completed=1`，无重复终态。
2. **自动门禁：通过**
   - `cargo test --all-features --locked`：49 passed。
   - `cargo clippy --all-targets --all-features --locked -- -D warnings`：通过。
   - `pnpm test:chat`：18 passed；`pnpm test:canvas`：13 passed；性能基线：200 节点通过。
   - `pnpm build`：通过；`check-secrets.mjs`（全工作树/仅 tracked）均通过；PEC 留存校验通过。
3. **已接收的同 Build ID 真实证据**
   - 员工02证据已确认错误 Key 安全提示、无 Mock 静默回退、无运行副作用。
   - 员工02证据已确认流式强退恢复为 `failed + application_interrupted`、部分内容保留、无永久 pending；此前用户取消证据也已确认唯一 `cancelled` 终态。

## 当前剩余真实动作

- 仅剩网络事实矩阵中的“流式中断网/异常断流”尚未有本轮窗口证据；连接、首 Token、空闲超时需要可控故障注入，当前正式 UI 没有安全的故障注入入口，不能伪造通过。
- 需要创始人配合：在同一 Build ID、真实 DeepSeek、长回答已经出现正文后临时断开网络 5～10 秒并恢复，记录窗口最终状态、是否有伪助手完成消息、是否出现重试入口。不要提供 Key、完整正文或数据库文件。
- 若网络恢复后运行正常完成，记录为“断网未命中”；若进入失败，员工05再只读核对错误码、事件唯一性和无永久 pending。

## 交付与安全

- 验收程序：`D:\Project\MindScape\artifacts\acceptance\versions\20260820-142838-prov-empty-response-fix-0cb1c0053b5a-dirty\mindscape-desktop.exe`
- SHA-256：`03B9B611511FEC24035D41F0A4F72F8F12BB3BAAD2377C0F81F7159958CEEC0A`
- 只读复核使用数据库 `integrity_check=ok` 与聚合字段；没有读取、记录或提交 API Key、request JSON、完整回答或原始事件正文。
- 当前验收版本仍标记 dirty；M1 最终 Go 仍需员工01统一 clean 集成基线与 Draft PR 收口。

## 协作请求（最多两个 WIP）

1. 请创始人完成一次“流式中断网→恢复网络”窗口动作，并只回报时间、窗口状态和是否保留正文。
2. 请员工01将本报告中的用量/事件通过证据纳入统一 M1 验收表；网络故障若未命中，不得标记为通过。
