# 员工05｜B1 新 Release 与 B5 真实测试待验收

> 提交时间：2026-09-02 15:08（UTC+8）  
> 关闭项：`B1-RELEASE` 二次 clean Acceptance Build  以及重复导入修复交付  
> 状态：**新 Release 已完成并启动；B5 真实纵向验收等待创始人操作结果**

## 已阅读与落实

- 已读取最新控制 PEC 与员工01《B1 二次 Clean 集成冻结》，确认本次冻结纳入重复导入幂等、跨会话错误、结构化错误可读化及弹窗滚动修复。
- 只从 clean HEAD 构建，不使用旧 `bd2ab97` Build、dirty Build 或开发服务器。

## 本次关闭的 B 编号

### B1-RELEASE：新唯一 Acceptance Build 已发布

- Commit：`85bdbb50f7e3`（`fix(m2): refresh clean acceptance integration`）
- Build ID：`20260902-150735-m2-b5-import-fix-85bdbb50f7e3`
- 程序：[mindscape-desktop.exe](D:/Project/MindScape/artifacts/acceptance/versions/20260902-150735-m2-b5-import-fix-85bdbb50f7e3/mindscape-desktop.exe)
- SHA-256：`2A213F53B035AE925E4B4FD762B3C4E54B5EB3941997D6E9B6B9C208503C5457`
- manifest 已确认 `sourceTreeDirty=false`、`tauri-release-no-bundle`；从归档路径启动成功，`localhost:1420` 无监听。

## 跨模块证据

1. 新 Build 包含 `ImportStorage duplicate → SQLite content-hash lookup → KernelService → Tauri import command → ImportIntakeDialog` 幂等链路；同会话重复内容复用既有来源，跨会话重复给出安全提示。
2. 导入、来源恢复、提案请求、审核和历史读取统一保留 `CommandError.safeMessage`，不再显示 `[object Object]`。
3. 冻结前门禁：Rust `245 passed / 0 failed / 1 ignored`，B5 audit `4/4`；Clippy、fmt、Chat `69/69`、Canvas `34/34`、性能 `2/2`、Vite `2100 modules`、PEC retention、secret scan 全部通过。
4. 已启动当前唯一 Release，进程路径指向上述 Build；未启动开发服务器。

## 剩余阻断与协作

1. **B5 尚未关闭**：请在当前 Build 完成真实导入 → 请求建议 → Confirm/Reject → FocusFrame candidate → 选择/生成 → Close/Reopen → 完全退出重启；随后我执行脱敏 SQLite/Vault 审计。
2. 需要特别复验：同一会话再次导入相同 `pasted-session.txt` 不应失败；应复用已有记录并继续选择消息。若跨会话导入相同内容，应看到明确安全提示。
3. Review Lab `full` 登记仍未自动接线；本 Release 不重复编译第二份程序，登记缺口继续保留。

## 下一步 WIP

1. 等待创始人完成当前 Build 的真实路径并反馈结果。
2. 保存同一 Build ID 的脱敏审计和纵向验收结论，提交员工01做 B5 Go/No-Go 评审。
