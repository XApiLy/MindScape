# 员工05｜B5候选生成复验与实体入口阻断

时间：2026-09-01 21:42（Asia/Shanghai）  
覆盖阻断：`B5`同一真实样本纵向验收  
状态：**候选选择/生成/重放工程链通过；真实导入到用户确认KnowledgeEntity仍无生产UI入口，B5继续No-Go**

## 已阅读与落实

- 已读取员工01候选生成重放审计、员工02真实来源与schema v17报告、员工03/04正式候选选择入口报告，并复核实际调用图和共享工作树。
- Rust/Tauri规范落实为完整targets/features/locked门禁、typed IPC与事务测试；未因测试通过而忽略生产UI不可达，也未手工修改正式SQLite/Vault造样本。

## 本次推进的B编号

### B5：关闭候选生成工程复验，保留真实实体入口阻断

- 已确认`ConversationCanvas → ChatWorkspace → App → kernelClient → Tauri command → KernelService → SQLite v17`接通候选多选、原子generation receipt、同ID重放、冲突与stale rollback。
- schema v17要求候选来自同会话、分支本地、candidate/inferred状态并携带可解析ImportContent EvidenceRef；前端筛选只负责呈现，内核事务内重新验证。
- 但生产调用图中`kernelClient.upsertKnowledgeEntity`没有任何UI调用方；ImportIntakeDialog只负责导入、Bundle/原文读取与预览，不生成或确认KnowledgeEntity。全新正式数据库无法从用户操作产生第一条可选实体，因此选择器仍为空。

## 跨模块证据

- Rust library `224 passed / 0 failed / 1 ignored`；B5 audit `4/4`；Clippy全targets/features/locked `-D warnings`和fmt通过。
- 前端Vite 2098 modules、Chat `63/63`、Canvas `34/34`、Canvas性能`2/2`；秘密扫描550个文本文件、PEC retention与`git diff --check`通过。
- 真实调用扫描：`upsertKnowledgeEntity`仅存在于`src/app/kernelClient.ts`定义；`src/App.tsx`、ImportIntakeDialog和正式组件均无调用。后端测试`import_backed_entity_enters_focus_promotion_with_resolved_evidence`证明Kernel能力，但不能替代用户入口。
- 当前共享树仍dirty，包含员工02～04未冻结实现与PEC变更；本轮没有发布Acceptance Build或覆盖旧B1版本。

## 仍剩阻断 / 需要配合

1. **员工01**：冻结“导入来源 → candidate/inferred实体建议 → 用户确认/保存”的正式领域入口；必须区分模型/规则生成的候选与用户确认，不能让前端直接拼KnowledgeEntity绕过EvidenceRef校验。
2. **员工02**：提供上述入口的原子持久化与真实ImportContent EvidenceRef解析，或明确现有哪个正式命令承担该职责；测试fixture不计生产入口。
3. **员工03/04**：在导入/知识库存正式UI消费该typed入口，让用户能看见来源、确认或否决实体，再进入现有FocusFrame候选选择器。
4. 完成后由员工01形成新的clean commit；员工05才发布新唯一Acceptance Build并重跑B5。当前dirty debug/preview均不能验收。

## 下一步WIP（最多两个）

1. 复核员工01冻结的实体建议/确认契约及员工02事务接入，防止导入正文被自动当成已确认知识。
2. 新clean Build到位后，从真实导入开始执行完整B5并请求创始人完成界面动作。
