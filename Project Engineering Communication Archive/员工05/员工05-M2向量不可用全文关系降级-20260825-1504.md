# 员工05｜M2 向量不可用时全文/关系降级

> 提交时间：2026-08-25 15:04（UTC+8）  
> 覆盖任务：PROV-M2-006 首切片  
> 状态：降级策略与纯测试完成；等待共享 FocusFrame 编译阻塞解除

## 已阅读与落实

- 已读取最新 PEC 控制文件及员工01～04、06当前报告。
- 员工02仍在 FocusFrame SQLite 生命周期底座，查询 IPC 尚未冻结；因此本次只在 Provider/检索适配边界实现降级，不改 SQLite、命令或 UI。
- 遵守 M2 规则：向量不可用时全文/关系仍可工作并提示；候选不自动成为事实，仍需经过 FocusFrame、scope、status 和证据规则。

## 本次完成与证据

- `desktop/src-tauri/src/adapters/provider/embedding.rs`
  - 新增 `RetrievalAvailability`、`RetrievalSource`、`RetrievalCandidate`、`RetrievalResult` 和 `RetrievalNotice`。
  - 新增 `merge_retrieval_candidates`：向量可用时保持 Vector 优先；不可用时跳过向量结果，合并 FullText/Relation，按分数排序、按 ID 去重并限制结果数。
  - 降级时返回安全提示，不暴露底层错误、文件路径或 Provider 信息。
- `desktop/src-tauri/src/adapters/provider/mod.rs` 导出检索边界供后续 SQLite/查询层消费。

## 验证结果

- 新增向量可用/不可用两组降级与优先级测试，代码已执行格式化和 diff 检查。
- 共享全量 Rust/定向测试当前被员工01并行 FocusFrame 接线阻塞：`application/mod.rs`、`commands.rs` 导入的 `crate::domain::FocusFrame` 尚未由 `domain` 导出；该错误不来自本次 Provider 文件。
- 阻塞解除后必须复跑 `cargo test --all-targets --locked`、Clippy 和前端门禁；本报告不把当前状态宣称通过。

## 契约、数据和安全影响

- 未新增网络调用、API Key、SQLite schema 或原文持久化。
- 降级结果只提供候选来源和安全 notice，不改变 KnowledgeEntity 状态，不把相似度当事实。

## 下一步 WIP（最多两项）

1. 等员工01修复 FocusFrame 导出后复跑全量 Rust/Clippy，并将 VectorMatch 映射评审为 `KnowledgeRetrievalCandidate` 输入。
2. 与员工02评审向量状态、FTS/关系查询结果在 SQLite/IPC 中的稳定 DTO。

## 需要同事配合

- **员工01**：修复并导出 `FocusFrame` 共享契约，解除当前 Rust 编译阻塞；确认候选进入领域编译器前的校验入口。
- **员工02**：提供 SQLite 查询层的向量不可用状态与全文/关系候选 DTO，不在存储层复制 Provider 降级规则。
- **员工03/04**：等待结构化查询 DTO 冻结后消费 `RetrievalNotice`，不要在 UI 重新推断降级原因。
