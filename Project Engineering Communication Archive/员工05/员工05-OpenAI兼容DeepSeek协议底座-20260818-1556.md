# 员工05｜OpenAI兼容DeepSeek协议底座

> 提交时间：2026-08-18 15:56（UTC+8）  
> 覆盖任务：PROV-005～008、PROV-013～017  
> 状态：协议底座已完成，等待真实Key与Windows Tauri联调  
> 当前分支：`docs/REL-002-pec-retention`（`.git`只读，无法建立目标分支或Draft PR）

## 相比上一份的变化

- 从开工审计推进到可编译、可测试的OpenAI-compatible真实HTTP/SSE Provider，并将DeepSeek注册到统一Provider注册表。
- 完成安全凭据解析、流式事件映射、取消、分层超时、用量归一化、无自动重试及脱敏错误分类。
- 修复SSE解码器在多字节UTF-8字符被网络分片时的破损问题。

## 已阅读与落实

- 已完整阅读V1范围、2026-08-17产品共识、V1总路线、M1派单、内核总览、前五人责任书、Context/Runtime Frozen V1、威胁模型、Git/PEC制度、凭据边界及员工01～04当前PEC；写入新PEC前已重新读取当前PEC六文件。
- 具体落实：适配器只消费冻结ContextSnapshot与当前输入，只输出Frozen V1统一事件；Key仅在Rust Provider内通过`CredentialService::resolve`读取；不记录请求体、密钥或厂商错误原文。
- 契约无冲突：员工01已冻结终态语义，员工02统一启动/恢复与员工04注册表UI可直接消费本次加法实现；但可信模型上下文窗口及真实端点矩阵仍需联调确认。

## 本次完成与证据

- `desktop/src-tauri/src/adapters/provider/openai_compatible.rs`：新增DeepSeek配置、HTTPS请求、SSE流、取消/超时和安全错误映射。
- `desktop/src-tauri/src/adapters/provider/sse.rs`：改用字节缓冲，覆盖CRLF/LF、半包、心跳、多行data、UTF-8跨分片和无空行收尾。
- `desktop/src-tauri/src/commands.rs`：注入`CredentialService`并注册DeepSeek真实Provider；`Cargo.toml/Cargo.lock`加入离线可解析的reqwest/tokio依赖。
- DeepSeek当前默认端点为`https://api.deepseek.com/chat/completions`，官方变更日志显示旧`deepseek-chat`已停用，注册模型更新为`deepseek-v4-flash`。

## 契约、数据、安全与恢复影响

- 未修改Frozen V1字段、枚举、SQLite schema或恢复语义；Provider注册为加法变更。
- 仅允许HTTPS端点；无Key、401/403、402、429、4xx/5xx和超时转为统一结构化错误，不回传可能含隐私的响应体。
- 首Token、流闲置和总运行预算分层超时；排队心跳不误延首Token截止时间；潜在计费请求不自动重试。
- 取消会中断正在等待的HTTP/流读取并产生唯一cancelled终态；用量事件在completed前归一化输出。

## 验证结果

- `cargo test --offline`：33/33通过。
- `cargo clippy --all-targets --all-features -- -D warnings`：通过。
- `pnpm test:chat`：5/5通过；`pnpm build`：通过（1821模块）。
- `git diff --check`：通过；SSE和Provider单测覆盖请求体、UTF-8分片、HTTPS约束及脱敏HTTP错误。

## 风险与阻塞

- 尚无写入操作系统凭据库的真实DeepSeek Key，因此未宣称真实成功/取消/错误矩阵验收通过。
- Provider注册表的`contextWindowTokens`仍为空，避免用未验证数值导致错误裁剪；需与员工01/02接入可信模型元数据。
- 全局离线Cargo缓存缺少futures 0.3.34源码，当前直接锁定缓存中的0.3.32以完成可重现编译；临时`.tmp-cargo-home`清理被环境安全策略拦截，该目录未进入Git变更。
- 共享工作树含多人并行变更；`.git`只读，无法建分支、提交或Draft PR。

## 下一步 WIP（最多两项）

1. PROV-005～008：在真实Windows Tauri窗口使用OS凭据完成连接测试和DeepSeek成功/无Key/错Key/无效模型矩阵。
2. PROV-013～017：补真实取消、首Token/闲置超时、用量和异常流终止联调，并接入可信上下文窗口。

## 需要同事配合

- **员工01/02**：评审Provider统一事件/错误终态和取消语义，并确认可信`contextWindowTokens`到上下文编译器的接线。
- **员工04**：在真实Tauri窗口从注册表选择`deepseek-v4-flash`，共同验证Key操作、发送、停止和结构化错误展示。
