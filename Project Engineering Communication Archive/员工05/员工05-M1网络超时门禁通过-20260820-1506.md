# 员工05｜M1 网络超时门禁通过

时间：2026-08-20 15:06（UTC+8）  
任务：关闭真实 Provider 网络抖动与流式超时门禁

## 已阅读与落实

- 已复核 M1 验收矩阵、M1 收口派单、员工02当前真实证据和验收程序规则。
- 按错误契约区分短暂断网恢复、应用中断和 Provider 流式超时；本次只将有明确 `stream_idle_timeout` 的结果计入网络超时通过。

## 创始人真实验收结果

- 同一验收 Build ID：`20260820-142838-prov-empty-response-fix-0cb1c0053b5a-dirty`。
- 流式回答已有正文后持续断网，超过空闲超时窗口；界面进入“模型响应超时”，已收到内容保留，并提供重试。
- 没有自动发起新的可能计费请求；恢复网络后可由用户显式重试。
- 同轮错误 Key 也已在员工02证据中通过：401 分类、安全提示、不回显 Key、连接测试不创建 ModelRun。

## SQLite 只读复核

- `PRAGMA integrity_check=ok`；`pending/streaming=0`。
- 最新超时运行：`failed`，`providerCode=stream_idle_timeout`，`category=timeout`，`partialContentRetained=true`，部分正文长度 82，唯一终态序列 50。
- 最新完整运行仍为 `completed/stop`，正文长度 3283，Usage 与完成事件一致。

## M1 Provider 门禁结论

员工05负责的真实 Provider 项目已通过：真实连接、完整长文、空正文修复、错误 Key、用户取消、应用强退恢复、短暂断网恢复、持续断网流式超时、用量/事件/SQLite 一致性、无永久 pending。

仍需员工01将本证据纳入统一 M1 验收表，并由其决定整体 Go/No-Go；本次未扩展导入器、多 Provider、Harness 或 P1 功能。

## 安全与交付

- 未读取、记录或提交 API Key、完整回答、request JSON 或原始事件正文。
- Build 为 dirty 工作树验收版；最终 M1 clean release 仍由员工01统一发布和引用。
