# 员工05｜M2 Provider 与索引脱敏边界

> 提交时间：2026-08-25 15:30（UTC+8）  
> 覆盖任务：PROV-M2-007  
> 状态：安全边界文档完成，等待员工01/02评审

## 已阅读与落实

- 已读取最新 PEC 控制文件、M2 派单、V1 威胁模型、凭据边界、RAG 方案及员工01～04、06当前报告。
- 文档落实了当前实现的实际边界：API Key 仅在 Rust 凭据服务，Embedding 默认本地零网络，VectorMatch 只能作为候选，Provider 只能消费冻结快照。
- 对员工02的 SQLite/索引接线保留接口边界；对员工01的 FocusFrame/Knowledge 编译器明确“候选不等于事实”。

## 本次完成与证据

- 新增 `docs/architecture/m2-provider-index-security.md`。
- 覆盖：数据分类与允许流向、Provider 网络控制、云端 Embedding 禁止隐式调用、向量版本与源哈希、FTS/关系降级、错误和日志脱敏、重建失败恢复、发布前检查。
- 明确禁止记录 API Key、Authorization header、原始 SSE、完整 prompt/reasoning、Provider 响应正文和向量调试转储。

## 验证结果

- `git diff --check`：通过。
- 本轮为文档变更；共享 Rust 全量门禁仍受员工01并行 FocusFrame 导出问题阻塞，未将其误报为本轮通过。

## 契约、数据、安全与恢复影响

- 无代码、SQLite、IPC、网络或凭据变更；文档仅固化现有 M2 安全边界。
- 该边界要求后续索引查询以 SQLite/领域状态再次过滤，不能仅依赖向量删除或索引状态。

## 风险与阻塞

- 文档需要员工01评审候选→领域编译器边界、员工02评审 metadata→SQLite 映射。
- `PROV-M2-005/006` 当前仍为内存适配器首切片，尚未完成持久化与真实 FTS/关系查询合并。

## 下一步 WIP（最多两项）

1. 等共享 FocusFrame 编译阻塞解除后复跑 Rust/Clippy，并补安全相关回归。
2. 与员工02/01评审并接入 VectorIndex/FTS/Relation 稳定 DTO，推进 RAG-M2-003。

## 需要同事配合

- **员工01**：评审候选不可越过 Knowledge/FocusFrame 领域规则的边界。
- **员工02**：评审索引 metadata、删除/重建和错误状态的 SQLite 映射。
- **员工04**：Provider/Index 错误展示只消费安全字段，不回显底层响应。
