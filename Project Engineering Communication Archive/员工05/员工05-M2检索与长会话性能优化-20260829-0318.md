# 员工05｜M2 检索与长会话性能优化

时间：2026-08-29 03:18（Asia/Shanghai）  
覆盖任务：RAG-M2 检索热路径／CHAT-M2 长会话渲染性能  
状态：代码与全量门禁完成；真实大数据量与长会话人工采样待统一 Release

## 已阅读与落实

- 已阅读 PEC 控制文件及员工01～04、06当前报告；员工02已将 Entity/FTS/Vector/MarkdownProjection 收敛为原子事务，本轮不再把旧事务风险列为代码阻塞。
- 按员工05职责复核 Provider、Embedding、VectorIndex 和 Markdown 流式阅读路径；按 Rust 性能规范先减少全量克隆、完整排序与中间集合，按 React 性能规范稳定 Markdown renderer 配置并跳过离屏历史卡片绘制。
- 本轮不改变知识状态、检索来源、排序、原始 Markdown、安全 renderer、流式终态或 IPC 契约。

## 相比上一份 PEC 的变化

- `LocalVectorIndex::search` 从“克隆全部匹配项并完整排序”改为：轻量 `(record index, score)` 评分、top-K 分区、只排序和克隆最终 K 项；空索引和 `limit=0` 直接返回。
- 向量／全文／关系候选改为迭代器单遍合并，不再为向量候选和 fallback 候选各创建中间 `Vec`。
- `SafeMarkdown` 的 components 映射提升为模块级稳定常量，避免每次流式更新创建新映射。
- 已完成历史卡片启用 Chromium `content-visibility: auto` 与固有高度占位；运行中卡片排除在外，停止、超时、流式跟随语义不变。
- 新增 1000 记录 top-K 回归，证明优化结果与全量有序结果前 10 项完全一致，并覆盖零 limit。

## 验证证据

- Rust：160/160。
- 新 top-K 定向样本：1000 条记录通过，结果与全量排序一致。
- Chat：50/50；Canvas：34/34；Canvas 性能：2/2。
- 200 节点样本：初始投影约 `4.724ms`，5000 次平移约 `1.502ms`，5000 次缩放约 `1.955ms`，1000 次选择约 `5.929ms`，50 次流式投影约 `160.868ms`。
- TypeScript/Vite production：2094 modules，通过。
- Clippy 全目标／全特性 `-D warnings` 与专项 `-D clippy::perf`、Rust fmt、秘密扫描（1568 文本文件）、`git diff --check`：通过。

## 契约、安全、恢复与风险

- top-K 仍以分数降序、ID 升序稳定裁决；Embedding provenance 仅延迟克隆，不删除或重写。
- 向量不可用时全文／关系 fallback 行为不变；持久记录损坏与重建路径不变。
- 离屏优化只作用于已完成历史 DOM，不修改 raw Markdown、复制文本或数据库内容。
- 当前共享树仍为 dirty；本轮未重复发布 Release。需在代码冻结后基于同一构建做真实 1k+ 知识记录、多轮长 Markdown 会话和内存峰值采样后，才能声明量化性能验收完成。

## 需要配合与下一步 WIP

1. 员工03/04：在下一统一 Release 中复核长会话滚动锚点、定位到最新、复制正文与聚焦阅读，确认 `content-visibility` 未造成可见跳动。
2. 员工05：补真实 1k+ 持久向量检索延迟/峰值内存与长流式 Markdown 帧稳定性证据，再决定是否引入跨查询索引缓存；缓存失效契约未证明前不提前加入全局状态。

