# 员工05｜Tokio网络驱动修复与DeepSeek重编译

> 提交时间：2026-08-18 16:45（UTC+8）  
> 覆盖任务：PROV-005～008、PROV-013～017  
> 状态：用户发现的网络I/O panic已修复，修复版EXE已就绪

## 相比上一份的变化

- 真实连接测试暴露Tokio runtime只启用`time`、未启用I/O驱动，`reqwest`DNS/网络请求因此panic。
- `tokio`新增`net`特性，连接测试与真实流式生成统一通过`build_provider_runtime()`启用`enable_io + enable_time`。
- 新增网络驱动回归测试；因旧EXE被用户运行中锁定，修复版输出到独立`target-fixed`目录。

## 已阅读与落实

- 已重读PEC控制文件与员工01～06最新报告，复核用户提供的DeepSeek官方中文文档。
- 官方文档确认连接检查使用`GET /models`，因此不修改厂商URL；故障仅在本地Tokio运行时配置。
- 修复同时覆盖连接测试和Chat Completions，不改动Frozen V1事件、凭据、脱敏或无计费重试边界。

## 本次完成与证据

- `desktop/src-tauri/Cargo.toml`：Tokio特性加入`net`。
- `desktop/src-tauri/src/adapters/provider/openai_compatible.rs`：统一启用I/O与计时驱动，新增`provider_runtime_enables_tokio_network_io`。
- 修复版产物：`D:/Project/MindScape/desktop/src-tauri/target-fixed/debug/mindscape-desktop.exe`。

## 契约、数据、安全与恢复影响

- 无领域契约、SQLite schema或恢复语义变化；仅修正Provider本地执行器。
- Key仍仅由Windows凭据库解析，不读取/记录失败响应体，不自动重试生成请求。

## 验证结果

- `cargo test --offline --no-fail-fast`：42/42通过，包含I/O驱动回归测试。
- `cargo clippy --offline --all-targets --all-features -- -D warnings`：通过。
- `pnpm test:chat`：13/13通过。
- `pnpm tauri build --debug --no-bundle`：修复版独立构建通过，前端1823模块。

## 风险与阻塞

- 需用户关闭旧版窗口后启动`target-fixed`中的修复版；否则旧进程仍会展示已修复的panic。
- 真实连接、生成、取消、超时和用量矩阵尚待用户在修复版内再次执行。
- `.git`只读仍阻止提交与Draft PR。

## 下一步 WIP（最多两项）

1. 用户关闭旧版，启动修复版并再次点击“测试连接”。
2. 连接成功后验收首次真实生成、流式停止、用量与异常断流。

## 需要同事配合

- **用户**：关闭旧窗口并从新路径启动；只反馈“连接成功”或脱敏错误，不发送完整Key。
- **员工01/02/04**：连接成功后继续真实终态、预算和恢复矩阵验收。
