# 员工05｜M2 Provider 能力目录首切片

> 提交时间：2026-08-24 17:46（UTC+8）  
> 覆盖任务：PROV-M2-001  
> 状态：实现完成，等待共享 Rust 基线恢复后全量复验  
> 基线提交：`98e12706c2f4` 后共享 M2 工作树

## 相比上一份的变化

- M1 Provider 门禁已完成，正式进入 M2。
- 完成员工05唯一主交付的第一切片：扩展 Provider 模型能力目录及 TypeScript IPC 镜像。
- Effective Run Profile 已由员工01并行加入权威契约；本切片不自行创建另一份运行档案，只提供后续确定性能力校验所需的模型快照。

## 已阅读与落实

- 已阅读当前 PEC 控制文件、M2 派单、V1 范围/产品共识/总路线/Core Kernel/前五员工职责、RC1、威胁模型、Git 与 PEC 制度，以及两份 M2 知识/Markdown 架构方案和模型控制方案。
- 已读取员工01、02、04、05当前 PEC；落实为只修改 Provider 能力目录、OpenAI-compatible/Mock 适配器和前端镜像，不进入导入、Vault、FocusFrame UI 或数据迁移。
- 已使用 Tauri 与 Rust 工程规范；新增公共类型采用 Rust 权威定义、serde camelCase、TypeScript 同步镜像、描述性单元测试和 Clippy 门禁。
- 冲突判断：员工01的 `EffectiveRunProfile.reasoning_mode` 是跨厂商运行选择；员工05的 `ProviderReasoningMode` 是厂商能力集合，已明确改名避免两者类型冲突。

## 本次完成与证据

- `desktop/src-tauri/src/adapters/provider/registry.rs`
  - 新增 `ReasoningControl`、`ProviderReasoningMode`、`ParameterSupport`、`GenerationParameterCapabilities`、`InputModality`。
  - `ModelCapabilities` 增加 reasoning、结构化输出、生成参数和输入模态快照。
- `desktop/src-tauri/src/adapters/provider/openai_compatible.rs`
  - DeepSeek V4 声明 `disabled/high/max`、effort 控制、JSON 结构化输出、文本输入。
  - `temperature/top_p` 标记为 `nonReasoningOnly`，防止思考模式下静默发送无效参数；`seed` 标记不支持。
- `desktop/src-tauri/src/adapters/provider/mock.rs`
  - Mock 明确声明无 reasoning/结构化输出能力，不伪装真实 Provider 能力。
- `desktop/src/domain/provider.ts`、`desktop/src/App.tsx`、`desktop/src/app/providerCatalog.test.ts`
  - 同步 Tauri IPC 的前端镜像和 Preview/测试夹具。
- 新增两项回归：DeepSeek 条件参数能力、camelCase IPC 序列化快照。

能力事实依据 DeepSeek 官方 Chat Completion 与 Thinking Mode 文档：V4 支持 `thinking.enabled/disabled`、`reasoning_effort=high/max`、JSON Output、tools、temperature/top_p；思考模式下 temperature/top_p 不生效。当前 `tool_calling` 仍保持 false，因为 MindScape 运行时尚未实现工具事件闭环，不能把厂商原生能力冒充为本地已实现能力。

## 契约、数据和安全影响

- 这是 ProviderDescriptor 的加法字段；Rust 与 TypeScript 镜像同步，不修改 M1 运行终态、取消、错误、SQLite schema 或凭据边界。
- 不记录 reasoning 正文，不读取/发送 API Key，不调用云端 Embedding。
- Effective Run Profile 的 DeepSeek 请求映射进入下一切片，必须消费员工01最终契约并先做不支持参数预检。

## 验证结果

- Rust 库测试：55 passed（包含员工05新增测试）。
- 前端生产构建：通过；Chat 18 passed；Canvas 14 passed；200 节点性能基线通过。
- 全工作树与 tracked 秘密扫描：通过；`git diff --check`：通过。
- 全量 Rust 暂未绿：员工01并行新增的 `domain/focused_context.rs` 当前存在 `HashSet<&str>` / `String` 借用类型错误及格式差异；本报告不把共享失败写成员工05通过。

## 风险与阻塞

- ProviderDescriptor 是跨模块公共接口，合并前需员工01与员工04评审字段语义和前端消费方式。
- `structured_output=true` 表示厂商模型能力；发送前仍必须由 Effective Run Profile 映射层验证 prompt/response format，当前切片不启用该请求参数。
- 当前共享工作树含多员工 M2 并行改动，员工05不得单独发布验收 Build 或声明整体 M2 可运行。

## 下一步 WIP（最多两项）

1. `PROV-M2-002`：员工01 Effective Run Profile 契约稳定后，实现 DeepSeek thinking/effort/生成参数确定性预检与请求映射。
2. 评审/联调：等待员工01修复 focused context 基线，复跑 Rust 全量门禁并核对员工04运行控制消费字段。

## 需要同事配合

- **员工01**：评审 Provider 能力字段与 Effective Run Profile 的映射边界；修复 `focused_context.rs` 后通知员工05复跑全量 Rust。
- **员工04**：仅消费能力快照显示支持/不支持状态；在 PROV-M2-002 前不要静默删除参数或自行推断 DeepSeek 思考模式。
- **员工02**：当前无 schema 变更；后续运行档案持久化继续以员工01契约和员工02迁移为准。
