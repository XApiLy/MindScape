# 员工05｜B1 Clean Release 候选门禁

时间：2026-08-31 22:13（Asia/Shanghai）  
覆盖阻断：`B1` 员工05统一回归与唯一 Release 责任  
状态：**候选代码门禁全绿；clean 集成提交尚未由员工01交付，因此未发布 Acceptance Build，B1仍未关闭**

## 已阅读与落实

- 已重新阅读控制 PEC 的 B1/B5 执行表、2026-08-31现场复审、Acceptance Build规则，以及员工01《B2/B3评审关闭与B1冻结入口》。严格执行“员工01冻结单一 clean commit，员工05只从该 commit 发布一次”的责任边界。
- 已落实发布规则：没有运行会产生 `-dirty` Build ID 的发布脚本，没有从 `target/`交付 EXE，也没有把门禁通过写成 clean Release 已完成。
- Rust/Tauri规范继续影响本轮：执行全 targets/features/locked 的测试和 Clippy，并要求最终产物必须由 `pnpm tauri build --no-bundle` 经标准脚本生成、无开发服务器独立启动。

## 本次推进的 B 编号

### B1：Release 候选门禁已通过，clean 来源仍阻断

- Rust：library `205 passed / 0 failed / 1 ignored`；B5 audit example `2 passed / 0 failed`。ignored 为需显式真实模型包的固定质量测试，B4已在上一轮单独用 Release 通过。
- `cargo clippy --all-targets --all-features --locked -- -D warnings`、`cargo fmt --all -- --check`：通过。
- TypeScript/Vite production build：2097 modules，通过；Chat `60/60`、Canvas `34/34`、Canvas性能 `2/2`。
- 脱敏扫描：`1646`个文本文件通过；PEC retention 与 `git diff --check`通过。
- 本轮没有用户可见功能变化，只执行候选门禁与发布前审计，因此不发布新的 Review Lab preview。

## 跨模块证据

- 同一次候选回归覆盖 Provider终态/取消/错误、B2四动作typed UI、SQLite/Vault journal与重启、B3 EvidenceRef/Wikilink/Discussion、B4 hybrid retrieval与semantic stale/rebuild、Chat/Canvas/字体和B5审计工具。
- B5 audit example随 `--all-targets --all-features --locked`执行，证明审计工具与当前Kernel/Vault契约能共同编译并通过，不是孤立脚本。

## 当前精确阻断

- HEAD：`f4759c21460cd206e64ee56f8b8ac689216fd1bc`（`docs(pec): close B3 engineering review`）。
- 工作树仍有`56`条tracked变化、合计`1715`条状态。大头包含`example/`外部视觉参考、`output/playwright/`生成物、Review Lab/视觉工具、历史PEC归档，以及员工02～05正式实现；这些不能由员工05用`git add -A`无归属地并入发布提交。
- 员工01最新PEC明确其下一步是“共享树清点、生成物隔离和clean集成冻结”。在其交付单一clean commit前，标准脚本只会生成`-dirty`版本，不能关闭B1。

## 仍剩阻断 / 需要配合

1. **员工01**：完成文件归属审计，排除`.playwright-cli/`、`output/`、外部参考仓和其他生成物，形成并交付唯一clean集成commit；同时确认应纳入的字体、docs、PEC归档、B2/B3/B4代码和B5审计工具。
2. **员工05**：收到clean commit后立即复核`git status`为空，运行`scripts/publish-acceptance.ps1`，记录唯一Build ID、EXE、SHA-256、包体积和无`localhost:1420/1428`独立启动，再进入B5真实样本。

## 下一步 WIP（最多两个）

1. 等待并校验员工01的clean集成commit；只处理冻结后确定性回归。
2. 从该commit发布唯一Acceptance Release并执行B5纵向验收，随后补Review Lab `full`登记缺口说明。
