# 员工05｜B5 真实样本入口阻断

时间：2026-09-01 10:50（Asia/Shanghai）  
覆盖阻断：`B5`同一真实样本纵向验收  
状态：**B1已关闭；B5基线审计通过数据完整性但发现正式用户无法生成首个回流候选，B5仍为No-Go**

## 已阅读与落实

- 已读取控制PEC的B5完成证据、M2纵向验收矩阵、员工01clean冻结报告和只读B5审计工具；固定使用Build `20260901-104100-m2-b1-clean-release-e40dde0af587`，没有重新编译Release。
- Rust审计规范落实为SQLite `query_only`、只输出稳定ID短指纹、不打印正文；未直接修改创始人的正式数据库或Vault，也未用测试夹具冒充用户动作。

## 本次推进的B编号

### B5：真实数据基线审计已执行，入口阻断已定位

- 正式数据：`schemaVersion=16`、`integrityCheck=ok`、foreign key violations `0`。
- Vault实体索引存在；Focus、通用删除、Discussion三类pending journal均为`0`。
- 正式数据库`decisionCount=0`，四动作集合为空；审计按设计返回失败：`the acceptance database does not contain all four B2 actions`。
- 代码复核确认正式UI创建FocusFrame时将`memoryScope.promoteRefs`固定为空；内核因此不会返回正式候选。四动作按钮仅在`get_focus_promotion_candidates`已有候选时呈现，用户无法从当前产品入口创建首个候选。

## 跨模块证据

- clean Release真实启动 → 正式AppData SQLite/Vault → 只读B5 audit，证明审计针对实际验收程序数据而非单元测试TempDir。
- `App.createFocusFrameForNode`固定空`promoteRefs` → Kernel lifecycle门禁 → `get_focus_promotion_candidates`空态 → Canvas四动作UI不可达；这是React/Tauri/领域契约/SQLite的跨模块入口缺口。

## 阻断与需要配合

1. **员工01主责领域入口**：冻结用户如何从已确认KnowledgeEntity显式选择`promoteRefs`，且不能让前端伪造不存在的候选。
2. **员工03/04 UI协作**：在现有FocusFrame创建/编辑流程提供受控候选选择，并保持Active隐藏、Closed显示及typed IPC；不得增加测试专用后门。
3. **员工02数据协作**：确认候选来源实体/EvidenceRef能从真实导入与确认链进入该选择，不能要求手工改SQLite。
4. 修复需形成新的clean commit和新的唯一Acceptance Build；旧Build保留为B5失败证据，不覆盖、不冒充通过。

## 下一步WIP（最多两个）

1. 等员工01冻结正式候选生成入口后，复核其不绕过领域/证据/分支隔离边界。
2. 新clean Build到位后重新从导入开始执行B5；届时只请求创始人做可见界面动作，不要求修改数据库。
