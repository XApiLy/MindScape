# 员工05｜B4 真实语义检索跨模块闭环

时间：2026-08-31 10:23（Asia/Shanghai）  
覆盖阻断：`B4` 员工05主责（Embedding 适配、质量样本、失败回退）  
状态：**B4 员工05主交付与员工02数据协作闭环已关闭；仍待员工01接口评审，M2 继续 No-Go**

## 已阅读与落实

- 已重新阅读控制 PEC 顶部“2026-08-30：M2 Go 阻断收敛”、员工直接执行表，以及员工01、02、04当前 PEC；本轮只关闭 B4，不把已通过的 M1/字体/局部性能切片重复计入完成。
- 已复核 M2 派单、Embedding/VectorIndex 架构边界、威胁模型和员工05长期职责：模型包必须由用户显式安装，普通启动不联网；正文只在本机推理，不写日志、不进入普通数据库；模型缺失、损坏、安装或推理失败均诚实回退 FTS/Relation。
- `rust-best-practices` 使模型安装、校验、加载、推理和索引替换全部使用显式 `Result` 与有限错误面，不在生产故障路径 panic；`tauri-development` 使模型状态/安装保持 typed IPC，模型事实与密钥不下放前端。

## 本次关闭的 B 编号

### B4：真实语义检索主交付已关闭

- 固定本地模型为 `Qdrant/paraphrase-multilingual-MiniLM-L12-v2-onnx-Q`，revision `faf4aa4225822f3bc6376869cb1164e8e3feedd0`，运行版本 `paraphrase-multilingual-minilm-l12-v2-q-faf4aa4`，Apache-2.0，384 维；固定模型、tokenizer、配置和 ONNX Runtime 文件的字节数与 SHA-256。
- 设置页提供明确的约 330 MB 本地语义模型安装动作和 missing/corrupt/ready 状态。下载进入 staging，全部校验后原子切换；官方源不可用时只允许使用同内容镜像并继续以固定 SHA 验证。
- ONNX Runtime 动态加载，未安装模型不影响应用启动；普通状态检查不联网。完整校验后写入仅包含版本、文件名、大小、mtime 的本地验证标记，热启动按元数据快速验证；任何大小或时间变化都会回到完整 SHA 校验。
- 查询使用真实 384 维预计算向量；索引缺失、stale、模型/维度不符或记录不完整时按当前 Confirmed 实体懒重建。模型不可用、查询推理失败或重建失败时明确走 FTS/Relation，不再让 32 维 hash 占位进入生产语义路径。
- 接入员工02 SQLite v15：真实模型版本、维度、ready/stale 与记录原子持久化；实体更新触发 stale，否决/删除清理派生记录，重启后恢复或按源版本重建。

## 跨模块证据

- 真实中文非字面质量矩阵（固定生产模型）：
  - “外面下雨需要带伞吗”更接近“出门前请携带雨具，以免被淋湿”，而非 SQLite 文本。
  - “怎样避免讨论结论在重启后消失”更接近“写入本地持久化存储”，而非公园散步。
  - “轻量单机应用应该把结构化数据放在哪里”更接近 SQLite，而非雨具文本。
  - Release 热启动加载加 7 条中文向量推理 `1609 ms`，测试总计 `1.78 s`；三组断言全部通过。
- `semantic_vector_recall_returns_the_confirmed_evidence_ref`：384 维语义向量 → VectorIndex → hybrid retrieval → Confirmed KnowledgeEntity，最终 `rain-guidance` 候选保留正确 `evidence-rain`、模型版本、维度和向量来源。
- `semantic_rebuild_rejects_stale_source_and_survives_restart`（员工02）：KnowledgeEntity/source hash → SQLite v15 原子替换 → 旧源拒绝 → 当前向量提交 → 重启恢复 → Provider semantic retrieval，证明持久化与重建不是内存假象。
- `present_but_invalid_pack_is_reported_as_corrupt`、`missing_pack_is_reported_without_creating_it`、`corrupt_vector_snapshot_falls_back_and_rebuild_restores_vector_retrieval` 覆盖模型缺失/损坏、索引损坏、回退与修复；安装失败不会替换当前有效目录。

## 验证结果

- Rust：`181 passed / 0 failed / 1 ignored`；ignored 即需显式下载真实模型的质量测试，已单独在 Debug 与 Release 各通过一次。
- Release 真实模型质量：`1 passed / 0 failed`；推理 `1609 ms`，测试 `1.78 s`。
- `cargo fmt -- --check`、`cargo clippy --lib -- -D warnings`：通过。
- TypeScript/Vite production build：2096 modules，通过；Chat `57/57`、Canvas `34/34`、Canvas 性能 `2/2`。
- 本轮未发布验收程序：共享工作树仍 dirty，不能冒充 B1 clean Release。

## 契约、数据、安全与恢复变化

- 新增语义模型包状态和安装 typed IPC；Embedding contract 从生产禁用的 32 维 hash 占位切换为固定 384 维真实模型，不改变 EvidenceRef 与 KnowledgeEntity 领域真相。
- 模型包位于应用数据 `models/`，不捆绑进安装程序；用户不点击安装时不下载。验证标记不含正文、查询、密钥或网络响应。
- 同尺寸且能伪造文件 mtime/验证标记的本机恶意写入不属于普通损坏模型；首次安装始终做完整 SHA，元数据不符也重新做完整 SHA。员工01应评审该本地信任边界和 v15 接口。

## 仍剩阻断 / 需要配合

1. **B4 只剩员工01评审**：请评审固定模型契约、元数据快速验证边界、SQLite v15 stale/rebuild 语义及“生产路径禁止 hash 降级”。若评审无接口问题，B4 可整体标记关闭。
2. **B1/B2/B5 仍未关闭**：B1 缺 clean 集成提交与唯一 Release；B2 仍缺正式候选确认/否决/回流 UI+原子动作闭环；B5 必须等 B1/B2 后在同一 clean Release 运行纵向真实样本。B6 不抢占这些主阻断。

## 下一步 WIP（最多两个）

1. 配合员工01完成 B4 接口评审并只修复评审发现的阻断问题。
2. 等 B1 clean 冻结与 B2 闭环后，执行 B5 同一真实样本矩阵和唯一 Release 全量回归；此前不发布个人 dirty Build。
