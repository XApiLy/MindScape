# 员工05｜RAG-M2 索引失效与源级删除

> 提交时间：2026-08-26 15:32（UTC+8）  
> 覆盖任务：PROV-M2-005～006、RAG-M2-003 前置  
> 状态：本地失效策略完成，等待 SQLite 事务接线

## 已阅读与落实

- 已读取最新 PEC 控制文件及员工01～04、06当前报告。
- 员工02已完成知识查询 IPC，并明确员工05负责 FTS/VectorIndex/Relation 查询与失效重建；统一检索 DTO 尚未冻结，因此本次继续在 Index 适配器边界推进，不改 SQLite/IPC。
- 员工01已冻结 KnowledgeEntity/Relation/EvidenceRef 校验；本次只删除派生向量，不改变领域对象状态。

## 本次完成与证据

- `desktop/src-tauri/src/adapters/provider/embedding.rs`
  - 新增 `LocalVectorIndex::remove_source(source_hash)`，按原始来源哈希删除全部派生向量并返回移除数量。
  - 新增 `LocalVectorIndex::remove_stale(adapter, chunk_version)`，移除模型版本、维度、向量长度或 chunk version 不匹配的陈旧记录。
  - 新增回归覆盖来源删除和版本变化后的索引失效。

## 契约、数据、安全与恢复影响

- 仅修改内存派生索引，不删除原文、SQLite 领域事实、EvidenceRef 或知识修订。
- 返回移除数量供未来 SQLite 事务、日志和重建审计使用；不返回向量正文或用户内容。
- rejected/superseded/stale 等领域状态仍由员工01/02权威规则触发 ID/source 失效，不由 VectorIndex 自行推断。

## 验证结果

- `cargo test --manifest-path desktop/src-tauri/Cargo.toml --all-targets --locked`：122/122 通过。
- `cargo clippy --manifest-path desktop/src-tauri/Cargo.toml --all-targets --all-features --locked -- -D warnings`：通过。
- `cargo fmt --all`、`git diff --check`：通过。

## 风险与阻塞

- 当前失效操作尚未与员工02 SQLite 知识事务绑定；进程重启后的持久化索引失效仍待数据层接线。
- 统一 evidence/scope/provenance 检索 DTO 尚未冻结，不能宣称 RAG-M2-003 完整闭环。

## 下一步 WIP（最多两项）

1. 与员工02将 source/id/status 变化映射到 VectorIndex 删除与重建事务，补重启回归。
2. 与员工01冻结统一 KnowledgeRetrievalProjection 后接入 Vector/FTS/Relation 候选合并和领域过滤。

## 需要同事配合

- **员工02**：提供知识对象状态变更事务钩子和索引持久化入口。
- **员工01**：冻结 unified retrieval DTO 的 evidence/scope/omitted reason 字段。
- **员工04**：只显示结构化失效/降级结果，不自行推断删除或否决状态。
