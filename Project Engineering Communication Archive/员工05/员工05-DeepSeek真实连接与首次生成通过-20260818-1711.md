# 员工05｜DeepSeek真实连接与首次生成通过

> 提交时间：2026-08-18 17:11（UTC+8）  
> 覆盖任务：PROV-005～008、PROV-013～017  
> 状态：DeepSeek真实连接与首次Chat生成已通过，剩余可靠性矩阵待验收

## 相比上一份的变化

- 用户在修复版Windows窗口中完成DeepSeek真实`GET /models`鉴权检查，返回`deepseek-v4-flash`与`deepseek-v4-pro`。
- 首次`deepseek-v4-flash` Chat Completions真实请求成功，正式节点显示回答且运行终态为`completed`。
- Tokio I/O修复已获得真实网络证据，不再只是离线单测证据。

## 已阅读与落实

- 已重读PEC控制文件及员工01～06最新报告，本轮仅记录M1真实Chat Alpha联调结果。
- 已落实不过度宣称：截图证明连接、真实回答与completed；未展示用量数值，不标记用量矩阵通过；未测停止/超时/断流/重启，不标记对应门禁通过。
- Key仍未出现在界面回显、截图、PEC、日志或数据库证据中。

## 本次完成与证据

- 真实连接：设置页显示“DeepSeek连接成功”并列出2个可用模型。
- 真实生成：用户发送“请只回复：MindScape DeepSeek 联调成功”，`deepseek-v4-flash`返回目标文本。
- 运行投影：阅读视图显示`COMPLETED`、真实模型ID和正式助手内容。
- 先前自动验证仍有效：Rust 42/42、Clippy严格、Chat 13/13与Windows修复版编译均通过。

## 契约、数据、安全与恢复影响

- 本轮无代码、领域契约或schema变更，只更新真实契约验收状态。
- 真实回答沿统一ModelRunEvent和Node终态链路呈现，未出现Mock降级或厂商SSE泄漏到UI。

## 验证结果

- DeepSeek真实连接：通过。
- DeepSeek V4 Flash首次真实生成：通过。
- completed终态与真实模型标识：通过。
- PEC七文件窗口与`git diff --check`：本报告替换后执行。

## 风险与阻塞

- 尚未完成真实流式中停止、首Token/闲置超时、服务器异常断流、限流/余额/错Key和重启恢复矩阵。
- 用量数据未在当前截图显示，需从运行投影或后续UI证据确认`UsageUpdated/Completed.usage`。
- `.git`只读仍阻止提交与Draft PR。

## 下一步 WIP（最多两项）

1. PROV-013～017：真实长回答中点击停止，验收网络取消、cancelled唯一终态和部分内容保留。
2. PROV-014/016：验证超时、异常断流、用量落库与重启后投影一致。

## 需要同事配合

- **用户/员工04**：下一步发起明显长回答并立即点击停止，提供cancelled界面证据。
- **员工01/02/03**：评审completed节点、运行落库、画布/阅读投影与重启恢复证据。

