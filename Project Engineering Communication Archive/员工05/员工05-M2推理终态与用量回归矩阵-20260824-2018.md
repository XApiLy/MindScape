# 员工05｜M2 推理终态与用量回归矩阵

> 提交时间：2026-08-24 20:18（UTC+8）  
> 覆盖任务：PROV-M2-003  
> 状态：Provider 回归矩阵完成，等待真实 Key 验收窗口

## 已阅读与落实

- 已读取最新 PEC 控制文件及员工01～06当前报告。
- 员工04已确认 `effectiveRunProfile` 已从 UI 草稿进入真实 IPC；因此本次在已接线链路上补 Provider 终态矩阵，不修改 UI、SQLite 或导入边界。
- 继续保持 reasoning 与 visible content 分离：reasoning-only 不得伪装成功正文，Provider 终态错误优先于部分正文成功。

## 本次完成与证据

- `desktop/src-tauri/src/adapters/provider/openai_compatible.rs`
  - 新增 reasoning-only Stop → `empty_visible_response` 失败回归。
  - 新增 reasoning + visible content → Completed 且完整保留 usage 回归。
  - 新增 Provider 终态错误覆盖部分正文的回归。
  - 与已有 reasoning-only length、visible partial length、参数预检、请求 JSON 序列化测试共同形成 M2 首个 Provider 终态矩阵。

## 验证结果

- `cargo test --all-targets --locked`：79/79 通过。
- `cargo clippy --all-targets --all-features --locked -- -D warnings`：通过。
- `cargo fmt --all`、`git diff --check`：通过。

## 契约、数据和安全影响

- 仅新增 Provider 纯逻辑测试，不修改运行终态枚举、SQLite schema、凭据或网络错误安全消息。
- 测试证明 reasoning 内容不会进入 `TextDelta`，只有 visible content 才能使运行成功。

## 风险与阻塞

- 尚未使用真实 DeepSeek Key 做联网验收；流式 usage、断网恢复和取消仍需用户/员工04在桌面程序中配合验证。
- 本报告不宣称 M2 整体验收完成。

## 下一步 WIP（最多两项）

1. 与员工04进行真实 DeepSeek Off/Standard/Deep/Custom、usage 和错误提示联调。
2. 根据真实流式样本补充 SSE fixture，核对 reasoning/content/usage 到 UI 的完整投影。

## 需要同事配合

- **员工04**：提供真实 Key 验收窗口，覆盖参数冲突、空正文、限额和取消/断网恢复。
- **员工01**：复核运行档案终态与 FocusFrame 恢复后的投影字段。
- **员工06**：仅在真实状态字段稳定后调整表现层，不改变 Provider 事件语义。
