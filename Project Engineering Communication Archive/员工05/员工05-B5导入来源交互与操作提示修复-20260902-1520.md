# 员工05｜B5 导入来源交互与操作提示修复

> 提交时间：2026-09-02 15:20（UTC+8）  
> 覆盖：`B5-UI` 来源点击闪退、提案操作不可见与导入使用指引  
> 状态：**代码修复与预览已完成；等待员工01再次 clean 冻结后纳入 Acceptance Release，B5 仍未关闭**

## 已阅读与落实

- 已读取控制 PEC 的 B1/B5 执行表，以及员工01《B1 二次 Clean 集成冻结》、员工04《B5 导入弹窗可视高度修复》。
- 遵守前端 UI-HANDOFF-06 与 React 性能边界：只修正状态承载与功能提示，不改变 ImportSource/Proposal/ EvidenceRef 契约，不读取本地路径或正文之外的数据。

## 本次推进

- 修复来源点击闪退：加载新的 ImportBundle 时保留当前预览，直到新结果成功或错误明确返回；不再先清空 `bundleResult`。
- 在“源文件”视图补充固定下一步提示：切回“安全渲染”或“Markdown 原文”后，勾选消息并生成知识建议。
- 保留之前已完成的重复内容幂等与 `[object Object]` 可读错误修复。

## 跨模块证据

1. `import source button → loadBundle state → ImportBundlePreview`：加载期间不再出现空白闪退；异步失败仍保留可见旧状态与安全错误。
2. Vite `2100 modules`、Chat `69/69` 通过；Review Lab preview `20260902T072135Z-导入来源点击与下一步提示修复-461a` 状态 `ready`。
3. 当前唯一已发布 Release `20260902-150735-m2-b5-import-fix-85bdbb50f7e3` 未被覆盖；本轮改动尚未进入 Acceptance EXE。

## 用户操作路径

1. 启动最新 Acceptance Build，点击“导入已有 AI 会话”。
2. 选择“粘贴会话文本”，粘贴内容后点击一次“生成本地预览”。
3. 在“本地导入预览”中点击“安全渲染”或“Markdown 原文”；不要停留在“源文件”视图。
4. 在消息标题左侧勾选一条或多条消息。
5. 在“从来源提炼知识”区域选择知识落点，点击“生成知识建议”。
6. 对生成的建议执行 Confirm 或 Reject；若已存在相同来源，点击“查看预览”恢复，不要重复生成导入。

## 剩余阻断与协作

1. **Release 接入**：本轮改动使工作树重新 dirty；请员工01再次冻结后，员工05才能发布包含本修复的新唯一 Acceptance Build。
2. **B5 真实验收**：新 Build 上仍需完成导入 → 建议 → Confirm/Reject → FocusFrame candidate → 选择/生成 → Close/Reopen/重启，并运行脱敏审计。
3. Review Lab `full` 登记接口仍未接通；不为登记重复编译第二份 EXE。
