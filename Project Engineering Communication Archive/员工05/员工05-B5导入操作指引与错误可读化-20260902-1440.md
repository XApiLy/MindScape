# 员工05｜B5 导入操作指引与错误可读化

> 提交时间：2026-09-02 14:40（UTC+8）  
> 覆盖：`B5` 真实样本导入可操作性与结构化命令错误呈现  
> 状态：**操作入口已澄清、错误呈现已修复；正式 Release 不被覆盖，B5 仍等待真实纵向结果**

## 已阅读与落实

- 已读取控制 PEC 当前 B1/B5 要求、员工01 clean 冻结报告与员工04导入恢复 UI 报告。当前唯一 Release 仍是 `20260902-141043-m2-b5-provider-bd2ab97f7b7b`，不得为本轮小修复覆盖或伪造第二份 Release。
- 使用 React 错误呈现共用 `commandErrorMessage`，保留 Tauri CommandError 的 safeMessage，不再把结构化对象用 `String(error)` 渲染为 `[object Object]`。

## 本次推进

- 明确真实用户路径：粘贴/选择文件后，先点右下角“生成本地预览”完成导入；随后在出现的“本地导入预览”中选择至少一条消息、选择目标（会话或 Active FocusFrame），再点“请求知识建议”；最后对建议 Confirm 或 Reject。
- 修复 `ImportIntakeDialog` 的导入、bundle、原文、提案、审核与历史读取错误分支，统一显示安全可读消息，而非 `[object Object]`。
- 发布 Review Lab 员工确认主线 preview：`20260902T063907Z-导入错误提示可读化-5525`，状态 `ready`；重点观察重复/失败导入的错误详情与重试动作。

## 跨模块证据

1. `ImportIntakeDialog → commandErrorMessage → Tauri CommandError.safeMessage`：导入 SQLite/存储错误现在保留内核可安全显示的文本，不泄漏 Key、正文或路径。
2. Vite production build `2100 modules`、Chat `69/69` 通过；Review Lab preview 状态 `ready`。
3. 不改变 ImportSource/Revision、proposal、EvidenceRef、数据库写路径或当前唯一 Acceptance Build 的哈希。

## 剩余阻断与协作

1. **B5 仍未关闭**：创始人需在 Build `20260902-141043-m2-b5-provider-bd2ab97f7b7b` 完成真实导入、建议、Confirm/Reject、FocusFrame 后半段和重启；随后员工05运行只读脱敏审计。
2. 该可读化修复尚未进入新的 clean 集成提交，不能装入或替换当前 Acceptance Release；请员工01决定是否把它纳入下一次 clean 修订。当前 Release 继续作为唯一验收对象。

## 下一步 WIP

1. 收集当前唯一 Release 的真实 B5 操作结果，执行脱敏 SQLite/Vault 审计。
2. 如员工01冻结错误呈现修复，按唯一 Release 规则发布新 Build，不覆盖当前 Build。
