# 员工05｜DeepSeek安全连接测试与上下文窗口

> 提交时间：2026-08-18 16:07（UTC+8）  
> 覆盖任务：PROV-005～008、PROV-013～017  
> 状态：连接测试契约已完成，等待真实Key与Windows Tauri矩阵联调

## 相比上一份的变化

- 新增统一`test_provider_connection`命令，DeepSeek使用官方`GET /models`做非生成、非计费的凭据与端点检查。
- 官方当前定价/模型页确认V4 Flash为1M context length，注册表写入保守值`1_000_000`，已被统一启动路径用于输出预留后的上下文预算。
- 禁用HTTP重定向、限制模型列表响应为64KiB，避免Authorization跨端点传递和无界响应占用。

## 已阅读与落实

- 已重读PEC控制文件、M1派单、员工05长期任务及员工01/03/04/05最新PEC；员工02并行归档一度导致窗口中间态，校验前已恢复并补读其`M1上下文预算与视口持久化`报告。
- 具体落实：连接测试不调用Chat Completions、不产生ModelRun、不写数据库；只从OS凭据服务读取Key，仅返回脱敏的认证状态、模型ID和检查时间。
- 对员工01影响：之前阻塞的可信`contextWindowTokens`已有DeepSeek官方来源并接入；不修改Frozen V1契约。

## 本次完成与证据

- `desktop/src-tauri/src/adapters/provider/registry.rs`：新增`ProviderConnectionTestResult`、适配器测试边界及注册表路由。
- `desktop/src-tauri/src/adapters/provider/openai_compatible.rs`：DeepSeek官方模型列表检查、超时、响应上限、HTTPS校验与1M窗口。
- `desktop/src-tauri/src/commands.rs`、`lib.rs`：新增Tauri命令和脱敏结构化错误边界。
- `desktop/src/domain/provider.ts`、`desktop/src/app/kernelClient.ts`：同步TypeScript契约与前端调用入口，交由员工04接入设置页。
- 并行集成修复：恢复`Conversation`公共导出，修正Chat Node ESM测试的显式`.ts`路径，不改变业务语义。

## 契约、数据、安全与恢复影响

- 新增的连接检查为Provider层加法契约，不修改Runtime/Context Frozen V1、SQLite schema或恢复语义。
- Key完整生命周期仍在Rust/OS凭据边界；成功响应仅解析最多64KiB模型列表，失败响应体不读取、不记录、不上送。
- 连接测试不带生成消息且无自动重试；401/403、429、5xx等仍转为统一可执行错误。

## 验证结果

- `cargo test --offline --no-fail-fast`：41/41通过。
- `cargo clippy --offline --all-targets --all-features -- -D warnings`：通过。
- 员工05触及Rust文件的`cargo fmt --check`：通过；全树fmt仍受其他并行未格式化文件影响。
- `pnpm test:chat`：10/10通过；`pnpm test:canvas`：9/9通过；200节点基准：1/1通过；`pnpm build`：通过（1822模块）。
- `git diff --check`：通过。

## 风险与阻塞

- 真实DeepSeek Key尚未由用户写入OS凭据库，因此本轮仅交付真实网络入口和离线契约证据，不宣称端点矩阵通过。
- 员工04设置页尚未消费`test_provider_connection`；前端契约已就绪，需其补按钮和结果展示。
- PEC并行归档中间态已解除，当前六文件窗口恢复并通过保留检查。
- `.git`只读仍阻止分支、提交与Draft PR。

## 下一步 WIP（最多两项）

1. PROV-005～008：等待OS真实Key后执行`GET /models`成功/无Key/错Key/限流/网络矩阵，再进入首次Chat生成。
2. PROV-013～017：在真实Windows Tauri窗口完成流式停止、首Token/闲置超时、用量和异常断流矩阵。

## 需要同事配合

- **员工01/02**：评审1M窗口到输入预算的不变量与Provider连接错误边界。
- **员工04**：消费`kernelClient.testProviderConnection`，展示成功模型与结构化失败，不自行调用厂商端点。
- **员工03/04**：真实Key就绪后共同验收取消、失败、部分内容与重启恢复的画布/阅读一致性。
