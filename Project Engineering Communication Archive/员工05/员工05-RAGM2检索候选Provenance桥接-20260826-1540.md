# 员工05｜RAG-M2 检索候选 Provenance 桥接

> 提交时间：2026-08-26 15:40（UTC+8）  
> 覆盖任务：RAG-M2-003、PROV-M2-005～006 接线前置  
> 状态：候选来源桥接完成，等待统一查询 DTO

## 已阅读与落实

- 已读取最新 PEC 控制文件及员工01～04、06当前报告。
- 员工02已完成知识查询 IPC，并将下一步明确为员工05接入 FTS/VectorIndex/Relation；员工01已冻结 Entity/Relation/EvidenceRef 领域校验。
- 当前仍未冻结统一查询 DTO，因此本次只扩展 Provider/Index 候选结构，不进入 SQLite、不复制领域排序或排除规则。

## 本次完成与证据

- `desktop/src-tauri/src/adapters/provider/embedding.rs`
  - `RetrievalCandidate` 增加可选 `embedding` provenance metadata。
  - 新增 `From<&VectorMatch>` 转换，保留 source、score、稳定 ID 及模型版本/维度/source hash/chunk version。
  - FTS/Relation 候选继续可不携带 embedding metadata，保持同一候选结构兼容。
  - 新增回归确认 VectorMatch 转换不会丢失 Embedding provenance。
- 现有 `merge_retrieval_candidates` 继续执行来源去重、向量优先和不可用降级 notice。

## 契约、数据、安全和恢复影响

- 只增加派生候选字段，不修改 KnowledgeEntity、Relation、EvidenceRef、FocusFrame 或 SQLite schema。
- provenance metadata 不包含原文、Prompt、API Key 或 reasoning；候选仍必须经过员工01领域校验后才能进入上下文。
- 该字段为后续 unified evidence/scope DTO 保留来源链，避免在 Vector/FTS/Relation 合并时丢审计信息。

## 验证结果

- `cargo test --manifest-path desktop/src-tauri/Cargo.toml --all-targets --locked`：123/123 通过。
- `cargo clippy --manifest-path desktop/src-tauri/Cargo.toml --all-targets --all-features --locked -- -D warnings`：通过。
- `cargo fmt --all`、`git diff --check`：通过。

## 风险与阻塞

- SQLite FTS/VectorIndex/Relation 统一查询与状态失效事务尚未接入，不能宣称 RAG-M2-003 完整闭环。
- `RetrievalCandidate.embedding` 不是领域 EvidenceRef；后续查询层必须补齐已校验的 entity/scope/evidence，再交给 Context Compiler。

## 下一步 WIP（最多两项）

1. 与员工02冻结 unified retrieval DTO 及 SQLite 索引持久化字段，接入 Vector/FTS/Relation 查询。
2. 与员工01补候选 provenance 到 `KnowledgeRetrievalCandidate` 的安全映射和删除/否决失效回归。

## 需要同事配合

- **员工02**：提供统一检索查询 DTO、索引持久化与状态变更钩子。
- **员工01**：确认候选映射必须通过 Entity/Relation/EvidenceRef 与 FocusFrame 领域门禁。
- **员工04**：DTO 冻结后只消费来源和降级 notice，不在 UI 重算检索或 provenance。
