# MindScape V1 总开发路线与责任归属

> 状态：V1 执行总路线  
> 版本：1.0  
> 日期：2026-08-17  
> 负责人：产品负责人 / 总经理

## 1. 这份文档解决什么

本文件只回答三个问题：

1. V1 从现在到发布要经过哪些里程碑。
2. 每个里程碑完成什么、不做什么、如何验收。
3. 员工01～05和后续专项负责人的责任如何贯穿整个 V1。

文档权威关系如下：

```text
docs/v1-product-scope.md
    决定 V1 做什么与不做什么
        ↓
本文件
    决定里程碑顺序、阶段出口和长期责任
        ↓
docs/engineering/v1-execution-dispatch-20260817.md
    只决定 M1 当前执行窗口的具体派单
        ↓
PEC 当前文件
    只汇报个人最新进展、证据、阻塞和下一步
```

长期任务池不等于同时开工清单。每个人始终最多两个 WIP；只有当前里程碑的任务可以进入开发。

## 2. V1 最终交付

V1 只交付一个完整产品闭环：

> 用户可以直接开始 AI 会话，或导入其他 AI 平台的历史会话，在无限画布中保留上下文和分支，选择不同厂商模型继续，并在关闭与重启后可靠恢复。

V1 包含：

- 基础 Chat。
- 无限会话画布。
- 外部 AI 会话导入。
- 多厂商模型 API。
- 原样、快速、详细三种导入接续方式。
- 本地保存、安全凭据、错误、取消、重试和恢复。
- 不依赖教程的首次使用体验。

V1 不包含：

- Skill / MCP 自动编排与插件市场。
- DeepSeek Harness 产品集成。
- AI IDE、代码仓库修改、终端、测试或部署。
- 云同步、多人协作和社区。
- 漫游分析、完整透明推理图和复杂成果生态。

## 3. 总体历程

```text
M0 工程底座
    ↓
M1 真实 Chat Alpha
    ↓
M2 会话导入 Alpha
    ↓
M3 多模型与接续分析 Beta
    ↓
M4 V1 发布候选
    ↓
M5 内测与 V1 发布
```

| 里程碑 | 计划窗口 | 核心结果 | 当前状态 |
|---|---|---|---|
| M0 工程底座 | 2026-08-14 至 08-17 | 正式工程、会话图、SQLite、冻结上下文、Mock 流式骨架 | 基本完成，契约待冻结 |
| M1 真实 Chat Alpha | 2026-08-18 至 08-24 | 一个真实模型可发送、流式、停止、落库和重启恢复 | 当前执行 |
| M2 会话导入 Alpha | 2026-08-25 至 09-07 | 通用文件可安全导入、预览、保留原文并原样继续 | 未开始 |
| M3 多模型与接续分析 Beta | 2026-09-08 至 09-21 | 五类 Provider、平台专用导入、快速/详细分析 | 未开始 |
| M4 V1 发布候选 | 2026-09-22 至 10-05 | 恢复、安全、性能、错误、安装包和全部 P0 验收 | 未开始 |
| M5 内测与 V1 发布 | 2026-10-06 至 10-12 | 真实用户连续使用、阻断缺陷清零、发布 | 未开始 |

日期是当前管理基线。若人员、真实 API 凭据或导出样本未按时到位，调整的是里程碑日期，不允许通过删除安全、恢复或数据完整性验收来“按时完成”。

## 4. M0：工程底座

### 已有结果

- `desktop/` 正式 Tauri、Rust、React、TypeScript、Vite、SQLite 工程。
- 会话、问答节点、语义边、位置和 ContextSnapshot。
- SQLite schema v4、ModelRun 与统一运行事件持久化。
- Mock Provider、Tauri Channel、取消命令、Chat reducer 和画布投影。
- 操作系统安全凭据服务底座。
- Git、CI、评审、PEC 和七文件时效治理。

### 未关闭项

- RC1 尚未正式冻结为 Frozen V1。
- 运行生命周期仍有一部分由前端跨层编排。
- 启动恢复扫描尚未完成。
- 本次 Rust 基线验证被 crates.io / Schannel 环境问题阻断，需要工程环境修复后重新执行。

M0 的未关闭项全部并入 M1，不再单独扩展 M0。

## 5. M1：真实 Chat Alpha

### 目标

证明 MindScape 不是 Mock 演示，而是一款能够安全使用真实模型、可靠保存并恢复的 Chat 应用。

### 范围

- 首个协议底座：OpenAI-compatible。
- 首个真实验收端点：DeepSeek。
- 后端统一运行协调器。
- 真正网络取消、结构化错误、用量与脱敏。
- 运行中强制退出后的明确恢复。
- 画布、阅读视图、节点和视口一致恢复。

### 不做

- 不做导入器。
- 不做 Anthropic、Gemini 同时接入。
- 不做 Harness、Skill / MCP 和视觉重构。

### 出口

在真实 Windows Tauri 应用中完成：配置 Key、发送、流式、停止、重试、错误分类、分支上下文检查、强制退出、重启恢复和 Key 泄漏检查。任一关键项失败，M1 不通过。

M1 具体到人的任务、分支和日期见 [真实 Chat Alpha 派单](v1-execution-dispatch-20260817.md)。

## 6. M2：会话导入 Alpha

### 目标

证明用户可以把外部聊天带进 MindScape，原始内容不丢失、不被分析覆盖，并能够从导入历史继续聊天。

### 范围

- 文件选择、拖放和粘贴。
- 文件类型、大小、编码和风险预检。
- Markdown、TXT、通用 JSON、JSONL。
- 原文件不可变保存、指纹、重复检测和解析版本。
- 角色、顺序、时间、分支和未知内容块保真映射。
- 导入完整性报告和预览。
- 原样继续，不调用生成式分析。
- 导入中断与重启恢复。
- 画布展示导入来源，且不会一次性把超长历史全部铺开。

### 不做

- 不做快速或详细 AI 分析。
- 不承诺 ChatGPT、Claude、Codex 所有官方格式已经完整兼容。
- 不执行导入历史中的命令和工具调用。

### 出口

四种通用格式均有固定正常、损坏和大文件样本；导入后原文可查、报告准确、重复可识别、失败可降级为纯文本、原样继续不产生分析费用。

## 7. M3：多模型与接续分析 Beta

### 目标

证明 MindScape 的模型无关和跨平台接续价值成立，而不是只有一个模型和一个通用文本导入器。

### 范围

- OpenAI、Anthropic、Gemini、DeepSeek、OpenAI-compatible / OpenRouter。
- 每类 Provider 至少一个真实模型完成流式、停止、错误和用量矩阵。
- ChatGPT、Claude、Codex 专用导入适配器。
- 快速识别：目标、最新进展、关键约束和下一步。
- 详细分析：阶段、决策、分歧、工具记录、成果和未解决问题。
- 分析前展示发送范围、预计成本和隐私边界。
- 原文轨与接续轨分离；分析可修正、删除、重建并引用原消息。
- 从同一冻结上下文切换模型继续，旧节点模型归属不改变。

### 不做

- 不做自动模型路由。
- 不做漫游分析。
- 不做 Harness 运行模式。
- 不做后台全量分析。

### 出口

三种真实平台导出样本能够导入；原样、快速、详细三档结果与费用边界清楚；五类 Provider 真实契约通过；分析失败仍可原样继续。

## 8. M4：V1 发布候选

### 目标

停止新增功能，把 V1 从“能跑”提升到“可交付”。

### 范围

- 全部 P0 E2E。
- 数据库迁移失败、流式崩溃、导入崩溃与备份恢复。
- API Key、日志、导入注入、路径和超大文件安全测试。
- 200 节点性能、冷启动、会话打开和窄窗口验收。
- 会话、节点、导入原文件、分析层和凭据的删除边界。
- Windows 安装包、版本、升级、卸载和回滚。
- 首次使用 3 分钟内完成首次提问或导入。

### 不做

- 不接受任何新 P1 或 V2 功能。
- 不用“已知问题”豁免数据丢失、Key 泄漏、无法停止或无法恢复。

### 出口

全部 P0 通过，P0 阻断缺陷为零；安装包在干净 Windows 环境完成安装、升级和卸载；发布回滚方案经过演练。

## 9. M5：内测与 V1 发布

### 目标

让真实目标用户连续使用产品，验证闭环而非演示脚本。

### 范围

- 选择至少 20 名同时使用两个以上 AI 平台的目标用户。
- 连续 7 天观察首次价值时间、生成失败率、恢复成功率、导入成功率、导入后继续率和分支使用率。
- 每日缺陷分诊，只修 P0 / P1 缺陷，不扩功能。
- 发布说明、已知限制、数据与费用说明。

### 出口

没有数据损坏、安全和核心流程阻断；关键指标可采集且不上传私人会话正文；总经理签署 V1 发布决定。

## 10. 员工01～05贯穿责任

| 人员 | 固定责任 | M1 | M2 | M3 | M4～M5 |
|---|---|---|---|---|---|
| 员工01 | 领域契约、会话图、上下文 | 冻结运行契约与分支快照 | 导入领域对象与原文进入上下文 | 接续轨、证据引用与模型切换语义 | 内核不变量与破坏性变更签署 |
| 员工02 | 数据、桌面、恢复 | 运行协调与崩溃恢复 | 原文件、修订、指纹、导入事务 | 接续轨版本、迁移与数据清理 | 备份恢复、删除与数据安全签署 |
| 员工03 | 无限画布 | 节点终态、视口恢复、基准 | 导入来源与长历史的画布投影 | 分析层、来源和模型分支展示 | 200 节点、窄窗、交互稳定签署 |
| 员工04 | Chat、工作区、用户流程 | 真实设置、发送、停止、重试 | 导入入口、预览、原样继续流程 | 分析档位、范围费用、模型切换 | 首次体验、错误、可访问性签署 |
| 员工05 | Provider 运行时 | OpenAI-compatible + DeepSeek | OpenAI / OpenRouter 与运行可靠性 | Anthropic、Gemini、完整契约矩阵 | Provider 安全、错误与支持矩阵签署 |

这五人是主骨架负责人，不承担全部 V1 实现。每人负责自己领域的最终正确性，但不得跨越所有权替其他人完成整条链路。

## 11. 必须追加的四个专项责任席位

员工01～05负责工程主骨架，但不能同时承担独立体验设计、导入、接续分析和发布验收。需要从现有团队明确四名专项负责人：

| 待任命角色 | 开始时间 | 最终负责 | 与核心五人的接口 |
|---|---|---|---|
| 员工06｜视觉与交互设计工程师 | M1 立即介入 | 视觉语言、交互原型、动效规格、创始人实时确认与真实应用视觉验收 | 与员工03、04共同落地，产品负责人控制范围 |
| 导入负责人 | M2 前 | `IMP-001～023`，解析、适配、完整性报告与回归样本 | 员工01定契约、员工02存储、员工04承接用户流程 |
| 接续分析负责人 | M3 前 | `AIX-001～014`，快速/详细分析、引用、版本与质量评测 | 员工01定证据语义、员工02存储、员工05提供统一模型运行 |
| QA / 发布负责人 | M1 即介入，M4 全职 | E2E、恢复、安全、Provider、安装包与发布门禁 | 独立验收所有负责人，不作为其代测人员 |

员工06应立即加入 M1，但不得阻塞运行协调器和真实 Provider 底层开发。没有任命导入负责人，M2 不允许开始；没有任命接续分析负责人，M3 的快速/详细分析不允许开始；没有独立 QA / 发布负责人，M4 不允许宣布发布候选。

当前 PEC 七文件制度覆盖员工01～06及一份控制文件；员工06固定为视觉与交互设计工程师。导入、接续分析和 QA / 发布负责人确认后，仍需另行决定其报告渠道，不能直接突破七文件硬约束。员工06详细职责见 [视觉与交互设计工程师职责](../design/visual-interaction-design-engineer.md)。

## 12. 任务如何派发

每个里程碑开始时只发布一份阶段派单，规则如下：

1. 每人只有一个主交付和最多一个评审 / 联调任务。
2. 每项交付只有一个直接负责人。
3. 阶段任务必须指向 `docs/v1-work-breakdown.md` 中的稳定编号。
4. 阶段出口未通过，不派发下一阶段开发任务。
5. 发现新需求先进入待评审池，不直接进入个人 WIP。
6. 任何人不能用 PEC 声明代替代码、测试、真实样本或 E2E 证据。

## 13. 当前立即执行

现在只执行 M1，不同时执行 M2～M5：

- 员工01～05按照 [真实 Chat Alpha 派单](v1-execution-dispatch-20260817.md) 开工。
- 员工06立即承接视觉与交互设计工程师职责；管理层在 2026-08-24 前确定导入负责人、接续分析负责人、QA / 发布负责人。
- 工程负责人修复 Rust 依赖下载环境并重新运行测试与 Clippy。
- 2026-08-24 只做 M1 Go / No-Go；通过后才发布 M2 派单。

DeepSeek Harness 作为 V1 之后的独立实验候选，不在本路线中占用人力和日期。
