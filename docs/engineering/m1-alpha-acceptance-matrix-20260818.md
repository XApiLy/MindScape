# MindScape M1 真实 Chat Alpha 统一验收表

- Owner：员工01（技术负责人）
- 日期：2026-08-18
- 当前结论：**No-Go**。在本表的真实运行证据全部闭环前，不进入 M2。
- 验收口径：[v1-execution-dispatch-20260817.md](./v1-execution-dispatch-20260817.md#11-m1-真实-chat-alpha-统一验收脚本) 与 [m1-pec-review-and-closeout-20260818.md](./m1-pec-review-and-closeout-20260818.md)

## 脚本 1—10

| # | 真实验收动作 | 当前状态 | 已有证据与缺口 | 放行影响 |
|---|---|---|---|---|
| 1 | 新用户创建本地对话并配置 DeepSeek 凭据 | 部分通过 | 已有真实 `GET /models` 成功证据；仍需从空状态完整演示首次配置与创建对话。 | 普通 |
| 2 | 根节点真实流式回答，显示真实 provider/model | 通过（待基线复演） | 员工05已记录真实 DeepSeek 首次生成并进入 `completed`；集成基线须再跑一次。 | **失败即 No-Go** |
| 3 | 建立至少两类分支并核对实际 ContextSnapshot | 待验收 | 代码与单元测试存在，缺真实分支运行后的快照核对。 | 普通 |
| 4 | 第二次回答中途停止，保留部分文本且不得为 completed | 待验收 | 员工04已完成停止交互和取消回退；缺真实 provider 流中断证据。 | **失败即 No-Go** |
| 5 | 流式期间强制关闭，重启后无永久 pending 且历史完整 | 待验收 | 已有恢复实现和自动化测试；缺真实窗口强退/重启证据。 | **失败即 No-Go** |
| 6 | 使用无效 Key，得到可修复认证错误且不无限重试 | 待验收 | 错误映射已实现；缺真实无效 Key 验收记录。 | **失败即 No-Go** |
| 7 | 断网/超时，进入正确错误态且不生成伪助手消息 | 待验收 | 自动化覆盖存在；缺真实网络故障注入证据。 | 普通 |
| 8 | 重启恢复会话、节点、边、模型、位置和 viewport | 部分通过 | 员工03已用真实 Tauri 窗口和 SQLite 交叉证明 A/B 会话、节点、边、位置、viewport 在正常关闭重启后恢复；仍需补模型选择/运行投影完整对照与强退恢复。 | **失败即 No-Go** |
| 9 | 数据库、日志、崩溃材料均不含完整 API Key | 部分通过 | 工作树静态扫描通过；员工02真实 SQLite 证据只含聚合计数且数据库不入库，仍需日志/崩溃产物检查。 | **失败即 No-Go** |
| 10 | 前端、Rust、PEC、仓库治理全部通过 | 通过（本地集成树） | Chat 14/14、Canvas 13/13、性能 1/1、Rust 46/46、Review Lab 8/8、前端与 Tauri debug 构建、PEC 和工作树治理均通过；提交后再执行 tracked 扫描。 | 普通 |

## 员工01底层不变量复核

| 场景 | 必须满足的不变量 | 结论 |
|---|---|---|
| 正常完成 | Node、ModelRun、ContextSnapshot 关联一致；完成态不可被迟到事件覆盖 | 待真实复核 |
| 用户取消 | 部分文本可保留；run 为 cancelled；节点不得误记 completed | 待真实复核 |
| Provider 失败 | 错误可诊断；不得生成伪完成消息；不得无限重试 | 待真实复核 |
| 应用中断与恢复 | pending run 被确定性恢复；历史、图结构及 viewport 不丢失 | 待真实复核 |
| 幂等与并发 | 同一幂等键只创建一个有效运行；重复完成/取消无副作用 | 待真实复核 |

## 集成冻结清单

- [x] 创建 `codex/m1-integration-recovery` 集成恢复分支。
- [x] 忽略 `desktop/MindScape-DeepSeek-Fixed.exe` 与 `desktop/src-tauri/target-fixed/`。
- [x] 按合同/上下文、数据/恢复、Provider、Chat、Canvas、Review Lab、文档/PEC 七组完成审阅。
- [x] 对工作树执行密钥扫描和禁止文件检查；暂存区扫描在提交前执行。
- [x] 在同一集成树复跑前端、Rust、Review Lab、Tauri 门禁。
- [x] 形成可回滚、披露员工01—06共同贡献的集成提交并推送恢复分支。
- [ ] 创建 Draft PR，并在真实验收未闭环时保持 No-Go（本机缺少 GitHub CLI，已提供远端创建入口）。

## 证据规则

验收必须记录操作、期望结果、实际结果、数据库/日志核对及证据位置。截图、文件名、终端输出和命令历史不得包含完整 API Key；失败证据与通过证据同样保留，禁止用自动化测试替代脚本 1—9 的真实运行验收。
