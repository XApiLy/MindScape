# MindScape 关键决策记录

> 本文记录已经达成的产品决策及其理由。状态包括：`已确认`、`暂定`、`已取代`。

## D-001：MindScape 不定位为单一学习软件

- 日期：2026-08-13
- 状态：已确认
- 决策：MindScape 是模型无关的 AI 工作空间，学习只是众多使用场景之一。
- 原因：项目讨论、软件开发、创作和普通聊天经常交叉出现，单一学习定位会限制产品和 AI 的能力。

## D-002：AI 是引擎，MindScape 是能力与工作操作层

- 日期：2026-08-13
- 状态：已确认
- 决策：不自建一套僵硬的 AI 行为体系，不用固定教学流程约束模型；重点建设上下文、工具、交互和成果层。
- 原因：基础模型会持续升级，产品应从模型能力提升中受益，而不是被固定流程锁死。

## D-003：使用可组合会话协议，不使用互相隔离的硬模式

- 日期：2026-08-13
- 状态：已确认
- 决策：学习、开发、项目和创作等是会话意图预设，可改变、可组合、可覆盖。
- 原因：真实任务具有混合性。预设能降低上手成本，但不应成为功能边界。

## D-004：通过前两轮对话进行意图识别和能力建议

- 日期：2026-08-13
- 状态：暂定
- 决策：系统根据会话前一到两轮识别任务、项目和期望成果，并按需建议或准备 Skill、MCP、插件、项目上下文和工作方法。
- 原因：减少用户手动配置，让能力自然贴合当前工作。
- 约束：识别和装载过程必须透明、可解释、可调整；敏感操作与外部副作用不能因自动识别而绕过授权。

## D-005：规范成果，而不是限制 AI 表达

- 日期：2026-08-13
- 状态：已确认
- 决策：保留 AI 的自由回答，同时识别决策、任务、风险、代码、引用和文档等结构，并将其转化为可操作成果。
- 原因：用户需要的是可继续推进的结果，而不是被模板压缩的模型能力。

## D-006：首次引导通过真实任务完成

- 日期：2026-08-13
- 状态：已确认
- 决策：不采用可被连续跳过的弹窗教程，改为让用户带着真实目标完成第一次会话和成果交付。
- 原因：用户在获得即时价值的过程中更容易理解产品。

## D-007：建立持续本地记录

- 日期：2026-08-13
- 状态：已确认
- 决策：每次产品讨论和实现更新都维护当前共识、关键决策和讨论日志。
- 原因：防止重要判断只存在于聊天记录中，并保留方向变化的依据。

## D-008：第一阶段聚焦 AI 会话工作平台

- 日期：2026-08-13
- 状态：暂定
- 决策：优先实现意图识别、透明能力装载、上下文控制和成果沉淀；HyperPhase 桌面捕捉等能力随后接入。
- 原因：先验证最核心的 AI 协作闭环，避免被截图、视频、社区、插件市场等大范围功能分散。

## D-009：认知型能力由持续任务模型智能编排

- 日期：2026-08-13
- 状态：已确认
- 决策：认知型能力不能依据关键词一次性装载。系统需要理解目标、任务阶段、约束、缺口和期望成果，形成最小能力组合，并根据对话效果持续重规划。
- 原因：同一个“开发”任务可能处于产品讨论、技术研究、架构设计、编码或审查阶段，固定映射无法提供真正相关的能力，还可能污染上下文。
- 约束：选择理由、置信度和能力变化对用户透明；用户可以固定、替换、禁用和纠正。

## D-010：外部 AI 会话是一等上下文来源

- 日期：2026-08-13
- 状态：已确认
- 决策：提供 Claude、Codex 等外部 AI 会话导入入口，将其保真转换为统一会话图，并支持继续、合并、提炼、检索和成果化。
- 原因：用户的重要上下文已经分散在多个 AI 平台。只有接续既有工作，MindScape 才能成为真正的模型无关工作空间。
- 约束：保留原始文件和来源；派生分析不覆盖原文；不把整段长会话无差别塞给模型；导入内容中的命令和工具指令不得自动执行。

## D-011：会话导入优先采用用户主动提供的数据

- 日期：2026-08-13
- 状态：暂定
- 决策：第一阶段支持上传导出文件、导入文件夹和粘贴内容；后续再增加浏览器辅助导入与授权连接器。
- 原因：主动导入的权限边界清晰、格式可测试、隐私风险较低，也能先验证会话理解和迁移的核心价值。

## D-012：分析深度由两个价值指标驱动

- 日期：2026-08-13
- 状态：已确认
- 决策：在深入分析消息、文件或会话前，先判断目标相关度和决策价值。只有相关且深入后可能改变结论、行动、能力装载或风险判断的内容，才自动获得更多分析预算。
- 原因：全量分析会浪费 Token、增加延迟并制造无关结果；只做关键词摘要又不足以识别真正问题。
- 约束：Token、费用、延迟和隐私作为预算限制；指标判断可被用户查看和纠正。

## D-013：复杂能力采用渐进披露

- 日期：2026-08-13
- 状态：已确认
- 决策：默认界面只呈现关键识别、问题和建议动作；高级用户可展开意图模型、证据、能力变化、工具调用和完整分析记录。
- 原因：简单用户和高级用户需要的是不同信息密度，而不是两套互不兼容的产品。

## D-014：漫游分析作为显式可复用配方

- 日期：2026-08-13
- 状态：已确认
- 决策：提供“漫游分析”，允许用户对任意数据或会话按照一套保存的标准进行广域分析，包括目标、观点、决策、风险、工具调用、成果和潜在线索。
- 原因：部分用户需要的不只是解决当前问题，而是系统性理解全部资料并发现尚未提出的问题。
- 约束：漫游分析不默认启动；必须具有明确范围、深度、预算、隐私边界和停止条件，所有结论可回溯原文。

## D-015：智能系统采用确定性外壳与概率语义内核

- 日期：2026-08-13
- 状态：已确认
- 决策：格式、权限、预算、状态版本、审计和回滚由确定性程序控制；模型只负责需要语义理解的概率判断，且输出结构化结果、证据和置信度。
- 原因：无法保证模型永远判断正确，但可以保证错误可见、可纠正、不直接越权，并能够通过回放和评测持续改进。

## D-016：任务状态采用事件溯源和增量更新

- 日期：2026-08-13
- 状态：暂定
- 决策：原始消息、导入、用户修正、能力变化和工具结果保存为不可变事件；当前任务模型由事件增量归并生成，并保留历史版本。
- 原因：需要解释“为什么系统这样理解”，支持纠错、回滚、导入合并和算法升级后的重新计算。

## D-017：先建设评测管线，再扩大 Skill 和 UI 范围

- 日期：2026-08-13
- 状态：已确认
- 决策：第一阶段先完成统一事件、任务状态、能力清单、回放器和标注评测集，再实现大规模 Skill、复杂画布和自动化体验。
- 原因：没有可回放样本和指标，所谓“更智能”只能凭主观感觉判断，无法稳定优化。

## D-018：导入目标与分析档位分离

- 日期：2026-08-13
- 状态：已确认
- 决策：用户导入会话后，可以选择原样继续、快速识别、详细分析或漫游分析；“继续会话”“并入项目”等目标不强制绑定某个分析档位。
- 原因：部分会话短且目标清楚，分析没有必要；复杂会话则可能从结构化理解中明显获益。
- 约束：不分析仍执行本地格式解析、完整性检查和安全隔离，但不调用模型做语义提炼。

## D-019：分析推荐必须证明增量价值

- 日期：2026-08-13
- 状态：已确认
- 决策：系统可以推荐分析，但不得宣称分析永远优于原样继续。推荐必须展示原因、预计收益、范围、成本和数据发送边界；结果必须有原文引用并可撤销。
- 原因：用户信任应建立在可验证收益上，而不是强制默认、复杂度展示或营销承诺。

## D-020：成本展示区分前置分析与后续上下文

- 日期：2026-08-13
- 状态：已确认
- 决策：分析档位同时展示一次性分析成本和后续每轮上下文成本；原样继续不能笼统标记为“最省 Token”。
- 原因：长会话全量续接会反复消耗大量 Token，而只携带最近消息又可能遗漏关键上下文；快速分析可能通过可复用上下文包降低长期成本。

## D-021：默认采用原文与分析双轨接续

- 日期：2026-08-14
- 状态：已确认
- 决策：导入会话时完整保留不可变原文轨；分析仅生成可修正、可删除的接续轨。主 AI 使用精简接续状态，并按需回溯原始消息、附件和工具记录。
- 原因：仅用原文可能成本高且难以跨模型接手，仅用摘要又会损失细节。双轨结构兼顾保真、效率和可纠错性。

## D-022：不承诺复现原平台的相同输出

- 日期：2026-08-14
- 状态：已确认
- 决策：MindScape 以语义连续性、任务连续性和执行状态恢复为迁移目标，不承诺相同提示再次得到逐字相同结果。
- 原因：导出文件通常缺少原平台系统提示、隐藏记忆、精确模型版本、服务端上下文编排和完整工具环境；模型本身也可能具有非确定性。
- 约束：导入后显示接续完整度报告，明确已恢复、部分恢复和无法恢复的状态。

## D-023：MindScape 不定位为 AI IDE

- 日期：2026-08-14
- 状态：已确认
- 决策：MindScape 聚焦学习、探索、知识理解和 AI 会话，不以终端代理、仓库自主修改、持续测试和代码交付为核心能力。
- 原因：模型可以具备编程知识，但产品竞争维度与 Codex 类软件工程代理不同；复制其落地执行面会扩大范围并削弱认知体验。
- 约束：允许解释代码、讨论技术和导入 Codex 会话，但第一阶段不提供 IDE 级执行闭环。

## D-024：透明性以可审计工作轨迹为准

- 日期：2026-08-14
- 状态：已确认
- 决策：展示目标理解、上下文选择、Skill 装载、工具调用、公开判断、证据、假设、反例、结论和验证结果；不承诺或伪造模型不可见的原始隐藏思维链。
- 原因：多数模型 API 不提供完整内部思考，生成一段事后解释也不等于真实内部过程。用户真正需要的是能核验逻辑和来源的透明性。

## D-025：AI 可调用全部已注册认知工具

- 日期：2026-08-14
- 状态：已确认
- 决策：MindScape 内部工具通过统一清单向会话 AI 暴露，模型可以根据目标组合调用搜索、阅读、知识检索、卡片、图谱、计算、可视化和成果生成等能力。
- 原因：工具不应被固定在孤立页面中，AI 应能在对话过程中主动使用软件能力完成更高质量的探索。
- 约束：工具仍遵守权限、预算、隐私和副作用规则；不提供无限制系统控制权。

## D-026：产品进入垂直切片开发阶段

- 日期：2026-08-14
- 状态：已取代
- 决策：停止继续扩展首版理论范围，围绕“导入或粘贴会话 → 选择分析深度 → 继续对话 → 查看透明轨迹 → 调用内部认知工具”建设第一个可运行版本。
- 原因：核心定位和边界已经足够明确，剩余问题更适合通过原型、成本数据和用户体验验证。
- 约束：首版不实现完整无限画布、社区、插件市场、HyperPhase 全套能力、IDE 级执行或多供应商复杂编排。

## D-027：第一版以无限会话画布为核心工作区

- 日期：2026-08-14
- 状态：已确认
- 决策：第一版范围调整为无限会话画布、外部会话导入、多厂商模型 API 与基础 Chat。无限画布不再后置，而是承载会话、分支和接续关系的主要界面。
- 原因：画布是用户能够立即感知的核心差异，也是后续学习、探索和会话迁移能力的共同容器；团队具备前后端并行开发资源，可以承担这一范围。
- 约束：第一版只做二维会话节点、连线、缩放、拖拽、分支和聚焦阅读，不引入 3D、社区、插件市场或 IDE 级执行能力。

## D-028：模型层首版即采用统一多厂商适配器

- 日期：2026-08-14
- 状态：已确认
- 决策：应用层只依赖统一流式事件接口，首批适配 OpenAI-compatible、Anthropic 与 Gemini；DeepSeek、OpenRouter 和自定义服务复用兼容适配器配置。
- 原因：模型无关是 MindScape 的基础定位，首版锁定单一厂商会让会话迁移和模型切换能力从架构上失真。
- 约束：当前原型采用 BYOK；密钥不进入持久化状态。生产版仍需通过桌面安全凭据或后端代理解决密钥安全与浏览器 CORS。

## D-029：第一版采用单一正式范围基线

- 日期：2026-08-14
- 状态：已确认
- 决策：`docs/v1-product-scope.md` 是第一版唯一有效的产品范围基线。第一版由无限会话画布、外部会话导入、多厂商模型 API、基础 Chat 四个核心能力，以及本地保存、安全恢复、首次体验三个支撑能力组成。
- 原因：长期愿景较多，多团队并行时如果没有唯一边界，会分别把 Skill、MCP、生态、HyperPhase、透明推理和云协作提前带入版本，导致核心闭环失焦。
- 约束：新增范围必须说明替换的现有优先级、周期、依赖和验收影响；P1 不得阻塞 P0 发布。

## D-030：总经理职责与工程授权分离

- 日期：2026-08-14
- 状态：已确认
- 决策：本项目对话中的默认职责是产品与经营管理，包括方向、范围、优先级、组织拆分、接口协调、风险和验收。未经用户明确授权，不直接替工程团队实施代码。
- 原因：用户已有前端、后端等工程团队，需要的是统一决策和交付管理，而不是越过组织分工直接开发。
- 约束：可以读取和审查工程产出；任何代码修改、发布或其他工程执行需要明确任务授权。

## D-031：产品内核定义为可追溯会话图运行时

- 日期：2026-08-14
- 状态：已确认
- 决策：MindScape 内核由不可变来源与事件、可持久化会话图、上下文编译器、可替换模型运行时和可验证派生层组成。无限画布是内核的主要视图，Provider 是执行适配器，导入器是外部数据翻译层。
- 原因：如果将画布组件、某个模型 API 或前端消息数组当作产品内核，多团队会把上下文、分支和数据真实性分别实现，导致状态无法恢复、模型无法替换、导入结果无法追溯。
- 约束：原文不可覆盖、关系必须显式、每次运行具有上下文快照、派生不等于事实、UI 不是数据真相、失败必须真实记录。

## D-033：第一版按工作流与交付成果分配任务

- 日期：2026-08-14
- 状态：已确认
- 决策：V1 拆分为 PM、UX、ARC、CORE、DATA、CAN、CHAT、PROV、IMP、AIX、QA、REL 十二条工作流。任务按可独立验收的工程成果分配，不按页面或“前后端共同负责”分配。
- 原因：画布、Chat、导入和 Provider 共同依赖会话图、上下文快照和运行事件。如果按页面分组，各团队容易各自实现上下文和状态，最后无法可靠集成。
- 约束：每项任务只有一个直接负责人；跨团队契约先冻结；现有演示原型不自动计为完成；P0 任务同时满足正式契约、真实数据、失败恢复、测试和安全要求后才能关闭。

## D-032：正式工程独立于旧原型重新建立

- 日期：2026-08-14
- 状态：已确认
- 决策：`desktop/` 为 MindScape 正式桌面工程；`apps/` 下内容仅作为旧原型参考，不作为实际工程或领域契约来源。正式工程采用 Tauri v2、Rust、React、TypeScript、Vite 与 SQLite，并按领域、应用、适配器和 UI 分层。
- 原因：旧原型将会话语义、上下文选择和持久化集中在前端临时状态中，无法满足可追溯、可恢复、可替换模型和多团队稳定协作的内核要求。
- 约束：旧原型不被静默删除；后续能力通过正式工程中的领域契约和 Tauri 命令接入，不直接复制旧前端状态模型。

## D-034：首批五名工程人员承担五条技术主线

- 日期：2026-08-14
- 状态：暂定，待真实姓名与能力确认
- 决策：首批五名工程人员分别承担领域架构与上下文、桌面数据与恢复、无限画布、Chat与工作区、多厂商模型运行时五条技术主线，共负责111个V1任务。导入、接续分析、独立QA、产品设计和发布由后续人员承担。
- 原因：五条主线能够最早形成“输入 → 上下文 → 模型运行 → 持久化 → 画布呈现”的纵向技术骨架，同时保持每个核心模块有唯一责任人。
- 约束：这些是贯穿V1的责任任务池，不允许同时开工；个人WIP最多两个。真实人员能力与假设不符时必须调整角色，不以临时编号强行分配。

## D-035：Rust 领域契约作为 V1 代码级权威来源

- 日期：2026-08-14
- 状态：RC1，待员工02～05评审后冻结
- 决策：`desktop/src-tauri/src/domain` 是 V1 跨团队领域契约的代码级权威来源；`desktop/src/domain` 仅维护供前端消费的逐字段 TypeScript 镜像。SQLite 表、画布库对象、Provider SDK 对象和 UI 临时状态均不得反向定义领域语义。
- 原因：数据、画布、Chat 和 Provider 四条主线必须共享同一套会话图、上下文、运行事件和错误含义，避免各自维护平行类型并在集成阶段丢失语义。
- 约束：契约冻结前由员工02～05按消费方职责评审；冻结后的破坏性修改必须先提交 ADR，包含迁移、兼容窗口、受影响方和回滚方案。

## D-036：采用单一根仓库、受保护主干与 Squash Merge

- 日期：2026-08-14
- 状态：已确认
- 决策：`D:\Project\MindScape` 建立为唯一 Git 根仓库，长期分支仅保留 `main`。所有工程变化使用带任务编号的短生命周期分支，经 Pull Request、必需检查和非作者评审后默认以 Squash Merge 进入主干；禁止直接推送、强推、删除或重写 `main`。
- 原因：正式工程、旧原型、UI 参考、产品文档和 PEC 需要处于同一个可追溯历史中，同时必须明确 `desktop/` 才是正式工程。线性主干和原子化 Squash 提交可以让每个交付独立审计、回滚和关联任务。
- 约束：标准变更至少一名非作者批准；领域契约、迁移、凭据、Provider 网络、恢复和发布等高风险变更至少两名非作者批准，并提供测试、失败模式与回滚方案。远程仓库建立后必须启用分支保护和 `repository-hygiene`、`frontend`、`rust` 三项必需检查。

## D-037：PEC 采用六文件当前窗口与独立归档

- 日期：2026-08-15
- 状态：已由 D-042 取代
- 决策：`Project Engineering Communication/` 必须恰好保留 6 个 Markdown 文件：员工01～05各自唯一的最新 PEC，以及一份规则或当前任务派发文件。员工每次完成实质推进并提交新 PEC 时，必须在同一 PR 中先将自己的旧报告移动到 `Project Engineering Communication Archive/员工NN/`，不得删除或覆盖历史报告。
- 原因：PEC 的首要用途是快速展示团队当前状态。把全部历史报告与最新报告混放会降低时效性、增加阅读噪声，并使管理者难以判断哪份内容仍然有效；独立归档同时保留完整追溯能力。
- 约束：当前目录禁止子目录和附件；归档报告视为历史记录，不再原地修改；文件数量、员工唯一性、命名和归档目录由 `scripts/check-pec-retention.mjs` 在 CI 中强制检查。

## D-038：MindScape 长期聚焦 Chat AI，不进入本地执行代理赛道

- 日期：2026-08-17
- 状态：已确认
- 决策：MindScape 的产品身份是以会话为原生界面的 Chat AI 工作空间，核心场景为聊天、学习、探索和认知内容管理。产品长期不建设 AI IDE、编程 Agent、CLI Agent 或通用本地工作 Agent 的自主执行闭环。
- 原因：与 Codex、Claude Code、Cursor、Trae 等产品竞争仓库修改、终端、测试和部署，会显著扩大工程与安全边界，并稀释 MindScape 在上下文、会话连续性、探索、知识组织和透明工作轨迹上的核心优势。
- 约束：模型仍可讨论、解释或输出代码文本；应用仍可本地保存数据并读取用户主动导入的文件。但不得把代码生成等同于编程代理能力，也不得以此扩张到自主修改仓库、运行命令、测试、提交、部署或控制桌面。Harness 只可增强会话认知链路。

## D-039：下一工程里程碑为单 Provider 真实 Chat Alpha

- 日期：2026-08-17
- 状态：执行中
- 决策：员工01～05在 2026-08-18 至 2026-08-24 只交付一条使用真实 DeepSeek 端点的 Chat 纵向闭环，覆盖冻结上下文、后端运行协调、真实流式、停止、终态落库、强制退出恢复、画布一致性和凭据安全。OpenAI-compatible 作为首个协议底座，其他厂商、导入器和 Harness 不占用本轮 WIP。
- 原因：当前模拟 Provider 骨架已经贯通，最危险的缺口是没有真实模型、运行完成仍由前端跨层编排、重启恢复缺失。继续横向增加模块会掩盖纵向闭环无法交付的问题。
- 约束：本轮 No-Go 项包括真实流式、真实取消、重启恢复、错误分类、模型归属和 Key 不泄漏。完成页面、Mock 演示或单元测试不能代替真实 Tauri Windows E2E。

## D-040：V1 采用 M0～M5 单向里程碑治理

- 日期：2026-08-17
- 状态：已确认
- 决策：V1 依次经过工程底座、真实 Chat Alpha、会话导入 Alpha、多模型与接续分析 Beta、发布候选、内测与发布六个里程碑。`v1-product-scope.md` 决定范围，总路线决定顺序与长期责任，每个阶段派单只决定当前 WIP，PEC 只报告最新进展。
- 原因：此前长期任务池、当前一周派单和未来增员出现在同一层级，容易让成员误以为全部任务需要同时启动，造成跨模块并发、所有权重复和验收标准混乱。
- 约束：上一个里程碑出口未通过，不发布下一个里程碑开发派单；个人 WIP 始终最多两个；M2、M3、M4 分别受导入、接续分析、独立 QA / 发布负责人任命门禁约束。

## D-041：设置视觉与交互设计工程师并直接连接创始人

- 日期：2026-08-18
- 状态：已确认，待任命
- 决策：V1 设置一名兼具视觉、交互、动效和前端实现理解的 Design Engineer，直接与创始人实时沟通体验方向，向员工03、04交付可执行规格，并在真实应用中完成视觉与交互验收。
- 原因：员工03和员工04分别承担画布与 Chat 工程，若同时独立决定视觉和交互，会分散底层交付精力并形成不一致体验。该角色直接生产 MindScape 的核心用户价值，不属于额外审批层。
- 约束：该角色不改变产品范围、领域契约和工程所有权；可以实现纯表现层与动效原型，但不得绕过员工03、04修改业务状态。沟通结论必须落盘，特效遵守性能、可打断、减少动态效果和不干扰阅读原则。

## D-042：PEC 扩展为七文件当前窗口

- 日期：2026-08-18
- 状态：已确认
- 决策：`Project Engineering Communication/` 必须恰好保留 7 个 Markdown 文件：员工01～06各自唯一的最新 PEC，以及一份规则或当前任务派发文件。员工06固定为视觉与交互设计工程师席位。
- 原因：新增视觉与交互设计岗位需要与其他核心员工一样保持最新状态、证据、阻塞和协作请求，同时仍需维持低噪声当前窗口。
- 约束：旧报告仍原样移入 `Project Engineering Communication Archive/员工NN/`；控制文件仍只能有一份；自动校验、CI、权威制度和当前通知统一按七文件执行。其他专项负责人不得未经制度变更继续增加当前 PEC 文件。

## D-041：冻结 V1 上下文与模型运行契约

- 日期：2026-08-18
- 状态：已确认
- 决策：`mindscape.context.v1`、`mindscape.runtime.v1`及M1分支上下文语义进入Frozen V1。Chat只能通过单一`start_model_run`应用边界发起运行；completed、cancelled、failed和进程中断由统一事件决定Node与ModelRun结果。
- 原因：员工02～05已无M1消费阻断，schema v4完整请求往返、事件顺序、幂等、部分内容与终态映射均有自动化验证；继续允许UI三段式编排会保留崩溃不一致窗口。
- 约束：进程中断使用`failed`加`providerCode=application_interrupted`表达并保留已落库部分内容；冻结后的字段、枚举或语义变更必须先有ADR。Import/Evidence仍为RC1，不能借本决策提前宣称M2契约已冻结。

## D-043：V1上下文预算采用多语言保守估算

- 日期：2026-08-18
- 状态：已确认
- 决策：上下文编译器对ASCII内容按每4字符1 Token向上取整，对非ASCII字符按UTF-8字节数计入，并为当前输入及每条选中消息增加4 Token固定Chat封装开销。估算结果只用于发送前裁剪，账单与真实用量仍以Provider事件为准。
- 原因：原有统一“字符数除四”会严重低估中文、Emoji及混合文本，也没有计入Chat消息角色与边界开销，可能让冻结快照表面处于预算内但被真实模型拒绝。
- 约束：算法必须确定、可测试并保留完整问答组；当前输入不得静默截断；算法升级不得重算或回写历史ContextSnapshot。

## D-044：M1幂等键绑定完整执行输入

- 日期：2026-08-18
- 状态：已确认
- 决策：同一`idempotencyKey`只允许重放完全相同的会话、父节点、分支、标题、提示词、Provider、Model、能力与预算。单进程内的预查、Node创建和Run创建由共享准备锁串行化；同载荷返回原稳定ID，异载荷拒绝且不产生新事实。
- 原因：只依赖数据库唯一键会在并发请求中先产生孤儿Node，静默接受不同载荷又可能把错误内容绑定到旧运行或产生重复计费歧义。
- 约束：错误不得回显新旧请求正文；准备锁不覆盖Provider网络执行；若未来支持多个进程写同一数据库，必须将准备过程下沉为SQLite单事务。

## D-045：分层视觉系统先经独立原型与范围门禁

- 日期：2026-08-19
- 状态：已确认
- 决策：MindScape 后续视觉架构采用“背景主题 × 组件材质 × 氛围主题 × 区域级覆盖”的独立组合模型。员工06先完成现状审计、可点击原型、创始人确认和工程规格；在 M1 收口、原型签字和正式范围变更之前，不把整体视觉重构写入生产工程。
- 原因：背景、玻璃材质、Wallpaper Engine 和天气氛围具有较大的体验价值，也同时带来可读性、性能、本地文件安全和跨模块返工风险。先在独立原型中确认中间平衡，可以避免员工03、04各自实现不同设计语言，也不会破坏当前真实 Chat Alpha 的关键路径。
- 约束：全局环境调整轻微且可关闭，主要遮蔽由卡片局部承担；视觉偏好不得成为会话图或模型运行事实；Wallpaper Engine 首批只承诺安全可识别的图片/视频类型；氛围在输入、阅读和关键操作时必须退让；生产实现必须遵守 `docs/engineering/layered-visual-system-execution-plan-20260819.md` 的门禁与统一验收程序。

## D-046：视觉运行时采用PixiJS WebGPU与DOM混合架构

- 日期：2026-08-19
- 状态：已确认
- 决策：MindScape目标桌面框架采用Tauri v2 + Rust；主视觉画布采用PixiJS v8并优先WebGPU；输入、按钮、阅读、菜单和焦点语义保留在DOM；自定义片元着色器作为云、雨、光影、材质和颜色合成的正式能力，使用WGSL主路径与GLSL回退。Three.js只在明确3D场景中懒加载。前端候选基线为React 19或经评审的SolidJS + Vite，样式/动效为Tailwind CSS v4 + Motion One，本地数据/AI为SQLite + LanceDB Rust Native。
- 原因：该架构把高频图形合成与可访问工作界面分离，既能实现壁纸、空间氛围和定制材质，又避免React按帧重渲染或Canvas接管输入语义。双Shader路径和Classic回退为设备兼容、故障隔离与长期可维护性提供边界。
- 约束：Shader只消费视觉Uniform，不得接收Prompt、消息、Provider、API Key或业务终态；首批不执行用户或第三方任意Shader源码；Pixi/WebGPU失败不得阻断Chat与Canvas；选定框架不解除D-045的生产门禁。

## D-047：VIS-008冻结四个原仓的单一视觉职责

- 日期：2026-08-20
- 状态：已确认，等待 VIS-009 工程评审
- 决策：MindScape 后续视觉基线只采用 Ybouane LiquidGlass（Liquid Glass）、Glacé（毛玻璃）、React Weather Effects（天气）和 Drei `Cloud`（云）。四个来源通过统一 Appearance / Surface / Atmosphere 适配器进入现有研究工作区；不再把其他玻璃、体积云或自制近似实现混入同一职责。
- 原因：创始人要求效果必须独立对照原仓后再融合，不能以“看起来接近”的 CSS 或自定义 Shader 代替选定开源实现。明确一对一职责可以避免员工03、04各自重画材质，保留原仓的真实折射、模糊、天气分层和云层行为。
- 约束：独立原型可以直接 alias 本地示例源文件，但不得因此绕过生产门禁。Ybouane 许可证文本、Drei 远程纹理、React Weather 生命周期与 WebGL warning 必须在生产前处理；低性能/加载失败必须卸载动态 Canvas 并回退 Classic Opaque。M1 已于 2026-08-20 Go；D-045 的范围变更、员工03/04影响评估和正式开工指令仍然有效。

## D-048：每次模型运行必须有可见且可回放的有效运行档案

- 日期：2026-08-21
- 状态：已确认
- 决策：每次运行都冻结 Provider、模型、思考模式与预算、生成参数、上下文策略、工具权限和预算上限，并记录每个值来自用户设置、作用域继承、系统建议还是 Provider 能力限制。界面可以提供预设，但不能存在不可见的默认或静默降级。
- 原因：模型能力、厂商默认值和参数语义差异很大。用户需要知道“这一轮究竟用什么运行”，才能比较结果、控制成本并复现问题。
- 约束：不支持的参数必须明确提示；能力校验失败发生在创建 Node、ContextSnapshot 和 ModelRun 之前；旧运行档案不可被后续设置回写。

## D-049：连续性通过聚焦框与分层记忆实现，不通过全量历史续接

- 日期：2026-08-21
- 状态：已确认
- 决策：会话保留完整原始事件和历史档案，但每一轮先创建或更新 FocusFrame，再从当前输入、短期工作记忆、活动分支记忆、已确认项目记忆和检索证据编译有限上下文。用户可选择继续当前问题、聚焦新问题、从节点分支或原样续接。
- 原因：全量历史会增加 Token、延迟和偏题风险；只开新会话又会丢失已确认的项目事实。分层记忆可以同时提供专注和连续性。
- 约束：原文不可被摘要覆盖；记忆必须带证据、状态和版本；低相关历史默认只索引不注入；排除项及原因进入本轮上下文账单。

## D-050：思考透明性只承诺可观察轨迹，不承诺完整隐藏思维链

- 日期：2026-08-21
- 状态：已确认
- 决策：将系统事件、Provider 思考摘要、MindScape 证据验证和模型自述分层展示，禁止把模型自述或生成的解释标成真实内部思考。Provider 返回的 `reasoning_content` 与可见正文、工具事件、用量事件分离处理。
- 原因：不同模型接口并不提供同等的隐藏推理信息；可回溯的上下文、工具和证据比不可验证的长思维文本更可靠，也能避免思考预算耗尽后被错误标记为成功。
- 约束：是否保存和展示原始 reasoning 受 Provider 条款和用户隐私设置限制；没有返回就不伪造；reasoning 不能无条件进入下一轮上下文。

## D-051：会话数据实体化为本地可追溯认知对象

- 日期：2026-08-21
- 状态：已确认，待 M2/M3 范围门禁
- 决策：MindScape 将会话、导入资料和工具结果分为原文层、实体层、关系层和检索层。实体首批覆盖项目、主题、目标、决策、约束、问题、任务、成果、来源、Claim、ModelRun 和 ToolRun；每个实体和关系都必须有稳定 ID、作用域、状态、版本和 EvidenceRef。
- 原因：仅保存消息或建立向量索引无法支持跨会话连续性、事实确认、冲突处理、删除和可回放上下文。实体和关系是把聊天沉淀为本地认知资产的基础。
- 约束：原文不可被摘要或实体覆盖；模型推断默认只能是 candidate/inferred，用户确认后才可成为项目事实；会话、项目和工作区知识库必须分开控制范围；实体化不等于建设知识图谱编辑器。

## D-052：采用混合检索，向量索引不作为事实源

- 日期：2026-08-21
- 状态：已确认，待 M2/M3 范围门禁
- 决策：RAG 使用全文检索、向量召回、关系邻居扩展、来源权威、用户确认状态和新鲜度进行混合排序，再由 Context Compiler 按 FocusFrame、预算和权限生成冻结上下文。SQLite 与原文是事实源，LanceDB 或其他向量库只是可重建索引。
- 原因：纯关键词无法处理语义改写，纯向量会召回相似但不相关的内容，纯图遍历又会过度扩散。混合检索才能兼顾准确性、连续性、证据和 Token 成本。
- 约束：每个命中必须能说明来源和选择理由；向量记录 embedding 模型、版本、维度、源哈希和分块版本；删除、否决、过期和冲突必须阻止旧索引继续把内容当作有效事实召回。

## D-053：Markdown 作为可读可迁移的知识层，SQLite 作为运行事实层

- 日期：2026-08-21
- 状态：已确认，待 M2/M3 范围门禁
- 决策：MindScape 本地知识库采用 Markdown 知识层与 SQLite 运行层并存的方案。Markdown 保存用户可阅读、可编辑、可迁移的会话记录、讨论日志、项目知识、决策、约束和成果，并使用稳定 ID、YAML frontmatter、EvidenceRef 和 Wikilink；SQLite 保存事件、运行状态、事务、版本和关系权威数据；向量库只保存可重建索引。
- 原因：Markdown 适合个人知识库和 Obsidian 互操作，但流式事件、崩溃恢复、事务和运行状态不能靠普通文本文件可靠承载。分层可以同时获得可读性、可迁移性和运行一致性。
- 约束：第一阶段优先生成可被 Obsidian 直接打开的标准 Vault；用户编辑 Markdown 通过修订事件重新索引，不能静默覆盖历史；完整双向同步和插件级兼容等待后续范围门禁。

## D-054：会话分支同时承担焦点隔离和记忆隔离

- 日期：2026-08-21
- 状态：已确认
- 决策：会话分支不仅表达画布关系，还决定上下文和知识记忆作用域。主线、探索分支、任务分支和复盘分支分别维护 `inherit`、`local`、`exclude` 和 `promote` 集合；任务分支的临时修改、错误尝试和未验证推断不得自动污染主线，成果只能通过显式回流候选和用户确认进入主线或项目知识库。
- 原因：用户在长期方向讨论中经常需要临时处理一个局部问题。若所有消息共享同一记忆，琐碎修复会污染战略目标，主线又会被模型带偏。分支隔离能够保留连续性，同时控制记忆边界。
- 约束：会话分支、知识分支和 Git 代码分支是不同概念，只能通过来源链接关联；MindScape 不因创建任务分支而自动执行本地代码或提交工程变更。

## D-055：M2 重新定义为本地知识与聚焦连续性 Alpha，并纳入通用会话导入

- 日期：2026-08-21
- 状态：已确认，立即执行，M2 Go / No-Go 待统一验收
- 决策：M2 不再只是“会话导入 Alpha”，而是验证通用会话导入、模型运行控制、Markdown 知识层、讨论日志、FocusFrame、主线/任务分支记忆隔离、有限实体化和本地混合检索的一条纵向闭环。导入首批支持 Markdown、JSONL 和 TXT 退化格式，保存原文、来源、预览并支持原样继续。
- 原因：导入是本地知识内核的真实输入源，不能继续把它完全后置；但平台专用适配器和完整接续分析会显著扩大范围，应在 M3 基于 M2 的实体、日志、分支和检索底座实现。
- 约束：M2 不做 ChatGPT / Claude / Codex 专用适配器、快速/详细/漫游分析、Obsidian 双向同步、复杂知识图谱、自动永久记忆或 AI IDE 能力；任何导入内容中的命令和工具调用都只作为数据保存，不自动执行。

## D-056：视觉性能优化以固定画质和可观测帧预算为硬约束

- 日期：2026-08-21
- 状态：已确认，适用于 VIS-008 原型和后续 VIS-009 生产适配
- 决策：性能优化优先采用固定 GPU 资源、增量更新、可见性裁剪、帧内预算和生命周期回收；不得通过降低雨滴/云层数量、纹理尺寸、DPR、Shader 参数或隐藏已确认视觉效果换取平均 FPS。环境采样可以按玻璃面板轮转，但每块面板的采样周期必须保持不变。
- 原因：MindScape 的氛围和 Liquid Glass 是已确认的视觉基线，降低画质会直接改变设计结论。游戏引擎的稳定帧预算和资源复用可以消除突发卡顿，同时保留像素级材质质量。
- 约束：每次优化必须同时提交平均 FPS、P95/最大帧间隙、Long Task、Canvas 数量和控制台错误证据；输入/阅读/设置等焦点态可以暂停已退让的环境循环，但必须保留最后一帧并在恢复焦点后继续。Classic Opaque、静态背景和业务 DOM 始终是故障回退。

## D-057：M2 原生 Markdown 阅读与用户字体偏好

- 日期：2026-08-27
- 状态：已确认，纳入 M2；完整视觉表现仍需员工06在创始人直管下验收
- 决策：M2 必须把 AI 正文和导入 Markdown 以安全的结构化阅读视图呈现，而不是显示 Markdown 标记原文；同时提供阅读字体、字号、行高和阅读宽度偏好。原始 Markdown、解析 AST/DOM、复制/导出文本和字体偏好严格分层，任何阅读设置都不得进入会话、FocusFrame、实体、RAG、ModelRun 或 Provider 请求。
- 原因：MindScape 的核心场景包含长文本学习、探索、项目讨论和知识库回看。Markdown 层级决定内容能否被快速理解，字体和排版决定用户能否长时间阅读；两者是 Chat 体验和本地知识层的基础能力，不是装饰性换肤。
- M2 P0：标题、段落、强调、列表、引用、代码块、表格、链接和安全自动链接；Chat、聚焦阅读器、导入预览和 Markdown 知识页使用同一安全 renderer；用户可选系统/内置字体、四档字号、行高和阅读宽度；流式期间不强制下拉，原文可复制/导出。
- M2 P1：受控导入 TTF/OTF/WOFF2 字体文件、字体预览和失败回退；代码块复制、原始 Markdown 与渲染文本的明确分层；长回答、窄窗、重启偏好和 reduced-motion 验收。数学公式、Mermaid、任意 HTML、远程图片主动加载、插件/组件执行和完整文档编辑器后置。
- 安全与工程约束：默认跳过原始 HTML，拒绝脚本、事件属性、危险 URL、任意 iframe 和自动执行命令/工具调用；解析失败必须回退可读纯文本且保留 raw。流式展示可使用独立 reveal queue，但 raw 事件、持久化正文、用量和终态不受动画影响。画布节点保持轻量摘要，完整 Markdown 只在 Chat/聚焦阅读器重排。
- 责任：员工04负责安全 renderer、Chat/导入/阅读器接线和字体偏好占位；员工03负责画布轻量摘要与长文/聚焦阅读投影性能；员工05负责未闭合结构、空正文、取消、失败、恢复和终态 flush 夹具；员工01/02负责 raw/投影/受控字体文件存储边界；员工06负责字体层级、排版和动效的最终视觉验收，具体工作由创始人直接安排。

## D-058：M2 首批阅读字体预设名单

- 日期：2026-08-29
- 状态：产品名单、资源和工程接线已完成；2026-08-30 创始人现场视觉预验收通过，最终 clean Release 回归待完成
- 决策：首批阅读字体预设为 Caveat、Nunito、Lato、Cormorant、寒蝉半圆体（ChillRoundM）、小赖字体（Xiaolai SC）和得意黑（Smiley Sans）。七项均进入字体选择器的产品目录，但西文字体必须使用受控中文回退，代码块和 UI 字体不被阅读字体覆盖。
- 分组：Nunito、Lato、寒蝉半圆体主打清晰正文；小赖字体、得意黑主打个性阅读；Caveat、Cormorant主打标题与氛围。分组用于推荐而非限制用户选择。
- 资源门禁：官方固定版本字体及其 OFL 文件已保存到 `desktop/src/assets/fonts/`，清单记录来源、版本、内部 family name、字重、字节数和 SHA-256；七款已经注册并进入选择器、Markdown/聚焦阅读与画布问答内容层。运行时不得静默加载远程字体，修改/子集化含 Reserved Font Name 的字体前必须重新评审命名条件；最终结论仍需 clean Release 的断网、DPI、重启、缺字和包体回归。
- 详细规格：`docs/design/visual-and-interaction/06-reading-font-preset-catalog-m2.md`。

## D-059：用户授权的本地呈现模型与可撤销阅读标注

- 日期：2026-08-30
- 状态：产品方向已确认；建议进入 M3，当前不扩大 M2 WIP；具体里程碑待创始人门禁
- 决策：MindScape 增加独立于 Chat 模型的本地呈现模型。只有在用户明确同意配置和运行后，它才读取用户选定或已完成的正文，并以受控结构化结果提出专业术语、实体和金句标注。所有标注属于可关闭、可撤销、可重建的展示层，不改 raw Markdown，不进入 Chat ModelRun，不自动写入 RAG、会话记忆或知识事实。
- 同意边界：未授权时不下载模型、不扫描端口、不连接本地服务、不后台分析；用户可以跳过且继续使用完整 Chat/Markdown/字体/导入能力。下载模型、连接已有运行时和允许后台分析分别授权，局域网/远程 endpoint 不能冒充本地处理。
- 工程边界：使用 Markdown block ID、内容 hash 和 exact/prefix/suffix 选择器稳定锚定；确定性程序拒绝不匹配、跨块、冲突、过密和低置信度候选。默认只在终态后分析可见内容，缓存可删除；只有用户再次确认，术语或金句才能进入知识候选。
- 范围门禁：当前 M2 继续完成 clean Release，不临时加入模型运行时。M3 第一条切片优先支持用户已有的 loopback OpenAI-compatible 本地模型、三类标注、取消、缓存和清除；受管模型下载、硬件自动选择和更多标注 Skill 后置。
- 详细规格：`docs/design/visual-and-interaction/07-local-presentation-intelligence-layer.md`。

## D-060：Review Lab 可视进度与正式 Acceptance Release 双轨发布

- 日期：2026-08-31
- 状态：创始人已确认，立即执行
- 决策：可观察的 UI/交互里程碑必须及时进入 Review Lab“员工确认主线” `preview`；唯一正式 Windows 程序继续由 clean 集成源通过 `scripts/publish-acceptance.ps1` 发布到 `artifacts/acceptance/`。preview 可以来自明确标注的 dirty 源，但不能冒充 Release 或 Go 证据；正式 Acceptance Build 发布后必须以同一 Build ID、commit、SHA-256 和路径登记为 Review Lab `full` 里程碑引用，不得为登记重新编译第二份 EXE。
- 责任：员工03/04的可见切片必须发布 preview；员工01/02/05仅在改动影响用户可见行为时发布，纯后端切片在 PEC 中声明无需视觉版本；员工01冻结正式集成源，员工05统一发布 Acceptance Build。员工06继续由创始人直管，不替其他员工补发版本。
- 过渡约束：当前 Review Lab 与 Acceptance 发布脚本尚无自动登记接口。在接口落地前，员工05必须在 Release PEC 报告 `full` 登记缺口；自动化不得改变正式程序、哈希或唯一性，涉及 Review Lab 本体的实现由创始人另行决定是否交给员工06。
