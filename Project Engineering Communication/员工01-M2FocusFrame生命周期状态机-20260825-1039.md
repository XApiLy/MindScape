# 员工01｜M2 FocusFrame 生命周期状态机

> 提交时间：2026-08-25 10:39（UTC+8）  
> 覆盖任务：`CORE-M2-001`、`ARC-M2-002` 纯领域切片  
> 状态：关闭/恢复生命周期契约完成；SQLite 持久化、查询 IPC、切换命令和启动恢复仍待数据层接入

## 已阅读与落实

- 已阅读当前 PEC 控制文件、M2 派单、V1 产品范围/路线、Core Kernel、前五员工职责、RC1、威胁模型、架构走查、Git/PEC/验收制度，以及本地实体/RAG、Markdown Vault/分支隔离、模型控制/聚焦连续性方案。
- 已重新读取员工02～06当前报告：员工02已将 `validate_import_bundle` 接入 SQLite 事务前置校验并补父消息顺序/重复 hash 测试；员工03已完成知识上下文画布投影；员工04已完成运行档案/上下文账单占位交接；员工05已补 reasoning/content/usage/终态 fixture；员工06仍由创始人直管。
- 具体落实：本次只冻结 FocusFrame 生命周期纯领域入口与 TS 镜像，不改员工02 SQLite、员工03/04 UI、员工05 Provider 或员工06视觉原型。

## 本次推进

- 新增 `desktop/src-tauri/src/domain/focus_lifecycle.rs`：
  - `FocusFrameLifecycleSnapshot` 独立记录 `active/closed`、revision、更新时间和关闭时间。
  - `close_focus_frame` 只允许 `Active → Closed`；`reopen_focus_frame` 只允许 `Closed → Active`。
  - 关闭/恢复保持 FocusFrame 稳定 ID，revision 单调递增；空时间、重复关闭/恢复和 revision 溢出显式失败。
- 从 `domain/mod.rs` 导出生命周期契约和入口。
- 在 `desktop/src/domain/focus.ts` 添加 `FocusFrameLifecycleSnapshot` 镜像。
- 更新 `docs/contracts/m2-contract-baseline.md`，明确生命周期契约与持久化/查询/启动恢复边界。

## 与同事的接口沟通与落实

- **员工02接口请求**：SQLite 持久化应保存 `frame.id`、status、revision、updatedAt、closedAt，并在事务/启动恢复中调用同一状态语义；不得用画布当前选择推断 Active/Closed。
- **员工03接口请求**：画布只投影生命周期状态和 revision，不复制 close/reopen 转换；关闭 FocusFrame 不能删除其历史快照。
- **员工04接口请求**：Chat/账单显示查询返回的生命周期状态；关闭或恢复命令通过 IPC 调用领域入口，不在组件中修改 status。
- **员工05接口确认**：运行档案和 reasoning fixture 已进入共享门禁，本切片不改 Provider 事件语义。

## 交付与验证证据

- Rust 单测：`cargo test --all-targets --locked`，88 passed / 0 failed；新增关闭→恢复、非法重复转换、空时间和 revision 溢出测试。
- Rust 门禁：`cargo fmt --all -- --check`、`cargo clippy --all-targets --all-features --locked -- -D warnings` 通过。
- 前端门禁：`pnpm exec tsc --noEmit`、`pnpm test:chat`（32/32）、`pnpm test:canvas`（18/18）、`pnpm test:canvas:perf`（1/1）通过。
- 构建：`pnpm build` 通过（1830 modules）。
- 仓库门禁：`git diff --check`、`node scripts/check-pec-retention.mjs`、`node scripts/check-secrets.mjs` 通过。

## 契约、数据、安全、恢复与接口影响

- 新契约为加法，不修改 M1 `ContextSnapshot`、runtime、SQLite 现有 schema 或 Provider 请求；不读取密钥、raw storage 或用户原文。
- 生命周期状态不是删除语义：关闭只阻止当前 FocusFrame 继续作为活动焦点，历史 `FocusedContextSnapshot`、实体 EvidenceRef 和运行档案仍可审计/回放。
- 本切片没有接入命令或存储，因此不能宣称 FocusFrame 已可在重启后恢复，也不能宣称 `CORE-M2-001` 完整完成。

## 风险与下一步 WIP（最多两个）

1. **DATA-M2-001 / CORE-M2-001 联调**：与员工02冻结 FocusFrame 表/事务/查询 DTO，覆盖关闭、恢复、重复命令、崩溃重启和 revision 冲突。
2. **CHAT-M2-001 / CAN-M2-003 联调**：与员工03/04消费生命周期查询结果，确保切换分支、账单和画布状态只读领域快照。

## 当前阻塞

- FocusFrame 生命周期命令、SQLite 持久化和 Tauri 查询 IPC 尚未交付；当前只提供可编译、可测试的领域权威语义。
- 员工05的真实 DeepSeek Key/Tauri 验收仍未完成；M2 不能据此标记 Go。

