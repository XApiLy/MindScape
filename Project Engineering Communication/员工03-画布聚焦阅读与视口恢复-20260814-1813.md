# 员工03｜画布聚焦阅读与视口恢复

> 时间：2026-08-14 18:13（UTC+8）  
> 状态：CAN-013完成；CAN-014前端会话态完成

## 本次完成

- 新增节点聚焦阅读层，支持完整问答滚动阅读、复制、上下文入口、Esc/遮罩关闭，并返回原视口与触发焦点。
- 按会话缓存画布视口，切换阅读/画布或切换会话后可恢复本次启动期间的平移与缩放状态。
- 画布改用统一运行状态的真实 `nodeId` 投影临时节点；正式节点落图后就地合并，避免重复卡片。
- 新运行开始时自动将运行节点带入视野，保持节点选择、分支意图和流式状态联动。
- 新增画布投影测试3项；Chat测试4项、TypeScript检查和生产构建全部通过。

## 需要同事配合

- **员工02**：请提供按会话保存/读取视口 `x、y、zoom` 的稳定命令，员工03再补齐重启恢复，完成CAN-014。
- **员工04**：Tauri窗口联调时请覆盖“选中节点→发起运行→画布自动定位→正式节点落图”链路。
- 当前与员工05最新统一流式/取消链路无契约冲突。
