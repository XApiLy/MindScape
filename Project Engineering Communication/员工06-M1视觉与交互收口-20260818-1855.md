# 员工06｜M1 视觉与交互收口

> 提交时间：2026-08-18 18:55（UTC+8）  
> 覆盖任务：M1 PEC 收口派单中员工06的 1～4 项  
> 状态：视觉收口稿和可点击原型已提交，待创始人确认；Review Lab 功能冻结  
> 基线提交：`f54363e8`（共享工作树）

## 相比上一份的变化

- 按 `docs/engineering/m1-pec-review-and-closeout-20260818.md` 结束 Review Lab 扩展，回到 MindScape 本体做 M1 视觉与交互收口。
- 修正 Review Lab 传入 Tauri CLI 的 `CI=1` 兼容问题；在应用 release EXE 已成功编译、仅安装包阶段因图标配置失败后，将 `full` 语义收敛为 Tauri `build --no-bundle` 和原生 EXE 归档，不再误把 MSI/NSIS 制作等同于视觉验收。
- 提交当前 M1 界面的“保留 / 修改 / 删除 / 后置”清单和空白、发送、流式、停止中、已停止、完成、失败、恢复、无 Key 九态矩阵。
- 新增不依赖 Provider 和 SQLite 的可点击原型，覆盖发送、流式、停止、节点生成、聚焦阅读及非成功状态。

## 已阅读与落实

- 重读 M1 PEC 审阅与收口派单，将本轮限定为“真实 Chat Alpha 状态收口”，没有修改 `desktop/` 正式产品代码，没有扩大到导入器、多 Provider、新画布能力或整体视觉重构。
- 核对当前员工03视口验收、员工04停止交互和员工05真实取消验收 PEC，交互规格不越过他们对视口、运行和 Provider 事实的所有权。
- 延续产品范围、产品根基、内核总览、跨团队契约、威胁模型、Git 规范、PEC 规则和视觉岗位职责的阅读结论：原型只是视觉投影，不成为 Node、ModelRun、取消、恢复、用量或错误事实源。
- 与收口派单没有冲突；尚未获得创始人视觉确认，因此本次不向员工03/04发改动任务。

## 交付物与可核验证据

### M1 视觉收口稿

- `docs/design/m1-chat-alpha-visual-closeout-20260818.md`
- 包含：保留 / 修改 / 删除 / 后置清单、九态矩阵、共同验收条件、原型操作路径和 5 个待创始人确认的高影响决定。
- 主要判断：保留三区布局、双视图、底部输入器、单点停止和聚焦阅读；M1 只修可读性、状态层级、停止过渡、失败行动与恢复告知。

### 可点击原型

- `docs/design/prototypes/m1-chat-alpha-closeout/index.html`
- `styles.css` 和 `app.js` 与入口同目录，纯 HTML/CSS/JS，可直接本地打开或通过任意本机静态服务器访问。
- 主路径：空白 → 发送 → 流式 → 停止中 / 完成 → 已停止 / 聚焦阅读。
- 顶部快捷入口：无 Key、失败、恢复、重置演示；支持用 query 参数形成确定性状态链接，便于视觉回归。

### Review Lab 完整构建收口

- `tools/review-lab/server.mjs`：`CI` 由 `1` 修正为 Tauri CLI 接受的 `true`；`full` 改为 `tauri build --no-bundle`，归档可直接启动的 release EXE。
- `tools/review-lab/README.md`：明确 Review Lab 不制作安装包，安装包图标和签名不是视觉版本验收阻断。
- 失败版本记录继续保留：原 `CI=1` 失败和应用 EXE 成功、安装包图标失败都可在历史中追溯。
- 最终重建版本：`20260818T105419Z-can-014-画布视口持久化-原生exe重建-b64a`。
- 最终状态：`ready`；`sourceChangedDuringBuild=false`；归档原生产物 `native/mindscape-desktop.exe`。

## 验证结果

- Review Lab `npm run test`：8/8 通过。
- `node --check tools/review-lab/server.mjs`：通过。
- `node --check docs/design/prototypes/m1-chat-alpha-closeout/app.js`：通过。
- Review Lab `full` 重建：TypeScript/Vite 生产构建通过，Tauri release 构建退出码 0，原生 EXE 已归档且构建期间源码指纹未变。
- Review Lab 归档启动验证：`POST /api/versions/<id>/launch` 返回 `launched=true`，进程路径指向该版本 `native/mindscape-desktop.exe`，窗口标题为 `MindScape`。
- 可点击原型已用 Edge 1440×900 实际渲染查验：空白、无 Key、已停止和聚焦阅读视图正常；发现并修正了空状态在全局通知隐藏时的 CSS Grid 行塑缩问题。
- 停止演示保留部分文本，显示“已停止”、不完整说明、手动重试和聚焦阅读；阅读视图具有关闭和返回画布路径。
- PEC 当前窗口校验：本报告原子替换后执行。

## 契约、数据、安全与恢复影响

- 未修改 `desktop/` 产品代码、Frozen V1 契约、SQLite schema、Provider 实现、凭据边界或恢复算法。
- 原型是无后端、无持久化的视觉模拟；不发出网络请求，不读写凭据和用户真实对话。
- 状态矩阵对契约事实的投影仍需员工01确认不变量、员工02确认恢复事实、员工05确认错误/用量映射。
- Review Lab 仍只监听 `127.0.0.1`；完整版本只归档本机 EXE，未引入安装、分发、签名或远程暴露。

## 风险与阻塞

- M1 视觉方向尚未获创始人确认；本轮产品修改和向员工03/04的局部派单因此正确暂停。
- 原型只能验证交互表达，不能代替真实 Provider 的网络取消、无后续 delta、唯一终态、超时、异常断流、用量和启动恢复验收。
- 当前工作树仍是跨员工共享的集成恢复现场，本 PEC 不把其他员工修改声称为员工06独立提交。

## 下一步 WIP（最多两项）

1. 请创始人打开收口文档和可点击原型，对 5 个高影响决定给出保留、修改、否决或后置。
2. 只在创始人确认后，将必要的画布局部表现修正发给员工03，将 Chat 状态、停止、失败与恢复的局部表现修正发给员工04；不发整体重构。

## 需要谁配合

- **创始人**：完成本轮唯一不可替代动作——视觉与交互方向确认。
- **员工03/04**：现阶段不需要预先改界面；等员工06携创始人确认结论发局部规格。
- **员工01/02/05**：在状态文案落地前，分别确认契约、恢复和 Provider 事实，不参与重做视觉方案。
