# 员工04｜PROV-013 真实取消闭环

> 提交时间：2026-08-19 14:52（UTC+8）  
> 覆盖任务：CHAT-012、CHAT-015、PROV-013  
> 状态：真实取消与阅读视图单卡复验通过；M1 其他 No-Go 项继续推进

## 相比上一份的变化

- 创始人在统一 Tauri release 验收版本中重新打开“新会话 3”，确认已停止回答只显示一张卡片。
- 修复后的单卡继续保留 DeepSeek provider/model、部分回答、“已停止”状态和“重试”入口。
- 统一验收脚本 4 从“部分通过”更新为“通过”；用户取消不变量证据已闭环，等待员工01签署。
- 纠正上一份 PEC：此前交付的 `target/debug` 程序会访问 `localhost:1420`，不符合新生效的验收程序规则，其程序与结论均已作废。

## 已阅读与落实

- 本轮开工前重新读取 PEC 控制文件、员工04当前报告、统一验收表，并完整读取新生效的 `docs/engineering/acceptance-build-policy.md`。
- 验收程序只认 `artifacts/acceptance/versions/<build-id>/` 中由统一脚本发布的 Tauri release；不再引用 `target/` 调试程序作为验收事实。
- 继续落实 V1 产品、Core Kernel、Frozen V1、安全和 M1 收口边界；本轮只关闭 PROV-013 界面缺口，不进入 M2 或扩展能力。

## 本次证据

- 证据目录：`docs/engineering/evidence/employee04/prov-013-real-cancel-20260819/`。
- 修复后截图：`03-release-single-card-recheck.png`，SHA-256 `D34BFE4AE7A32B3DC5E48A62FDBF81B910D58AB599ECBBFCBBD3BB7E40F43C93`。
- Build ID：`20260819-144548-chat-012-single-card-recheck-0cb1c0053b5a-dirty`。
- 程序路径：`D:\Project\MindScape\artifacts\acceptance\versions\20260819-144548-chat-012-single-card-recheck-0cb1c0053b5a-dirty\mindscape-desktop.exe`。
- 程序 SHA-256：`AF0378DAF3D4B680A60272283CCA047698CCC71008A0CF52494FF090185D30D4`。
- 来源提交：`0cb1c0053b5a`，manifest 明示 `sourceTreeDirty=true`。
- 现场进程路径与上述发布目录一致；该版本已在无开发服务器时独立显示 MindScape 工作区。

## 验收结论

- 真实网络/数据：唯一 `cancelled` 终态、终态后 delta 为 0、部分内容保留、单一 Node/Run、SQLite 完整性通过。
- 真实界面：同一次已停止运行只显示一张卡片，未再出现 `NodeCard` 与 `ActiveRunCard` 双重投影。
- 交互：运行卡显示 `deepseek-v4-flash · deepseek`、“已停止”、保留内容说明和“重试”入口。
- 底部 `mock-stream-v1` 是下一次发送的当前选择，不会改写已停止运行的 provider/model。
- PROV-013 / 统一验收脚本 4：通过。

## 契约、数据、安全与恢复影响

- 本轮没有代码、Frozen V1、SQLite schema、Provider 协议或持久化语义变更，只新增正式验收证据与状态更新。
- 截图未打开设置页、未包含 API Key；证据仍只保存脱敏聚合事实。
- 工作树为 dirty，因此该 Build ID 适合当前专项验收，不作为里程碑最终 clean Go 版本。

## 验证结果

- 正式 release 单卡人工复验：通过。
- 发布程序 SHA-256 与 manifest / `SHA256SUMS.txt`：一致。
- PEC 七文件、秘密扫描、JSON 证据解析和 `git diff --check`：本报告替换后复跑。

## 风险与剩余 No-Go

- M1 仍有首次空状态、真实分支 ContextSnapshot、强退恢复、无效 Key、断网/超时、日志/崩溃脱敏等缺口，不因脚本 4 通过而进入 M2。
- 最终里程碑验收仍需要来源可追溯的 clean commit release Build ID。

## 下一步 WIP（最多两项）

1. 与员工05继续执行无效 Key、断网/超时和异常断流矩阵。
2. 配合员工02/03完成取消后重启与画布唯一节点复核。

## 需要同事配合

- **员工01**：签署用户取消不变量，并确认统一验收表脚本 4 正式关闭。
- **员工02/03**：复用同一证据核对取消后重启、画布节点与边稳定性。
- **员工05**：签署网络取消事实并推进 PROV-014/016/017。
