# 员工01｜M2 知识状态与分支检索首切片

> 提交时间：2026-08-24 18:24（UTC+8）  
> 覆盖任务：`CORE-M2-003`（纯领域首切片）  
> 状态：知识状态机与 FocusFrame 检索隔离规则完成；事务持久化、Context Compiler 和 IPC 仍待联调

## 已阅读与落实

- 已阅读当前 PEC 控制文件、M2 派单、V1 产品/内核/契约/威胁模型、架构走查、Git/PEC/验收制度，以及本地实体/RAG、Markdown Vault/分支隔离、模型控制与聚焦连续性方案。
- 已重新读取员工02～06当前 PEC：员工02已完成 SQLite v6 导入表但尚未提供事务 IPC；员工03只做契约到画布的纯投影；员工04只读展示能力快照并等待生命周期命令；员工05的运行档案映射仍未挂入 `ModelRunRequest`；员工06仅维护视觉原型。
- 这些边界落实为：本次只新增 Rust 纯领域规则和契约说明，不改 SQLite、导入存储、Provider 请求、UI、凭据或视觉原型，不复制其他员工的解析/持久化/展示算法。

## 本次推进

- 新增 `desktop/src-tauri/src/domain/knowledge.rs`：
  - `candidate/inferred -> confirmed/rejected`；`confirmed -> superseded/stale`；终态不可复活；取代必须提供不同的新实体 ID。
  - 每次合法状态变更递增 `revision`，更新 `updated_at` 与 `generator`；空时间和 revision 溢出显式报错。
  - `retrieval_decision` 执行状态优先排除、`exclude` 优先于 `include/inherit/local`、作用域匹配和 FocusFrame 身份校验。
  - 主线允许按作用域读取；任务/探索/复盘分支必须显式引用继承或包含；候选/推断即使被选中也只能作为 `CandidateOnly`。
- 在 `desktop/src-tauri/src/domain/mod.rs` 导出领域入口。
- 更新 `docs/contracts/m2-contract-baseline.md`，冻结 `CORE-M2-003` 的状态和分支污染语义，并明确持久化、Markdown、检索索引、Context Compiler 和 Provider 映射尚未完成。

## 交付与验证证据

- Rust 单测：`cargo test --all-targets --locked`，70 passed / 0 failed；包含状态流转、禁止复活、任务分支显式继承、候选不冒充确认事实、焦点帧隔离等测试。
- 格式：`cargo fmt --all -- --check` 通过。
- 前端类型与回归：`pnpm exec tsc --noEmit`、`pnpm test:chat`（24/24）通过。
- 构建：`pnpm build` 通过（Vite 1828 modules）。
- 仓库门禁：`git diff --check`、`node scripts/check-pec-retention.mjs`、`node scripts/check-secrets.mjs` 通过。
- `cargo clippy --all-targets --all-features --locked -- -D warnings` 当前未通过：员工05尚未接线的 `map_effective_deepseek_profile` 及其参数快照在非测试构建中触发 2 个 dead-code；不属于本切片代码，待其挂入 `ModelRunRequest` 后复测。

## 契约、数据、安全、恢复与接口影响

- 未改变 M1 `mindscape.context.v1`、`mindscape.runtime.v1`、SQLite v5 语义；未新增 schema、IPC、网络请求或 UI 状态。
- 规则只返回纯领域决策，不写用户原文、API Key、完整推理或日志；旧实体版本由后续事务层负责保留，本切片不宣称已持久化。
- 当前 `KnowledgeTransition` 的取代关系仅校验替代 ID，关系记录、删除/否决命令、检索索引和 Context Compiler 接线仍未交付。

## 下一步 WIP（最多两个）

1. **CORE-M2-004**：与员工02评审知识实体/关系/证据的 SQLite 事务字段和 revision 历史写入边界；与员工03/04确认结构化查询返回不复制领域规则。
2. **CORE-M2-004 / PROV-M2-002 联调**：在 EffectiveRunProfile 生命周期字段挂入 `ModelRunRequest` 后，与员工05接入创建前能力预检，并恢复全量 Clippy 门禁。

## 需要同事配合

- **员工02**：确认 `KnowledgeEntity` 状态、scope、revision、generator 与 v6/v下一版事务表的一一映射，再接入命令与重启恢复测试。
- **员工03/04**：消费 `retrieval_decision`/结构化查询结果，不在画布或 Intake 中自行实现继承、排除和状态转换算法。
- **员工05**：完成 EffectiveRunProfile 到真实发送链路的接线，清理当前 Clippy dead-code 阻塞并提供统一 `ProviderError`。
