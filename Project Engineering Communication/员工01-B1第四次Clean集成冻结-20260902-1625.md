# 员工01｜B1 第四次 Clean 集成冻结

> 提交时间：2026-09-02 16:25（UTC+8）  
> 关闭项：`B1` clean 集成基线（第四次冻结）  
> 状态：**弹窗实际 WebView 顶部/滚动修复已冻结；B5 / M2 仍等待真实纵向验收**

## 本次完成

- 纳入员工04 `styles.css` 修复：导入弹窗使用 `100vh` 回退与 `100dvh` 增强，遮罩顶部对齐并允许滚动，内容网格从顶部排列，解决矮 WebView 下标题被垂直居中裁切的问题。
- 归档员工01第三次冻结报告、员工04上一份 UI 报告、员工05上一份来源交互报告；当前 PEC 窗口保持各员工唯一最新报告。
- 本轮不改变 ImportSource、Proposal、EvidenceRef、SQLite 或 typed IPC 契约。

## 跨模块证据

1. `ImportIntakeDialog` 现有导入/来源/提案结构不变，仅通过 CSS 保障矮窗口下的顶部、内容滚动和 footer 可达；Review Lab preview `20260902T080049Z-b5-导入弹窗顶部与滚动修复-c53a` 已 ready。
2. 员工04现场实测 826×516：顶部完整出现、内容可滚动、底部操作可访问；Chat `69/69`、Canvas `34/34`、性能 `2/2`、Vite `2100 modules` 通过。
3. 本轮仅前端样式变更；此前 Rust `245 passed / 0 failed / 1 ignored`、B5 audit `4/4`、Clippy/fmt 已由上一冻结验证，PEC retention、secret scan、`git diff --check` 本轮复核通过。

## 提交信息

- Commit：待写入（最终哈希以远端 HEAD 为准）
- 上一冻结：`a2cdf48cc0aa` `fix(m2): freeze import workflow polish`
- Branch：`codex/m1-integration-recovery`

## 剩余阻断与协作

1. **员工05**：从本次 clean commit 重新生成唯一 Acceptance Release，更新 Build ID、程序路径、SHA-256；旧 Release 不包含本轮弹窗修复。
2. **员工05 + 员工01～04**：在新 Build 完成 B5 同一真实样本导入、提案、Confirm/Reject、FocusFrame 回流、Close/Reopen/重启及故障矩阵。
3. 本提交仅再次关闭 B1 clean 集成冻结，不宣称 B5 或 M2 Go。
