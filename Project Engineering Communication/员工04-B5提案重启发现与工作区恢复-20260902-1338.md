# 员工04｜B5 提案重启发现与工作区恢复

> 提交时间：2026-09-02 13:38（UTC+8）  
> 覆盖阻断：`B2/B5-UI` 导入提案重启发现、pending/completed 恢复与 Chat 工作区落点隔离  
> 状态：**员工04责任项已关闭；B5 / M2 仍为 No-Go，等待唯一 clean Release 纵向验收**

## 已阅读与落实

- 已重读控制 PEC 顶部“2026-08-30：M2 Go 阻断收敛”和员工直接执行表，并读取员工01《B2/B5 提案重启发现入口契约冻结》、员工02《B5 导入实体提案原子持久化》、员工03《B5 导入知识建议审核入口》、员工05《B5 建议 Producer 与 Request 链闭环》。
- 已落实员工01冻结的 `discover_import_knowledge_proposals` 查询边界：UI 不缓存 requestId 作为事实，而是在来源预览打开时按 source/revision 发现 durable request；只展示 Kernel 返回的 pending/completed、proposal/review 计数和已完成批次。
- 已落实 M2 Markdown/安全 renderer 与 UI-HANDOFF-06 边界：恢复面板只呈现状态与可执行动作，不读取正文路径、不改变原始 Markdown/证据分层。

## 本次关闭的 B 编号

### B2/B5-UI：关闭员工04负责的提案重启发现与工作区恢复接线

- `App → kernelClient → discover_import_knowledge_proposals` 已接通；浏览器预览明确禁用本地发现，Tauri 错误统一映射为安全提示。
- `ImportKnowledgeProposalPanel` 在每个已加载来源上执行有界发现（limit=8），展示“正在恢复 / 恢复失败 / 已恢复请求”；completed 批次可恢复查看并继续加载 review 历史，pending 保持诚实等待态，不伪造 batch。
- 既有生成、Confirm/Reject、同 requestId/decisionId 重试和知识库存刷新保持不变；scope 仍由 `importKnowledgeProposalTargets.ts` 过滤为当前会话 + 当前会话内 Active 非 mainline FocusFrame。

## 跨模块证据

1. 跨模块链路：`ImportBundle source/revision → discover query → SQLite v18 durable request/batch → ImportKnowledgeProposalPanel → get batch/list reviews → Confirm/Reject`；发现查询仅返回脱敏状态和计数。
2. 前端验证：Chat `69/69`、Canvas `34/34`、Canvas 性能 `2/2`、Vite production build `2100 modules`；新增落点隔离回归覆盖跨会话、Closed、mainline 和重复 FocusFrame。
3. 内核验证：Rust `cargo test --all-targets --all-features --locked` 为 `243 passed / 0 failed / 1 ignored`，B5 audit `4/4`；包含 source/revision 重启发现、pending/completed 排序、request 精确重放、Confirm/Reject 原子事务、Vault/FTS/vector 恢复。
4. Review Lab 员工确认主线 preview 已 ready：`20260902T044046Z-b5-提案重启发现与落点恢复-921d`。重点观察：导入来源预览中的提案历史、恢复查看与重试状态；该 preview 仅证明可见 UI，不替代 Tauri Release。

## 契约、数据、安全与恢复影响

- 不新增领域对象、数据库写路径或 Provider 行为；只读消费员工01/02已冻结的发现投影。
- 恢复路径不依赖 React 临时状态；pending 只表示 durable receipt 尚未形成 batch，completed 才允许恢复查看。
- 不输出 Key、正文、绝对路径、完整稳定 ID 或未授权来源内容。

## 剩余阻断与协作

1. **B5 本体仍未关闭**：需要员工05在唯一 clean Acceptance Release 中执行同一真实样本全链（导入 → 显式提案 → Confirm FocusFrame candidate → 候选选择/生成 → Close 四动作 → Reopen/重启），并提交脱敏跨模块证据。
2. **B1 仍未关闭**：员工01需冻结 clean 集成提交；员工05负责唯一 Release、Build ID、路径、SHA-256 及全量回归。当前共享树 dirty，本轮不发布个人 Acceptance Build。

## 下一步 WIP（最多两个）

1. 配合员工05在 clean Release 中复验导入提案发现/恢复与后半段 FocusFrame 回流链。
2. 若真实验收发现查询分页或失败状态不足，先回 PEC 登记再扩展，不在 UI 侧自行推断状态。
