# 员工03｜B5 导入知识建议审核入口

> 提交时间：2026-09-01 23:51（UTC+8）  
> 关闭项：`B2/B5` 中员工03负责的“导入详情消费提案/审核 typed UI”  
> 状态：**员工03 UI 子项已关闭；B5 全链仍为 No-Go**

## 本次关闭的 B 编号

- 关闭 `B2/B5` 的员工03前端责任项：导入详情已能显式勾选 ImportedMessage、选择 Conversation 或 Active FocusFrame、请求来源约束建议，并逐条查看不可编辑 EvidenceRef、修改允许字段、Confirm/Reject。
- 导入后不会自动分析；无选择不可生成。失败重试复用原 `requestId/decisionId`，React 不构造 EvidenceRef、entityId、status、revision 或 generator。
- Conversation Confirm 与 FocusFrame candidate 的结果提示分离；确认后只通过 Kernel 投影刷新知识库存。

## 跨模块证据

- 上游契约：员工01提交 `21c5ba8 feat(core): freeze import knowledge proposal flow`，冻结 `mindscape.import-knowledge-proposal.v1`；本次直接消费其 TypeScript 镜像与四个保留 IPC 名称。
- 前端链路：`src/app/importKnowledgeProposal.ts` → `src/app/kernelClient.ts` → `src/App.tsx` → `src/components/ChatWorkspace.tsx` → `src/components/ImportIntakeDialog.tsx`，正式 target scope 只来自当前 Conversation/Active FocusFrame 投影。
- 回归：Chat `66/66`、Canvas `34/34`、Canvas 性能 `2/2`、Vite production build `2099 modules`，`git diff --check` 通过。
- 可见交付：Review Lab 主线 `preview` 已 ready，ID `20260901T155050Z-b5-导入知识建议与审核入口-f995`；预览不伪造提案数据，命令缺失时展示真实不可用态。
- 同步看到员工02正在未提交工作区实现 schema v18 request/batch/review 原子持久化；当前仅 SQLite adapter 可见，尚不能作为 Tauri 端到端证据。

## 剩余阻断与协作

1. **员工02**：补齐 application service、四个 reserved Tauri commands 与 invoke handler，并给出全新数据库 request/batch/review/重启回放证据；当前前端调用会因命令未注册而进入错误态。
2. **员工05**：补齐受 EffectiveRunProfile 约束的 Provider/确定性规则提案生成，覆盖失败、取消、响应丢失、重试不重复计费；当前无可消费的 suggestion producer。
3. **员工01/02**：确认重启后如何从 import source/revision 权威发现既有 request/batch。现有 `get_import_knowledge_proposal_batch(requestId)` 只有已知 requestId 才可查询，前端不能凭本地缓存宣称完成崩溃恢复。
4. **员工05验收**：上述接线后，从全新数据库执行“真实导入 → 显式建议 → FocusFrame candidate → 选择/生成 → Close → 四动作 → Reopen/重启”同一 clean Release；在此之前 B5 不可关闭。

## 下一步 WIP（最多两个）

1. 员工02/05交付后消费真实 commands/provider，补齐全链错误语义与重启恢复 UI。
2. 配合员工05跑全新数据库端到端验收，仅在跨模块证据成立后更新 B5 状态。
