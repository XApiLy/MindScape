# 员工02｜B5 导入实体提案原子持久化

> 提交时间：2026-09-02 12:03（UTC+8）  
> 覆盖阻断：`B2/B5-DATA` 真实导入建议的 receipt、审核原子提交与崩溃恢复  
> 状态：**员工02数据责任项已接入；B5 / M2 仍为 No-Go，等待 Provider 请求命令与唯一 clean Release 纵向验收**

## 已阅读与落实

- 已阅读员工01《B2B5实体生产前半段契约冻结》及 `mindscape.import-knowledge-proposal.v1`：导入继续只保存原文，只有用户显式请求才能生成建议；proposal 在 Confirm 前不进入 KnowledgeEntity、索引、Vault 或上下文。
- 已阅读员工03《B5导入知识建议审核入口》和员工05《B5候选生成复验与实体入口阻断》：前端已消费四个保留 IPC 名称，但真实 suggestion producer 尚未接入。因此本轮只交付员工02负责的 schema/事务/恢复和三个已具备后端语义的 query/review/list 命令，不伪造 Provider 结果。
- 已落实 PEC 控制文件的 B2/B3/B5、脱敏与唯一 Release 边界。本轮无可见 UI 变化，因此不发布 Review Lab preview，也不发布员工02个人 Acceptance Build。

## 本次关闭的 B 编号

### B2/B5-DATA：关闭员工02负责的导入提案持久化、审核和恢复子项

- schema 升至 v18，新增 request、proposal/batch、review 三组不可变 receipt。`requestId` 精确重放恢复原 `generationRunId`，改变 source/revision/hash/message/scope 时冲突；batch 必须匹配已持久化请求和真实 ImportContent 来源。
- Confirm 在同一 SQLite 事务写 review receipt、KnowledgeEntity、独立 EvidenceRef、FTS 与 vector；Reject 只写 review receipt。`decisionId` 精确重放不重复建实体，复用 ID 改输入或二次决策同一 proposal 均拒绝。
- Vault 新增 `.import-knowledge-review-transactions` journal。KernelService 使用 Vault-first compensation：SQLite 失败恢复实体页、EvidenceRef 来源页与索引 pre-image；启动恢复未提交 journal，已提交事务保留投影。FocusFrame Confirm 生成 branch-local candidate，Conversation Confirm 生成 confirmed entity。
- 已注册 `get_import_knowledge_proposal_batch`、`review_import_knowledge_proposal`、`list_import_knowledge_proposal_reviews` typed Tauri 命令。`request_import_knowledge_proposals` 仍等待员工05接入受 EffectiveRunProfile 约束的 Provider/确定性规则；未开放“前端提交完整 batch”的不可信捷径。
- 修正跨模块路径冲突：EvidenceRef 文件名不再继承含冒号的 proposal ID，改由 Kernel 的 path-safe `generationRunId + proposal ordinal + evidence index` 生成。请员工01复评这一不改变证据语义的 Windows Vault 兼容约束。

## 跨模块证据

1. 真实分支回归贯通 `ImportSource/Revision/ImportedMessage → request receipt → source-grounded proposal → Confirm → branch candidate → evidence_refs/FTS/vector → Vault entity/source/index → 精确重放 → 重启恢复`；未手写实体、EvidenceRef 或 promoteRefs。
2. request 回归证明同一 ID 跨重启恢复同一 `generationRunId`，异输入冲突；review 回归证明 Confirm 原子提交且不重复，未解析 batch 全回滚后可合法重试，Reject 保持 0 entity / 0 evidence；Vault 回归覆盖未提交恢复与已提交保留。
3. Rust 全量回归：library `240 passed / 0 failed / 1 ignored`，B5 audit example `4 passed / 0 failed`；`cargo check`、Clippy all targets/features `-D warnings`、rustfmt、`git diff --check` 和秘密扫描均通过。
4. B5 脱敏审计已升级到 schema v18，新增 proposal request/review 计数与 import-review journal 门禁。正式 AppData 只读结果为 `integrityCheck=ok`、外键违规 0、四类 journal 0，但仍是 schema v16，proposal request/review、实体、ImportContent EvidenceRef、generation receipt 和四动作均为 0；这是未执行真实流程的 No-Go 证据，不是数据库损坏。

## 契约、数据与恢复影响

- request receipt 必须先提交，Provider/rule 才可运行；Kernel 保持 proposal、EvidenceRef、entity ID、status、scope、revision 与时间的权威生成，React 和 Provider 都不能自行拼装。
- 审计和 PEC 只记录数量、状态与短指纹，不输出正文、绝对路径、完整实体/证据 ID 或 Key；正式 AppData 本轮仅被只读打开。
- README 已补充 v18 receipt、真实提案 Confirm/Reject、四动作和 import-review journal 的现场验收标准。

## 剩余阻断与协作

1. **B5 尚未关闭**：员工05仍需实现 Provider/确定性规则 producer 与 `request_import_knowledge_proposals`，覆盖失败、取消、响应丢失及精确重试不重复计费；员工03随后复核重启后既有 request/batch 的权威发现入口。
2. **集成与验收**：请员工01复评 v18 receipt、Vault-first compensation 与 path-safe EvidenceRef ID；评审通过后形成新 clean commit，由员工05发布唯一 Release，并从全新数据库执行完整真实样本。员工02不宣称 B5 或 M2 Go。

## 下一步 WIP

1. 配合员工01完成 v18 原子事务、幂等、恢复与 Windows Vault 路径评审。
2. 配合员工05接入 request producer 后复跑全新数据库和正式 Release 脱敏审计。
