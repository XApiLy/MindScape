# 员工01｜M1 clean 基线发布与剩余验收

> 提交时间：2026-08-20 16:03（UTC+8）  
> 覆盖任务：M1-GATE-001、M1-BUILD-001  
> 状态：clean 工程基线通过；整体仍因脚本 1、3、8 为 No-Go

## 相比上一份的变化

- M1 Provider、Chat 修复、测试、规则与脱敏证据已提交并推送；提交为 `f38c1b9`、`98e1270`。
- 修复验收发布脚本在 detached clean worktree 中读取空分支名崩溃的问题。
- 从 clean commit 发布统一 Tauri release，自动门禁、哈希和无开发服务器独立启动通过。

## 已阅读与落实

- 继续落实 M1 派单、Core Kernel、RC1 契约、威胁模型、Git/PEC 与验收构建规则，并消费员工02～06当前 PEC。
- 本轮只提交 M1 代码、测试、证据和验收工具；员工06视觉对照台及其源码仍留在共享工作树，未混入 M1 提交或 Build。
- Rust Provider 保持 `Result` 错误边界和安全消息；React 只增加确定性时间线投影与空回答呈现，不引入按帧状态或重复运行卡。

## 交付物

- Build ID：`20260820-155308-m1-clean-gate-98e12706c2f4`。
- 程序：`D:\Project\MindScape\artifacts\acceptance\versions\20260820-155308-m1-clean-gate-98e12706c2f4\mindscape-desktop.exe`。
- SHA-256：`6FE02B0BD551288805B8034AE39DB19528815F901F24D1C69471865985ABA59A`。
- 来源提交：`98e12706c2f4`；manifest 为 `sourceTreeDirty=false`。
- 无 `localhost:1420` 监听时独立启动，实际显示 MindScape 工作区；检查后进程已关闭。
- 统一矩阵：`docs/engineering/m1-alpha-acceptance-matrix-20260818.md`。

## 验证结果

- Chat 18/18；Canvas 13/13；Canvas 性能 1/1；Review Lab 9/9。
- Rust 49/49；`cargo fmt --check`、Clippy `-D warnings` 通过。
- 前端 production build、Tauri release、PEC 七文件与 clean 树秘密扫描 280 文件通过。
- 发布目录复制后重新计算 SHA-256，与 manifest 一致。

## 契约、数据、安全与恢复影响

- Provider 请求为 DeepSeek 禁用 thinking；无可显示正文不得写成 completed，并有 Rust 回归测试。
- Chat 时间线同一 Node/Run 只投影一张卡；终态空正文不再显示永久恢复中。
- 未修改 Frozen V1、SQLite schema、IPC 命令、凭据存储或 Canvas 领域事实。

## 剩余 No-Go

- 脚本 1：缺从首次空状态到凭据配置、创建会话和首次真实发送的同 Build 证据。
- 脚本 3：真实库仍只有 `continues`，缺至少两种分支及实际 ContextSnapshot 核对。
- 脚本 8：缺取消、失败、强退后的节点、边、位置、viewport 在同一 clean Build 的联合窗口证据。

## 下一步 WIP（最多两项）

1. 使用本 Build 与员工04/05补跑首次空状态和两类真实分支，员工01只读核对 ContextSnapshot。
2. 与员工02/03联合补齐取消、失败、强退后的画布恢复证据，再签署最终 Go/No-Go。

## 需要同事配合

- **员工02/03**：对本 Build 的终态恢复结果联合核对 Node、Edge、Position、Viewport。
- **员工04/05**：在本 Build 中完成至少两类真实分支和首次空状态操作，不重复长文、错误 Key、取消或网络超时专项。
- **创始人**：只需按后续简短步骤操作脚本 1、3、8；其他已通过场景不重复消耗时间和额度。
