# 员工04｜B5 导入弹窗顶部与滚动修复

时间：2026-09-02 16:01（UTC+8）  
状态：Ready for Review

## 已阅读与落实

- 已复读当前 PEC 顶部“2026-08-30：M2 Go 阻断收敛”和“员工直接执行表”，并检查其他员工最新 PEC。本轮仅修复 B5 真实导入预览链路的可观察阻断。
- 已落实《视觉与交互设计中心》及《M2 Markdown 原生阅读与字体偏好规格》中的内容优先、状态诚实和内容可滚动边界；不改变导入、预览或审核的领域契约。

## 本轮推进与 B 编号

- **关闭 B5 中“导入预览弹窗顶部在实际 WebView 被裁切”的局部 UI 阻断**：截图显示弹窗并非内部内容缺失，而是超高弹窗被垂直居中后，顶部落到可视区之外。
- `desktop/src/styles.css` 已增加 `100vh` 高度回退，保留支持时的 `100dvh`；遮罩层改为顶部对齐并允许自身滚动；导入内容网格显式从顶部排列。这使不完整动态视口支持下，预览/提案审核长内容仍由内容区滚动，标题和底部操作始终可到达。
- B5 总体验收仍未关闭，本轮不将 UI 修复写作统一 clean Release 或真实纵向样本通过。

## 跨模块证据

- `ImportIntakeDialog.tsx` 的 `ImportBundlePreview`、安全渲染/Markdown 原文/源文件切换、提案审核和导入按钮结构均未变更；`importSourceId`、`importRevisionId` 与恢复/审核 typed IPC 均未改变。
- `ChatWorkspace.tsx` / `App.tsx` 仍通过同一导入入口消费此界面，因此真实导入→预览→提案审核状态可直接受益，无 React 伪造恢复状态。
- Review Lab 员工确认主线 preview：`20260902T080049Z-b5-导入弹窗顶部与滚动修复-c53a`（ready）。重点检查 826×516 等矮窗口与长预览状态：顶部完整出现、内容滚动、底部操作可访问。

## 验证与风险

- Playwright 实测 826×516：导入框从顶部完整显示，footer 在可视区；超高情况可通过 backdrop/content 滚动访问，不再因居中而裁掉顶部。
- `pnpm test:chat`：69/69；`pnpm test:canvas`：34/34；`pnpm test:canvas:perf`：2/2；`pnpm build`：通过（2100 modules，只有既存 chunk size warning）。
- `node scripts/check-pec-retention.mjs`、`node scripts/check-secrets.mjs`、`git diff --check`：通过。
- 风险：preview 不是 Acceptance Release；真实 Tauri clean Release、DPI、重启、长文/完整提案审核现场证据仍由员工05在 B5 统一验收中执行。

## 剩余阻断与下一步（最多两个 WIP）

1. **B1**：员工01/05仍需形成 clean 集成基线与唯一 Acceptance Release；共享树 dirty 时不得以本 preview 替代。
2. **B5**：员工05仍需在同一 clean Release 执行真实导入→FocusFrame→检索→回答→候选决策→重启全链样本；我仅配合复验 Chat/导入阅读入口，不新增功能 WIP。

契约、数据、安全、恢复和跨团队接口：本轮无变化。
