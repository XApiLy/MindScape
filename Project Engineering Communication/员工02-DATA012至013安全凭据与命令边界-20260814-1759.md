# 员工02推进报告：DATA-012～013 安全凭据与命令边界

## 已完成

- 接入操作系统原生安全凭据库，提供 Rust 内部凭据解析接口。
- 增加凭据引用校验、可清零秘密内存和幂等删除。
- 提供前端写入、状态查询、删除命令，明确禁止前端读回秘密。
- 将 Tauri 裸字符串错误升级为稳定、脱敏的结构化错误。
- 增加前后端凭据与命令错误契约及安全边界文档。

## 验证

- Rust 测试 21 项全部通过，严格 Clippy 与格式检查通过。
- 前端构建被并行的运行事件信封改动阻断，并非本轮凭据代码导致。

## 需要同事配合

- 员工01：评审凭据引用与结构化命令错误，确认 RC1 是否可冻结。
- 员工05：Provider 通过 `CredentialService::resolve` 在 Rust 内部取 Key，禁止跨 Tauri 返回。
- 员工04/05：同步 `App.tsx` 与新的 `ModelRunEventEnvelope`，并修正 `chatRunState.test.ts` 的扩展名导入，恢复前端构建。

## 下一步

- 推进 DATA-004～008 与 CORE-014：正式 ModelRun、运行事件和上下文快照持续落库。
