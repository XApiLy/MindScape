# 员工01｜M2 分支回流候选契约与 Markdown 事务评审

时间：2026-08-29 01:39（Asia/Shanghai）

覆盖任务：ARC-M2-002 / CORE-M2-003 / DATA-M2-010 评审

状态：分支回流候选领域边界完成；Markdown 用户编辑回流仍需原子事务收口

## 已阅读与落实

- 已复读当前 PEC 控制文件、员工02～06当前报告、M2 派单、M2 契约基线与 Markdown Vault/分支日志架构。
- 按 `rust-best-practices` 将回流规则实现为返回 `KernelResult` 的纯领域入口，并为成功、主线越权、空候选和空白引用分别建立测试；按 `tauri-development` 保持规则在 IPC/SQLite 之前执行。
- 严守员工01职责：本轮只冻结领域契约并评审员工02接线，不替代其 SQLite/Markdown 数据层实现。

## 本次完成

- 新增 `FocusPromotionCandidateSet`，冻结来源 FocusFrame、会话、分支类型、memory version 和候选引用。
- 新增 `FocusFrame::promotion_candidates()`：只允许任务、探索、复盘分支生成非空候选集合；主线不得声明或生成回流候选。
- 回流集合仅代表“等待用户确认”，不会自动修改主线或项目知识；确认后仍必须经知识状态机创建新版本实体/关系。
- 收紧 FocusFrame 引用校验，拒绝首尾空白，防止以 `entity-x` / ` entity-x ` 绕过集合互斥检查。
- 更新 `docs/contracts/m2-contract-baseline.md`，明确员工03/04只展示候选，不在 UI 自行确认或合并。

## Markdown 用户编辑回流评审

- 当前共享工作树已出现 `import_markdown_entity_edit`、内容 hash 无变化短路、实体 revision 递增和投影历史写入，方向符合 ARC-M2-004。
- 发现阻塞：应用层先独立提交 `upsert_knowledge_entity`，随后才独立执行 `persist_markdown_projection`。若第二步失败，会留下实体 revision 已升级、Markdown 修订未记录的半完成状态。
- 该风险属于员工02 `DATA-M2-010` 的事务边界；员工01不直接改其共享数据层。需要把实体事实、FTS/Vector 派生更新与 Markdown projection revision 作为一个 SQLite 事务提交，并补失败回滚/重启恢复测试。

## 验证证据

- Focus promotion 定向测试：4/4 通过。
- Rust 全量：158/158 通过。
- Clippy：`--all-targets --all-features --locked -D warnings` 通过。
- `cargo fmt --all -- --check` 通过。
- 本轮未生成新 Tauri Release；员工05报告的统一程序来源树为 dirty，且缺少真实确认/否决/删除/降级/重启/作用域样本，因此仍不能作为 M2 Go 证据。

## 契约、数据、安全与恢复边界

- 新类型是 `mindscape.focus.v1` 的加法投影，不改变现有 FocusFrame JSON 字段和 SQLite schema。
- 回流候选保留来源 frame 与 memory version，避免 UI 把过期分支结果静默写入主线。
- 本轮不写 SQLite、不改 Vault 文件、不触发 Provider，也不把候选冒充 confirmed 事实。

## 需要配合 / 未关闭项

1. **员工02**：将 Markdown 编辑后的实体/FTS/Vector/Projection 修订合并为原子事务，覆盖投影冲突或写入失败时实体 revision 不前移的回归。
2. **员工03/04**：后续消费 `FocusPromotionCandidateSet` 时只展示“待确认”，不得在前端复制状态转换或自动回流。
3. **员工02～05**：下一次统一 Release 必须来自可追溯 clean source，并用同一 Build ID 完成真实跨模块样本。

## 下一步（最多两个 WIP）

1. 复核员工02的 Markdown 原子修订事务和 schema v13 迁移/回滚测试。
2. 在 clean 统一 Release 上复核 M2 确认、否决、删除、索引降级重建、重启与分支隔离证据。
